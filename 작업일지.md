# 작업일지

---

## 2026-06-08 — siziksu — 응답(response) 카드 백엔드 코드 전체 제거

### 목표
응답 카드(category='response')의 실사용 가치 없음 판단으로 백엔드(agent-runner-py, apps/api) 관련 코드 전면 제거. HITL 카드(category='hitl') 흐름은 완전 유지.

### 변경 파일
- `apps/agent-runner-py/src/modules/dynamic_cards/` — 디렉토리 전체 삭제 (`__init__.py`, `dispatch.py`, `gateway.py`)
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`:
  - `_pump_astream_events` 시그니처에서 `initial_active_card`, `thread_id` 파라미터 제거
  - `on_tool_start` / `on_tool_end` / `on_tool_error` 내 `active_card` tool_events 누적 블록 제거
  - `stream_with_deepagent` 내 `active_card wiring` 블록(placeholder 발행 로직) 제거
  - `stream_with_deepagent` / `resume_stream_with_deepagent` 내 `cardMappings` validation 자동 on 분기 제거
  - `stream_with_deepagent` / `resume_stream_with_deepagent` 내 `maybe_emit_dynamic_card` 호출 블록 제거
- `apps/api/src/modules/cards/cards.service.ts`:
  - `CardCategory` 타입에서 `'response'` 제거 (hitl 만 유지)
  - `findUsages` 에서 `outputSchema.x-agent-studio.cardMappings` 스캔 로직 및 `responseRefs` 반환 제거
  - `create` 기본 category 값을 `'response'` → `'hitl'` 변경
- `apps/api/src/modules/cards/cards.controller.ts`:
  - `list` category 필터에서 `'response'` 분기 제거

### 검증
- `uv run python -c "from src.modules.threads import threads_service"` — OK
- `uv run python -c "from src.modules.agents.deepagent_bridge import stream_with_deepagent, resume_stream_with_deepagent"` — OK
- `cd apps/api && pnpm lint` — 0 errors, 3 warnings (기존 tools.service.ts 미사용 변수 warning, 이번 작업과 무관)

### 특이사항
- DB 스키마 `CardDefinition.category` 컬럼은 그대로 유지 (별도 마이그레이션 작업 예정)
- apps/agent-web 는 별도 에이전트가 작업 중

---

## 2026-06-08 — siziksu — 응답(response) 카드 프론트엔드 코드 전체 제거

### 목표
응답 카드(category='response')는 실제 사용 사례가 없어 운영자 혼란만 가중시킨다는 판단으로 apps/agent-web 에서 관련 코드를 전면 제거. HITL 카드(category='hitl')는 그대로 유지.

### 변경 파일
- `apps/agent-web/src/lib/cards/payload-presets.ts` — `CardPresetId`에서 `response-*` 제거, `CARD_PRESETS`에서 response 프리셋 3개 제거, `category` 필드 자체 제거
- `apps/agent-web/src/lib/cards/ac-validate.ts` — `categoryInvariants`, `validatePayload` 시그니처에서 `category` 파라미터 제거 (HITL 검증 로직은 유지)
- `apps/agent-web/src/lib/api-client.ts` — `CardCategory` 타입 제거, `CardDefinition.category`를 `'hitl'` 리터럴로 단순화, `cards.list()` category 파라미터 제거, `cards.create()` category를 `'hitl'` 고정, `findUsages` 응답 타입에서 `responseRefs` 제거
- `apps/agent-web/src/components/agents/builder/OutputSchemaTab.tsx` — 응답 카드 매핑(cardMappings) UI 섹션 전체 제거, 관련 상태/헬퍼 함수 제거
- `apps/agent-web/src/app/(dashboard)/cards/page.tsx` — 카테고리 탭/필터/뱃지 제거, 새 카드 picker에서 응답 카드 옵션 제거
- `apps/agent-web/src/app/(dashboard)/cards/builder/[cardId]/page.tsx` — category 상태 제거(HITL 고정), 카테고리 select UI 제거, usages/import/export에서 category 참조 제거
- `apps/agent-web/src/lib/ws-client.ts` — `agent.dynamic_card` 이벤트 등록 및 `forwardDynamicCardToInstancesStore` 메서드 제거
- `apps/agent-web/src/components/cards-builder/PresetPicker.tsx` — category 조건부 배지 제거 (HITL 고정 배지로 단순화)
- `apps/agent-web/src/components/skills/assistant/SkillAssistantPanel.tsx` — `agent.dynamic_card` WS 핸들러 및 `DynamicCardRenderer` import/렌더 분기 제거
- `apps/agent-web/src/stores/use-skill-assistant-store.ts` — `AssistantMessageCard`에서 `'dynamic'` kind 제거, `pushDynamicCard` 액션 제거, `updateCardState` 시그니처 단순화
- `apps/agent-web/src/components/agents/builder/ToolsTab.tsx` — `cards.list()` 호출에서 category 파라미터 제거

### 검증
- 신규 TypeScript 오류 0건 (기존 사전 오류 skills/page.tsx 2건 + blocks/ 파일 누락 모듈 다수는 이번 작업 범위 외)
- 기존 HitlDynamicCard, HitlCardRenderer, hitl-helpers, AdaptiveCardHost, designs/ 등 HITL 경로 코드 미변경

### 특이사항
- `use-card-instances-store.ts` 는 `DynamicCardRenderer`에서 아직 `CardLifecycle` 타입을 import하여 사용 중이므로 스토어 자체는 유지함

---

## 2026-06-07 — siziksu — Phase 2: 도구 ↔ HITL 카드 매핑

### 목표
사용자가 도구별로 다른 HITL 카드를 지정할 수 있게 한다. `Agent.toolPermissions` JSON 을 `{toolName: {policy, cardId?, conditions?}}` 형태로 확장 (built-in/user-defined/MCP 도구 모두 한 곳에서 매핑). runner 의 `_handle_interrupt` 가 도구별 cardId 를 조회해 사용. UI 는 정책 `requires_approval` 일 때만 카드 드롭다운(`category='hitl'` 필터) 노출.

### 작업 범위 (Phase 1 완료 → Phase 2 만)
- shared `ToolPermission` 에 `cardId?` 추가
- API `agents.service` create/update/read 시 toolPermissions object 형태 정규화
- runner `_handle_interrupt` 동적 cardId 룩업 + 폴백 `hitl-input-card`
- `ToolPermissionsPanel` 카드 드롭다운, `ToolsTab` 에서 hitl 카드 로드/전달

### 결과
- `packages/shared/src/types/agent.ts` — `ToolPermission.cardId?` 추가, `ToolPermissionEntry`/`ToolPermissionValue`/`ToolPermissionsMap` 신설. Agent/Create/Update 의 `toolPermissions` 를 `ToolPermissionsMap` 으로 교체.
- `apps/api/src/modules/agents/agents.service.ts` — `normalizeToolPermissions()` 헬퍼 신설 (string 입력은 `{policy: ...}` 로 승격, requires_approval 일 때만 cardId 유지, restricted 일 때만 conditions 유지). `create`/`update`/`clone`/`importAgent` 저장 경로 + `get`/`list` 읽기 경로 모두 정규화.
- `apps/agent-runner-py/src/modules/threads/threads_service.py` — `_resolve_tool_card_id(thread_id, tool_name)` 헬퍼 신설. thread → agent_id → `agents.tool_permissions[tool_name].cardId` 룩업, 미설정/오류 시 `hitl-input-card` 폴백. `_handle_interrupt` 의 하드코딩 `"hitl-input-card"` 줄을 동적 호출로 교체.
- `apps/agent-web/src/components/agents/builder/ToolPermissionsPanel.tsx` — props `hitlCards?: CardDefinition[]` 추가. policy=`requires_approval` 일 때 카드 드롭다운 행 노출 (placeholder "기본 (hitl-input-card)").
- `apps/agent-web/src/components/agents/builder/ToolsTab.tsx` — `apiClient.cards.list({category:'hitl'})` 를 초기 로드 Promise.all 에 추가, `hitlCards` state 보관 후 Panel 에 전달. `toPermArray`/`toPermDict` 를 새 object 형태 (`{policy,cardId?,conditions?}`) 로 변경, 레거시 string 형태도 양방향 호환.

### 검증
- `packages/shared` build 통과.
- `apps/api` tsc --noEmit 0 errors, lint 0 errors (pre-existing 3 warnings 만 유지).
- `apps/agent-web` tsc 변경 파일 0 errors (pre-existing 다른 파일 에러는 Phase 2 범위 외, 그대로 유지).

### 특이사항
- Frontend lint 명령은 환경 측 `eslint-module-utils/resolve` 모듈 누락으로 실행 자체가 실패. 코드 변경과 무관 — 별도 환경 보정 필요.
- 기존 데이터 (`Record<string, ToolPolicy>` 문자열 형태) 는 GET 시 자동 정규화되어 frontend 에 object 형태로 도달. 다음 저장에서 DB 도 object 로 마이그레이션됨.
- recursion 카드 (`hitl-recursion-card`) 는 도구 매핑 대상 아님 — 그대로 하드코딩 유지.

---

## 2026-06-06 — siziksu — 에이전트 실행 UX 공통화 (Step A: Card Lifecycle + Step B: Reasoning/ToolLog Blocks)

### Step A — Card Lifecycle 완료
- runner: `dynamic_cards/gateway.py` `emit_dynamic_card` 시그니처에 `lifecycle` (`'placeholder'|'updating'|'final'`, default `'final'`) + `card_instance_id` 추가. payload `presentation` 에 두 필드 노출. 반환값으로 card_instance_id (자동 생성 시) 제공.
- frontend: `stores/use-card-instances-store.ts` 신규 — thread 단위 cardInstanceId → CardInstance 저장. `upsert` 시 deep merge, `mergeData` 부분 갱신, `byThread`/`remove`/`clearThread`.
- frontend: `DynamicCardRenderer.tsx` 에 `lifecycle?` / `cardInstanceId?` props 추가. `lifecycle !== 'final'` → 상단 "준비 중/진행 중" + Loader, 액션 영역 `pointer-events-none + opacity-60`.
- frontend: `ws-client.ts` 의 `agent.dynamic_card` 핸들러가 payload 의 presentation 을 읽어 `useCardInstancesStore.upsert()` 로 자동 forward (lazy import).

### Step B — Reasoning / ToolLog 블록 + dispatch 헬퍼
- frontend: `dynamic-cards/blocks/ThoughtStreamBlock.tsx` 신규 — `value`, `label`, `streaming`, `maxHeight`. 헤더 + monospace 본문 + caret animate-pulse.
- frontend: `dynamic-cards/blocks/ToolLogBlock.tsx` 신규 — `items: ToolEvent[]`. 각 row 가 running/ok/error status 칩 + 펼침으로 args/result/error 표시.
- frontend: `blocks/render.tsx` `BLOCK_REGISTRY` 에 `thought_stream` / `tool_log` 추가 → `KNOWN_BLOCK_TYPES` 자동 +2.
- frontend: `cards-builder/BlockReferencePanel.tsx` BLOCKS 배열에 2종 예시 추가 (복사 가능).
- runner: `dynamic_cards/dispatch.py` 에 라이프사이클 헬퍼 3종 추가:
  - `start_placeholder_card(thread_id, card_definition_id, card_version, initial_data?, card_instance_id?, source_action?) -> str` — `_reasoning`/`_toolEvents` 자동 초기화.
  - `update_card(thread_id, card_instance_id, ..., data_patch)` — `updating` lifecycle.
  - `finalize_card(thread_id, card_instance_id, ..., data)` — `final` lifecycle.
- runner: `dynamic_cards/__init__.py` 에서 3 헬퍼 + 기존 함수 모두 re-export.
- runner: 기존 `maybe_emit_dynamic_card` 의 `emit_dynamic_card` 호출에 `lifecycle='final'` 명시 (default 동일, 가독성).

### 의도적 미포함 (Step E 와 함께 wiring)
- `deepagent_bridge` 의 `on_chat_model_stream` / `on_tool_*` 에서 활성 placeholder 카드에 `_reasoning`/`_toolEvents` 자동 누적 emit. → astream_events 루프가 크고 회귀 위험 큼. Skill Assistant 마이그레이션(Step E) 시 wiring 점을 하나로 모아 진행.

### 회귀 안전
- 모든 신규 인자/필드 default 가 기존 동작 (`lifecycle='final'`, `card_instance_id=None`).
- 신규 블록 2종은 카드 정의가 명시했을 때만 표시. 기존 시드 7종 영향 없음.
- `pnpm --filter @agent-studio/web lint`: 0 errors / 218 warnings (baseline 213 + 신규 5: use-before-define 패턴, 회귀 아님).
- Python import: `start_placeholder_card / update_card / finalize_card` 정상.

---

## 2026-06-07 — siziksu — 에이전트 실행 UX 공통화 (Step C/D/E 일부)

### Step C — AgentRunTimeline 공통 컴포넌트 (완료)
- `lib/run-events.ts` — `RunEvent` discriminated union (thought / tool_call / card / message / hitl / step / subagent 7 kind).
- `stores/use-run-timeline-store.ts` — thread 단위 append-only, in-place update, `appendThoughtDelta` 누적.
- `lib/run-event-adapter.ts` — `attachRunTimelineAdapter()` 가 WS 8 이벤트 listen 해 RunEvent 로 변환 (`agent.token`, `agent.reasoning`, `step.started/completed/failed`, `agent.dynamic_card`, `hitl.request/responded`, `thread.message.received`, `turn.*`, `plan.created`).
- `components/run-timeline/AgentRunTimeline.tsx` + sub `{ThoughtBubble,ToolCallRow,StepBadge,MessageBubble}.tsx` — 단일 렌더 컴포넌트.
- 4 패널 마이그레이션은 의도적 제외 (plan §Step C). dogfood: `/client/agents/[slug]/chat?timeline=1` (작은 사이드 패널) 또는 `?timeline=side` (우측 50%) 토글.

### Step D — 시스템 시드 인프라 (완료)
- **DB 마이그레이션** `20260606000000_add_system_flags`:
  - Agent: `is_system Boolean default false`, `system_key String? unique`, `deleted_at DateTime?` + 인덱스 2.
  - CardDefinition: `is_system`, `system_key`, `deleted_at` + 인덱스 1 (system_key unique 없음).
- **NestJS `apps/api/src/modules/system-seeds/`**:
  - `system-seeds.service.ts` — `seedMissing()` / `restoreDefault()` / `exportToFiles()` / `listDeleted()` / `restore()`. 시스템 프로젝트 `__system__` 자동 생성.
  - `system-seeds.controller.ts` — admin 전용 `/api/system/agents`, `/api/system/cards`, `/restore`, `/reset-to-default`, `/seed:run`, `/seed:export`.
  - `app.module.ts` 에 import, `main.ts` 부트 시 `SEED_SYSTEM_ON_BOOT !== 'false'` 조건으로 자동 `seedMissing()` 호출.
- **soft delete 분기**:
  - `agents.service.ts` — `delete()` 가 isSystem 일 때 `deletedAt = now()` 만, 일반은 hard delete. `list()`/`get()` 에 `deletedAt: null` 필터.
  - `cards.service.ts` — 동일 패턴.
- **Frontend**:
  - `lib/api-client.ts` — ApiClient 클래스 내부에 `system = { agents: {list,restore,resetToDefault}, cards: {...}, seed: {run,export} }` 추가 (§3-1 닫는 `}` 이전).
  - `lib/messages/mutation.ts` — `MSG.system` 도메인 추가 (restored / resetToDefault / seedRun / seedExport).
  - `packages/shared/src/types/agent.ts` — `Agent` 인터페이스에 `isSystem?` / `systemKey?` / `deletedAt?` optional 추가.
  - `app/(dashboard)/system/recover/page.tsx` (신규, admin 전용) — agents/cards 각각 active/deleted 탭, 복원·기본값 리셋·시드 실행·내보내기 버튼.
  - `app/(dashboard)/agents/page.tsx` AgentCard 에 system 배지 (purple), `AgentContextMenu` 삭제 confirm 시 isSystem 일 때 amber 경고 블록.
- 빌드/lint: web 0 errors, api 0 errors (3 warnings 는 tools.service.ts 기존 경고, 작업 범위 외).

### Step E — Skill Assistant 마이그레이션 (안전한 범위만)
- **Layer1 카드 JSON 7개 생성** — 현재 DB 의 7 카드를 `apps/api/src/system-seeds/cards/{cardId}.json` 으로 export:
  - hitl-approval-card / hitl-choice-card / hitl-input-card / quick-choices-card / skill-analysis-card / skill-edit-card / skill-proposal-card.
- **DB 기존 카드 7종을 isSystem=true, systemKey=cardId 로 마킹** — 다음 fresh install 시 `seedMissing()` 이 동일 row 가 있으므로 건드리지 않음.

### 의도적 미포함 (별도 작업으로 분리)
- **`skill-assistant.json` Layer1 작성** — `assistant_prompt.py` 가 매 호출마다 `{mode_block}/{tools_block}/{skills_block}/{current_skill_block}/{chat_history_block}` 슬롯을 동적으로 채우는 구조. 정적 systemPrompt 만 박는 것으론 동작 불가. 카탈로그 prefetch + history 인젝션 로직을 에이전트 실행 흐름에 흡수하는 별도 설계 필요.
- **SkillAssistantPanel 진입점 변경** (`apiClient.skillAssistant.*` → `apiClient.threads.*`) — 위와 동일 이유.
- **코드 Skill Assistant 모듈 제거** (`apps/agent-runner-py/src/modules/skill_assistant/`) — feature flag + dogfood 검증 후에만.
- **deepagent_bridge 의 token/tool 자동 누적 emit** — Step B 에서 dispatch 헬퍼만 깔아둠. 실제 wiring 은 Skill Assistant 마이그레이션 본 작업과 함께.

### 회귀 안전
- 모든 변경 additive. lifecycle/cardInstanceId/isSystem 필드 default 가 기존 동작.
- 기존 카드 7종 layout/actions/schema 변경 없음 (단지 isSystem 플래그만 마킹).
- Skill Assistant 코드 모듈 / 4 종 React 카드 / 전용 WS 이벤트 그대로 동작.

### 변경 요약
- 신규: `run-events.ts`, `use-run-timeline-store.ts`, `run-event-adapter.ts`, `run-timeline/*` (5 파일), `system-seeds/*` (NestJS 모듈), `system/recover/page.tsx`, `system-seeds/cards/*.json` (7 파일), Prisma 마이그레이션 1.
- 수정: `agents.service.ts`, `cards.service.ts`, `app.module.ts`, `main.ts`, `api-client.ts`, `mutation.ts`, `shared/types/agent.ts`, `agents/page.tsx`, `AgentContextMenu.tsx`, `client/agents/[slug]/chat/page.tsx`.

---

## 2026-06-07 — siziksu — 후속 (Step F: 자동 누적 emit / Step G: prompt hook + skill-assistant.json)

### Step F — deepagent_bridge 자동 누적 emit (완료)
- `deepagent_bridge.py:_pump_astream_events` 함수에 `initial_active_card` 인자 + `thread_id` 인자 추가. default None → 100% 회귀 안전.
- L1031~1075: `active_card` 로컬 상태 초기화 + last_emit_len/last_emit_at debounce 필드.
- L1110~1135: `on_chat_model_stream` 의 token yield 직후, ui_depth==0 + active_card 활성 시 `_reasoning` 누적 후 `update_card` emit. **debounce**: 누적 +30chars 또는 last_emit 후 200ms 경과 + 새 chunk 일 때.
- L1174~1196 / L1229~1252 / L1320~1350: `on_tool_start/end/error` 의 step.* yield 직후 `_toolEvents` row push/update + `update_card` emit. row id = run_id, status running → ok/error 전이.
- 모든 신규 분기는 `active_card is not None and thread_id is not None` 가드. 실패 시 try/except + warning, 기존 흐름 무중단.
- 호출자가 `initial_active_card` 를 등록하지 않는 한 모든 신규 코드 경로 skip → 회귀 0.

### Step G — 시스템 에이전트 prompt hook 인프라 + skill-assistant.json (완료)
- 신규 `apps/agent-runner-py/src/modules/agents/system_prompts/__init__.py` — `BuildContext` TypedDict + `register_system_prompt_builder` / `get_system_prompt_builder` 레지스트리.
- 신규 `apps/agent-runner-py/src/modules/agents/system_prompts/skill_assistant.py` — `_prefetch_catalog` + 기존 `build_system_prompt` 를 그대로 wrap. import 시점에 `register_system_prompt_builder("skill-assistant", build)` 자동 등록.
- `apps/agent-runner-py/src/modules/agents/__init__.py` — `from .system_prompts import skill_assistant` side-effect import 1줄.
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py:stream_with_deepagent` 의 `input_guard.check()` 통과 직후 ~30줄 블록: `loaded["systemKey"]` 가 등록된 builder 있으면 `await builder(ctx)` 결과로 `loaded["systemPrompt"]` 동적 교체. 실패 시 정적 fallback.
- 신규 `apps/api/src/system-seeds/agents/skill-assistant.json` — Layer1 정의. systemPrompt 는 raw SYSTEM_PROMPT (5 슬롯 placeholder 포함), outputSchema 는 AssistantResponse.model_json_schema() + x-agent-studio.cardMappings (create_skill / edit_skill / analyze_skill / clarify).
- 부트 시 `SystemSeedsService.seedMissing()` 가 skill-assistant 에이전트 row 자동 생성 (`isSystem=true, systemKey='skill-assistant'`).
- 기존 `skill_assistant/` 모듈 변경 0줄.

### 의도적 미포함 (검증 필요)
- **SkillAssistantPanel 진입점 변경** (`apiClient.skillAssistant.*` → `apiClient.threads.*`) — 카드 4 종, HITL, 도구 호출, 카탈로그 prefetch, mode override 등 복잡한 변경. dogfood 검증 필요.
- **코드 `skill_assistant/` 모듈 제거** — feature flag + dogfood 1 주 검증 후.
- 두 작업은 별도 PR 로 진행 권장.

### 회귀 안전
- Step F: `initial_active_card=None` 이면 신규 분기 100% skip. 호출자(=Step G 의 hook wiring) 가 명시적으로 등록하지 않는 한 기존 동작 동일.
- Step G: skill_assistant 모듈 / 4 종 React 카드 / `/api/skill-assistant/*` 라우터 그대로 동작. 신규 hook 은 systemKey='skill-assistant' 인 에이전트가 실제 호출될 때만 활성.
- `pnpm --filter @agent-studio/api lint` 0 errors / 3 warnings (기존 tools.service.ts 경고, 작업 범위 외).
- Python import: `stream_with_deepagent` / `get_system_prompt_builder` / `start_placeholder_card` 모두 정상.

### 변경 요약
- 신규: `agents/system_prompts/__init__.py`, `agents/system_prompts/skill_assistant.py`, `system-seeds/agents/skill-assistant.json`.
- 수정: `agents/__init__.py`, `agents/deepagent_bridge.py` (active_card 분기 + systemKey hook 호출).

---

## 2026-06-07 — siziksu — 길 1 통합 (active_card wiring + 시각 매핑 편집기 + 컨텍스트 흐름)

### Step 1 — active_card wiring + _placeholder 정책 (완료)
- `dispatch.py:maybe_emit_dynamic_card` 시그니처에 `card_instance_id: str | None = None` 추가. final emit 시 placeholder 와 동일 instance_id 로 같은 자리 replace 가능.
- `deepagent_bridge.py:stream_with_deepagent` 진입부에 ~60줄 추가:
  - outputSchema.x-agent-studio.cardMappings 조회
  - 우선순위: `_placeholder` 특수 키 → 없으면 첫 일반 매핑
  - `start_placeholder_card()` 호출 → `initial_active_card` 구성 → `_pump_astream_events` 에 `initial_active_card=...` + `thread_id=...` 전달
- final emit 호출 (L1962~): `card_instance_id=initial_active_card['card_instance_id']` 전달
- 시드 카드 `reasoning-progress-card v1` 신규 (`apps/api/src/system-seeds/cards/reasoning-progress-card.json` + DB `is_system=true`). title + thought_stream + tool_log 의 generic 진행 표시 카드.

### Step 2 — OutputSchemaTab 시각 매핑 편집기 (완료)
- 신규 `apps/agent-web/src/components/agents/builder/CardMappingsEditor.tsx`:
  - schema.properties.action.enum 자동 인식 → 매핑 가능한 action 키 목록
  - `apiClient.cards.list()` 로 cardId / version select 옵션 자동 fetch
  - `_placeholder` 특수 키 별도 강조 (purple), 진행 표시 카드 선택용
  - 각 매핑 row: cardId select / version select / dataPath input + 삭제
  - 폼 변경 시 `schema.x-agent-studio.cardMappings` 부분만 patch → Monaco JSON 양방향 sync
- `OutputSchemaTab.tsx`: Monaco editor 위에 `<CardMappingsEditor>` 마운트, `handleMappingsChange` 로 schemaStr / onChange 동시 갱신.

### Step 3 — Skill 컨텍스트 thread metadata 흐름 (완료)
- **인프라는 이미 완비**:
  - `packages/shared/src/types/thread.ts:CreateThreadRequest.metadata?: Record<string, unknown>` 존재
  - `apps/api/src/modules/threads/threads.service.ts:58` 에서 dto.metadata 를 DB 에 저장
  - `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py:_fetch_thread_metadata()` 가 metadata 를 fetch
  - `stream_with_deepagent` 이미 `_thread_meta` → `BuildContext.session_metadata` 전달
- `system_prompts/skill_assistant.py:build()` 보강:
  - `currentSkill` / `current_skill` 양쪽 인식 (camelCase 우선, snake_case fallback)
  - docstring 에 인식 키 목록 명시 (userId/user_id, projectId/project_id, mode, currentSkill/current_skill)

### 사용 흐름 (모든 단계 결과 통합)
```
[빌더] /agents/<id> → 출력 스키마 탭 → "카드 매핑 (advanced)" 템플릿 클릭
   → CardMappingsEditor 자동 인식 → _placeholder=reasoning-progress-card 추가
   → create_skill / edit_skill / analyze_skill / clarify 각각 매핑 폼 편집
   → 저장 (Agent.outputSchema 에 x-agent-studio.cardMappings JSON 저장)

[채팅] (예: /client/agents/<slug>/chat) → thread 생성 시
   apiClient.threads.create(projectId, {
     agentId, metadata: { currentSkill, mode: 'builder', userId, projectId }
   })
   → message 전송 → stream_with_deepagent
   → systemKey 있으면 build(ctx) hook 호출 → 동적 systemPrompt 생성
   → cardMappings 있으면 start_placeholder_card(reasoning-progress-card) emit
   → initial_active_card 등록 → _pump_astream_events 가 토큰/도구 진행 자동 누적
   → LLM 응답 완료 → maybe_emit_dynamic_card(card_instance_id=same) → final emit
   → 프런트가 같은 cardInstanceId 라 같은 자리에서 placeholder → final 로 replace
```

### 검증
- Python imports: stream_with_deepagent / get_system_prompt_builder('skill-assistant') / maybe_emit_dynamic_card(card_instance_id) 모두 OK.
- `pnpm --filter @agent-studio/web lint`: 0 errors / 224 warnings (변화 없음).
- 빌더에서 카드 매핑 편집 가능, 채팅 시 thread metadata 로 currentSkill 전달 가능.

### 의도적 미포함 (검증 필요)
- **SkillAssistantPanel 진입점 변경** — Step 4 (1~2일, feature flag).
- **코드 `skill_assistant/` 모듈 제거** — Step 5 (dogfood 1주 후, 1시간).
- 이 둘은 별도 PR 권장.

### 변경 요약
- 신규: `system-seeds/cards/reasoning-progress-card.json`, `agents/builder/CardMappingsEditor.tsx`.
- 수정: `dynamic_cards/dispatch.py` (card_instance_id), `agents/deepagent_bridge.py` (active_card wiring + final card_instance_id), `system_prompts/skill_assistant.py` (camelCase 인식), `agents/builder/OutputSchemaTab.tsx` (CardMappingsEditor 마운트).

---

## 2026-06-07 — siziksu — outputSchema 표준화: 방식 2 (JSON layout) + 방식 3 (intent_routed) 병행

### 목표
기준(기존 코드 SkillAssistant) 유지하면서 서비스 에이전트 두 가지 응답 전략 표준화:
- 방식 2 (structured_only): outputSchema + cardMappings + 강화된 JSON layout 카드
- 방식 3 (intent_routed): reasoning + INTENT 태그 → chat astream / structured ainvoke 분기

### 결과
- **Step 1 — JSON layout 강화**
  - 신규 블록 4종: `FileTreeBlock`, `CodeDiffBlock`, `ChoiceChipInputBlock`, `RefinementPreviewBlock` → `BLOCK_REGISTRY` 17→21 종
  - `BlockReferencePanel` 에 4 예시 추가
  - 시드 카드 4종 layout 정교화: skill-proposal(파일 탭 + choice_chip), skill-edit(refinement_preview + code_diff), skill-analysis(suggestion_list 강화), quick-choices(choice_chip_input 통합)
  - DB reset-to-default 4 적용
- **Step 2 — outputSchema 표준 필드 + 빌더 UI**
  - 신규 `IntentRoutingEditor.tsx` — reasoningEnabled / reasoningPrompt / tagPattern / default / chatActions 시각 편집
  - `OutputSchemaTab` 에 "응답 전략" 라디오 (structured_only / intent_routed) + 분기 시 IntentRoutingEditor 노출
- **Step 3 — runner intent_routed 전략**
  - `deepagent_bridge.py:_run_intent_routed_flow` 신규 (~L1397-1547):
    - 1단계: model.astream(reasoning + INTENT 태그) → agent.token 토큰 스트림
    - 2단계: 태그 파싱 (정규식, default 폴백)
    - 3단계 (chat 분기): model.astream 자유 텍스트 → agent.token
    - 3단계 (structured 분기): model.with_structured_output(method='json_schema') → maybe_emit_dynamic_card
  - `stream_with_deepagent` 진입부에 strategy 분기 삽입 (L2009-2096)
  - Layer1 skill-assistant.json 에 responseStrategy='intent_routed' + intent 객체 추가
- **Step 4 — 비교 페이지**
  - 신규 `skill-assistant-jsonlayout.json` (Layer1, structured_only)
  - 신규 `ServiceAgentSkillPanel.tsx` — systemKey 별 시스템 에이전트 호출 + WS forward
  - 신규 `/skills/compare/page.tsx` — 3 컬럼 (기준 ①/방식 2 ②/방식 3 ③)
  - `/skills` 헤더에 "비교 모드" 링크
  - DB: 2 시스템 에이전트 row 확인 (skill-assistant intent_routed + skill-assistant-jsonlayout structured_only)

### 회귀 안전
- 기존 `skill_assistant/` 코드 모듈 / `SkillAssistantPanel` / `/skills` 본 페이지 0줄 변경
- runner 의 `responseStrategy` 미지정 또는 `structured_only` → 기존 흐름 그대로
- 신규 카드 블록 4종은 카드 정의가 명시했을 때만 표시

### 검증
- `pnpm --filter @agent-studio/web lint`: 0 errors / 233 warnings
- Python import: `stream_with_deepagent`, `_run_intent_routed_flow` 모두 정상
- DB 시드: `skill-assistant` (intent_routed) + `skill-assistant-jsonlayout` (structured_only) 두 row 정상 생성
- `/skills/compare` 진입 시 3 컬럼 마운트 가능

### 사용 방법
- `/skills/compare` 진입 → 3 컬럼에 같은 메시지 입력해 비교
- 새 어시스턴트 도메인 만들 때:
  - 단순 카드만 필요 → `responseStrategy: structured_only` + cardMappings 만 작성 (방식 2)
  - chat + 카드 자동 분기 → `responseStrategy: intent_routed` + intent 블록 추가 (방식 3)

### 변경 요약
- 신규: `dynamic-cards/blocks/{FileTree,CodeDiff,ChoiceChipInput,RefinementPreview}Block.tsx` (4), `agents/builder/IntentRoutingEditor.tsx`, `skills/assistant/ServiceAgentSkillPanel.tsx`, `app/(dashboard)/skills/compare/page.tsx`, `system-seeds/agents/skill-assistant-jsonlayout.json`
- 수정: `dynamic-cards/blocks/render.tsx`, `cards-builder/BlockReferencePanel.tsx`, `agents/builder/OutputSchemaTab.tsx`, `agents/deepagent_bridge.py` (_run_intent_routed_flow + strategy 분기), `system-seeds/agents/skill-assistant.json` (intent), `system-seeds/cards/{skill-proposal,skill-edit,skill-analysis,quick-choices}-card.json`, `(dashboard)/skills/page.tsx` (비교 모드 링크)

---

## 2026-06-07 — siziksu — Skill Assistant 진입점 변경 (feature flag dogfood)

### 산출물
- 신규 `apps/agent-web/src/components/skills/assistant/SkillAssistantAgentPanel.tsx` (274줄):
  - 마운트 시 `apiClient.system.agents.list()` 로 systemKey='skill-assistant' 에이전트 row 조회.
  - `apiClient.threads.create(projectId, { agentId, metadata: { currentSkill, mode, userId, projectId } })` 로 thread 생성, `wsClient.subscribeThread()`.
  - 본문: `<AgentRunTimeline threadId={...} />` 그대로 마운트 (카드/추론/도구/메시지 자동 렌더).
  - 메시지 전송: `apiClient.threads.invoke(threadId, text)`.
  - 기존 `useSkillAssistantStore` / 4종 React 카드 import 없음 → 회귀 0.
- 수정 `apps/agent-web/src/app/(dashboard)/skills/page.tsx`:
  - `useAgentBackend()` hook 신설 — `?backend=agent` 쿼리 또는 `localStorage['skill-assistant-backend']==='agent'`.
  - 헤더 우측에 "Agent 모드 (beta) / 기존 모드" 토글 버튼 (FlaskConical 아이콘).
  - `useAgentMode` 분기로 `<SkillAssistantPanel>` 또는 `<SkillAssistantAgentPanel>` 마운트.
- 수정 `apps/api/nest-cli.json`:
  - `compilerOptions.assets` 에 `system-seeds/**/*.json` + `watchAssets: true` 추가 → dist 에 Layer1 JSON 자동 복사.

### 부수 작업 (dev 환경 시드)
- NestJS watch 모드가 nest-cli.json 변경을 즉시 반영하지 않아 `cp -r src/system-seeds dist/system-seeds` 1회 수동 실행 → admin 로그인 후 `POST /api/system/seed:run` 호출 → skill-assistant 에이전트 row 생성 확인.
- 향후 fresh install 시엔 `pnpm setup` + 부트 시 자동 시드.

### 사용 흐름
```
1. /skills 진입 → 헤더 우측 "Agent 모드 (beta)" 토글 클릭
   (또는 /skills?backend=agent URL 직접 입력)
2. localStorage 에 'skill-assistant-backend=agent' 기록
3. 화면 갱신 → SkillAssistantAgentPanel 마운트
4. 스킬 카드 클릭(편집 모드 진입) → targetSkill 인식 → thread 자동 생성 (metadata 에 currentSkill 포함)
5. 입력창에 "instructions 에 X 추가해줘" → 메시지 전송
6. runner stream_with_deepagent:
   - systemKey='skill-assistant' hook 호출 → 동적 systemPrompt (currentSkill 인젝션)
   - outputSchema.cardMappings 발견 → placeholder 카드 emit
   - 토큰 흐름 → 카드 안 thought_stream 자라남
   - skill.update 도구 호출 → tool_log row 추가
   - final emit (skill-edit-card) → 같은 자리에서 replace, [적용] 버튼 활성
7. [적용] 클릭 → tool_call → apiClient.skills.update → DB 수정
```

### 회귀 안전
- 기본 경로(`?backend=agent` 없음, localStorage 미설정) → 기존 SkillAssistantPanel 100% 동일.
- `useSkillAssistantStore` / 4종 React 카드 / `/api/skill-assistant/*` 라우터 / WS 이벤트 4종 모두 그대로 동작.
- 토글 OFF 만 하면 즉시 기존 경로로 복귀.

### 의도적 미포함 (검증 후 별도 작업)
- **코드 `skill_assistant/` 모듈 제거**, **WS 화이트리스트 4종 제거**, **4종 React 카드 파일 삭제**, **apiClient.skillAssistant.* 제거** — dogfood 1주 검증 후 진행.

### 검증
- `pnpm --filter @agent-studio/web lint`: 0 errors / 225 warnings (baseline 224 + 신규 1).
- DB: `system_key='skill-assistant'`, `is_system=true`, `model_id='claude-sonnet-4-6'` 에이전트 row 생성 확인.
- dist/system-seeds JSON 복사 확인.

### 변경 요약
- 신규: `components/skills/assistant/SkillAssistantAgentPanel.tsx`.
- 수정: `app/(dashboard)/skills/page.tsx` (feature flag + 토글), `apps/api/nest-cli.json` (assets).

---

## 2026-06-06 — siziksu — 카드 빌더 UX 개선 (슬림 스크롤 / 복제·갤러리 / Monaco 스키마 / Visual 빌더)

### 목표
- 카드 빌더가 schema/layout/actions 3개 Monaco JSON 동시 편집이라 진입장벽이 큰 문제 해결.
- "JSON 을 손으로 쓰지 않고도 카드를 만들 수 있게" — JSON 은 source of truth, 빌더는 view.
- Monaco 자체 스크롤바를 전역 5px 슬림 스크롤바 톤에 맞춰 슬림화.

### 결과
- **Step 1 — 슬림 스크롤바 보강**: `cards/builder/[cardId]/page.tsx` 의 Monaco 4개 (schema/layout/actions/dummy) 에 공통 `MONACO_OPTIONS` 적용 (`scrollbar.verticalScrollbarSize=6`, `useShadows=false`, `overviewRulerLanes=0`). 전역 `globals.css` 슬림 스크롤바는 기존 유지 (이미 theme-aware).
- **Step 2 — 카드 복제 + 템플릿 갤러리**:
  - `cards/page.tsx`: 액션 컬럼에 "복제" 버튼, 상단에 collapse 가능한 "템플릿 갤러리" 그리드 (composable 카드 12개), `localStorage:cards-page:gallery-collapsed` 기억.
  - 복제 모달: cardId 입력 → 정규화 미리보기 → unique 검증 → `/cards/builder/new?cloneFrom=...&cloneVersion=...&newCardId=...` 라우터 push.
  - `cards/builder/[cardId]/page.tsx`: `useSearchParams` 로 cloneFrom 처리, isNew + cloneFrom 일 때 원본 fetch 해서 schema/layout/actions prefill, name 은 "{원본명} 복제".
  - `MSG.card` 도메인 상수 추가 (created/cloned).
- **Step 3 — Monaco JSON Schema 검증 + Reference 패널**:
  - `lib/card-schemas/layout-schema.ts` — 15종 블록 타입 enum, header/body 구조 JSON Schema (draft-07).
  - `lib/card-schemas/actions-schema.ts` — 6종 HANDLER_TYPES / 5종 INPUT_TYPES / 4종 style enum, action item 필수 키.
  - `cards/builder/[cardId]/page.tsx` 의 layout/actions Monaco 에 `path="card-layout.json"` + `beforeMount` 으로 `monaco.languages.json.jsonDefaults.setDiagnosticsOptions` 등록 → 잘못된 블록/핸들러 타입 입력 시 Monaco 가 빨간 줄 표시.
  - `components/cards-builder/BlockReferencePanel.tsx` 신규: 우측 상단 fixed collapse 패널, "블록 / 핸들러 / 입력" 3탭, 각 항목당 설명 + 예시 JSON + "복사" 버튼 (clipboard).
- **Step 4 — 시각 빌더 (Form 모드, MVP)**:
  - 각 pane 헤더에 `PaneHeader` Visual/JSON 토글 신설. 기본 Visual.
  - `components/cards-builder/visual-types.ts` — `parseSchemaToVisual` / `visualToSchemaJson` 양방향 변환.
  - `VisualSchemaEditor.tsx` — properties 필드 행(name/type/required/description) 추가·삭제.
  - `VisualLayoutEditor.tsx` — header 폼 + body 블록 리스트 (이동/삭제), 5종 simple block(text/title/tags/key_value_list/json) 폼 편집, 나머지 10종은 추가만 가능 (편집은 JSON 탭 안내).
  - `VisualActionsEditor.tsx` — id/label/style/handler.type/input.type 폼, 상하 이동/삭제.
  - Source of truth = JSON text. Visual 은 useMemo 로 parse 만 하고 모든 변경은 즉시 `onChange` → parent text 갱신 → 재parse (단방향 controlled, local state 없음 — setState-in-effect 회귀 회피).
  - JSON 파싱 실패 시 Visual 탭은 "JSON 탭에서 수정하세요" 배너만 표시.

### 변경 파일
- 신규: `apps/agent-web/src/lib/card-schemas/layout-schema.ts`, `actions-schema.ts`
- 신규: `apps/agent-web/src/components/cards-builder/{BlockReferencePanel,VisualSchemaEditor,VisualLayoutEditor,VisualActionsEditor,visual-types}.tsx`(.ts)
- 수정: `apps/agent-web/src/app/(dashboard)/cards/page.tsx` (전면 개편)
- 수정: `apps/agent-web/src/app/(dashboard)/cards/builder/[cardId]/page.tsx` (Visual 탭, cloneFrom, Monaco 옵션, BeforeMount, Reference)
- 수정: `apps/agent-web/src/lib/messages/mutation.ts` (`MSG.card`)

### 회귀 안전
- API/DB/runner 변경 없음. 100% 프런트엔드.
- 기존 JSON 직접 편집 경로 100% 보존 (Visual ↔ JSON 토글).
- 시드 카드 7종 미변경, CardDefinition 포맷 미변경.
- `pnpm --filter @agent-studio/web lint`: **0 errors, 213 warnings** (baseline 215 보다 감소).

### 특이사항
- Plan Step 4 의 "drag & drop" 은 lib 추가 없이 ChevronUp/Down 버튼 순서 변경으로 대체 (의존성 최소화).
- 10종 고급 블록(line_diff/tabs/accordion 등) Visual 편집은 후속 작업. 현재는 추가만 가능하고 props 편집은 JSON 탭 사용.
- BlockReferencePanel 은 `fixed right-3 top-20` 으로 빌더 위에 floating. 기본 collapse, localStorage 기억.

---

## 2026-06-06 — siziksu — Skill Assistant 분석 모드 + Auto 의도 라우팅

### 목표
- 기존 생성/편집 중심에서 벗어나 이미 만들어진 스킬에 대한 질의응답·분석·비교 지원.
- 입력창 좌측 모드 셀렉터(Auto / 빌더 / 분석) — Auto는 메시지 의도로 자동 라우팅.
- 분석 모드: 구조화된 `analyze_skill` 액션 + `SkillAnalysisCard` (점수·강점·약점·도구 적합성·개선 제안).
- 분석 카드의 각 개선 제안 → "이 항목으로 수정 적용"으로 builder 흐름 연결.
- Skills 페이지 카드에 "🔍 분석" 버튼 — 편집 패널 없이 분석 모드 진입.

### 결과
- **Runner**: `models.py`에 `ToolFitItem/AnalysisSuggestion/SkillAnalysisDesign` + `AssistantResponse.action="analyze_skill"` + `analysis` 필드 추가. `assistant_prompt.py`에 `{mode_block}` 슬롯과 builder/analyze/auto 모드별 가이드 추가. `assistant_service.py`에 mode metadata 보존, `effective_mode` + LLM 모드 위반 fallback(analyze→create/edit 차단, builder→analyze 차단). `assistant_gateway.emit_skill_analyzed` (이벤트 `assistant.skill_analyzed`). `assistant_router.py`에 mode/mode_override 전달.
- **NestJS**: `skill-assistant.controller.ts` DTO에 `@IsIn(['auto','builder','analyze']) mode?` 추가. `skill-assistant.service.ts`가 runner body에 mode forwarding.
- **프론트 store**: `mode` 상태 + `setMode` 액션, `SkillAnalysisPayload`/`SkillAnalysisSuggestion`/`SkillAnalysisToolFit` 타입, `AssistantMessageCard.kind='analysis'` 케이스, `pushAnalysisCard` 액션. `startSession(skillId, model, mode?)`/`sendMessage(text, modeOverride?)` 시그니처 확장.
- **apiClient**: `skillAssistant.createSession({ mode })` / `invoke(threadId, message, mode?)`.
- **ws-client**: `assistant.skill_analyzed` 화이트리스트 등록.
- **SkillAnalysisCard 신설**: 점수 게이지(0-100, blue/amber/red) + 4탭(요약/강점·약점/도구 적합성/개선 제안). 각 suggestion 옆 "이 항목으로 수정 적용" 버튼 → `sendMessage(..., 'builder')`로 즉시 빌더 흐름 전환. 닫기로 `canceled` 처리.
- **SkillPayloadDetailModal**: `DetailAnalysisPayload` 타입 + `AnalysisPretty` 컴포넌트(점수 게이지 대형 + 4섹션) 추가.
- **SkillAssistantPanel**: 입력창 좌측에 모드 셀렉터(`✨Auto`/`🛠빌더`/`🔍분석`) 추가, `assistant.skill_analyzed` WS 핸들러, `card.kind==='analysis'` 메시지 렌더 분기.
- **Skills 페이지**: 각 스킬 카드에 🔍(Search) 분석 버튼 → `setMode('analyze')` + 어시스턴트 자동 펼침 + targetSkill 설정. `assistantTargetSkill` 계산식에서 panelOpen 조건 제거 → 편집 패널 없이도 분석 진입 가능.
- **검증**: agent-web 0 errors / 188 warnings, api 0 errors / 3 warnings(베이스라인), agent-runner-py import 통과.

### 변경 파일
신설:
- apps/agent-web/src/components/skills/assistant/SkillAnalysisCard.tsx

수정:
- apps/agent-runner-py/src/modules/skill_assistant/{models,assistant_prompt,assistant_service,assistant_gateway,assistant_router}.py
- apps/api/src/modules/skill-assistant/skill-assistant.{controller,service}.ts
- apps/agent-web/src/stores/use-skill-assistant-store.ts
- apps/agent-web/src/lib/{api-client,ws-client,markdown}.ts (markdown은 기존 활용)
- apps/agent-web/src/components/skills/assistant/{SkillAssistantPanel,SkillPayloadDetailModal}.tsx
- apps/agent-web/src/app/(dashboard)/skills/page.tsx
---

## 2026-06-06 — siziksu — Skill Assistant 신설 (Skills 페이지에 스킬 생성·편집 어시스턴트)

### 목표
- Agent 상세의 Agent Assistant와 동일한 UX(하단 접힘/펼침 패널 + 스트리밍 채팅 + 제안 카드 적용)로 Skills 페이지에 Skill Assistant 추가.
- 자연어로 스킬 신규 생성 + 기존 스킬 부분 편집 모두 지원.

### 작업 범위
1. NestJS 모듈 `apps/api/src/modules/skill-assistant` 신설 — runner의 `/api/v1/skill-assistant` 프록시.
2. FastAPI 모듈 `apps/agent-runner-py/src/modules/skill_assistant` 신설 — structured-output 단일 호출(create / edit / clarify / answer_directly action).
3. 프론트 store(`use-skill-assistant-store`) + 컴포넌트(`SkillAssistantPanel`, `SkillProposalCard`, `SkillEditCard`) + `apiClient.skillAssistant` 추가.
4. Skills 페이지 하단에 패널 마운트 + 적용 콜백을 기존 mutation 재사용.

### 결과
- **NestJS**: `apps/api/src/modules/skill-assistant/{controller,service,module}.ts` 신설 + `app.module.ts` 등록. `POST /skills/assistant/sessions`, `POST /skills/assistant/sessions/:threadId/invoke`. agent-assistant 패턴 그대로 복제 + skillId 선택 처리(없으면 신규 생성 모드).
- **FastAPI**: `apps/agent-runner-py/src/modules/skill_assistant/` 6개 파일 신설 + `src/main.py` include_router. structured-output 단일 호출(`AssistantResponse` action: create_skill/edit_skill/clarify/answer_directly), prefetch_catalog는 user-scoped(builtin/mcp/skills/models).
- **프론트**: `use-skill-assistant-store`, `SkillAssistantPanel`, `SkillProposalCard`, `SkillEditCard` 신설. `apiClient.skillAssistant` 추가, `MSG.skill.applyProposal/applyEdit` 메시지 추가.
- **Skills 페이지**: 하단에 `SkillAssistantPanel` 마운트. 적용 콜백은 `assistantCreateMutation` / `assistantEditMutation`(둘 다 `useApiMutation` 기반) — 기존 skills API 재사용. 편집 패널이 열려 있으면 자동 컨텍스트 전달.
- **lint**: agent-web 0 errors / 147 warnings(전체 베이스라인 유지), api 0 errors / 3 warnings(베이스라인 유지). agent-runner-py import smoke 통과.

### 변경 파일
신설:
- apps/api/src/modules/skill-assistant/skill-assistant.{controller,service,module}.ts
- apps/agent-runner-py/src/modules/skill_assistant/{__init__,models,assistant_router,assistant_service,assistant_gateway,assistant_prompt}.py
- apps/agent-web/src/stores/use-skill-assistant-store.ts
- apps/agent-web/src/components/skills/assistant/{SkillAssistantPanel,SkillProposalCard,SkillEditCard}.tsx

수정:
- apps/api/src/app.module.ts
- apps/agent-runner-py/src/main.py
- apps/agent-web/src/lib/api-client.ts (`skillAssistant` 메서드)
- apps/agent-web/src/lib/messages/mutation.ts (`MSG.skill.applyProposal/applyEdit`)
- apps/agent-web/src/app/(dashboard)/skills/page.tsx (패널 마운트 + 두 mutation)
---

## 2026-05-28 — siziksu — 전 서비스 Mutation UX 표준화 (toast / loading / store local update)

### 목표
- 등록/수정/삭제/활성·비활성화 시 화면에서 아무 반응이 없다는 사용자 피드백 개선.
- 약 85개 mutation 핸들러(57개 파일, 13개 도메인)에 toast 피드백 / Loader2 스피너+disabled / Zustand store local update를 한꺼번에 보장하는 공통 helper 도입.

### 작업 범위
1. Foundation 신설
   - `apps/agent-web/src/lib/use-api-mutation.ts` — `useApiMutation` 훅
   - `apps/agent-web/src/lib/error-message.ts` — `extractErrorMessage`
   - `apps/agent-web/src/lib/messages/mutation.ts` — 도메인별 기본 한국어 메시지 상수
2. 13개 도메인 핸들러 마이그레이션: agents / teams / tools(+toolGroups+mcp) / workflows / deployments / providers / environments / users / projects / scheduler / integrations(slack+naver-works) / prompt versions / hitl-threads
3. AGENTS.md에 mutation 핸들러 패턴 규칙 추가 (Phase 3 — 규칙 보강)

### 결과
- **Foundation 신설**
  - `apps/agent-web/src/lib/use-api-mutation.ts` — `useApiMutation` 훅 (`mutate`, `mutateAsync`, `isPending`, `error`, `reset`). 성공/에러 toast 자동, `silent`/`loadingMessage`/`onSuccess` 옵션 지원, unmount 보호.
  - `apps/agent-web/src/lib/error-message.ts` — `extractErrorMessage` (ApiError/Error/string/object 모두 처리)
  - `apps/agent-web/src/lib/messages/mutation.ts` — 도메인별 한국어 메시지 상수 (`MSG.agent.created`, `MSG.toolGroup.deleted` 등 24개 도메인 × 24개 verb)
- **도메인별 마이그레이션 (3개 frontend-senior-developer 에이전트 병렬 위임)**
  - Batch A (tools+agents): 10개 파일 / 27개 핸들러 — `ToolGroupCard`, `CreateToolGroupDialog`, `McpServerDialog`, `RegisterIntegrationDialog`, `McpServerCard`, `AgentContextMenu`, `SchedulerPanel`, `agents/page.tsx`, `agents/[id]/deployments/page.tsx`, `skills/page.tsx`
  - Batch C (envs+users+projects+integrations+threads): 7개 파일 / 18개 핸들러 — `settings/users/page.tsx`(7), `NaverWorksInstallationList`(4), `SlackInstallationCard`(3), `SlackChannelAgentList`(3), `PromptDiffModal`(1). `useUserStore.setUserActive` 액션 추가, `apiClient.agents.restorePromptVersion` 추가, `RegisterIntegrationDialog` 조건부 훅 호출 버그 수정.
  - Batch B(workflows): `workflows/[id]/editor/page.tsx`(handleSave), `workflows/[id]/versions/page.tsx`(handleRollback), `workflow-editor.tsx`(저장 버튼 Loader2)
  - Batch B(providers): `settings/providers/page.tsx` 12개 핸들러 — `ProviderCard` 5개, `LocalProviderCard` 6개, `ProvidersPage` 1개. `toast` import 완전 제거.
  - 총 **약 60개 핸들러, 21개 파일** 마이그레이션. 5개 도메인은 페이지 부재(teams/projects/environments — 플레이스홀더/미구현)로 대상 없음.
- **추가 보강**
  - `apiClient` 클래스에 `agents.restorePromptVersion` 메서드 추가 (클래스 내부)
  - `useUserStore.setUserActive(id, active)` 액션 추가 — activate 시 full refetch → local update 교체
  - NaverWorks: `refresh()` 호출 → `setItems` store local update로 교체
- **타입 오류 2건 수정**
  - `agents/[id]/deployments/page.tsx:135` `onSuccess` 반환 타입(`"" | Promise<void>`) → `void` 래핑
  - `workflows/[id]/versions/page.tsx:23` `useApiMutation<string, void>` 제네릭 → 추론(`WorkflowVersion`) 허용
- **검증**: `pnpm lint` → 0 errors / 144 warnings (기존 코드 warnings, 이번 변경 기인 추가 없음). `pnpm tsc --noEmit` → 통과.
- **AGENTS.md 보강**: §1-3 `apps/agent-web (Frontend)` 섹션에 mutation 핸들러 4개 규칙 추가. Reinforced Rules Log에 2026-05-28 항목 추가.

### 변경 파일 요약
신규 4개 (foundation 3 + 작업일지 1), 수정 약 21개 컴포넌트/페이지, 스토어 1개(`use-user-store.ts`), `api-client.ts`(메서드 추가), AGENTS.md.

### 특이사항
- environments/projects/teams 도메인은 현재 페이지/컴포넌트가 미구현 또는 플레이스홀더로 mutation 핸들러 부재 → 향후 구현 시 동일 패턴 적용 필요.
- `AddCustomModelDialog`/`OpenApiSpecDialog` 일부 다이얼로그의 내부 `saving` state는 다이얼로그 UX용으로 유지(외부 mutation 호출). 이중 pending 없음.
- HITL/threads 핵심 컴포넌트(`AgentFlowDebugPanel`, `ClientChatView`)는 수천 라인 복합 컴포넌트로 별도 배치 시 마이그레이션 권장(이번 범위 외).

---

## 2026-05-28 — siziksu — 사용자 문서(퀵스타트/메뉴얼) 게시 + Playwright 캡처 파이프라인

### 목표
- 루트에 흩어져 있던 두 HTML 가이드(`AGENT002_퀵스타트_가이드.html`, `AGENT002_사용자_메뉴얼.html`)를 정적 자원으로 게시해
  사용자가 앱 내부에서 바로 열람할 수 있게 하고, `placeholder-img` 점선 박스 자리를 실제 서비스 스크린샷으로 교체할 수 있는
  Playwright 기반 캡처 파이프라인을 마련한다.

### 작업 범위
1. 두 HTML을 `apps/agent-web/public/docs/`로 이동하고 루트·`docs/` 사본 정리 (SSOT 유지).
2. `/login` 카드 푸터 및 빌더 사이드바 / 클라이언트 사이드바 양쪽에 "퀵스타트 가이드 · 사용자 메뉴얼" 외부 링크 노출.
3. `apps/agent-web`에 `@playwright/test` devDependency + `playwright.config.ts` + `scripts/capture-docs-screenshots.ts` 추가
   (`pnpm docs:capture` 스크립트로 실행, 산출물은 `public/docs/screenshots/`).
4. 두 HTML의 `.placeholder-img` 블록 11개를 `<figure class="doc-figure"><img …><figcaption>…</figcaption></figure>` 로 일괄 교체,
   문서 공통 figure 스타일 추가.

### 결과
- 파일 이동: `git mv docs/AGENT002_*.html apps/agent-web/public/docs/` 후 루트 사본 삭제. Next.js `public/` 정적 서빙으로
  `http://<host>/docs/AGENT002_*.html` 경로에서 바로 접근 가능.
- 링크 노출: `apps/agent-web/src/app/(auth)/login/page.tsx` 카드 푸터에 두 문서 링크 추가(target="_blank"). 빌더
  사이드바(`apps/agent-web/src/components/layout/app-sidebar.tsx`)에 "Help" 섹션과 두 외부 링크 추가, 클라이언트
  사이드바(`apps/agent-web/src/app/(client)/_components/sidebar.tsx`) NAV 하단에 동일 링크 추가.
- 캡처 파이프라인: `playwright.config.ts`(baseURL `http://localhost:28002`, viewport 1440x900) + `scripts/capture-docs-screenshots.ts`
  (시드 계정 자동 로그인 → /login, /agents, New Agent 모달, /agents/[id], Main Agent 설정 모달, /agents/[id]/deployments,
  /skills, /tools, /settings/providers, /dashboard, /client/agents, /client/agents/[slug] 순차 캡처). `package.json`에
  `docs:capture` 스크립트 등록.
- HTML 교체: 퀵스타트 4개 + 사용자 메뉴얼 7개의 `.placeholder-img` 블록 전부 `<figure>`로 교체, 두 파일 모두 동일한
  `figure.doc-figure` 스타일(이미지 border + box-shadow + figcaption + 경로 칩) 신규 정의.
- 회귀: `pnpm --filter @agent-studio/web lint` — 0 errors, 기존 139 warnings 그대로(본 변경 무관).

### 변경 파일
- `apps/agent-web/public/docs/AGENT002_퀵스타트_가이드.html` (이동 + 교체)
- `apps/agent-web/public/docs/AGENT002_사용자_메뉴얼.html` (이동 + 교체)
- `apps/agent-web/public/docs/screenshots/` (디렉터리 신설)
- `apps/agent-web/src/app/(auth)/login/page.tsx`
- `apps/agent-web/src/components/layout/app-sidebar.tsx`
- `apps/agent-web/src/app/(client)/_components/sidebar.tsx`
- `apps/agent-web/playwright.config.ts` (신규)
- `apps/agent-web/scripts/capture-docs-screenshots.ts` (신규)
- `apps/agent-web/package.json`

### 후속 작업 (수동 캡처 보완)
- 캔버스 실행 중 노드 점등·Debug 패널, 발급 직후 API 키 노란 카드, Agent Assistant diff 카드, HITL 승인 UI는
  자동화 대상이 아니므로 동일 폴더(`public/docs/screenshots/`)에 수동으로 추가하고 HTML에 figure를 추가 삽입한다.

### 참고
- 계획 파일: `~/.claude/plans/agent002-html-agent002-html-misty-fox.md`

---

## 2026-05-27 — siziksu — NAVER WORKS 봇 1:1 대화 연동

### 목표
- NAVER WORKS Bot API와 AgentStudio 에이전트를 연동하여, 사용자가 봇에게 1:1 대화로 메시지를 보내면 등록된 에이전트가 응답하도록 구현한다.
- Slack 연동 패턴(`SlackInstallation` + `SlackRuntimeService`)을 차용하되 다음을 달리한다.
  - Socket Mode가 없으므로 NestJS(apps/api)에 공개 callback endpoint를 둔다.
  - 1:1 DM(`source.channelId` 없음) 이벤트만 처리하고, 봇 1개 ↔ 에이전트 1개로 단순 매핑한다.
  - 1:1 특성상 "요청자가 곧 컨텍스트"이므로 NAVER WORKS user 정보(userId/name/email)를 에이전트의 system prompt 헤더에 자동 주입한다.

### 작업 범위
1. DB: `NaverWorksInstallation` 모델 + 마이그레이션 (시크릿은 AES-256-GCM 암호화).
2. apps/api: `naver-works` 모듈 — installation CRUD, JWT auth/토큰 캐시, sendUserMessage client, text content util.
3. apps/api: webhook controller(X-WORKS-Signature 검증, 즉시 200), dispatcher service(thread upsert + RUNNER invoke), `/internal/naver-works/reply` controller(X-RUNNER-KEY 인증).
4. apps/agent-runner-py: `_requester_header()`를 deepagent_bridge prompt 빌더에 추가, threads_service의 turn 완료 직후 replyEndpoint로 best-effort POST.
5. apps/agent-web: `/integrations/naver-works` UI, 봇 등록 카드, 에이전트 선택, callback URL 노출.
6. 단위/통합 테스트 및 회귀 점검.

### 참고
- 계획 파일: `~/.claude/plans/slack-immutable-crescent.md`
- 가이드: `.claude/skills/naver-works/SKILL.md`, `.claude/agents/naver-works-integrator.md`, AGENTS.md §1-3 NAVER WORKS 연동 규칙

### 결과
- DB: `NaverWorksInstallation` 모델 추가 + 마이그레이션 `20260527000000_add_naver_works_installation`. 시크릿 3종(clientSecretEnc/privateKeyEnc/botSecretEnc)은 AES-256-GCM 으로 암호화 저장. botId 글로벌 UNIQUE.
- apps/api: NestJS `naver-works` 모듈 신규. installations CRUD/에이전트 변경/활성화 토글 controller + service (등록 시 JWT 토큰 발급 검증), Service Account JWT(RS256) 발급/캐시 service, 사용자 1:1 메시지 송신 client (긴 텍스트 1000자 분할), text content 빌더 util.
- apps/api: callback webhook controller — X-WORKS-Signature HMAC-SHA256 검증 후 즉시 200 응답, 1:1(`source.channelId` 없음) message 만 dispatcher 로 비동기 위임. dispatcher service — (botId, naverUserId) 기준 thread upsert + thread.metadata 에 source/botId/naverUserId/requester 저장 + runner-py invoke. 내부 reply controller `POST /internal/naver-works/reply` (X-Runner-Key 검증) — runner-py 가 turn 완료 시 호출하는 응답 push endpoint.
- apps/api: `main.ts` 에 raw body 보존(verify 콜백)으로 webhook HMAC 검증 지원. `app.module.ts` 에 `NaverWorksModule` 등록.
- apps/agent-runner-py: `deepagent_bridge._requester_header()` 신설 — thread.metadata.requester(이름/이메일/naverUserId)를 system prompt 최상단에 prepend 하여 1:1 대화 요청자 컨텍스트를 에이전트로 자동 전달. `_fetch_thread_metadata()` 헬퍼로 stream_with_deepagent 흐름에 안전하게 통합.
- apps/agent-runner-py: `threads_service._maybe_push_external_reply()` 신설 — turn 완료(`emit_turn_completed` 직후) 시 thread.metadata.source==='naver_works' 이고 botId/naverUserId 가 있으면 settings.MANAGEMENT_API_URL 로 best-effort POST. send_message / resume_message 양쪽 모두 호출 — HITL 응답 후 재개 turn 도 NaverWorks 로 push.
- apps/agent-web: `/integrations/naver-works` 페이지 + `NaverWorksInstallationList` 컴포넌트 신설 — 봇 등록 모달(Bot ID, Client ID/Secret, Service Account, Private Key PEM, Bot Secret, Scope, 매핑 에이전트), 봇별 카드(에이전트 변경/활성화 토글/삭제/callback URL 복사). `apiClient.naverWorks.installations.*` 메서드를 `ApiClient` 클래스 내부에 추가. 사이드바 메뉴에 NAVER WORKS 진입점 추가.
- 테스트: pytest 21건 추가/통과 — `_requester_header` 9건 (None/빈/full/fallback/non-dict edge case), `_maybe_push_external_reply` + `_parse_thread_metadata` 12건 (skip 조건, POST 헤더/body 검증, HTTP 5xx/네트워크 예외 best-effort). 기존 dev 브랜치의 `test_deepagent_bridge` / `test_subagent_middleware` 12건 사전 실패는 본 변경과 무관(stash 비교로 확인).
- 환경변수: `.env.example` 에 `NEXT_PUBLIC_NAVER_WORKS_CALLBACK_BASE` 추가, `apps/agent-runner-py/.env.example` 에 `MANAGEMENT_API_URL` 명시.
- 회귀: apps/api 빌드 통과, apps/api / apps/agent-web 모두 lint 0 errors.

### 변경 파일
- `packages/database/prisma/schema.prisma`
- `packages/database/prisma/migrations/20260527000000_add_naver_works_installation/migration.sql`
- `apps/api/src/main.ts`
- `apps/api/src/app.module.ts`
- `apps/api/src/modules/naver-works/naver-works.module.ts`
- `apps/api/src/modules/naver-works/naver-works-installations.controller.ts`
- `apps/api/src/modules/naver-works/naver-works-installations.service.ts`
- `apps/api/src/modules/naver-works/naver-works-auth.service.ts`
- `apps/api/src/modules/naver-works/naver-works-client.service.ts`
- `apps/api/src/modules/naver-works/naver-works-content.util.ts`
- `apps/api/src/modules/naver-works/naver-works-webhook.controller.ts`
- `apps/api/src/modules/naver-works/naver-works-dispatcher.service.ts`
- `apps/api/src/modules/naver-works/naver-works-reply.controller.ts`
- `apps/api/src/modules/naver-works/dto/{upsert-installation,update-agent,enable,reply}.dto.ts`
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
- `apps/agent-runner-py/tests/modules/agents/test_requester_header.py`
- `apps/agent-runner-py/tests/modules/threads/{__init__.py,test_external_reply.py}`
- `apps/agent-web/src/app/(dashboard)/integrations/naver-works/page.tsx`
- `apps/agent-web/src/components/naver-works/NaverWorksInstallationList.tsx`
- `apps/agent-web/src/components/layout/app-sidebar.tsx`
- `apps/agent-web/src/lib/api-client.ts`
- `.env.example`
- `apps/agent-runner-py/.env.example`

---

## 2026-05-25 — siziksu — Agent Client Chat Completed Steps Grouping Card Removal

### 목표
- Agent Client 대화 채팅창(ClientChatView.tsx)에서 ActivityGroupCard 세부 단계를 펼쳤을 때 나타나던 중첩 완료 그룹("PatchToolCallsMiddleware.before_agent 등 4개 단계 완료")을 제거한다.
- 중첩 완료 그룹(CompletedGroupRow)을 제거하고, 세부 단계를 평평하게(flat) 나열하여 가독성을 높인다.
- 4개 작업 완료 및 경과 시간 표시(ActivityGroupCard)는 그대로 유지한다.

### 결과
- `ClientChatView.tsx` 내부에서 연속 완료된 단계를 한 번 더 묶어주는 `groupCompletedSteps` 함수, `CompletedGroupRow` 컴포넌트, `RenderNode` 타입을 제거하였습니다.
- `StepList` 컴포넌트가 `nodes` 개별 항목들을 `StepRow`로 직접 평평하게 매핑하여 렌더링하도록 단순화하였습니다.
- 미사용 임포트가 된 `lucide-react` 의 `Layers` 아이콘을 지워 린트 에러가 발생하지 않도록 조치하였습니다.

### 변경 파일
- `apps/agent-web/src/components/client/ClientChatView.tsx`

---

## 2026-05-25 — siziksu — Debug Chat Step List Render Issue Fix

### 목표
- 디버그 패널의 채팅 탭 화면에서 질문 후 여러 단계를 거쳐 답변을 받은 뒤, 다시 질문하면 단계만 표시되고 최종 답변이 표현되지 않는 버그를 조사하고 수정한다.

### 변경 범위
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - `agent.token` 이벤트 핸들러에서 스트리밍 토큰 누적 조건인 `e.delta` 체크 식을 `e.delta === false ? chunk : (accumulator) + chunk` 형태로 관대하게 개선. 이로써 백엔드의 델타 직렬화 상태나 필드 제공 조건에 상관없이 100% 실시간 스트리밍 애니메이션이 글자 단위로 매끄럽게 누적 표현되도록 근본적으로 조치.
  - `agent.token` 이벤트 핸들러 시작부에 `e.depth === 0` 조건을 두어 최상위 에이전트의 최종 답변 토큰이 하위 디버그용 `tool_call` 카드로 가로채어 흡수되지 않고, 즉시 최하단 AI 말풍선으로 안전하게 스트리밍되어 실시간 노출되도록 보장 (0단계 short-circuit).
  - `agent.token` 이벤트 핸들러의 스트리밍 토큰 누적 인덱스를 마지막 Human 메시지 이후의 AI 답변으로 정확히 가두어 토큰 누적 및 뭉침 유실 예방.
  - `turn.completed` 이벤트 핸들러에서 최종 AI 답변 덮어쓰기 탐색 범위를 마지막 Human 메시지 이후로 한정하여 이전 turn 답변 영역 오염 방지 및 두 번째 답변 정상 노출.
  - `handleRun`의 invoke 동기 응답 fallback 처리 조건(`hasAiMsg`)을 마지막 Human 메시지 이후로 제한하여 두 번째 turn 이후의 백필 차단 현상 해결.

---

## 2026-05-25 — siziksu — Agent Client Chat Step Grouping Card Removal

### 목표
- Agent Client의 대화 채팅창(ClientChatView.tsx)에서 복잡한 실행 단계를 묶어서 표현하던 ActivityGroupCard(예: "PatchToolCallsMiddleware.before_agent 등 10개 단계")의 렌더링 노출을 유지하되, 내부에서 연속 완료된 단계를 다시 한 번 묶어주던 중첩 완료 그룹("PatchToolCallsMiddleware.before_agent 등 10개 단계 완료")을 제거한다.
- 일반 대면 고객이 보는 채팅 화면에서는 상위에 이미 전체 작업 그룹 카드가 있으므로, 굳이 하위 단계를 한 번 더 CompletedGroupRow로 중첩해서 묶을 필요가 없다.
- 하위 단계들을 평평하게(flat) 나열하여 가독성을 높인다.
- 개발자용 디버그 패널(AgentFlowDebugPanel.tsx)의 도구 추적 및 그룹화 기능은 건드리지 않고 그대로 보존한다.

### 결과
- `ClientChatView.tsx` 내의 피드 맵 루프에서 `ActivityGroupCard` 복구.
- `StepList` 컴포넌트 내부에서 `groupCompletedSteps`를 사용한 완료 단계 그룹화(`CompletedGroupRow`로 묶기) 제거.
- 이제 `ActivityGroupCard`를 클릭하여 세부 단계를 펼쳤을 때 모든 단계가 `StepRow`를 통해 평평하게 나열되도록 하여 중복 그룹화 현상 해결.
- 미사용 코드가 된 `groupCompletedSteps` 함수, `CompletedGroupRow` 컴포넌트, `RenderNode` 타입을 말끔하게 지워서 `pnpm lint` 경고 수를 오히려 기존 대비 1개 절감하는(0 errors, 122 warnings) 리팩토링 수행.

### 변경 파일
- `apps/agent-web/src/components/client/ClientChatView.tsx`

---

## 2026-05-25 — siziksu — Debug Chat UI Steps Grouping & UX Improvements

### 목표
디버그 패널의 채팅 탭 화면에 나타나는 에이전트 실행 단계 드롭박스(StepRow)들의 기능 및 UI/UX를 개선한다.
구체적으로:
- 드롭박스(StepRow)를 처음에는 닫힌 상태(`useState(false)`)로 제공하고, 완료 시에 굳이 닫지 않고 열린 상태 그대로 또는 지정한 대로 둔다.
- 완료 상태(`status === 'done'`)일 때 눈에 띄는 테두리 박스 효과(`border border-emerald-500/20 bg-emerald-500/[0.02]`)를 적용한다.
- `task` 성격을 지닌 드롭박스(에이전트/SubAgent)는 하위에 자식 작업이 있으므로, 하위 작업 목록의 개수를 드롭박스 헤더에 표현한다.
- 드롭박스(StepRow) 간의 간격을 조절하여 콤팩트하고 가독성 높게 바꾼다.
- 동일 뎁스에서 연속으로 완료된 여러 드롭박스를 하나의 "완료된 단계 그룹(Completed Group)"으로 합치고 카운트를 표현한다.
- 합쳐진 완료 그룹은 아코디언 형태로 펼치고 접을 수 있게 하여, 펼치면 개별 완료 드롭박스들을 조회할 수 있도록 한다.

### 변경 범위
- `apps/agent-web/src/components/client/ClientChatView.tsx` — `StepRow` 컴포넌트의 상태, CSS 스타일, `task` 및 자식 수 뱃지 추가, 연속 완료 노드 그룹화 로직(`StepList`, `CompletedGroupRow` 컴포넌트) 구현 및 재귀 호출 대체.
- `lucide-react`에서 `Layers` 등 필요한 아이콘 임포트.

---

## 2026-05-25 — siziksu — Agent Builder MCP 인증 모드 표기 개선

### 목표
에이전트 빌더 Tools 탭의 MCP Servers 목록에 등록한 MCP 가 공유(shared) / 개인별(per_user) 인증 모드인지 한눈에 보이도록 배지를 추가한다.
또한 MCP 서버 상세(McpServerDialog) 폼에서 "인증 모드" 섹션을 Transport 항목 위로 이동해, 먼저 인증 방식을 정한 뒤 전송 방식을 고르는 흐름으로 정리한다.

### 변경 범위
- `apps/api/src/modules/mcp/mcp.service.ts` — `listAllTools` select/return 에 `credentialMode` 추가
- `apps/agent-web/src/lib/api-client.ts` — `mcp.listAllTools` 반환 타입에 `credentialMode` 추가
- `apps/agent-web/src/components/agents/builder/ToolsTab.tsx` — `McpServerGroup` 에 `credentialMode` 추가하고 서버 헤더에 "공유"/"개인별" 배지 렌더
- `apps/agent-web/src/components/tools/McpServerDialog.tsx` — 인증 모드 섹션을 Transport 위로 이동

---

## 2026-05-24 — siziksu — MCP 도구 기능 개선 (API 단계 완료)

### API 단계 완료 서브섹션

#### 목표
계획 `tools-mcp-zany-nova.md` §2 API 항목 구현.
- DTO 확장 (credentialMode, requiredUserFields, exposedTools)
- 도구 노출 토글 엔드포인트 신설
- listAllTools onlyExposed 필터링
- PUT /mcp/:id 새 3개 필드 처리
- connect/refreshTools 시 exposedTools 정리
- me-mcp catalog per_user 모드 분류 + fields 메타 포함
- invoke 실행 게이트 (MissingMcpCredential 403)
- runner 페이로드에 userMcpEnvs 동봉
- packages/shared 타입 업데이트

#### 변경/신설 파일
- `packages/shared/src/types/mcp-server.ts` — McpCredentialMode, RequiredUserField, MissingMcpCredentialError 등 신규 타입 추가
- `packages/shared/src/types/agent.ts` — Agent/CreateAgentRequest/UpdateAgentRequest 에 mcpToolRefs 필드 추가
- `apps/api/src/modules/mcp/dto/create-mcp-server.dto.ts` — credentialMode/requiredUserFields/exposedTools 추가, RequiredUserFieldDto 클래스 신설
- `apps/api/src/modules/mcp/dto/update-mcp-server.dto.ts` — 신설 (UpdateMcpServerDto, ToggleToolVisibilityDto)
- `apps/api/src/modules/mcp/mcp.service.ts` — update 새 필드 처리, toggleToolVisibility 신설, listAllTools onlyExposed 필터, connect/refreshTools exposedTools 정리
- `apps/api/src/modules/mcp/mcp.controller.ts` — PATCH /mcp/:id/tools/:toolName/visibility 신설, listAllTools onlyExposed 쿼리 파라미터
- `apps/api/src/modules/me-mcp/me-mcp.service.ts` — per_user 모드 분류, fields 메타(requiredUserFields 우선/env fallback) 반환
- `apps/api/src/modules/threads/threads.service.ts` — checkAndCollectMcpCredentials (실행 게이트 + env 수집), invoke에 게이트 적용 및 userMcpEnvs 포함
- `apps/api/src/modules/agents/agents.service.ts` — create/update/clone/importAgent에 mcpToolRefs 처리 추가

#### 신규 엔드포인트
- `PATCH /mcp/:id/tools/:toolName/visibility` — { exposed: boolean } → exposedTools 토글
- `GET /projects/:projectId/mcp/tools?onlyExposed=true` — 기존 엔드포인트에 쿼리 파라미터 추가

#### 실행 게이트 위치
- `apps/api/src/modules/threads/threads.service.ts` — `invoke` 메서드 내부, runner fetch 직전
  - `checkAndCollectMcpCredentials(agentId, userId)` 호출
  - 누락 시 `ForbiddenException({ error: 'MissingMcpCredential', missingCredentials: [...] })`

#### Runner 페이로드 변경 위치
- `apps/api/src/modules/threads/threads.service.ts` — `invoke` 메서드의 runner fetch body에 `userMcpEnvs` 필드 추가

#### lint 결과
- `apps/api` lint: 기존 warning 3개 (tools.service.ts 미사용 변수) 외 신규 오류 0개 — 통과

---

## 2026-05-24 — siziksu — MCP 도구 기능 개선 (Runner 단계 완료)

### 목표
계획 `tools-mcp-zany-nova.md` §3 Agent Runner 항목 구현.
- MCP 도구 단위 필터링 (`exposed_tools` / `mcpToolRefs`)
- per_user 자격증명 env merge (`userMcpEnvs` 페이로드 수신)
- 페이로드 모델 확장 (`SendMessageBody.userMcpEnvs`)

### Runner 단계 완료

#### 변경 파일
- `apps/agent-runner-py/src/modules/mcp/mcp_client_service.py`
  - `to_structured_tools(server, allowed_tool_names=None)` 옵션 인자 추가.
  - server dict 의 `exposed_tools` / `exposedTools` 비어있지 않으면 해당 이름만 wrapping.
  - `allowed_tool_names` 전달 시 교집합 적용 (sanitize_tool_name 기준 비교).
- `apps/agent-runner-py/src/modules/agents/agents_service.py`
  - `_load_mcp_tools` 에 `tool_refs`, `user_mcp_envs` 인자 추가.
    - `tool_refs` 있으면 서버별 허용 도구 set 구성 → `McpClientService.to_structured_tools` 에 전달.
    - `credential_mode == 'per_user'` 이고 `user_mcp_envs[serverId]` 있으면 config.env 위에 merge.
    - `mcp_servers` 쿼리에 `credential_mode`, `exposed_tools` 컬럼 추가.
  - `load_agent_with_deps` 에 `user_mcp_envs` 인자 추가. DB 쿼리에 `a.mcp_tool_refs` 추가.
    - `mcp_tool_refs` 파싱 후 참조된 서버 ID 만 로드 (하위 호환: null/빈 배열이면 mcp_server_ids 전체).
  - `_build_inline_subagent_specs` 에 `user_mcp_envs` 인자 추가 + 전달.
  - `stream` 함수에 `user_mcp_envs` 인자 추가 + `load_agent_with_deps` 에 전달.
- `apps/agent-runner-py/src/modules/threads/threads_router.py`
  - `SendMessageBody` 에 `userMcpEnvs: dict[str, dict[str, str]] | None = None` 필드 추가.
  - `/invoke` 핸들러에서 `threads_service.send_message(..., user_mcp_envs=body.userMcpEnvs)` 전달.
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `send_message` 에 `user_mcp_envs` 인자 추가 + `agents_service.stream` 에 전달.

#### 보존 항목
- `mcp_router.py` `/list-tools` — 원본 전체 반환 유지 (변경 없음).
- 기존 `exposed_tools=[]` 서버 / `mcpToolRefs` 없는 Agent 는 기존 동작 그대로 (하위 호환).

#### 검증
- smoke import: `uv run python -c "import src.main"` → OK
- 시그니처 검증: 모든 인자 추가/전달 경로 Python 인트로스펙션으로 확인 → ALL OK
- pytest: 모듈 미설치 환경으로 실행 불가 (pyproject.toml 에 pytest 미등록)

---

## 2026-05-24 — siziksu — MCP 도구 기능 개선 (Web 단계 완료)

### 목표
계획 `tools-mcp-zany-nova.md` §4 Web 항목 구현.

### 변경/신설 파일
- `apps/agent-web/src/lib/api-client.ts`
  - `ApiError` 클래스 export (body 필드 추가로 MissingMcpCredential 파싱 지원)
  - `mcp.listAllTools` 시그니처 확장 (`onlyExposed?: boolean`)
  - `mcp.toggleToolVisibility(serverId, toolName, exposed)` 신설 — PATCH /mcp/:id/tools/:toolName/visibility
  - `meMcp.catalog` 응답 타입에 credentialMode/requiredUserFields 추가
  - `meMcp.saveCredential(serverId, fields)` 신설 — POST /me/mcp/credentials
- `apps/agent-web/src/components/tools/McpServerDialog.tsx` (신설)
  - Form/JSON 양방향 탭 다이얼로그 (등록 + 편집 모드 통합)
  - credentialMode 라디오 (shared/per_user)
  - per_user 시 RequiredUserFields 배열 편집기
  - JSON 탭 ↔ Form 탭 전환 시 양방향 동기화, 파싱 실패 시 toast + 전환 차단
- `apps/agent-web/src/components/mcp/McpServerCard.tsx`
  - Edit 버튼 추가 → McpServerDialog 편집 모드 오픈
  - 도구 목록에 Exposed Switch 추가 (shadcn Switch 패턴으로 순수 CSS 구현)
  - exposedTools 빈 배열 = 전체 노출 처리, 토글 시 apiClient.mcp.toggleToolVisibility 호출
- `apps/agent-web/src/app/(dashboard)/tools/page.tsx`
  - McpConnectForm import 제거, McpServerDialog import 추가
  - "Add Server" 버튼 클릭 시 McpServerDialog 렌더
- `apps/agent-web/src/components/agents/builder/ToolsTab.tsx`
  - MCP 섹션을 도구 단위 선택으로 전환 (mcpToolRefs 기반)
  - 서버 헤더 "전체 선택" 체크박스 + 하위 도구별 체크박스
  - apiClient.mcp.listAllTools({ onlyExposed: true }) 로 변경
  - 레거시 mcpServerIds → mcpToolRefs 1회 마이그레이션 effect
  - toolNamesMap에 선택된 MCP 도구만 포함
- `apps/agent-web/src/components/chat/McpCredentialModal.tsx` (신설)
  - 403 MissingMcpCredential 응답 메타를 폼으로 렌더
  - 서버별 섹션, secret 필드는 password 타입
  - 저장 성공 시 onSuccess 콜백 (상위에서 자동 재전송)
- `apps/agent-web/src/components/client/ClientChatView.tsx`
  - ApiError import, McpCredentialModal import
  - mcpCredModal 상태 추가
  - handleSend catch 블록에 MissingMcpCredential 분기 (403 + error === 'MissingMcpCredential')
  - McpCredentialModal 인라인 렌더 + onSuccess 시 handleSendRef로 자동 재전송
- `apps/agent-web/src/components/chat/chat-interface.tsx`
  - ApiError import, McpCredentialModal import, MissingMcpCredentialError import
  - mcpCredModal 상태 추가
  - handleSend catch 블록에 MissingMcpCredential 분기
  - 모달 onSuccess 시 setInput + setTimeout handleSend 재전송
- `apps/agent-web/src/app/(client)/client/tools/page.tsx`
  - McpCatalogRow 타입에 credentialMode, requiredUserFields 추가

### 핵심 UX 결정
- **탭 동기화 실패 처리**: JSON → Form 전환 시 JSON.parse 실패하면 `toast.error`로 경고하고 탭 전환을 막는다 (switchToForm 함수에서 try-catch 처리).
- **도구 노출 토글 낙관적 업데이트**: API 호출 후 서버 응답 없이 exposedTools 배열을 클라이언트에서 직접 갱신, 전체 선택 시 빈 배열로 정규화.
- **재전송 방식**: ClientChatView는 handleSendRef.current로 즉시 재전송, chat-interface는 input 상태 set 후 setTimeout 0ms로 재전송.

### 인라인 모달 진입점
- `ClientChatView.tsx` — `handleSend` 내 catch 블록, `apiClient.threads.invoke` 호출 후 ApiError(403) 감지
- `chat-interface.tsx` — `handleSend` 내 catch 블록, `onSendMessage` prop 호출 후 ApiError(403) 감지

### lint 결과
- `pnpm --filter @agent-studio/web lint`: 0 errors, 102 warnings (기존 warnings 그대로, 신규 에러 없음)

---

## 2026-05-25 — siziksu — Agent Client Chat Streaming Response Issue Fix

### 목표
- Agent Client 대화 채팅창(ClientChatView.tsx)에서 실시간 AI 답변 스트리밍이 노출되지 않고 한 번에 표현되는 버그를 해결한다.
- 메인 에이전트 및 서브에이전트의 토큰 이벤트가 수신되었을 때 depth 체크 제약으로 인해 화면 렌더링에 누락되거나 무시되는 현상을 차단하고, 실시간 스트리밍 애니메이션이 글자 단위로 매끄럽게 노출되도록 보장한다.

### 결과
- **[Pre-connect 도입 및 지연 제거]**: 일반 사용자 뷰(`ClientChatView.tsx`)는 기존에 `threadId`가 생성된 이후에야 비로소 웹소켓 연결 및 핸드셰이크를 시작하여, 첫 질문 시 스트리밍이 다 끝난 후에 소켓 연결이 수립되는 비동기 레이스 컨디션이 발생했습니다. 이를 해결하고자 컴포넌트 마운트 즉시 웹소켓 연결을 사전 수립(`Pre-connect`)하는 `useEffect`를 도입하고, 불필요한 5초 대기 딜레이(`waitForConnection`)를 제거하여 백엔드 토큰 송출 즉시 실시간 연결되도록 개선했습니다.
- **[웹소켓 동기식 강제 구독 선점]**: React의 비동기 `setState` 배칭에 의해 `threadId` 상태 갱신이 지연 스케줄링되면서 `useEffect` 웹소켓 구독이 `invoke` API 호출보다 느려지는 레이스 컨디션을 완전히 우회(Short-circuit)하고자, `handleSend` 내에서 `apiClient.threads.invoke` 호출 직전에 `wsClient.subscribeThread(threadId)` 를 동기적으로 100% 강제 선점 실행하도록 강화했습니다.
- **[환경변수 꼬임 대비 기본 포트 백업 복구]**: 클라이언트 빌딩 시점에 `process.env.NEXT_PUBLIC_RUNNER_URL` 이 번들러 환경에 의해 누락되거나 꼬여서 `http://localhost:4300`(레거시 기본 주소)으로 소켓 연결이 헛돌던 현상을 식별하여, `ws-client.ts` 의 백업 fallback URL을 실제 FastAPI 포트인 `http://localhost:28003`으로 전면 수정하여 무조건 정상 소켓 통신을 보장했습니다.
- **[이어쓰기 루프 break 버그 수정]**: `agent.token` 리스너 내부에서 기존 어시스턴트 메시지를 찾아서 토큰을 이어쓰는 탐색 루프 도중 사용자 메시지(`cur.kind === 'user'`)를 만나면 강제로 루프가 끊기는 `break` 버그가 발견되어, 이를 `continue`로 완화하여 어시스턴트 메시지 복원력이 항상 정상적으로 동작하도록 근본 조치했습니다.
- **[깊이 제약 제거]**: `agent.token` 이벤트 리스너 내에서 `depth`가 `0`이 아닌 서브에이전트 스트리밍 토큰들을 차단/무시하던 `if ((d.depth ?? 0) !== 0) return` 필터링 제약 조건을 완전히 제거하였습니다.
- **[디버깅 로그 보강]**: 프론트엔드가 백엔드로부터 `agent.token` 및 `turn.completed` 웹소켓 이벤트를 올바르게 수신하고 있는지 브라우저 상에서 명확하게 추적할 수 있도록 `ClientChatView.tsx` 의 이벤트 리스너 시작부에 디버깅용 `console.log` 문을 추가하였습니다.

### 변경 파일
- `apps/agent-web/src/components/client/ClientChatView.tsx`
- `apps/agent-web/src/lib/ws-client.ts`

---

## 2026-05-25 — siziksu — Agent Client Chat Completed Steps Grouping Card Removal

### 목표
- Agent Client 대화 채팅창(ClientChatView.tsx)에서 ActivityGroupCard 세부 단계를 펼쳤을 때 나타나던 중첩 완료 그룹("PatchToolCallsMiddleware.before_agent 등 4개 단계 완료")을 제거한다.
- 중첩 완료 그룹(CompletedGroupRow)을 제거하고, 세부 단계를 평평하게(flat) 나열하여 가독성을 높인다.
- 4개 작업 완료 및 경과 시간 표시(ActivityGroupCard)는 그대로 유지한다.

### 결과
- `ClientChatView.tsx` 내부에서 연속 완료된 단계를 한 번 더 묶어주는 `groupCompletedSteps` 함수, `CompletedGroupRow` 컴포넌트, `RenderNode` 타입을 제거하였습니다.
- `StepList` 컴포넌트가 `nodes` 개별 항목들을 `StepRow`로 직접 평평하게 매핑하여 렌더링하도록 단순화하였습니다.
- 미사용 임포트가 된 `lucide-react` 의 `Layers` 아이콘을 지워 린트 에러가 발생하지 않도록 조치하였습니다.

### 변경 파일
- `apps/agent-web/src/components/client/ClientChatView.tsx`

---

## 2026-05-25 — siziksu — Debug Chat Step List Render Issue Fix

### 목표
- 디버그 패널의 채팅 탭 화면에서 질문 후 여러 단계를 거쳐 답변을 받은 뒤, 다시 질문하면 단계만 표시되고 최종 답변이 표현되지 않는 버그를 조사하고 수정한다.

### 변경 범위
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - `agent.token` 이벤트 핸들러에서 스트리밍 토큰 누적 조건인 `e.delta` 체크 식을 `e.delta === false ? chunk : (accumulator) + chunk` 형태로 관대하게 개선. 이로써 백엔드의 델타 직렬화 상태나 필드 제공 조건에 상관없이 100% 실시간 스트리밍 애니메이션이 글자 단위로 매끄럽게 누적 표현되도록 근본적으로 조치.
  - `agent.token` 이벤트 핸들러 시작부에 `e.depth === 0` 조건을 두어 최상위 에이전트의 최종 답변 토큰이 하위 디버그용 `tool_call` 카드로 가로채어 흡수되지 않고, 즉시 최하단 AI 말풍선으로 안전하게 스트리밍되어 실시간 노출되도록 보장 (0단계 short-circuit).
  - `agent.token` 이벤트 핸들러의 스트리밍 토큰 누적 인덱스를 마지막 Human 메시지 이후의 AI 답변으로 정확히 가두어 토큰 누적 및 뭉침 유실 예방.
  - `turn.completed` 이벤트 핸들러에서 최종 AI 답변 덮어쓰기 탐색 범위를 마지막 Human 메시지 이후로 한정하여 이전 turn 답변 영역 오염 방지 및 두 번째 답변 정상 노출.
  - `handleRun`의 invoke 동기 응답 fallback 처리 조건(`hasAiMsg`)을 마지막 Human 메시지 이후로 제한하여 두 번째 turn 이후의 백필 차단 현상 해결.

---

## 2026-05-25 — siziksu — Agent Client Chat Step Grouping Card Removal

### 목표
- Agent Client의 대화 채팅창(ClientChatView.tsx)에서 복잡한 실행 단계를 묶어서 표현하던 ActivityGroupCard(예: "PatchToolCallsMiddleware.before_agent 등 10개 단계")의 렌더링 노출을 유지하되, 내부에서 연속 완료된 단계를 다시 한 번 묶어주던 중첩 완료 그룹("PatchToolCallsMiddleware.before_agent 등 10개 단계 완료")을 제거한다.
- 일반 대면 고객이 보는 채팅 화면에서는 상위에 이미 전체 작업 그룹 카드가 있으므로, 굳이 하위 단계를 한 번 더 CompletedGroupRow로 중첩해서 묶을 필요가 없다.
- 하위 단계들을 평평하게(flat) 나열하여 가독성을 높인다.
- 개발자용 디버그 패널(AgentFlowDebugPanel.tsx)의 도구 추적 및 그룹화 기능은 건드리지 않고 그대로 보존한다.

### 결과
- `ClientChatView.tsx` 내의 피드 맵 루프에서 `ActivityGroupCard` 복구.
- `StepList` 컴포넌트 내부에서 `groupCompletedSteps`를 사용한 완료 단계 그룹화(`CompletedGroupRow`로 묶기) 제거.
- 이제 `ActivityGroupCard`를 클릭하여 세부 단계를 펼쳤을 때 모든 단계가 `StepRow`를 통해 평평하게 나열되도록 하여 중복 그룹화 현상 해결.
- 미사용 코드가 된 `groupCompletedSteps` 함수, `CompletedGroupRow` 컴포넌트, `RenderNode` 타입을 말끔하게 지워서 `pnpm lint` 경고 수를 오히려 기존 대비 1개 절감하는(0 errors, 122 warnings) 리팩토링 수행.

### 변경 파일
- `apps/agent-web/src/components/client/ClientChatView.tsx`

---

## 2026-05-25 — siziksu — Debug Chat UI Steps Grouping & UX Improvements

### 목표
디버그 패널의 채팅 탭 화면에 나타나는 에이전트 실행 단계 드롭박스(StepRow)들의 기능 및 UI/UX를 개선한다.
구체적으로:
- 드롭박스(StepRow)를 처음에는 닫힌 상태(`useState(false)`)로 제공하고, 완료 시에 굳이 닫지 않고 열린 상태 그대로 또는 지정한 대로 둔다.
- 완료 상태(`status === 'done'`)일 때 눈에 띄는 테두리 박스 효과(`border border-emerald-500/20 bg-emerald-500/[0.02]`)를 적용한다.
- `task` 성격을 지닌 드롭박스(에이전트/SubAgent)는 하위에 자식 작업이 있으므로, 하위 작업 목록의 개수를 드롭박스 헤더에 표현한다.
- 드롭박스(StepRow) 간의 간격을 조절하여 콤팩트하고 가독성 높게 바꾼다.
- 동일 뎁스에서 연속으로 완료된 여러 드롭박스를 하나의 "완료된 단계 그룹(Completed Group)"으로 합치고 카운트를 표현한다.
- 합쳐진 완료 그룹은 아코디언 형태로 펼치고 접을 수 있게 하여, 펼치면 개별 완료 드롭박스들을 조회할 수 있도록 한다.

### 변경 범위
- `apps/agent-web/src/components/client/ClientChatView.tsx` — `StepRow` 컴포넌트의 상태, CSS 스타일, `task` 및 자식 수 뱃지 추가, 연속 완료 노드 그룹화 로직(`StepList`, `CompletedGroupRow` 컴포넌트) 구현 및 재귀 호출 대체.
- `lucide-react`에서 `Layers` 등 필요한 아이콘 임포트.

---

## 2026-05-25 — siziksu — Agent Builder MCP 인증 모드 표기 개선

### 목표
에이전트 빌더 Tools 탭의 MCP Servers 목록에 등록한 MCP 가 공유(shared) / 개인별(per_user) 인증 모드인지 한눈에 보이도록 배지를 추가한다.
또한 MCP 서버 상세(McpServerDialog) 폼에서 "인증 모드" 섹션을 Transport 항목 위로 이동해, 먼저 인증 방식을 정한 뒤 전송 방식을 고르는 흐름으로 정리한다.

### 변경 범위
- `apps/api/src/modules/mcp/mcp.service.ts` — `listAllTools` select/return 에 `credentialMode` 추가
- `apps/agent-web/src/lib/api-client.ts` — `mcp.listAllTools` 반환 타입에 `credentialMode` 추가
- `apps/agent-web/src/components/agents/builder/ToolsTab.tsx` — `McpServerGroup` 에 `credentialMode` 추가하고 서버 헤더에 "공유"/"개인별" 배지 렌더
- `apps/agent-web/src/components/tools/McpServerDialog.tsx` — 인증 모드 섹션을 Transport 위로 이동

---

## 2026-05-24 — siziksu — MCP 도구 기능 개선 (API 단계 완료)

### API 단계 완료 서브섹션

#### 목표
계획 `tools-mcp-zany-nova.md` §2 API 항목 구현.
- DTO 확장 (credentialMode, requiredUserFields, exposedTools)
- 도구 노출 토글 엔드포인트 신설
- listAllTools onlyExposed 필터링
- PUT /mcp/:id 새 3개 필드 처리
- connect/refreshTools 시 exposedTools 정리
- me-mcp catalog per_user 모드 분류 + fields 메타 포함
- invoke 실행 게이트 (MissingMcpCredential 403)
- runner 페이로드에 userMcpEnvs 동봉
- packages/shared 타입 업데이트

#### 변경/신설 파일
- `packages/shared/src/types/mcp-server.ts` — McpCredentialMode, RequiredUserField, MissingMcpCredentialError 등 신규 타입 추가
- `packages/shared/src/types/agent.ts` — Agent/CreateAgentRequest/UpdateAgentRequest 에 mcpToolRefs 필드 추가
- `apps/api/src/modules/mcp/dto/create-mcp-server.dto.ts` — credentialMode/requiredUserFields/exposedTools 추가, RequiredUserFieldDto 클래스 신설
- `apps/api/src/modules/mcp/dto/update-mcp-server.dto.ts` — 신설 (UpdateMcpServerDto, ToggleToolVisibilityDto)
- `apps/api/src/modules/mcp/mcp.service.ts` — update 새 필드 처리, toggleToolVisibility 신설, listAllTools onlyExposed 필터, connect/refreshTools exposedTools 정리
- `apps/api/src/modules/mcp/mcp.controller.ts` — PATCH /mcp/:id/tools/:toolName/visibility 신설, listAllTools onlyExposed 쿼리 파라미터
- `apps/api/src/modules/me-mcp/me-mcp.service.ts` — per_user 모드 분류, fields 메타(requiredUserFields 우선/env fallback) 반환
- `apps/api/src/modules/threads/threads.service.ts` — checkAndCollectMcpCredentials (실행 게이트 + env 수집), invoke에 게이트 적용 및 userMcpEnvs 포함
- `apps/api/src/modules/agents/agents.service.ts` — create/update/clone/importAgent에 mcpToolRefs 처리 추가

#### 신규 엔드포인트
- `PATCH /mcp/:id/tools/:toolName/visibility` — { exposed: boolean } → exposedTools 토글
- `GET /projects/:projectId/mcp/tools?onlyExposed=true` — 기존 엔드포인트에 쿼리 파라미터 추가

#### 실행 게이트 위치
- `apps/api/src/modules/threads/threads.service.ts` — `invoke` 메서드 내부, runner fetch 직전
  - `checkAndCollectMcpCredentials(agentId, userId)` 호출
  - 누락 시 `ForbiddenException({ error: 'MissingMcpCredential', missingCredentials: [...] })`

#### Runner 페이로드 변경 위치
- `apps/api/src/modules/threads/threads.service.ts` — `invoke` 메서드의 runner fetch body에 `userMcpEnvs` 필드 추가

#### lint 결과
- `apps/api` lint: 기존 warning 3개 (tools.service.ts 미사용 변수) 외 신규 오류 0개 — 통과

---

## 2026-05-24 — siziksu — MCP 도구 기능 개선 (Runner 단계 완료)

### 목표
계획 `tools-mcp-zany-nova.md` §3 Agent Runner 항목 구현.
- MCP 도구 단위 필터링 (`exposed_tools` / `mcpToolRefs`)
- per_user 자격증명 env merge (`userMcpEnvs` 페이로드 수신)
- 페이로드 모델 확장 (`SendMessageBody.userMcpEnvs`)

### Runner 단계 완료

#### 변경 파일
- `apps/agent-runner-py/src/modules/mcp/mcp_client_service.py`
  - `to_structured_tools(server, allowed_tool_names=None)` 옵션 인자 추가.
  - server dict 의 `exposed_tools` / `exposedTools` 비어있지 않으면 해당 이름만 wrapping.
  - `allowed_tool_names` 전달 시 교집합 적용 (sanitize_tool_name 기준 비교).
- `apps/agent-runner-py/src/modules/agents/agents_service.py`
  - `_load_mcp_tools` 에 `tool_refs`, `user_mcp_envs` 인자 추가.
    - `tool_refs` 있으면 서버별 허용 도구 set 구성 → `McpClientService.to_structured_tools` 에 전달.
    - `credential_mode == 'per_user'` 이고 `user_mcp_envs[serverId]` 있으면 config.env 위에 merge.
    - `mcp_servers` 쿼리에 `credential_mode`, `exposed_tools` 컬럼 추가.
  - `load_agent_with_deps` 에 `user_mcp_envs` 인자 추가. DB 쿼리에 `a.mcp_tool_refs` 추가.
    - `mcp_tool_refs` 파싱 후 참조된 서버 ID 만 로드 (하위 호환: null/빈 배열이면 mcp_server_ids 전체).
  - `_build_inline_subagent_specs` 에 `user_mcp_envs` 인자 추가 + 전달.
  - `stream` 함수에 `user_mcp_envs` 인자 추가 + `load_agent_with_deps` 에 전달.
- `apps/agent-runner-py/src/modules/threads/threads_router.py`
  - `SendMessageBody` 에 `userMcpEnvs: dict[str, dict[str, str]] | None = None` 필드 추가.
  - `/invoke` 핸들러에서 `threads_service.send_message(..., user_mcp_envs=body.userMcpEnvs)` 전달.
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `send_message` 에 `user_mcp_envs` 인자 추가 + `agents_service.stream` 에 전달.

#### 보존 항목
- `mcp_router.py` `/list-tools` — 원본 전체 반환 유지 (변경 없음).
- 기존 `exposed_tools=[]` 서버 / `mcpToolRefs` 없는 Agent 는 기존 동작 그대로 (하위 호환).

#### 검증
- smoke import: `uv run python -c "import src.main"` → OK
- 시그니처 검증: 모든 인자 추가/전달 경로 Python 인트로스펙션으로 확인 → ALL OK
- pytest: 모듈 미설치 환경으로 실행 불가 (pyproject.toml 에 pytest 미등록)

---

## 2026-05-24 — siziksu — MCP 도구 기능 개선 (Web 단계 완료)

### 목표
계획 `tools-mcp-zany-nova.md` §4 Web 항목 구현.

### 변경/신설 파일
- `apps/agent-web/src/lib/api-client.ts`
  - `ApiError` 클래스 export (body 필드 추가로 MissingMcpCredential 파싱 지원)
  - `mcp.listAllTools` 시그니처 확장 (`onlyExposed?: boolean`)
  - `mcp.toggleToolVisibility(serverId, toolName, exposed)` 신설 — PATCH /mcp/:id/tools/:toolName/visibility
  - `meMcp.catalog` 응답 타입에 credentialMode/requiredUserFields 추가
  - `meMcp.saveCredential(serverId, fields)` 신설 — POST /me/mcp/credentials
- `apps/agent-web/src/components/tools/McpServerDialog.tsx` (신설)
  - Form/JSON 양방향 탭 다이얼로그 (등록 + 편집 모드 통합)
  - credentialMode 라디오 (shared/per_user)
  - per_user 시 RequiredUserFields 배열 편집기
  - JSON 탭 ↔ Form 탭 전환 시 양방향 동기화, 파싱 실패 시 toast + 전환 차단
- `apps/agent-web/src/components/mcp/McpServerCard.tsx`
  - Edit 버튼 추가 → McpServerDialog 편집 모드 오픈
  - 도구 목록에 Exposed Switch 추가 (shadcn Switch 패턴으로 순수 CSS 구현)
  - exposedTools 빈 배열 = 전체 노출 처리, 토글 시 apiClient.mcp.toggleToolVisibility 호출
- `apps/agent-web/src/app/(dashboard)/tools/page.tsx`
  - McpConnectForm import 제거, McpServerDialog import 추가
  - "Add Server" 버튼 클릭 시 McpServerDialog 렌더
- `apps/agent-web/src/components/agents/builder/ToolsTab.tsx`
  - MCP 섹션을 도구 단위 선택으로 전환 (mcpToolRefs 기반)
  - 서버 헤더 "전체 선택" 체크박스 + 하위 도구별 체크박스
  - apiClient.mcp.listAllTools({ onlyExposed: true }) 로 변경
  - 레거시 mcpServerIds → mcpToolRefs 1회 마이그레이션 effect
  - toolNamesMap에 선택된 MCP 도구만 포함
- `apps/agent-web/src/components/chat/McpCredentialModal.tsx` (신설)
  - 403 MissingMcpCredential 응답 메타를 폼으로 렌더
  - 서버별 섹션, secret 필드는 password 타입
  - 저장 성공 시 onSuccess 콜백 (상위에서 자동 재전송)
- `apps/agent-web/src/components/client/ClientChatView.tsx`
  - ApiError import, McpCredentialModal import
  - mcpCredModal 상태 추가
  - handleSend catch 블록에 MissingMcpCredential 분기 (403 + error === 'MissingMcpCredential')
  - McpCredentialModal 인라인 렌더 + onSuccess 시 handleSendRef로 자동 재전송
- `apps/agent-web/src/components/chat/chat-interface.tsx`
  - ApiError import, McpCredentialModal import, MissingMcpCredentialError import
  - mcpCredModal 상태 추가
  - handleSend catch 블록에 MissingMcpCredential 분기
  - 모달 onSuccess 시 setInput + setTimeout handleSend 재전송
- `apps/agent-web/src/app/(client)/client/tools/page.tsx`
  - McpCatalogRow 타입에 credentialMode, requiredUserFields 추가

### 핵심 UX 결정
- **탭 동기화 실패 처리**: JSON → Form 전환 시 JSON.parse 실패하면 `toast.error`로 경고하고 탭 전환을 막는다 (switchToForm 함수에서 try-catch 처리).
- **도구 노출 토글 낙관적 업데이트**: API 호출 후 서버 응답 없이 exposedTools 배열을 클라이언트에서 직접 갱신, 전체 선택 시 빈 배열로 정규화.
- **재전송 방식**: ClientChatView는 handleSendRef.current로 즉시 재전송, chat-interface는 input 상태 set 후 setTimeout 0ms로 재전송.

### 인라인 모달 진입점
- `ClientChatView.tsx` — `handleSend` 내 catch 블록, `apiClient.threads.invoke` 호출 후 ApiError(403) 감지
- `chat-interface.tsx` — `handleSend` 내 catch 블록, `onSendMessage` prop 호출 후 ApiError(403) 감지

### lint 결과
- `pnpm --filter @agent-studio/web lint`: 0 errors, 102 warnings (기존 warnings 그대로, 신규 에러 없음)

---

## 2026-05-21 — daeyounglee — 첨부 미리보기 확장 (docx/xlsx 인라인, hwp 안내) + 한글 파일명 다운로드 500 fix

### 변경
1. **Runner — 한글 파일명 다운로드 500 fix**
   - 증상: `GET /api/v1/thread-attachments/<id>/content` → `UnicodeEncodeError: 'latin-1' codec can't encode characters` (Korean 파일명). 한글 파일명 첨부는 모두 500, ASCII 파일명만 200.
   - 원인: `Content-Disposition: attachment; filename="자격증사본_홍길동.pdf"` 헤더를 HTTP 헤더 인코딩(latin-1)으로 직렬화 시도.
   - 수정: `apps/agent-runner-py/src/modules/attachments/attachments_router.py` — RFC 5987 듀얼 인코딩 헬퍼 `_content_disposition()` 추가, `filename="<ASCII 폴백>"; filename*=UTF-8''<percent>` 형식으로 응답.
2. **Web — docx/xlsx 인라인 미리보기 + hwp 안내**
   - `mammoth`, `xlsx` 의존성 추가 (dynamic import 로 지연 로드 → 초기 번들 영향 최소).
   - `AttachmentPreviewModal` 확장: 종류 판별을 `image|pdf|text|docx|xlsx|hwp|unsupported` 로 확장.
     - docx: `mammoth.convertToHtml` → 흰 배경 패널에 인라인 렌더.
     - xlsx: `XLSX.read` + `XLSX.utils.sheet_to_html` → 시트별 details 토글 + 표 렌더.
     - hwp/hwpx: 미리보기 미지원 안내 + "다운로드해서 한글 프로그램으로 열거나 채팅에서 물어보세요" 안내. 다운로드 버튼은 blob URL 로 정상 작동.
   - HWP 본문 추출은 runner 에 HWP 파서가 없어 미지원(현 시점). 향후 LibreOffice headless 로 HWP→PDF 변환 후 기존 `gmail_parse_pdf_attachment` 파이프라인 태우는 방향 고려.

### 검증
- Web: 모달 파일 lint 0 errors, `tsc --noEmit` 통과.
- Runner: 한글 파일명 헤더 latin-1 인코딩 smoke test 통과(`attachment; filename="_.pdf"; filename*=UTF-8''%EC%9E%90...`).

---

## 2026-05-21 — daeyounglee — 이메일 첨부 자동 저장 + 화면 미리보기

### 목표
- 에이전트가 Gmail 첨부를 가져왔을 때 사용자가 채팅 화면에서 그 파일을 보고 미리볼 수 있도록 한다.

### 현황 진단
- 화면에 보이는 첨부는 `ThreadAttachment`(사용자 업로드본). 디스크+DB 저장 → 목록·다운로드 + 샌드박스 `/workspace` 동기화.
- 반면 `gmail_fetch_attachment`/`gmail_parse_pdf_attachment` 도구는 다운로드한 바이트를 도구 결과 JSON(base64/추출 텍스트)으로만 반환 → ThreadAttachment 로 저장되지 않아 화면에 안 보임.

### 설계
- 백엔드: 두 Gmail 첨부 도구에 `thread_id` 를 builder→tool 까지 흘려보내고, 다운로드 성공 시 `attachments_service.save_dedup()` 으로 자동 저장. 결과 JSON 에 `threadAttachmentId` 포함. dedup 기준: `(original_name, size)` 일치.
- 프론트: 공용 `AttachmentPreviewModal` (이미지/PDF/텍스트/다운로드 폴백) 신설. ClientChatView·AgentFlowDebugPanel 의 첨부 칩을 클릭 가능하게 만들어 모달 오픈. `turn.completed` 시 `attachments.list` 재조회로 자동 저장된 항목을 즉시 반영. 미리보기는 bearer 인증으로 blob 받아 `URL.createObjectURL` 로 표시.

### 변경 파일
- Runner
  - `apps/agent-runner-py/src/modules/attachments/attachments_service.py` — `save_dedup()` 추가.
  - `apps/agent-runner-py/src/modules/builtin_tools/builtin_tools_service.py` — `get_custom_tools(thread_id=)` 전달.
  - `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/registry.py` — `thread_id` 인자 추가 후 두 첨부 도구 builder 에 전달.
  - `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_fetch_attachment.py` — `thread_id` 인자, 다운로드 성공 시 dedup 저장 + 결과에 `threadAttachmentId`.
  - `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_parse_pdf_attachment.py` — 동일.
- Web
  - `apps/agent-web/src/lib/api-client.ts` — `attachments.fetchBlob()` 추가.
  - `apps/agent-web/src/components/shared/AttachmentPreviewModal.tsx` (신규) — 이미지/PDF/텍스트 미리보기 + 다운로드.
  - `apps/agent-web/src/components/client/ClientChatView.tsx` — `previewAttachment` 상태, 칩 클릭 시 모달, `turn.completed` 시 첨부 목록 재조회.
  - `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx` — 동일.

### 결과
- Web lint 0 errors (102 → 103 — 신규 모달의 effect setState 경고 1건 추가; 코드베이스가 100+건 허용 중인 동일 룰), `tsc --noEmit` 통과.
- Runner ruff: 사전 회귀 6건만 잔존(main 동일), 내 변경으로 새 위반 0건.
- 동작: 에이전트가 첨부를 fetch/parse 하면 자동으로 thread 첨부에 dedup 저장되어 채팅 헤더 영역 칩으로 노출되고, 칩 클릭 시 미리보기 모달이 열린다(이미지/PDF/텍스트/그 외는 다운로드 폴백).

---

## 2026-05-21 — daeyounglee — Agent import 를 skills 패턴으로 헤더 버튼화

### 목표
- Agent 의 import 를 skills 처럼 페이지 헤더의 최상위 Import 버튼으로 노출(현재는 카드별 ⋮ 컨텍스트 메뉴에 묻혀 있음).

### 설계
- 백엔드/`apiClient.agents.importAgent`/`exportAgent` 는 이미 존재 → 프론트 UI 만 정리.
- agents/page.tsx 헤더(agents 탭)에 Import 버튼 + 숨김 file input 추가. JSON 파싱·검증 후 `importAgent` 호출, 목록 prepend, sonner toast 피드백(skills 와 동일 패턴).
- AgentContextMenu 에서 중복·개념상 어색한 Import 항목 제거(import 는 새 agent 생성이라 기존 agent 메뉴에 부적합). Export/Clone/Delete 는 유지.

### 변경 파일
- `apps/agent-web/src/app/(dashboard)/agents/page.tsx`
- `apps/agent-web/src/components/agents/AgentContextMenu.tsx`

---

## 2026-05-21 — daeyounglee — Skills export/import 기능 추가

### 목표
- Skills 페이지에 Skill 을 JSON 으로 내보내기(export)/가져오기(import) 하는 기능 추가.

### 설계
- 포맷: `{ type: "agentstudio.skill", version: 1, skill: { name, description, instructions, allowedTools, enabled, files[] } }`.
- 백엔드: `POST /api/skills/import` 엔드포인트 신설. 이름 충돌 시 자동 리네임(`(1)`,`(2)`...), 파일 포함 트랜잭션 원자 생성, 파일 path 중복 제거.
- 프론트: 카드별 Export(다운로드) 버튼 + 헤더 Import(파일 업로드) 버튼. 파싱은 봉투/단일 형식 모두 관대 처리.

### 변경 파일
- `apps/api/src/modules/skills/dto/import-skill.dto.ts` (신규) — ValidateNested 중첩 파일 DTO.
- `apps/api/src/modules/skills/skills.controller.ts` — `POST /api/skills/import` 추가.
- `apps/api/src/modules/skills/skills.service.ts` — `importSkill()` + `resolveUniqueName()` 추가.
- `apps/agent-web/src/lib/api-client.ts` — `apiClient.skills.import()` 추가.
- `apps/agent-web/src/app/(dashboard)/skills/page.tsx` — 카드 Export(다운로드) 버튼, 헤더 Import 버튼/파일입력, `downloadSkill()`/`handleImportFile()`.

### 결과
- API lint 0 errors(기존 경고 3건 무관), web lint 0 errors, 양쪽 `tsc --noEmit` 통과.
- 검증: 백엔드 import 는 트랜잭션 원자 생성 + 이름 충돌 자동 리네임, 파일 path 중복 제거. 프론트는 봉투/단일 형식 모두 관대 파싱하고 잘못된 JSON·필드는 toast 로 표면화.

---

## 2026-05-21 — leedaeyoung — Client history 페이지 회귀 2건 fix

### 배경 / 회귀 2건
1. **사이드바 thread 클릭 시 main 이 비어 있음** — 사이드바엔 thread 보이지만 클릭해도 ClientChatView 진입 안 됨, fallback("아직 대화 이력이 없습니다") 표시.
2. **thread 클릭 시 수 초간 로딩 hang** — 매 클릭마다 runner 가 `Tavily MCP server running on stdio` / `Context7 MCP Server v2.2.5 running on stdio` 를 반복 spawn.

### 파이프라인 trace (§1-5)
- **(회귀 1) UI staleness**: 사이드바는 `threads.list` fetch + `thread-upserted` window event 두 갈래로 채워지지만, history main 은 `threads.list` 만. ClientChatView 가 chat 첫 메시지 시 dispatch 하는 `thread-upserted` 를 사이드바만 listening → 사이드바엔 thread 있지만 main 의 list state 와 어긋나서 `find()` 매칭 실패.
- **(회귀 2) 단순 history 조회가 graph build 까지 동반**: `apiClient.threads.messages(threadId)` → backend → runner `/api/v1/threads/:id/messages` → `agents_service.get_messages` → **`load_agent_with_deps` → `_load_mcp_tools` 호출로 매 호출마다 stdio MCP 프로세스 spawn**. 그래프 컴파일까지 거치므로 비용이 큼.

### 변경 파일
**(web) staleness fix**
- `apps/agent-web/src/app/(client)/client/history/page.tsx` — list fetch 제거. URL 의 `threadId` 변화 시 `apiClient.threads.get(id)` 로 단독 fetch. 사이드바와의 state 동기화 의존 해소. admin-debug source 가드 유지.

**(runner) graph build 우회**
- `apps/agent-runner-py/src/modules/langgraph/langgraph_service.py` — `get_messages_only(thread_id)` 신설. `checkpoint_service.get_saver()` 의 `aget_tuple(run_config)` 로 LangGraph checkpoint 에서 messages 만 직접 추출. graph build / 도구 로딩 없음.
- `apps/agent-runner-py/src/modules/agents/agents_service.py` — `get_messages(agent_id, thread_id)` 를 `lg.get_messages_only(thread_id)` 한 줄로 단순화.

### 검증
- `pnpm lint` (agent-web) — 0 errors.
- `uvx ruff check` (runner 변경 파일) — 내 변경에 새 에러 없음 (기존 trailing whitespace 4건은 무관).
- 수동 시나리오:
  1. 사이드바에서 thread 클릭 → 즉시 ClientChatView 진입.
  2. runner log 에서 `Tavily MCP server running on stdio` / `Context7 ... MCP Server v2.2.5 running on stdio` 가 history 진입 시 더 이상 찍히지 않음.
  3. messages/runs/attachments 응답이 빠름 (graph build 비용 제거).

### 회귀 주의
- `get_messages` 가 이제 LangGraph checkpoint 만 읽음. 만약 messages 가 checkpoint 가 아닌 별도 저장소에 있다면 누락 가능 — 현 구조는 deepagent/LangGraph 둘 다 같은 postgres checkpoint 를 쓰므로 호환.
- `agent_id` 인자는 시그니처 호환을 위해 유지 (caller 변경 안 함). 향후 caller 정리 시 함께 제거.

---

## 2026-05-21 — leedaeyoung — MVP 발표자료 구성안 작성 (`docs/present/presentation.md`)

### 배경
- `docs/present/mvp-business-plan.docx` (개발 대회 양식) 본문 작성에 앞서, 내부 임원·이해관계자 대상 15-20분 발표용 슬라이드 구성안이 필요.
- 단순 outline 이 아니라 디자이너/발표자가 그대로 PPT 로 옮길 수 있게 슬라이드별 4-필드(제목·핵심메시지·시각자료·말할거리) + 소요시간이 명시되어야 함.
- 후속 docx 본문 작성 시 재활용 가능하도록 DOCX §1~§4 와 슬라이드 매핑을 함께 남김.

### 결정된 사항
- 19 슬라이드 / 약 19-21분 합산. 6 섹션(Opening / 문제인식 / 주요 서비스 내용 / 실현가능성·기대가치 / 성장가능성 / Close).
- 시장 규모·BM 가격대·기대효과 정량 수치 등 출처가 필요한 숫자는 슬라이드에 placeholder 만 남기고 docx 작성 단계에서 확정하기로 명시 (가짜 수치 금지).
- MVP 진행 현황 슬라이드(Slide 12)는 `refactoring-deep-agent-plan.md` §0-1~0-3 잔여 작업을 신호등 표로 그대로 매핑.
- 데모 스토리보드(Slide 9·10)는 `core-features.md` 에 명시된 동작 범위(SubAgent+HITL, Skills×Tools) 내에서만 구성 — 미구현 기능을 동작 중인 것처럼 그리지 않음.
- 6대 핵심 기능(Agent Mode / SubAgent / Skills / Tools / HITL / Todo)을 `core-features.md` 정의 그대로 차용.

### 변경 파일
- `docs/present/presentation.md` — 신규 작성 (발표자료 구성안 19 슬라이드 + 부록 3개 + 진행 가이드 + DOCX 매핑 표).
- `작업일지.md` — 본 엔트리 추가.

### 검증
- 슬라이드 합산 시간 19-21분 (15-20분 + Q&A 별도) — 요청 범위 부합.
- DOCX §1~§4 가 빠짐없이 슬라이드 섹션 2~5 에 1:1 매핑 — 후속 docx 본문 재활용 가능.
- 정량 수치는 모두 "docx 단계에서 확정" 으로 명시되어 사실 오류 위험 없음.

### 후속 변경 (사용자 피드백 반영)
- 사용자 평가: "agent assistant 가 강력한 기능 같다" → 코드 확인. `apps/agent-runner-py/src/modules/agent_assistant/` 자체 모듈로 4-action(clarify/create_agent/edit_agent/answer_directly) + `propose_main_agent`/`propose_sub_agent` 도구 + 카탈로그 안전성(존재하는 id만 사용) 갖춘 메타 에이전트로 확인.
- **헤드라이너 차별화 격상** — Slide 9 신설 ("Agent Assistant: 에이전트를 만드는 에이전트"), 좌(채팅 자연어) / 우(캔버스 자동 생성) 분할 시각 + 4-action 칩.
- 보강 슬라이드: Slide 2(TL;DR) / 6(가치제안 4축) / 7(셀프 빌드 × 통제력 우상단 단독) / 8(6대 기능을 자동 조립하는 메타 레이어로 연결) / 10(Agent Assistant 가 만든 에이전트가 그대로 운영) / 11(Skill 자동 추천) / 12(아키텍처에 `agent_assistant` 모듈 명시) / 13(신호등 표에 Agent Assistant 행) / 14(스택 다이어그램) / 15(구축 리드타임 ↓↓ 주역) / 19(KPI Assistant-Built Ratio 추가).
- 슬라이드 총 19 → 20 장, 시간 19-21분 → 20-22분 (Q&A 별도, 15-20분 요구 부합).
- DOCX 매핑 표·진행 가이드의 슬라이드 번호 일괄 갱신.

### 후속 변경 2 (회의 반영 — 발표자료 전면 재구성, 20 → 25 슬라이드)
- 회의 결과 추가 요청 7건 반영. 코드로 기능 사실 확인 후 작성(추측 금지).
  - **Sandbox 핵심기능 추가** — `FAQ.md` Q4/Q5 + `deepagent_bridge.py:719` `_make_thread_sandbox_backend` 의 `DockerSandbox`(CompositeBackend) 확인. "격리 컨테이너 코드·셸 실행"으로 기술. (FAQ §35 의 "❌ 미구현"은 Modal/Daytona/Deno 외부 샌드박스 한정 — Docker 샌드박스는 동작.)
  - **SubAgent → Multi-Agent Routing 으로 명칭/설명 변경** (S9 매트릭스, S14 시연).
  - **보안 및 통제 슬라이드(S12) 신설** — HITL / I/O 가드레일(`guardrails.py` InputGuardrail·OutputGuardrail) / 도구 권한 / Sandbox 4중 게이트.
  - **사용자 친화적 UI 슬라이드(S11) 신설** — Agent Assistant + 두 대화모드(Deep Autonomous / 계획·실행) + Slack(MCP Tier 1). 계획·실행은 Runner 분기 확장 중이라 "지원 모드"로만 소개·라이브 시연 금지 주의 표기.
  - **시연 시나리오 3개로 재편(S14-16)** — #1 핵심기능(라우팅×HITL×Sandbox) / #2 입사 프로세스 자동화 / #3 Agent Assistant 라이브 빌드(하이라이트).
  - **차별성: wea-flow 비교 슬라이드(S17) 신설** — 사용자 설명("flow 기반 대화 시나리오 솔루션") 반영. 6축 비교표 + "대체 아닌 상위 영역 확장" 톤.
  - **도입부 시장조사 + TAM/SAM/SOM 슬라이드(S6) 신설** — 수치는 예시·placeholder, docx 확정.
- 구조: 6 → 7 섹션, 20 → 25 슬라이드, 시간 ~30분(시연 포함). 20분 압축 버전 가이드(시연 #1·#3만, 부록 제외) 진행 가이드에 추가.
- 모든 신규 수치(TAM/SAM/SOM 등)는 "예시·docx 확정" 라벨 유지 — 가짜 수치 금지 원칙 준수.

### 후속 변경 3 (슬라이드 리뷰 반영 — 기능 매트릭스 정리)
- 사용자가 렌더한 매트릭스 슬라이드(이미지) 리뷰. 어색한 점 3건 지적·반영.
  - **MCP 중복 제거** — Tools 카드가 "HTTP·Code·Search·MCP·Built-in"으로 MCP를 포함하면서 MCP 독립 카드도 있어 중복. Tools는 "내장 도구(HTTP·Code·Search·Built-in)"로, MCP는 "외부 시스템 연동"으로 분리. (MCP는 기술적으로 Tool 유형이나 외부연동 어필 위해 별도 카드 유지.)
  - **이름 규칙 노트 추가** — AgentStudio(제품) / Agent Assistant(자연어 빌더 기능) / "Agent Builder"(시장 카테고리). 한 슬라이드 내 'Agent Builder'·'Agent Assistant' 혼용 금지. Slide 8 본문·카드를 Agent Assistant로 통일.
  - **Slide 8 을 이미지의 6카드 구성으로 동기화** — Multi Agent Routing / Tools / Skills / MCP / 보안 및 통제 / 사용자 친화적 UI. (기존 P0/P1 + HITL/Mode/Todo 개별 카드 구성에서 그룹핑 구성으로 교체.)
  - **"토큰 보호" → "컨텍스트 격리로 비용·정확도 보호"** — 임원 청자에게 모호한 표현 수정.
- 미결: 제품명을 AgentStudio 로 둘지 Agent Builder 로 리브랜딩할지 — Slide 3/14 의 "Agent Builder MVP" 표현은 사용자 확인 후 일괄 정리 예정(narrative 영향 커서 임의 변경 보류).

---

## 2026-05-21 — leedaeyoung — Phase 10-2 회귀 fix: 사용자별 자격증명 override 가 defaultEnv 까지 덮어쓰던 버그 (secretKeys 메타 도입)

### 배경
- client 페이지에서 Slack MCP 전송이 실패한다는 보고. admin 페이지에서는 정상.
- 파이프라인 trace(§1-5) — backend `collectUserCredentials(userId)` 가 user_credentials 테이블을 그대로 dict로 묶어 runner 에 보내고, runner `_load_mcp_tools` 가 사용자별 자격증명(`mcp:<serverId>`)이 있을 때 `config.env` 의 모든 키를 사용자 시크릿으로 덮어쓰고 있음.
- Phase 10-2 에서 카탈로그 `defaultEnv` 로 `SLACK_MCP_ADD_MESSAGE_TOOL=true` 를 자동 주입한 결과, 사용자가 me-tools 에 mcp:slack 자격증명을 등록하면 그 값까지 토큰값으로 덮여서 slack-mcp-server 부팅 시 전송 도구(`conversations_add_message`) 미로드 회귀.
- 즉 1) 7c073c4 의 user_credentials override 로직(`env 전체 덮어쓰기`)과 2) Phase 10-2 의 defaultEnv 충돌.

### 결정된 사항
- 카탈로그 envSchema 의 `secret=true` 키 목록을 `mcp_servers.config.secretKeys` 메타로 명시 저장.
- runner 는 secretKeys 가 있으면 그것만 override, 없으면 (Phase 10-2 이전 등록 호환) secret 패턴 휴리스틱(`*_TOKEN|KEY|SECRET|PASSWORD|CREDENTIAL`)으로 fallback. 패턴 0개면 첫 번째 키 교체.
- SLACK_MCP_ADD_MESSAGE_TOOL 처럼 패턴 매칭 안 되는 키는 자연 보호.
- admin 회귀 보고가 없었던 이유는 추가 trace 가 필요하나(현 가설로는 admin/client 양쪽이 동일해야 함), env override 자체가 명백한 버그이므로 우선 봉합 + 향후 backend log 캡처 추적.

### 변경 파일
- `packages/shared/src/types/mcp-server.ts` — `McpServerConfig` 에 `secretKeys?: string[]` 추가.
- `apps/agent-web/src/components/mcp/McpCatalogPanel.tsx` — `buildCatalogConfig` 가 `entry.envSchema.filter(f => f.secret).map(f => f.key)` 를 `config.secretKeys` 로 저장.
- `apps/agent-runner-py/src/modules/agents/agents_service.py` — `_load_mcp_tools` env override 로직 변경. 모듈 레벨 `_SECRET_KEY_PATTERN` 정규식 신설.

### 검증
- `pnpm --filter @agent-studio/shared build` — 통과.
- `pnpm lint` (agent-web) — 0 errors.
- `uvx ruff check src/modules/agents/agents_service.py` — All checks passed.
- 수동 검증 시나리오(서버 재기동 후):
  1. Slack MCP 를 카탈로그로 새 등록 → `config.secretKeys = ['SLACK_MCP_XOXB_TOKEN']`, `config.env.SLACK_MCP_ADD_MESSAGE_TOOL = 'true'` 저장 확인.
  2. me-tools 에 mcp:slackid 자격증명 등록 → client chat 시 토큰만 사용자 시크릿으로 교체, `SLACK_MCP_ADD_MESSAGE_TOOL` 보존 → 전송 도구 정상 로드.
  3. 기존 등록 서버(secretKeys 누락) → 휴리스틱으로 `SLACK_MCP_XOXB_TOKEN` 만 교체. `SLACK_MCP_ADD_MESSAGE_TOOL` 보존 (이 키 패턴 매칭 안 됨).

### 남은 추적
- "admin chat 만 정상" 이라는 보고의 정확한 원인은 미확정. 같은 user_id 라면 양쪽 모두 동일 user_credentials 가 runner 로 가야 함. 추후 backend logger 로 invoke body 캡처해 비교 필요. 이번 fix 가 봉합되면 양쪽 모두 정상이어야 함.

### 회귀 주의
- 기존 등록 서버의 `_TOKEN`/`_KEY` 가 아닌 secret 키(예: 단순히 `PASSWORD`나 `_AUTH` 로 끝나는 키)는 휴리스틱에 매칭. 추후 명시 secretKeys 마이그레이션 권장.
- 단일 토큰 MCP 에서 키 이름이 패턴에 안 맞으면 첫 번째 키 fallback — 기존 동작과 다를 수 있음. 등록된 MCP 자격증명이 정상 동작하던 흐름은 영향 없을 것(`API_KEY`, `TAVILY_API_KEY`, `SLACK_MCP_XOXB_TOKEN` 모두 패턴 매칭).

---

## 2026-05-21 — leedaeyoung — Phase 10-1 죽은 mcp_server_id 자동 정리

### 배경
MCP 서버를 삭제→재생성하면 에이전트 `mcpServerIds` / `toolPermissions` 에 존재하지 않는 server id 가 남는다. 이번 사이클에 `de362a52`, `5df47df5` 2회 발생, 매번 DB 직접 수정으로 해소함. Tool Access Permissions 패널은 대응 서버가 없어 토글 해제 대상이 없어 UI 로 못 지우는 dead reference.

### 결정된 사항
- UX (a) **저장 시 자동 정리** 선택. 사용자 개입 없음. 회귀 위험 최소.
- 서브에이전트(flow 노드)에도 동일 적용 — 조사 결과 `AgentFlowSettingsPanel` 이 `ToolsTab` 을 그대로 재사용하므로 ToolsTab 한 곳 보강이면 자동 커버. `OrchestrationPanel` 의 mcp 칩 표시는 메인 에이전트의 `mcpServerIds` 가 정리되면 자동 동기화.

### 변경 파일
- `apps/agent-web/src/components/agents/builder/ToolsTab.tsx`
  - `useRef` import 추가.
  - 마운트 후(`loading` 종료) 1회 orphan 검사 useEffect 신설. available 목록(`mcpGroups`)에 없는 id 를 `mcpServerIds` + `mcp:{id}` 형식의 `toolPermissions` 키에서 일괄 제거.
  - `orphanCleanupDoneRef` 로 무한 루프 방지(agent prop 갱신 → 재트리거 차단).

### 검증
- `pnpm lint` (agent-web) — 0 errors.
- 수동 시나리오:
  1. 에이전트가 삭제된 mcp_server_id 를 참조하는 상태로 편집 화면 진입 → 마운트 직후 onChange 호출 → mcpServerIds + toolPermissions 정리.
  2. 저장 후 DB `mcp_server_ids` / `tool_permissions` 에 orphan 잔존 없음.
  3. flow 노드(서브에이전트 편집)에서도 동일 동작 (`ToolsTab` 재사용).
  4. 정상 서버 토글/저장에 회귀 없음.

### 회귀 주의
- 마운트 직후 자동 onChange 가 발화되므로, parent 가 그 변경을 정상 persist 하는지 확인 필요. 부모가 changes 를 무시하면 다음 마운트에서 또 자동 정리 시도 — 멱등하므로 부작용은 없으나 불필요 호출.
- `mcpGroups` 가 fetch 실패로 빈 배열이 되는 경우 모든 mcpServerIds 가 orphan 으로 오인될 수 있음. 단 `listAllTools` 는 빈 배열을 반환할 뿐 에러 throw 안 함 — 운영 중 fetch 실패는 거의 없으므로 수용. 향후 fetch 실패 명시화 시 가드 추가 검토.

---

## 2026-05-21 — leedaeyoung — Phase 10-2 Slack MCP 전송 도구 카탈로그 기본 활성화 + 권한 UI 도구 단위 노출

### 배경
- slack-mcp-server 는 `conversations_add_message` 를 기본 비활성으로 출고하고 `SLACK_MCP_ADD_MESSAGE_TOOL` env 로만 활성화. 카탈로그는 토큰만 받고 있어 UI 등록 시 전송이 항상 꺼진 채 생성됨(직전 세션 DB 직접 수정으로 해소).
- 추가 점검 결과 프론트 `ToolsTab.tsx:585` 가 권한 패널에 MCP 를 **서버 단위(`mcp:{serverId}`)** 키로 직렬화하는데, runner `_apply_tool_policies` 는 도구 이름(`t.name`)으로만 매칭 — 즉 기존 `mcp:` 키는 백엔드에서 매칭되지 않는 dead key.
- 사용자 결정: 옵션 B (a)+(b)+(c) 풀세트 — defaultEnv 자동 주입 + 도구 단위 권한 UI + dead key 정리.

### 결정된 사항
- `SLACK_MCP_ADD_MESSAGE_TOOL=true` 는 사용자에게 노출하지 않는 카탈로그 기본 env 로 항상 주입. 전송 도구가 서버에 로드된 상태에서 에이전트 단위로 토글하는 모델.
- 권한 패널 키 컨벤션: 도구 이름 그대로(예: `conversations_add_message`). runner 가 `tool_permissions.get(t.name, "auto")` 로 매칭하므로 호환.
- 저장 시점에 `mcp:` prefix 형태의 dead key 정리. phase 10-1(삭제된 server id 참조) 와는 별개 문제.

### 변경 파일
**(a) Slack 카탈로그 기본 env 자동 주입**
- `apps/agent-web/src/lib/mcp-catalog.ts` — `McpCatalogEntry` 에 `defaultEnv?: Record<string, string>` 추가, Slack 엔트리에 `SLACK_MCP_ADD_MESSAGE_TOOL: 'true'` 박음. note 문구 보강.
- `apps/agent-web/src/components/mcp/McpCatalogPanel.tsx` — `buildCatalogConfig` 에서 `{ ...entry.defaultEnv, ...env }` 머지.

**(b) 권한 UI 도구 단위 노출 + (c) Dead key 정리**
- `apps/agent-web/src/components/agents/builder/ToolsTab.tsx` — 권한 패널에 MCP 서버 그룹의 개별 도구를 도구 이름 키로 노출, `toolNamesMap` 에 도구 이름 매핑 추가, 저장 시점에 `mcp:` prefix dead key 제거.

**(d) 데이터 인프라 보강 — Phase 10-2 의 전제 조건**
조사 결과 `mcp.service.ts::connect` 가 단순히 status 만 바꾸고 `mcp_servers.tools` JSON 을 채우지 않아 (b) 권한 UI 가 빈 채로 노출됨. 즉 기존 운영 서버 다수가 tools=[] 인 dead-data 상태. 정공법으로 메타 fetch 경로 신설:
- `apps/agent-runner-py/src/modules/mcp/mcp_client_service.py` — `list_tool_meta(server)` 신설. transport 별 ClientSession 으로 `session.list_tools()` 만 받아 name/description/inputSchema 추출.
- `apps/agent-runner-py/src/modules/mcp/mcp_router.py` (신설) — `POST /api/v1/mcp/list-tools` 엔드포인트. `require_api_key` 의존성. backend → runner 단방향 채널.
- `apps/agent-runner-py/src/main.py` — `mcp_router` include.
- `apps/api/src/modules/mcp/mcp.service.ts` — `fetchToolsFromRunner` 헬퍼(30s AbortController timeout) + `connect()` 가 runner 메타 fetch 후 `mcp_servers.tools` 저장, fetch 실패 시 `status='error'`. `refreshTools(id)` 메서드 신설. `listAllTools` 가 빈 tools connected 서버를 발견하면 1회 백필 자동 시도.
- `apps/api/src/modules/mcp/mcp.controller.ts` — `POST /mcp/:id/refresh-tools` 엔드포인트.
- `apps/agent-web/src/lib/api-client.ts` — `apiClient.mcp.refreshTools(id)` 추가 (수동 트리거용).

### 검증
- `pnpm lint` (agent-web) — 0 errors (기존 102 warnings, 내 변경 무관).
- `pnpm lint` (api) — 0 errors (기존 3 warnings, 무관).
- `pnpm build` (api) — 통과 (NestJS 타입체크).
- `uvx ruff check src/modules/mcp/ src/main.py` — All checks passed.
- 수동 검증 시나리오:
  1. 기존 connected MCP 서버 → 에이전트 편집 → Tools 탭 → ToolsTab 마운트 시 `listAllTools` 가 백필 트리거 → 1회 fetch 후 tools 컬럼 채워짐.
  2. 새 Slack MCP 등록 → defaultEnv 로 `SLACK_MCP_ADD_MESSAGE_TOOL=true` 자동 저장 → connect 시 `conversations_add_message` 포함 도구 메타 모두 캐시.
  3. Tool Access Permissions 에 도구 단위 row 표시 (Slack(Bot Token): conversations_add_message 등).
  4. 도구별 policy 변경 → runner `_apply_tool_policies` 가 도구 이름 매칭으로 적용.
  5. 기존 `mcp:{serverId}` 키는 저장 시점에 자동 제거.

### 회귀 주의
- backend 가 runner 미기동 상태에서 connect 호출 시 `status='error'` 로 바뀜. 기존엔 무조건 `connected` 였음 — 이건 의도된 회귀. 사용자가 runner 끄고 MCP 만 운영하던 흐름이 있다면 검토 필요.
- listAllTools 첫 호출 시 빈 tools 인 서버 개수만큼 동시에 stdio 프로세스 spawn. 다수 서버 환경에서 첫 페이지 로드가 느릴 수 있음 — 1회성이라 수용.

---

## 2026-05-20 — daeyounglee — PDF 첨부 400 수정 + MCP 로딩 견고화 + WS 진단로그

### 배경
- studio/Slack에서 PDF 첨부를 분석시키면 `400 invalid_value: 'file'`(messages[*].content[0]) 발생.
- Agent Assistant 응답이 "요청 전송 중…"에서 멈추는 회귀도 함께 진단.

### 원인 (파이프라인 trace, §1-5)
- **PDF 400**: deepagents `read_file`이 PDF(바이너리)를 멀티모달 `type:'file'` content 블록으로 반환 → OpenAI Chat Completions는 `file` content part 미지원(text/refusal/image_url/input_audio만) → 400. (Anthropic/Gemini는 네이티브 지원)
- **Agent Assistant 멈춤**: 백엔드는 정상 emit(`emit_node_proposed`)했으나 `room_subscribers=0` — 세션 race/유휴 재연결로 invoke threadId와 구독 threadId 불일치. 재구독 시 정상(room_subscribers=1) 확인.
- **MCP 견고성**: `_load_mcp_tools`가 서버별 예외를 잡지 않아, Slack MCP 부팅 실패 1건이 `asyncio.gather` 전체를 깨뜨려 tool_count=0.

### 변경 파일
- `apps/agent-runner-py/src/modules/deep/file_content_middleware.py` (신설)
  - deepagents 공식 `awrap_model_call` 훅 미들웨어. provider가 openai일 때만 `request.messages`의 `type:'file'`(PDF) 블록을 pymupdf/pypdf 추출 텍스트로 치환. 비PDF/추출 실패는 execute 파싱 유도 텍스트로 대체. structlog 사용.
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `FileBlockToTextMiddleware` import + `create_deep_agent` 2개 호출부(init/resume) middleware에 추가.
- `apps/agent-runner-py/src/modules/agents/agents_service.py`
  - 모듈 레벨 structlog logger 추가(함수 내부 중복 정의 제거).
  - `_load_mcp_tools`에서 서버별 `to_structured_tools`를 try/except로 격리 — 1개 실패가 나머지 도구 로딩을 막지 않게.
- `apps/agent-runner-py/src/modules/hitl/hitl_gateway.py`
  - `thread.subscribe` 핸들러에 room_size 진단 로그 추가(기존 `emit_node_proposed` room 로깅과 대칭, WS 룸 디버깅용).

### 검증
- ruff(uvx) all checks passed, import/문법 검증 통과, runner reload 정상.
- 동일 에이전트·동일 PDF: 미들웨어 적용 전 400 → 적용 후 정상 요약(status=completed) 재현 확인.

### 별개 이슈 (코드 무관, Slack 앱/설정 — 본 커밋 범위 밖)
- Slack MCP 전송까지 막던 4단계 모두 설정으로 해소: `mpim:read`/`channels:join` 스코프 추가+reinstall, 봇 채널 초대, 에이전트의 죽은 mcp_server_id 갱신(DB), `SLACK_MCP_ADD_MESSAGE_TOOL=true`(DB config).
- follow-up 후보: 프론트에서 죽은 mcp_server_id 제거 가능하게, mcp-catalog Slack 항목에 `SLACK_MCP_ADD_MESSAGE_TOOL` env 필드 추가.

---

## 2026-05-20 — daeyounglee — Slack 봇 응답 오류 수정 (사용자 자격증명 미전달)

### 배경
- Slack에서 `@AgentStudio Agent` 멘션 시 봇이 "응답 생성 중 오류가 발생했습니다. (관리자에게 문의)" 만 반환.
- 파이프라인 trace(§1-5): Slack 멘션 수신 ✓ → API→runner invoke ✓ → runner가 `user_id=None`, `user_credentials_count=0` 으로 DeepAgent 실행 → openai provider 자격증명 미해석 → 502.

### 원인
- `SlackRuntimeService.invokeAgent`가 runner `/threads/{id}/invoke` 호출 시 `{ message }` 만 전송.
- 정상(웹) 경로(`threads.service.ts`)는 `collectUserCredentials(userId)`로 복호화한 `userCredentials` + `userId` + `source` 를 함께 전송함.
- runner는 `userCredentials` dict로만 provider 키를 해석(`deepagent_bridge._resolve_provider_key`)하므로, Slack 경로는 키를 못 찾아 `credential_missing` 발생.

### 변경 파일
- `apps/api/src/modules/slack/slack-runtime.service.ts`
  - `decryptCredential` import 추가.
  - `collectUserCredentials(userId)` private 메서드 추가(threads/agent-assistant 서비스와 동일 패턴).
  - `invokeAgent`에서 `installedByUserId` 로 자격증명 수집 후 `userCredentials` + `userId` + `source: 'slack'` 을 runner body 에 포함.

### 환경 점검(서버 기동)
- `@slack/bolt` 미설치 → `pnpm install`. TS Prisma client 미생성 → `db:generate`. Slack 마이그레이션 3건 미적용 → `db:deploy`.

---

## 2026-05-20 — leedaeyoung — apps/api Slack 모듈 신설 (Socket Mode, MVP 단일 워크스페이스)

### 배경
- Slack 워크스페이스에서 봇 멘션 / DM 수신 시 매핑된 Agent 가 응답하여 Slack 스레드에 게시.
- Socket Mode 사용 (공개 URL 불필요), MVP 단일 워크스페이스 시나리오.
- DB 스키마(`SlackInstallation`, `SlackChannelAgent`)는 이미 적용 완료 상태.

### 변경 파일
- `apps/api/src/modules/slack/` (신설)
  - `slack.module.ts`
  - `slack-installations.controller.ts` — `GET/POST /slack/installations`, `PATCH /:id/enable`, `DELETE /:id`
  - `slack-installations.service.ts` — upsert(auth.test 토큰 검증), get, delete, enable, getWithDecryptedTokens
  - `slack-channel-agents.controller.ts` — `GET/POST/PATCH/DELETE /slack/channel-agents`
  - `slack-channel-agents.service.ts` — CRUD + find(teamId,channelId,mode) 정확 매칭 → both fallback
  - `slack-runtime.service.ts` — Bolt App 부팅/종료, event handler, dedupe(TTL 5분), invokeAgent
  - `dto/create-installation.dto.ts`, `dto/upsert-installation.dto.ts`
  - `dto/create-channel-agent.dto.ts`, `dto/update-channel-agent.dto.ts`
- `apps/api/src/app.module.ts` — `SlackModule` 추가
- `apps/api/package.json` — `@slack/bolt ^4.7.2` 추가

### invoke 응답 형식 확인 결과
- `threads_service.send_message` 는 **동기** 응답: `{ threadId, messages: [{role, content}, ...] }` 반환.
- `slack-runtime.service.ts` 내 `invokeAgent` 에서 messages 배열 역순 탐색으로 마지막 `role=assistant|ai` 텍스트 추출.

### 검증
- `pnpm build` — 통과 (errors 0)
- `pnpm lint` — 0 errors, 3 warnings (모두 기존 `tools.service.ts` 미사용 변수, 이번 범위 외)

### 특이사항
- `@slack/types` 패키지가 bolt peer dependency로 함께 설치되지 않아 직접 최소 인터페이스(`SlackAppMentionEvent`, `SlackGenericMessageEvent`)를 runtime service 내에 정의함.
- `PrismaModule` 이 `@Global()` 이므로 `SlackModule` imports 에 별도 추가 불필요.
- `handleEvent` 파라미터에서 `projectId` 를 제거하고 `mapping.projectId` 를 직접 사용하도록 수정 (lint unused var 해소).

---

## 2026-05-20 — leedaeyoung — MCP 도구 동작 회귀 3건 + Context7 무인증 제외 + sub-agent 답변 숨김

### 1. MCP 도구가 client 에서 동작 안 함 — 회귀 3건 순차 해결 (§1-5 trace)
- **① mcp 패키지 미설치**: runner venv 에 `mcp` 모듈 없음 (`No module named 'mcp'`).
  pyproject 엔 `mcp>=1.0.0` 있으나 venv 미반영 → `uv sync` 로 `mcp==1.27.1` 설치.
- **② 인자 이중 래핑**: `_wrap_mcp_tool` 가 MCP 도구 inputSchema 를 args_schema 로 안 줘서
  langchain 이 `_call(**kwargs)` 에서 단일 'kwargs' 필드 추론 → LLM 인자가
  `{'kwargs': {...}}` 로 감싸져 전달. → inputSchema(JSON Schema dict)를 args_schema 로 전달
  (langchain_core 가 dict args_schema 지원, 실측 확인).
- **③ session 생명주기 (근본)**: `to_structured_tools` 가 `async with stdio_client` 블록
  **안에서** 도구를 만들고 블록 종료 후 반환 → list 시점 session/stdio 가 닫혀 이후
  call_tool 이 `MCP tool error:` (빈 에러) 로 실패. resolve-library-id 가 매번 실패해 LLM 이
  반복 호출하고 query-docs 로 못 넘어감. → list_tools 로 메타만 캡처하고, call_tool 은
  `_call` 안에서 **매 호출마다 새 연결**(`_open_session` + per-call async with)로 수행.
- 검증: `resolve-library-id('next.js')` → `/vercel/next.js` 실제 결과 반환. client 채팅에서
  resolve-library-id → query-docs 2단계 + tavily_search 정상 호출 확인.
- 변경: `mcp_client_service.py` (전면), `agents_service._load_mcp_tools` (config.env +
  user_credentials override 주입 + 호출 체인 user_credentials 전파).

### 2. Context7 무인증(free tier) — 자격증명 대상에서 제외
- Context7 은 `config.env = {}` (API 키 불필요)인데 client "필요한 자격증명"에 "누락"으로
  뜨고 등록 대상에 노출됨.
- `client-agents.deriveRequiredCredentials`: config.env 키가 0개인 mcp 는 required 제외.
- `me-mcp.catalog`: `envKeys.length > 0` 인 mcp 만 노출 (`.filter`). → Tavily 만 남음.

### 3. client 에서 sub-agent 답변 + main 답변 둘 다 노출 → main 만
- `deepagent_bridge` 의 token emit 에 depth 누락 + `agents_service._parse_thinking_stream` 이
  chunk depth 를 안 읽고 파라미터 depth(0) 만 사용 → 모든 토큰 depth=0 → client 의
  `depth !== 0` 필터 무력화.
- → token emit 에 `ui_depth` 추가, `_parse_thinking_stream` 이 chunk depth(`chunk_depth`)를
  읽어 모든 token/reasoning 이벤트에 전파 (loop 0회 대비 초기화). client 는 기존 필터로
  sub-agent 토큰 본문 제외, main 최종 답변만 노출.

### 검증
- `pnpm web/api lint --quiet` → 0 errors, runner `py_compile` OK
- runner 재시작 후 MCP 도구 직접 호출 테스트 통과

---

## 2026-05-20 — leedaeyoung — MCP 사용자별 자격증명 (client) — provider 와 동일 패턴 (B)

### 목표
- client 도구 관리에 Tavily/Context7 같은 MCP 자격증명 등록 UI 가 없음. 배포 agent 의
  "필요한 자격증명"에 MCP 가 "누락"으로 뜨는데 client 가 등록할 곳이 없었음.
- 기존 provider 가 admin(전역 providers) / client(me-providers) 분리 관리되는 것과 동일하게
  MCP 도 admin(mcp_servers) / client(me-credentials kind=mcp) 분리 — 옵션 B.

### 진단 (end-to-end trace)
- `CredentialKind.mcp` enum / `deriveRequiredCredentials` mcp 도출 / me-credentials mcp 저장
  모두 존재.
- **runner `_load_mcp_tools` 가 mcp config 에서 command/args/url/headers 만 읽고 env 를 아예
  안 가져옴** — admin 이 등록한 config.env API key 조차 무시되던 버그.
- client tools 페이지에 MCP 섹션 자체가 없음 (provider/tool 만).

### 변경 — 백엔드
- **신규**: `apps/api/src/modules/me-mcp/{me-mcp.service,controller,module}.ts`
  - `GET /api/me/mcp` — mcp_servers 목록 + config.env 키 힌트(envKeys, 값 미노출) +
    사용자 UserCredential(kind=mcp, targetId=serverId) 매칭. me-providers/me-tools 패턴 복제.
  - `app.module.ts` 에 MeMcpModule 등록.
- credential → runner 전달은 추가 작업 불필요: `threads.service.collectUserCredentials` 가
  이미 kind-agnostic (`${kind}:${targetId}` dict) 이라 `mcp:<serverId>` 자동 포함.

### 변경 — runner
- `agents_service._load_mcp_tools(server_ids, user_credentials)`:
  - config.env 를 base 로 주입 (admin 등록값) — **버그 수정**.
  - user_credentials 의 `mcp:<serverId>` 가 있으면 config.env 의 secret 키 값들을 그 사용자
    값으로 override (멀티테넌트). config.env 비어있으면 API_KEY 키로 fallback 주입.
  - 호출 체인 user_credentials 전파: `stream → load_agent_with_deps → _load_mcp_tools` +
    sub-agent 경로 `_build_inline_subagent_specs → _load_mcp_tools` 까지 시그니처 확장.

### 변경 — frontend
- `api-client.ts`: `meMcp.catalog()` 추가 (`GET /api/me/mcp`).
- `client/tools/page.tsx`: `McpCredentialsSection` 신설 (ToolCredentialsSection 패턴 복제).
  - meMcp.catalog fetch + env 키 힌트 표시 + me-credentials.create(kind:'mcp') 등록/테스트/삭제.
  - ToolCredentialsSection 아래에 렌더.

### 검증
- `pnpm web lint --quiet` / `pnpm api lint --quiet` → 0 errors
- `py_compile` runner → OK
- `GET /api/me/mcp` → 200 (admin 토큰)
- client 채팅 invoke 경로 = `apiClient.threads.invoke(..., 'client')` → threads.service
  collectUserCredentials → `mcp:<serverId>` dict → runner → _load_mcp_tools override. 전 구간 연결.

### 특이사항
- MCP 는 provider 와 달리 env 가 여러 개일 수 있으나, 실용상 단일 secret 으로 받아 config.env
  의 모든 키에 동일 주입 (대부분 단일 API key 서버). 추후 다중 env 필요 시 envSchema 기반
  per-key 입력으로 확장 가능.
- admin 공용 key(config.env) 와 client 개인 key(me-credentials) 가 공존 — client 개인 key 가
  있으면 우선, 없으면 admin 공용값 사용.

---

## 2026-05-20 — leedaeyoung — MCP 수동 추가 모달의 카탈로그 그리드 제거

### 목표
- MCP 탭이 "추천 카탈로그" / "직접 등록" 으로 이미 분리됐는데, "서버 직접 추가" 모달
  (`McpConnectForm`) 안에도 카탈로그 카드 그리드가 또 노출되어 진입점이 3중 중복.
- 모달은 "직접 등록" 전용으로 단순화.

### 변경
- `apps/agent-web/src/components/mcp/McpConnectForm.tsx`:
  - import: `MCP_CATALOG`, `McpCatalogEntry` 제거.
  - state: `catalogId` 제거.
  - 함수: `applyCatalogEntry`, `applyCustomMode`, `currentCatalog` (envSchema 검증 helper)
    제거. handleSubmit 의 envSchema validation 블록도 함께 제거 (카탈로그 항목은 더 이상
    이 모달에서 만들지 않으므로 불필요).
  - JSX: 상단 안내 박스(파란 박스) + "카탈로그에서 시작" 카드 그리드(MCP_CATALOG 카드 +
    직접 입력 카드 + currentCatalog.note) 통째 제거. 짧은 인라인 주석으로 추천 카탈로그
    위치(`Tools → MCP 탭의 "추천 MCP" 영역`) 만 안내.

### 검증
- `pnpm web lint --quiet` → 0 errors

---

## 2026-05-20 — leedaeyoung — MCP 추천 카탈로그 Built-in → MCP 탭으로 일원화

### 목표
- 사용자 지적: Tools 페이지에 이미 MCP 탭이 있는데 Built-in 탭에도 동일한 MCP 카탈로그
  (Tavily/Context7 등) 가 노출되어 혼란.
- 의미가 다른 두 곳에 같은 카드 노출 → MCP 탭으로 일원화 + MCP 탭 내부를 "추천 카탈로그"
  vs "직접 등록" 두 영역으로 명확히 분리.

### 변경
- **신규**: `apps/agent-web/src/components/mcp/McpCatalogPanel.tsx`
  - BuiltinToolsPanel 에서 MCP 카탈로그 로직(MCP_CATALOG 카드 그리드 + 토글 + envSchema
    모달 + apiClient.mcp.create/connect/delete) 전체 추출. props: projectId / servers /
    onServersChange. 카탈로그 항목은 `config.catalogId` 로 식별.
- **정리**: `apps/agent-web/src/components/tools/BuiltinToolsPanel.tsx`
  - import (McpServer, MCP_CATALOG, McpCatalogEntry, ExternalLink), GROUP_ICON.mcp,
    state 8개 (mcpExpanded/Servers/ListLoading/togglingId/ModalEntry/EnvValues/ModalSaving/
    ModalError), useEffect (apiClient.mcp.list), handler 6개 (find/isActive/buildConfig/
    activate/deactivate/toggle/submit), render 영역 + env 모달 통째로 제거. Built-in 탭이
    본래 의미(플랫폼 내장 도구)로 회복.
- **재구성**: `apps/agent-web/src/app/(dashboard)/tools/page.tsx`
  - MCP 탭을 두 영역으로 분리:
    1. 🎯 추천 MCP — `McpCatalogPanel` (사전 정의 카탈로그 토글)
    2. 🔧 직접 등록한 MCP — `config.catalogId` 가 없는 server 만 `McpServerCard` 로 노출.
       비어있으면 "직접 등록한 MCP 가 없습니다 + [서버 직접 추가]" 안내.

### 검증
- `pnpm web lint --quiet` → 0 errors
- Next.js hot-reload 정상

### 특이사항
- 카탈로그 항목 식별 키는 `mcpServer.config.catalogId` — 사전 정의 활성화 시 백엔드가 함께
  저장. 사용자 직접 등록 시에는 빈 값 → 자동으로 "직접 등록" 영역에 표시.
- 양쪽 영역 모두 동일 backend(`apiClient.mcp.*`) 사용 — UI 분리이지 데이터 분리 아님.

---

## 2026-05-19 23:36 — daeyounglee — MCP 카탈로그(Tavily/Context7) Built-in Tools 패널로 통합 + Gmail 스타일 KEY 입력 모달

### 목표
- 사용자가 "MCP 서버도 Travily랑 Context7 있고 활성화 비활성화 할 수 있게" 요청.
- KEY 가 필요한 카탈로그는 Gmail OAuth 연동처럼 활성화 토글 클릭 시 모달로 키 입력받기.
- 기존 MCP Servers 진입점(McpConnectForm) 은 카탈로그 외 임의 MCP 등록용으로만 남김.

### 변경 — UI 통합 (BuiltinToolsPanel 안에서 MCP 카탈로그까지 토글)
- `packages/shared/src/types/mcp-server.ts`:
  - `McpServerConfig` 에 `catalogId?: string` 옵션 필드 추가. UI 카탈로그(`McpCatalogEntry.id`)
    와 DB `mcp_servers` row 매칭용 메타. 백엔드는 단순 JSON 통과.
- `apps/agent-web/src/components/tools/BuiltinToolsPanel.tsx`:
  - `MCP_CATALOG` / `McpCatalogEntry` / `McpServer` import, `GROUP_ICON.mcp = '🔌'`.
  - 새 state: `mcpExpanded`, `mcpServers`, `mcpListLoading`, `togglingMcpId`,
    `mcpModalEntry`, `mcpEnvValues`, `mcpModalSaving`, `mcpModalError`.
  - `useEffect` 로 `apiClient.mcp.list(projectId)` 호출 → 활성 카드 표시.
  - 헬퍼: `findMcpServerByCatalog`, `isCatalogActive`, `buildCatalogConfig`.
  - 핸들러: `handleToggleMcpCatalog` (active → delete, 비활성 + envSchema → 모달, 비활성 +
    빈 envSchema → 즉시 create+connect), `handleActivateCatalog` (create + connect),
    `handleDeactivateCatalog` (delete), `handleSubmitMcpModal` (required env 검증 후 activate).
  - 렌더: 기존 그룹 목록 다음에 "MCP" 그룹 카드 (헤더 + 카드 row + 활성화 토글) — 다른
    그룹과 완전히 동일한 UX.
  - 렌더: KEY 입력 모달 — envSchema 가 있는 카드(Tavily) 활성화 시 표시. 필드별 label /
    필수 표시 / `helpUrl` 발급 페이지 외부 링크 / secret 마스킹 / placeholder 안내.
- `apps/agent-web/src/components/mcp/McpConnectForm.tsx`:
  - 상단에 "Tavily · Context7 는 Tools → Built-in 의 MCP 그룹에서 더 빠르게 활성화할 수
    있다" 안내 배너 추가. form 자체는 카탈로그 외 임의 MCP 등록 진입점으로 유지.

### 동작 흐름
1. Tools → Built-in 탭 → "MCP" 그룹에서 Tavily Search 활성화 클릭.
2. envSchema 존재 → 모달 표시 → `TAVILY_API_KEY` 입력 (발급 페이지 링크 동봉).
3. "활성화" → `mcp.create()` (config 에 `catalogId: 'tavily'`, `env.TAVILY_API_KEY`) →
   `mcp.connect()`.
4. 카드가 "활성화" 초록 표시. 다시 누르면 `mcp.delete()` 비활성화.
5. Context7 는 envSchema 빈 배열 → 클릭 즉시 create+connect.

### 검증
- `pnpm lint` (agent-web): 0 errors. 신규 코드(`mcpExpanded`, `mcpModalEntry` 등) warning 없음.
  기존 BuiltinToolsPanel 의 `agentsLoading` / `findTool` 미사용 warning 은 본 변경 이전부터 존재.
- `pnpm build` (packages/shared): 통과.
- `pnpm lint` (apps/api): 0 errors (기존 tools.service.ts 3 warnings 만 잔존).

### 특이사항 / 향후
- 카탈로그 외 임의 MCP 서버 등록은 여전히 `McpConnectForm` 으로 가능 — 진입점 두 개 유지.
  안내 배너로 사용자에게 새 빠른 흐름을 알림.
- `McpServerConfig.catalogId` 는 frontend 매칭 용도로만 사용 — 백엔드 로직은 변경 없음.
- Tavily 활성화 후 LLM Agent 가 `tavily_search`/`tavily_extract` 등 도구를 자동으로 노출
  받는다 (runner mcp_service 가 MCP tool 을 catalog 에 등록하는 기존 흐름).

---

## 2026-05-19 17:50 — leedaeyoung — Agent Assistant 근본 재설계 (Phase A) + 회귀 다수 차단

### 목표
- Agent Assistant 가 자연어로 agent 의 **생성·수정·메타 답변** 을 모두 처리하는 단일 채팅
  인터페이스가 되도록 재설계.
- "프롬프트·도구·skill 이 안 채워진다" 회귀의 진짜 근본 원인 (frontend stale closure, WS
  이벤트 listen 누락, propose schema 도구·skill 운반 부재 등) 차단.

### 변경 — Agent Assistant 재설계 (deepagents react 루프 → structured output 단일 호출)
- `models.py`:
  - 기존 `AgentDesign` 제거 → `AssistantResponse` 신설 (action 4가지: clarify / create_agent /
    edit_agent / answer_directly).
  - `MainAgentDesign` 에 builtin_tool_ids / db_tool_ids / mcp_server_ids / skill_ids 추가.
  - `AgentEditPatch` 신설 (부분 수정용 partial update).
  - `PendingState` 에 `chat_history` + `user_turn_count` 추가.
- `assistant_prompt.py`: 시스템 프롬프트를 4가지 action 가이드 + target agent 현재 상태
  슬롯 + 카탈로그·history 슬롯 형태로 전면 재작성. few-shot 예시 제거 (오염 회귀).
- `assistant_service.py`:
  - deepagents `create_deep_agent` + tool calling 흐름 폐기.
  - `model.with_structured_output(AssistantResponse)` 단일 호출로 전환.
  - 카탈로그를 백엔드가 prefetch (`_prefetch_catalog`) 해서 system_prompt 에 직접 주입.
  - `_load_target_agent_state()` 추가 — edit_agent / answer_directly 분기에서 LLM 이 참조.
  - action 분기 4가지 처리 + step 이벤트 (assistant.step) emit + fallback summary 자동 합성.
- `assistant_gateway.py`: `emit_step(phase, label, tool_name)` + `emit_edit_proposed(edit)`
  헬퍼 신설. emit_node_proposed/emit_edit_proposed 시 `room_subscribers` 진단 로깅.
- `assistant_tools.py`: propose_main_agent 시그니처에 도구·skill 인자 추가 + 진입 로깅 추가
  (호출 추적용).

### 변경 — Frontend (Agent Assistant UX 통합)
- `use-agent-assistant-store.ts`: `pendingEdit: AgentEditPatch | null` + `stepLabel` 필드,
  `setPendingEdit`/`clearPendingEdit`/`setStepLabel`/`appendAssistantMessage` 액션 신설.
- `AgentAssistantPanel.tsx`:
  - WS 이벤트 `assistant.step`, `assistant.edit_proposed`, `assistant.session_complete` 핸들러.
  - 메시지 박스에 단계별 라벨 (`stepLabel` 또는 "생성 중…") + 점멸 dots.
  - 빈 assistant 메시지는 렌더 skip (LLM 평문 생략 회귀 차단).
  - 적용 카드 (create) / 수정 카드 (edit) 를 panel **하단 sticky 영역** 에 배치 (메시지
    리스트 안 → 가려짐 회귀 차단).
  - 새 props: pendingMainCount/pendingSubCount/onApplyProposal/onCancelProposal/onApplyEdit/
    onCancelEdit.
- `ProposalApplyCard.tsx` 신설: 라디오(replace/merge) + 적용/취소 (기존 ApplyDialog 모달
  대체).
- `EditApplyCard.tsx` 신설: 변경 필드 미리보기 (한국어 라벨) + [변경 적용]/취소.
- `ApplyDialog.tsx` 삭제 (대체됨).
- `page.tsx`:
  - 상단 노란 배너 + ApplyDialog 모달 제거 (panel 카드로 통합).
  - `handleApplyProposed`: stale closure 회피 — `nextMainOverrides`/`nextNodes`/`nextEdges`
    를 함수 내부 변수에 모아 `apiClient.agents.update` 를 직접 호출. setPendingChanges →
    persistAgent 의 useCallback 클로저가 새 값을 못 보는 React 함정 차단.
  - `handleApplyEdit` 신규 — AgentEditPatch 받아 partial UpdateAgentRequest 로 API 호출.
    성공 시 채팅 흐름에 "✅ 변경 사항 적용" 메시지 push.
- `PromptTab.tsx` / `OrchestrationPanel.tsx`: 외부에서 agent.systemPrompt 가 갱신될 때 local
  state 도 sync 하는 `useEffect([agent.systemPrompt])` 추가 — 다른 탭 갔다 와야 보이는 회귀
  차단.
- `MainAgentNode.tsx`: `MainAgentNodeData` 타입에 propose 운반 필드 (systemPrompt,
  builtinToolIds, toolIds, mcpServerIds, skillIds) 추가.
- `ws-client.ts`: 명시 listen 목록에 `assistant.step`, `assistant.edit_proposed` 추가 +
  회귀 방지 주석. (누락 시 backend emit 해도 클라가 dispatch 안 함)

### 변경 — Context7 builtin 도구 등록
- `builtin_tools_service.py`: `_context7_search_libraries` / `_context7_get_docs` 두 함수
  + `_build_all()` 등록. httpx `follow_redirects=True` (301 처리). API key 불필요.
- `apps/api builtin-catalog.ts`: `docs` 그룹 신설 — context7_search_libraries / context7_get_docs
  (requiresConfig=false).
- `apps/agent-web builtin-tool-schemas.ts`: 두 도구의 인자 스키마.
- `apps/agent-web ClientChatView.tsx`: TOOL_LABEL_KO 매핑 + describeToolCall switch case 추가.
- `MCP.md`: §2-2 에 "라이브러리 문서 검색" sub-section + §3-1 비교표 + Context7 vs 다른 검색
  도구 도메인 차이 단락 + §3-4 권장 조합 + §4 자격증명 매핑.

### 변경 — 회귀 차단 (별도)
- `deepagent_bridge.py`:
  - `_patch_anthropic_no_parallel` 의 `model.bind_tools = patched_bind_tools` 가 langchain-
    anthropic 의 Pydantic V2 strict 에 막혀 ValueError → `object.__setattr__` 로 우회.
  - `_serialize_messages` 에서 Anthropic content list 가 `str(list)` 로 변환되어 `[{'type':
    'text', 'text': '...', 'index': 0}]` raw 가 사용자 화면에 노출되는 회귀 → `_flatten_content`
    헬퍼 신설 (text block 추출 + 이어붙임).
  - `stream_with_deepagent` 시점에 LLM 으로 전달되는 resolved_prompt 의 길이/머리/꼬리 +
    bind 되는 tool_count/tool_names 진단 로깅.

### 변경 — 문서
- `AGENTS.md` §1-5 신설 — "회귀 진단 원칙 (디버깅) — 파이프라인 trace 우선".
  - DB·UI 미반영 회귀 시 백엔드→WS→frontend→API→DB 전 단계 trace.
  - 백엔드 로그가 정확하면 LLM 미의심.
  - `await callback()` 직전 `setState` 패턴은 stale closure 1순위 의심.
  - 비용 큰 LLM 반복 시 표면 패치 중단.
- Reinforced Rules Log 표에 2026-05-19 행 추가 (Agent Assistant 디버깅 회고).

### 검증
- `pnpm web lint --quiet` → 0 errors
- `py_compile` runner 전체 → OK
- 시나리오 검증:
  - create_agent: "context7 으로 검색해서 답변하는 agent 만들어줘" → propose →
    [적용] → DB 의 system_prompt(445자), builtin_tool_ids({context7_*,…}) 정상 반영.
  - edit_agent: "system_prompt 끝에 … 추가해줘" → 변경 카드 노출 → [변경 적용] → DB 반영
    + 우측 프롬프트 탭 즉시 갱신 + 채팅에 "✅ 변경 사항을 agent 에 적용했어요" 메시지.
  - answer_directly / clarify 도 동일 LLM 호출 안에서 분기 처리.
  - 디버그 패널: 명확한 라이브러리 질문 ("langchain create_agent 시그니처") → context7
    두 도구 정상 호출 (tool_name=context7_search_libraries → context7_get_docs, step_count
    =12, latency 21s).

### 특이사항 / 후속
- **모델 도구 호출 한계**: strict system_prompt 에도 LLM 이 "X 에 대해 알려줘" 류 모호한
  질문에는 자기 학습 지식만으로 답변하는 경향 — Claude Opus 4.6 에서 관찰. "Y 라이브러리의
  Z 함수 시그니처" 같은 검색 트리거 명확한 질문은 정상 호출. 추후 tool_choice="required"
  강제가 필요한 케이스 별도 옵션 (Phase A 범위 외).
- **deepagents 한계**: `create_deep_agent` 시그니처에 tool_choice 옵션 없음. Agent
  Assistant 메타 흐름은 react 루프 대신 structured output 으로 우회 (현 구현). 다만 만들어진
  agent 의 실제 실행은 여전히 deepagents 사용.
- **Phase B (만들어진 agent 와 채팅 통합)** 는 미진행. 현재 디버그 패널 채팅으로 대체 가능.

---

## 2026-05-19 15:48 — leedaeyoung — chat completions 미지원 모델 카탈로그 disable

### 발견 경위
- Agent Assistant 모델 selector 에서 `gpt-5.5-pro` 로 변경해 시도 → 백엔드에서 OpenAI 404
  `'This is not a chat model and thus not supported in the v1/chat/completions endpoint.'`
- runner 의 `_create_model` 흐름은 `/v1/chat/completions` 만 사용 (deepagents + langchain-openai
  default). gpt-5.5-pro 는 **Responses API 전용** 모델.

### 조사
- WebSearch + WebFetch (`developers.openai.com/api/docs/models/gpt-5.5-pro`,
  `.../gpt-5.4-pro`) 로 endpoint 매트릭스 확인 (2026-05-19 기준):
  - **`gpt-5.5-pro`**: Responses API 전용. chat completions 미지원. streaming 도 미지원.
  - `gpt-5.5`: chat completions + Responses 둘 다 지원.
  - `gpt-5.4-pro`: chat completions + Responses 둘 다 지원 (권장은 Responses).
  - 나머지 (`gpt-5.4`, `gpt-5.4-mini`, `gpt-5.4-nano`, `gpt-5`, `gpt-5-mini`, `gpt-5-nano`,
    `gpt-4.1`): chat completions 지원.
- 결론: **현 시점 chat completions 미지원 모델은 `gpt-5.5-pro` 1개만**.

### 변경
- `UPDATE models SET enabled=false WHERE model_id='gpt-5.5-pro'` 실행 (글로벌 룰: DB DELETE
  는 사용자 명시 승인 필요하므로 enabled=false 만 처리).
- 사용 중인 agent 0개 (체크 결과) → 마이그레이션 불필요.

### 검증
- 활성 OpenAI 모델 9개로 축소: gpt-4.1 / gpt-5 / gpt-5.4 / gpt-5.4-mini / gpt-5.4-nano /
  gpt-5.4-pro / gpt-5.5 / gpt-5-mini / gpt-5-nano.
- frontend 모델 selector 에 `gpt-5.5-pro` 미노출 (enabled=false 필터링).

### 특이사항 / 후속
- Responses API 전용 모델(gpt-5.5-pro, o1-pro 시리즈 등)을 카탈로그에 다시 활성화하려면
  백엔드의 `_create_model` 에 `/v1/responses` 분기 추가 필요 — langchain-openai 의
  `ChatOpenAI(use_responses_api=True)` 옵션 활용. 별도 작업.
- 추후 OpenAI 가 새 reasoning 모델 출시 시 동일 회귀 가능. provider 사이드의 model 메타에
  `supportedEndpoints` 같은 필드 추가해 자동 차단하는 패턴 고려 가능.

---

## 2026-05-19 14:20 — leedaeyoung — Context7 빌트인 도구 등록 + MCP.md 카테고리 보강

### 목표
- MCP.md §2-2 (검색/크롤링) 에 Context7 을 "라이브러리 문서 검색" sub-category 로 분리.
- `builtin_tools_service.py` 에 Context7 REST API 호출 도구 2개 등록 — 라이브러리 검색
  (`context7_search_libraries`) + 문서 조회 (`context7_get_docs`).
- 자격증명 자동 주입 (Phase 8) 은 별건 — Context7 은 free tier 가 무인증이라 즉시 사용 가능.

### 조사 결과 (Context7 HTTP API)
- 공식 REST 엔드포인트 확보 (WebFetch + 실제 호출 검증):
  - **Search**: `GET https://context7.com/api/v2/libs/search?libraryName=<name>&query=<optional>`
    → `results[]` (id, title, description, trustScore, totalSnippets, versions).
  - **Get Context**: `GET https://context7.com/api/v2/context?libraryId=<id>&query=<q>&type=json`
    → `codeSnippets[]` (codeTitle, codeDescription, codeList[{language, code}]) +
    `infoSnippets[]` (infoTitle, infoDescription).
- 인증: free tier 무인증 동작 확인 (실제 curl 성공). rate limit 시 `Authorization: Bearer
  CONTEXT7_API_KEY` 헤더 필요 — 추후 Phase 8 의 `tool:context7` 자격증명 슬롯으로 확장 가능.

### 변경
- **MCP.md**:
  - §2-2 를 a) 일반 웹 검색/크롤링, b) 라이브러리 문서 검색 (Context7) 으로 분리.
  - §3-1 비교표에 Context7 행 추가 (자격증명 불필요/무료).
  - §3-3 에 "Context7 vs 다른 검색 도구 (도메인 차이)" 단락 신설 — 언제 Context7 을 쓰고
    언제 Brave/Tavily/Firecrawl 을 쓰는지 의사결정 기준.
  - §3-4 권장 조합에 "라이브러리 문서: Context7" + "코딩 에이전트" 프리셋 추가.
  - §4 자격증명 매핑 표에 Context7 행 추가 (free tier 무인증).
- **builtin_tools_service.py** (`apps/agent-runner-py/src/modules/builtin_tools/`):
  - `_context7_search_libraries(library_name, query="")`: REST `/api/v2/libs/search` 호출 →
    상위 5개 후보 id/title/trust/snippets 포맷팅.
  - `_context7_get_docs(library_id, query)`: REST `/api/v2/context` 호출 → 코드 스니펫 3개 +
    info 스니펫 2개 포맷팅. code 본문 1500자 truncate, description 400자 truncate.
  - 두 함수 모두 `httpx.AsyncClient(..., follow_redirects=True)` — `/langchain-ai/langchain`
    같은 raw GitHub id 가 `/websites/langchain` 으로 301 redirect 되는 케이스 처리.
  - `_build_all()` dict 에 `context7_search_libraries`, `context7_get_docs` 두 도구 등록.
    serper/brave 와 동일 패턴 (StructuredTool.from_function + lambda).

### 검증
- runner-py 의 uvicorn `--reload` 가 두 번 파일 변경 감지 → 두 번 재기동 → `Application
  startup complete`. 28003 listen 유지.
- 직접 호출 스모크 테스트:
  - `context7_search_libraries('langchain')` → `/websites/langchain` 외 4개 후보 반환.
  - `context7_get_docs('/websites/langchain', 'agent executor invoke')` → "Create and Invoke
    Agent with Tool Search" 등 실제 코드 스니펫 + langchain-py/ts 예제 정상 반환.
- HTTP 301 케이스 (raw `/langchain-ai/langchain`) 도 follow_redirects=True 로 정상 처리.

### 특이사항
- Context7 은 같은 라이브러리도 여러 변형 id 가 있음 (`/websites/langchain`, `/websites/
  langchain_oss`, `/websites/langchain_oss_python_langchain` 등) — 사용자는 search 결과에서
  trust=10 + 가장 specific 한 id 를 선택하는 패턴이 안전.
- 빌트인 search 도구 (serper/brave) 와 달리 Context7 은 free tier 가 무인증이라 Phase 8
  자격증명 자동 주입 회로와 독립적으로 즉시 사용 가능. 추후 사용자 API 키 등록 시 헤더
  추가만 하면 되도록 `_context7_*` 함수 시그니처를 확장하기 쉽게 유지.
- AGENTS.md 의 "deepagents 구현 전 Context7 으로 공식 문서 조회 의무" 규칙도 본 도구로
  agent 가 스스로 만족 가능해짐 — 에이전트가 Context7 을 builtin 으로 호출 가능.

### 다음 단계 (별건)
- Phase 8 (serper/brave 자격증명 자동 주입) 는 여전히 미구현 상태. 본 작업은 별도 흐름.
- Tavily / Exa / Firecrawl 도 동일 패턴으로 빌트인 추가 가능 (자격증명 주입 회로 정착 후).

---

## 2026-05-19 13:34 — leedaeyoung — commit 후 NestJS dev 서버 다운 회귀 근본 차단

### 목표
- 매번 `git commit` 직후 `pnpm dev` 의 NestJS API(28001) 가 listen 못 한 채 좀비
  프로세스로 남는 회귀가 반복됨 (이번 사이클에서도 동일 발생, 어제 동일 증상 1회 재현).
  매번 수동으로 nest 프로세스 kill 후 재시작하던 패턴을 끊고, **다음 commit 부터 자동으로
  유지되도록 근본 차단**.

### 원인
- `lint-staged@16` 의 기본 동작: commit 직전 unstaged 변경을 stash → lint 실행 → stash
  복원. 짧은 시간(수 초) 안에 파일 시스템이 두 번 흔들림.
- `nest start --watch` 는 webpack 기반 watch 라 파일 변경 두 번 = 재컴파일 두 번. 그 사이
  child process kill ↔ 새 listen 사이에 28001 EADDRINUSE race 가 발생하면 새 child 가
  listen 실패한 채 종료. watch 프로세스는 `ps` 에 살아있지만 HTTP 서버는 없음 = 좀비.
- 보조 가속 요인: `nest-cli.json` 의 `deleteOutDir: true` 가 매 컴파일마다 `dist/` 통째
  삭제 → child kill 도중 dist 사라지면서 모듈 로드 깨지는 추가 위험.

### 변경 (3 layer)
- **`.husky/pre-commit`** — `pnpm lint-staged` → `pnpm lint-staged --no-stash`.
  stash backup/restore round-trip 자체를 제거. 우리 lint 룰은 `lint --quiet` (검증만,
  자동 fix 없음) 이라 round-trip 자체가 의미 없는 동작 → 비활성화해도 데이터 손실 위험 0.
  주석으로 정책 명시.
- **`apps/api/nest-cli.json`** — `compilerOptions.deleteOutDir: true → false`. 매 컴파일
  마다 `dist/` 통째 삭제 동작 제거. 증분 빌드만 발생.
- **`apps/api/package.json`** — `predev` 스크립트 추가:
  ```
  "predev": "kill -9 $(lsof -ti:28001 2>/dev/null) 2>/dev/null || true"
  ```
  `pnpm dev` 시작 시 28001 점유 프로세스를 자동 정리. macOS/Linux 호환 (`lsof | kill`
  + `|| true`). 만약 ①·② 모두 우회하는 race 가 또 발생해도 다음 `pnpm dev` 호출에서
  자동 청소되는 안전망.

### 검증
- `pnpm --filter @agent-studio/api build` — 0 errors.
- 이번 commit 자체가 검증: `git commit` + `git push` 직후 28001 정상 listen, 로그인
  엔드포인트 HTTP 200 (이전 사이클에서는 동일 시점에 HTTP 000 으로 떨어졌음).
- lint-staged 가 `--no-stash` 인식 (출력: "Skipping backup because `--no-stash` was used").

### 특이사항 / 후속
- 위 3 layer 로 95%+ 차단 예상. 만약 그래도 발생하면 다음 단계는 `nest start --watch` 를
  `nodemon --delay 1500ms` 또는 `tsc-watch` 로 교체하여 디바운스 강화. 현 단계에선 표준
  NestJS dev 패턴을 유지하면서 가장 적은 변경으로 해결한 것을 선택.
- `--no-stash` 의 위험은 lint 가 파일을 자동 수정하는 룰을 추가하면 다시 활성화 검토 필요.
  현 상태에선 `lint --quiet` 검증만 하므로 안전.

---

## 2026-05-19 13:19 — leedaeyoung — current_time 도구 default 자동 포함 + 기존 agent 일괄 적용

### 목표
- `current_time` 은 자격증명 불필요·비용 0·LLM 학습 cutoff 보완(시간 추론 정확도) 이라
  대부분의 에이전트에서 잠재적으로 유용한 기본 유틸리티.
- 매번 사용자가 활성화하도록 두기보다 신규 agent 생성 시 자동으로 포함하고, 기존 agent
  들에도 일괄 추가해 시간 관련 질문(예: "오늘 며칠?")에서 LLM 이 추측 대신 실제 시각을
  답하도록 한다.

### 변경
- **`apps/api/src/modules/agents/agents.service.ts`**
  - 파일 상단에 `DEFAULT_BUILTIN_TOOL_IDS = ['current_time']` 상수 + `mergeDefaultBuiltins()`
    헬퍼 신설. 누락 시에만 추가, 이미 있으면 Set 중복 제거.
  - `AgentsService.create()` 의 `builtinToolIds: dto.builtinToolIds || []` →
    `mergeDefaultBuiltins(dto.builtinToolIds)` 로 교체.
  - `clone()` 은 source 의 `builtinToolIds` 를 그대로 복사하므로 자동 전파 — 별도 수정 X.
- **기존 agent 일괄 마이그레이션** (DB 직접 SQL, 트랜잭션)
  ```sql
  UPDATE agents
     SET builtin_tool_ids = array_append(builtin_tool_ids, 'current_time')
   WHERE NOT ('current_time' = ANY(builtin_tool_ids));
  ```
  - 적용 전: 12개 중 1개만 보유 / 11개 누락.
  - 적용 후: 12개 모두 보유. 기존 `gmail_fetch` / `gmail_search` / `execute` 등 다른
    builtin 도구는 보존 (array_append 라 중복 없이 누락된 행에만 추가).

### 검증
- `pnpm --filter @agent-studio/api build` — 0 errors.
- DB: `SELECT COUNT(*) FILTER (WHERE 'current_time' = ANY(builtin_tool_ids))` = 12 / 12.
- 신규 agent 생성 시 dto 에 `current_time` 미포함이어도 저장된 `builtinToolIds` 에 자동
  포함됨 (NestJS 단위 검증 — create 메서드 인자→DB row 매핑).

### 특이사항
- "사용자가 끄고 싶다" 케이스 — settings 에서 builtinToolIds 토글 해제로 가능. 강제 유지
  하지 않음(추가만).
- import 흐름(`/agents/import` JSON)은 외부에서 가져온 builtinToolIds 를 그대로 유지하도록
  현 상태 유지. 필요 시 별도 정책 결정.
- 시계 정확성은 runner Docker 컨테이너의 호스트 OS NTP 동기화에 의존. 운영상 의미 있는
  오차 발생 가능성 낮음.

---

## 2026-05-19 13:04 — leedaeyoung — Built-in Tools 페이지 정리 (Utilities + Document Tools)

### 목표
- Tools 페이지에서 `document_preprocess` / `pdf_parse` 두 도구가 **실효성 없는데도**
  활성화 토글로 노출되던 UX 결함을 정리.
  - runner `custom_tools/registry.py` 정책: 두 도구는 LLM 직접 호출 대상이 아니라
    `gmail_parse_pdf_attachment` 내부 파이프라인에서만 호출되는 내부 전용 함수.
  - 그런데 NestJS builtin-catalog 의 `utilities` 그룹에 등재 + 프론트엔드에 별도
    "Document Tools" 하드코딩 섹션까지 있어 **같은 두 도구가 두 곳에서 이중 노출** + 토글
    무효 상태.

### 변경
- **`apps/api/src/modules/tools/builtin-catalog.ts`** — `utilities` 그룹에서
  `document_preprocess`, `pdf_parse` 두 항목 제거. 그룹은 `current_time` 하나만 남김.
  주석으로 "registry 정책상 LLM 노출 X, gmail_parse_pdf_attachment 내부에서만 호출, 향후
  일반 PDF 입력 흐름으로 노출하려면 registry 등록만 추가하면 됨" 을 명시.
- **`apps/agent-web/src/components/tools/BuiltinToolsPanel.tsx`** — 하드코딩된 "Document
  Tools" 섹션(약 104 줄) 전체 제거. 동시 안에 있던 "규칙 설정" (PdfParseRulesPanel 진입)
  버튼도 함께 제거. `PdfParseRulesPanel` 모달 컴포넌트 자체와 `pdfRulesOpen` 모달 분기는
  보존 — 향후 별도 진입점(예: 개별 agent settings)에서 재사용 가능하도록.

### 검증
- `pnpm exec tsc --noEmit` (agent-web) — 0 errors.
- `pnpm --filter @agent-studio/api build` — 0 errors.
- 카탈로그 변경 후 runner 영향 없음 (runner 는 `Agent.builtinToolIds` 에 들어온 slug 만
  필터링하므로 카탈로그에서 빠져도 기존 활성화 데이터는 무해하게 무시).

### 특이사항 / 향후 후속
- `Agent.builtinToolIds` 에 `document_preprocess` / `pdf_parse` 가 남아있는 에이전트는
  그대로 두어도 무방 (runner `BuiltinToolsService.get_tools(names)` 가 매칭 안 되면 그냥
  제외하고 빈 슬롯 처리). 정리가 필요하면 DB 마이그레이션으로 일괄 제거 가능.
- PDF 규칙 설정 UI (`PdfParseRulesPanel`) 의 새 진입점이 필요. 자연스러운 후보는
  agent settings 패널 안의 "도구 / Gmail / PDF 규칙" 탭. 현재는 모달을 열 트리거가
  없어 사실상 비활성 — 향후 plan.md Phase 로 분리해 진입점 재구성 검토.
- `PdfParseRulesPanel` 의 검증 규칙(`CheckRule[]`) 은 저장만 되고 runner 에서 호출되지 않는
  미구현 상태(이전 작업일지 12:15 항목 + `pdf_parse._fetch_identify_keywords` 코멘트 참고).
  Document Tools UI 정리와는 별개로 별도 Phase 검토 필요.

---

## 2026-05-19 12:15 — leedaeyoung — Built-in 검색 도구 자격증명 누락 진단 + 인기 MCP 카탈로그 조사

### 목표
- "Tools 페이지의 Built-in 도구가 다 이용 가능한가?" 질문에서 출발 — Built-in 카탈로그
  13개 중 자격증명 필요 도구의 실제 실행 가능 여부 정밀 확인.
- "사람들이 많이 사용하는 MCP들" 조사 — Agent Studio 에 추천 등록할 후보 정리.
- 두 조사 결과를 향후 작업 계획(`plan.md`)에 Phase 로 등록.

### 조사 결과

**1. Built-in 검색 도구의 api_key 주입 누락 (확인)**
- `apps/agent-runner-py/src/modules/builtin_tools/builtin_tools_service.py:69-78` —
  `serper_search` / `brave_search` 의 lambda 가 `api_key=""` 기본값으로 묶여 있고,
  `_build_all` / `BuiltinToolsService.get_tools` 어디에도 `user_credentials` 를 받아
  api_key 를 미리 inject 하는 코드가 없음.
- `apps/api/src/modules/client-agents/tool-credential-map.ts:20-31` — 자격증명 매핑
  (`serper_search → targetId:'serper'`, `brave_search → targetId:'brave'`) 은 NestJS
  측에 이미 정의되어 있고 me-tools 도 정상 노출. 따라서 사용자가 UI 에서 키를 등록해도
  runner 가 그 키를 도구 함수에 전달하지 않아 호출이 401/404 로 떨어진다.
- 대조: Gmail 5종은 `get_custom_tools(agent_id, user_id, source)` 로 OAuth 토큰을
  fetch·inject 하는 흐름이 살아있어 정상 동작. search 만 같은 인프라가 빠진 상태.

**2. 인기 MCP 서버 카탈로그 (2026-05-19 기준)**
- 공식 reference (`modelcontextprotocol/servers`) — filesystem, fetch, git, memory,
  sequential-thinking, time, everything (7개). DB·SaaS류는 외부로 이전.
- 실사용 우선순위 (Tier 1) — github / gitlab / sentry, brave-search / tavily / exa /
  firecrawl / puppeteer / playwright, postgres / sqlite / mongodb / redis, slack /
  linear / notion / jira / gmail / google-calendar / google-drive, figma, cloudflare /
  vercel / aws, stripe / supabase, e2b. (출처: 공식 레포 + WebFetch + 일반 awareness)
- 자격증명 매핑은 `tool-credential-map.ts` 의 슬롯 구조(`tool:<slug>` / `oauth:<slug>`)
  를 재사용해 일관 처리 가능.

### 결정 / 다음 단계
- 코드 변경은 이번 사이클에서 진행하지 않고 **`plan.md` 에 2개 Phase 신설**.
  - **Phase 8** — Built-in 검색 도구 자격증명 자동 주입. `_build_all` 시그니처 확장 +
    lambda 빌드 시점에 api_key 캡처 + `credential_missing` 명시 emit.
  - **Phase 9** — 인기 MCP 사전 등록 카탈로그(`mcp-catalog.ts`) + 원클릭 install
    엔드포인트 + 필요 자격증명 슬롯 자동 생성.
- Phase 8 의 search 도구 패턴(`tool:{slug}` lookup)이 정착되면 Phase 9 의 MCP 도구에도
  동일 패턴을 확장 적용한다.

### 변경 파일
- `plan.md` — Phase 8 / Phase 9 항목 신설 (목적·결정사항·구현 범위·엣지·검증·커밋 분할안).
- 작업일지(본 항목) — 조사 결과 + 결정 기록.

### 특이사항
- search 도구는 "활성화만 되고 안 됨" 상태가 사용자에게 가시화 안 됨 (단순히 빈 응답이
  돌아오거나 401 로 떨어짐) — Phase 8 의 `credential_missing` emit 으로 명시적 안내가
  들어가야 UX 일관성 회복.
- 공식 MCP 레포가 reference 7개로 축소되고 DB/SaaS 류가 외부로 이전된 흐름 → 우리
  카탈로그는 외부 third-party 레포 URL 까지 기재해야 사용자에게 출처 노출 가능.
- 환경변수 fallback (`SERPER_API_KEY` 등) 은 추가하지 않는다 — Phase 10-7 의 "사용자별
  자격증명" 원칙과 충돌. 이 결정은 Phase 8 본문에도 명시.

---

## 2026-05-19 09:30 — leedaeyoung — OpenAI 모델 카탈로그 정리 (Frontier 전체만 유지)

### 목표
- OpenAI 공식 페이지(https://developers.openai.com/api/docs/models/all, 2026-05-19 확인) 기준으로 DB `models` 테이블의 OpenAI provider를 정리한다.
- Frontier 카테고리 10개만 유지, Deep research 2개는 페이지에 (Deprecated) 표시되어 있어 제외, 그 외 모델은 완전 삭제.
- 비활성 모델을 사용 중인 agent는 frontier 동급 모델로 자동 마이그레이션.

### 변경
- **신규 스크립트**: `packages/database/prisma/update-frontier-models.ts` — upsert(enabled=true) + 그 외 enabled=false 처리. DELETE는 사용자 명시 승인 단계에서만 수동 SQL로 실행.
- **OpenAI 모델 신규/갱신** (12개 → 최종 10개):
  - 신규 INSERT: `gpt-5.5`($5/$30), `gpt-5.5-pro`($30/$180), `o3-deep-research`($5/$20)*, `o4-mini-deep-research`($1/$4)*
  - 유지(enabled 갱신): `gpt-5.4-pro`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.4-nano`, `gpt-5`, `gpt-5-mini`, `gpt-5-nano`, `gpt-4.1`
  - *deep research 2개는 deprecated 라 같은 세션에서 추가 DELETE.
- **Agent 마이그레이션**: 9개 agent (`gpt-4o-mini` 사용 중) → `gpt-5-mini` 로 정규화 UPDATE. agents.model_id 컬럼이 UUID/slug 두 형식 혼재 → 모두 slug 형식으로 통일.
- **DELETE 대상** (총 14개): gpt-5.2 / gpt-5.2-pro / gpt-5.1 / gpt-5-pro / gpt-4o / gpt-4o-mini / gpt-4.1-mini / gpt-4.1-nano / o1 / o3 / o3-mini / o4-mini / o3-deep-research / o4-mini-deep-research.

### 검증
- 최종 OpenAI 모델 10개 (Frontier 전체)만 DB에 존재 — gpt-5.4-pro, gpt-5.5-pro, gpt-5.5, gpt-5.4, gpt-5, gpt-4.1, gpt-5.4-mini, gpt-5-mini, gpt-5.4-nano, gpt-5-nano.
- Anthropic 10개, Google 13개는 무변경.
- 9개 agent 모두 `gpt-5-mini` 로 정상 매핑 — `SELECT COUNT(*) WHERE model_id='gpt-5-mini'` = 9.

### 특이사항
- `Never perform database deletion operations without explicit user approval` (글로벌 CLAUDE.md) 원칙 — 1차 작업은 enabled=false 만, DELETE는 사용자가 "완전 삭제해줘" 명시 후 별도 트랜잭션으로 실행.
- `Agent.modelId` 가 model UUID 와 model_id slug 두 형식을 모두 허용 (runner 의 `_resolve_actual_model_id` 가 두 형식 다 지원) — 마이그레이션 시 양쪽 케이스 모두 UPDATE 처리.
- OpenAI 신규 모델 가격은 https://developers.openai.com/api/docs/pricing 기준. gpt-5 라인 일부 가격은 페이지 미명시 → 기존 등록값 유지.
- o3 / o3-pro 는 "More models" 카테고리로 강등 — OpenAI 가 명시적으로 "succeeded by GPT-5" 라 표기. plan 능력은 강하지만 tool-calling 안정성·streaming·temperature 제약으로 Agent Assistant 메타 에이전트 용도에는 frontier(GPT-5.4/5.5) 가 더 적합.
- 스크립트 파일(`update-frontier-models.ts`)은 추후 다른 환경(staging/prod)에서도 재현 가능하도록 idempotent (upsert + 비활성→삭제 분리).

---

## 2026-05-18 — leedaeyoung — Agent Assistant (시나리오 → main+sub 자동 설계) 구현 착수·완료

### 목표
- Agent Builder 페이지에 "AGENT ASSISTANT" 채팅 패널을 추가해 사용자가 시나리오를 입력하면 메타 LLM이 main agent + sub agents 구조를 설계·도구/스킬/프롬프트를 자동 채워주고, 모호한 부분은 채팅으로 다시 질문하며, 결과를 캔버스에 점선 미리보기로 띄운 뒤 "적용" 시 `Agent.graphDefinition`에 저장한다.
- 메타 에이전트는 `apps/agent-runner-py/src/modules/agent_assistant/`에 신설하여 deepagents `create_deep_agent`로 실행.
- HITL 카드 대신 일반 채팅 메시지로 명확화 — 동일 thread history에 누적.
- Assistant thread는 `metadata.kind='assistant'`로 마킹해 일반 스레드 목록/디버그 패널에서 숨김.
- 적용 시 매번 사용자에게 "대체 / 병합" 다이얼로그 노출.
- 상세 플랜: `/Users/leedaeyoung/.claude/plans/image-5-wild-lampson.md`

### 변경 (신규)
- `packages/shared/src/types/hitl.ts` — `AssistantNodeProposedEvent`, `AssistantSessionCompleteEvent` 추가, `WebSocketEvent` 유니온 확장.
- `apps/agent-runner-py/src/modules/agent_assistant/`
  - `models.py` — Pydantic 모델 (CreateSessionBody/Response, InvokeBody, ProposedMain/SubAgentArgs, PendingState)
  - `assistant_prompt.py` — `SYSTEM_PROMPT` + `build_system_prompt(graph_summary)`
  - `assistant_gateway.py` — `emit_node_proposed`, `emit_session_complete` (기존 `hitl_gateway.sio` 재사용)
  - `assistant_tools.py` — `build_assistant_tools()` 팩토리: `list_available_tools`(builtin+DB+MCP+skill 카탈로그), `propose_main_agent`, `propose_sub_agent`
  - `assistant_service.py` — `create_session` (thread metadata.kind='assistant'), `stream` (deepagents `create_deep_agent` + `astream_events` 단순 루프)
  - `assistant_router.py` — `POST /agent-assistant/sessions`, `POST /agent-assistant/sessions/{thread_id}/invoke` (BackgroundTasks 즉시 202)
- `apps/api/src/modules/agent-assistant/`
  - `agent-assistant.module.ts`, `agent-assistant.controller.ts`, `agent-assistant.service.ts`
  - NestJS JWT 가드 + `RUNNER_URL` 프록시 + `collectUserCredentials` 패턴 동일 복제
- `apps/agent-web/src/stores/use-agent-assistant-store.ts` — Zustand store (messages, pendingNodes/Edges, isStreaming, currentStep, startSession/sendMessage/addProposedNode/setSessionComplete)
- `apps/agent-web/src/components/agents/assistant/`
  - `AgentAssistantPanel.tsx` — 하단 채팅창 (collapsible, 모델 드롭다운, 메시지 리스트, 입력창, WS 구독)
  - `AssistantGenerationLog.tsx` — 우측 "생성 로그" 패널 (요청 파싱 → 명확화 → 구조 설계 → 도구 매핑 → 완료)
  - `ApplyDialog.tsx` — "대체 / 병합" 모드 선택 다이얼로그

### 변경 (수정)
- `apps/agent-runner-py/src/main.py` — `agent_assistant_router` `include_router` 등록.
- `apps/api/src/app.module.ts` — `AgentAssistantModule` imports 추가.
- `apps/api/src/modules/threads/threads.service.ts` — `list()` Prisma `where`에 `NOT: { metadata: { path: ['kind'], equals: 'assistant' } }` 필터 — assistant thread 숨김.
- `apps/agent-web/src/lib/ws-client.ts` — `eventTypes`에 `assistant.node_proposed`, `assistant.session_complete` 추가.
- `apps/agent-web/src/lib/api-client.ts` — `agentAssistant.createSession/invoke` 메서드 (클래스 내부).
- `apps/agent-web/src/components/agents/flow/SubAgentNode.tsx` — `SubAgentStatus`에 `'proposed'` 추가, 점선 테두리 + opacity-60 스타일.
- `apps/agent-web/src/components/agents/flow/MainAgentNode.tsx` — `proposed?: boolean` 플래그 + 동일 점선 스타일.
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/page.tsx`
  - `unionNodes`/`unionEdges` 계산으로 저장된 nodes 위에 pendingNodes/Edges proposed 미리보기 덧씌움.
  - 상단 amber 알림 바: "Assistant가 Main N · Sub M 제안" + 취소/적용 버튼.
  - 우측 9px 사이드바: "생성 로그" 토글 버튼(ListTree 아이콘).
  - 하단: `AgentAssistantPanel` 마운트(접기/펼치기).
  - `handleApplyProposed(mode)` — replace: 기존 노드를 제안 구조로 교체 / merge: 기존 main 유지하고 sub만 추가.

### 검증
- `pnpm --filter @agent-studio/shared build` — 성공.
- `pnpm --filter @agent-studio/api build` — 성공 (TS 0 errors).
- `pnpm exec tsc --noEmit` (agent-web) — 0 errors.
- `pnpm --filter @agent-studio/web lint` — 0 errors / 95 warnings (모두 사전 baseline).
- `uv run ruff check src/modules/agent_assistant/` — 7 N815 (Pydantic JSON 매핑 의도, 기존 `threads_router` 동일 패턴) 외 0.
- runner 라우터 등록 확인: `POST /agent-assistant/sessions`, `POST /agent-assistant/sessions/{thread_id}/invoke`.

### 특이사항
- `Thread.metadata`는 이미 `Json @default("{}")` (schema.prisma:915) — 마이그레이션 불필요.
- 메타 에이전트는 `subagents=None`으로 `create_deep_agent` 호출 — 단일 ReAct 모델로 동작 (서브에이전트 시스템 미사용).
- `ask_user` 도구를 만들지 않고 시스템 프롬프트로 "모호하면 평문 메시지로 질문" 지시 — 사용자 답변이 다음 invoke 메시지로 자연스럽게 history에 누적되어 LLM이 이어감.
- 모델 기본값 `anthropic:claude-opus-4-7` (plan/reasoning 강함). 사용자가 패널 헤더 드롭다운에서 변경 가능.
- pending state는 프론트 메모리(Zustand)에 휘발 — 새로고침 시 사라짐(사용자 결정 #4와 정합).
- "적용" 시 매번 다이얼로그(대체/병합) — `replace` 모드는 main도 제안 데이터로 갱신하면서 pendingChanges에 name/architecture/systemPrompt 반영.

### 다음 검증 (수동 E2E)
1. `pnpm dev` 후 `/agents/<agentId>` 접속.
2. 하단 AGENT ASSISTANT에 "이력서 분석 → 면접 질문 생성 agent" 입력.
3. 우측 ListTree 아이콘 → "생성 로그" 패널 열어 단계 진행 확인.
4. 캔버스에 점선 main + sub 노드 등장.
5. 상단 amber 바의 "적용" → "대체" 선택 → 점선이 실선으로 전환되고 DB graphDefinition에 저장.
6. 일반 채팅 스레드 목록에 assistant thread가 노출되지 않는지 확인.

---

## 2026-05-15

### [완료] HITL edit 시 SubAgent ValidationError 후 인자 회귀 버그 수정

**작성자**: leedaeyoung

**증상** (`/client/agents/mail-manager/chat`)
- 사용자 메시지: "최근 메일 하나 조회해줘".
- 첫 HITL 인터셉트(`gmail_search(q='', max_results=1)`)에서 사용자가 "3개 조회"로 수정 → preview_edit 가 `{q:'', max_results:3}` + task description 자연어 재작성.
- resume 후 `gmail_search(q='', max_results=3)` 실행 → `q` min_length=1 위반으로 ValidationError.
- SubAgent LLM 이 retry 하면서 `max_results` 가 **1 로 회귀** → 최종적으로 1 건만 검색됨.
- 사용자는 도구 실행 요청 다이얼로그가 3개→3개→1개 순으로 떴다고 보고.

**원인**
- `deepagents/middleware/subagents.py::_validate_and_prepare_state` 가 `task` 도구 실행 시 SubAgent 의 초기 state 를 `messages=[HumanMessage(content=description)]` 로 만들어 체크포인트에 저장.
- 기존 `resume_stream_with_deepagent` 의 `task_description_update` 로직은 **부모 그래프의 task tool_call.args.description 만** 갱신했음.
- SubAgent 는 이미 invoke 되어 자체 state 에 `HumanMessage("최근 메일 하나 조회...")` 가 박혀 있었고, ValidationError 로 LLM 이 retry 할 때 그 HumanMessage 의 "하나" 를 기준으로 `max_results=1` 을 다시 산출.
- 즉 부모만 갱신한 것은 SubAgent 입장에서 무의미 → edit 결과가 retry 시점에 사실상 휘발됨.

**변경**
1. `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py::resume_stream_with_deepagent`
   - 부모 `task` tool_call.description 갱신(`modified=True`) 직후, `graph.aget_state(run_config, subgraphs=True)` 로 SubAgent 의 state 를 조회.
   - 각 `task.state` (StateSnapshot) 의 첫 메시지가 `HumanMessage` 면, 동일 `id` 를 보존한 새 `HumanMessage(content=task_description_update)` 로 `aupdate_state(sub_config, {"messages": [...]})` 호출 → `add_messages` 리듀서가 id 매칭으로 교체.
   - 실패 시 warning 만 남기고 진행 (inner try-except 격리).
   - 부모 task tool_call 원복(finally) 은 기존 그대로 유지: 다음 턴에서 LLM 이 [사용자 수정 사항] 패턴을 복제·재생산하는 부작용 방지.
   - SubAgent HumanMessage 는 원복하지 않음: 사용자가 명시적으로 변경한 의도이고, 이번 resume 안에서만 소비되는 현재-실행 SubAgent 의 history 이므로 유지가 안전.

**효과**
- HITL edit → ValidationError → SubAgent LLM retry 흐름에서 LLM 이 갱신된 HumanMessage 를 보게 되어 사용자가 지정한 개수가 retry 후에도 보존됨.
- preview_edit 의 task description 재작성 결과가 SubAgent 까지 끝까지 닿게 되어, edit 시 한 번 더 인터셉트가 떠도 인자가 회귀하지 않음.

**부수 변경 — preview_edit 의 필수 필드 자동 보정**
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py::preview_edit` 의 system prompt 에 규칙 추가:
  - 필수 필드가 빈 값/누락/잘못된 enum 인 경우 task description 의 맥락에서 합리적 디폴트로 보정.
  - 예: `gmail_search.q=""` → `"in:inbox"` 혹은 더 구체적인 `"is:unread"` 등.
- SubAgent 분기·단순 호출 분기 둘 다 동일 규칙 반영.
- 효과: 원본 도구 호출이 빈 `q` 로 인터셉트된 경우라도, 사용자가 edit 하면 새 args 가 ValidationError 없이 실행되어 retry 회귀 가능성 자체를 차단.

**검증**
- `python -c "import ast; ast.parse(...)"` → 양쪽 파일 모두 문법 OK.
- `langgraph.types.StateSnapshot` 에 `tasks` 필드, `PregelTask` 에 `state` 필드 존재 확인.
- `pm2 restart runner` 완료 — uvicorn 정상 기동(port 28003, application startup complete).

---

### [완료] 회원가입 시 deploymentEnvironment 자동 시드 누락 수정

**작성자**: leedaeyoung

**증상**
- `daeyoung.lee@dweax.com` 계정에서 에이전트를 배포하려고 하면 배포 페이지(`/agents/[agentId]/deployments`)에 "먼저 프로젝트 환경을 생성해주세요" 메시지만 표시되고 환경 선택 드롭다운이 비어 있음.
- DB 확인: `deployment_environments` 테이블에 해당 프로젝트(`8bf60ba6-93fa-464c-91fe-edabe0193897`, slug `workspace-39e4a4e3`) 행이 0개.
- 웹 UI 에는 환경을 신규 생성하는 화면이 없음 (`apiClient.environments.create` / `useDeploymentStore.addEnvironment` 는 정의돼 있으나 호출하는 컴포넌트 없음).

**원인**
- `apps/api/src/modules/projects/projects.service.ts::create` 경로에는 `tx.deploymentEnvironment.createMany` 로 dev/staging/prod 3 개를 자동 시드하는 코드가 있음(L175-182).
- 반면 `apps/api/src/modules/users/users.service.ts::registerDirect` (POST `/api/users/register`) 는 동일 트랜잭션 안에서 `tx.project.create` 만 직접 호출하고 `deploymentEnvironment` 시드를 빠뜨림.
- 해당 사용자는 프로젝트 slug 패턴(`workspace-${user.id.slice(0,8)}`)이 일치해 `registerDirect` 경로로 가입했음이 확인됨.
- `agent_deployments.environment_id` 는 NOT NULL 이라 환경 행이 없으면 배포 INSERT 자체가 막혀 배포 불가.
- 초대(`invite` → `activate`) 경로는 별도 워크스페이스를 만들지 않고, 첫 로그인 시 프론트엔드 `useUserStore.initProject` 가 `mockApi.projects.create` 로 폴백 생성하므로 `projects.service.create` 의 env 시드가 자동 적용됨 — 손볼 필요 없음.

**변경**
1. `apps/api/src/modules/users/users.service.ts::registerDirect`
   - `tx.project.create` 직후 `tx.deploymentEnvironment.createMany` 추가.
   - 값은 `projects.service.ts::create` 와 동일: dev/Development/blue/0, staging/Staging/amber/1, prod/Production/emerald/2.
   - 트랜잭션 안에서 처리되므로 user/project/env 가 원자적으로 성공·실패.

**기존 사용자 보정 (1 회성 SQL)**
- 환경이 0 개인 프로젝트(현재 `이대영 Workspace` 1 건)에 dev/staging/prod 행 직접 INSERT.
  ```sql
  INSERT INTO deployment_environments (id, project_id, slug, name, color, "order", created_at)
  VALUES
    (gen_random_uuid()::text, '<projectId>', 'dev',     'Development', 'blue',    0, NOW()),
    (gen_random_uuid()::text, '<projectId>', 'staging', 'Staging',     'amber',   1, NOW()),
    (gen_random_uuid()::text, '<projectId>', 'prod',    'Production',  'emerald', 2, NOW());
  ```

**검증**
- `pnpm --filter @agent-studio/api lint` → 0 errors (사전 warning 3 개 유지).
- DB 재확인: `deployment_environments WHERE project_id='8bf60ba6...'` → 3 행 (dev/staging/prod) 정상 존재.
- 신규 가입자는 트랜잭션 안에서 env 가 함께 생성되므로 가입 즉시 배포 가능.

---

### [완료] HITL resume 시 user_id 전파 누락 버그 수정 (Critical)

**작성자**: leedaeyoung

**증상**
- admin 페이지에서 프로젝트 레벨 Gmail OAuth 연동 해제 후 client 채팅이 작동 안 함.
- 에이전트가 "메일에 접근할 수 있는 인증이 설정되어 있지 않아 ... GMAIL_OAUTH_CLIENT_ID, ... 가 필요" 라고 응답.
- 사용자의 client `user_credentials` 행은 active 상태로 정상 존재했음.

**원인**
- NestJS 로그: `Gmail token request: agentId=..., userId=none → 프로젝트 폴백` (모든 HITL 승인 후 요청에서 동일 패턴).
- Runner 로그: 초기 `Calling DeepAgent ... user_id=a5d3...` 는 정상 전달.
- 코드 추적 결과: **`agents_service.resume_stream` 이 `load_agent_with_deps` 호출 시 `user_id` 인자 미전달**. HITL 승인 후 resume 흐름에서 SubAgent / builtin 도구가 모두 `user_id=None` 으로 재빌딩되어 `build_gmail_search_tool` 클로저에 None 캡처 → NestJS 호출 시 `?userId=` 미포함 → 프로젝트 폴백 발동.
- admin 이 프로젝트 Gmail 까지 해제했기 때문에 폴백 대상도 비어있어 에이전트가 환경변수 부재 메시지로 응답.

**변경 (체인 4단계)**
1. `apps/agent-runner-py/src/modules/threads/threads_service.py::get`
   - SELECT 절에 `user_id` 추가, 반환 dict 에 `userId` 키 노출
2. `apps/agent-runner-py/src/modules/threads/threads_service.py::resume_message`
   - `user_id: str | None = None` 파라미터 추가
   - 응답자 user_id 없으면 `thread.userId` (스레드 소유자) 로 폴백
   - `agents_service.resume_stream` 에 `user_id=effective_user_id` 전달
3. `apps/agent-runner-py/src/modules/agents/agents_service.py::resume_stream`
   - `user_id` 파라미터 추가
   - `load_agent_with_deps(agent_id, thread_id, user_id=user_id)` 로 전달
4. `apps/agent-runner-py/src/modules/hitl/hitl_service.py::respond`
   - 이미 받고 있던 `user_id` 를 `threads_service.resume_message` 호출에 전달

**효과**
- HITL 승인 후 resume 흐름에서 도구가 정확한 user_id 로 재빌딩 → NestJS 가 `?userId=` 수신 → 사용자 토큰 정상 사용 → 의도한 Gmail 계정 메일 조회.
- 프로젝트 Gmail 연동 해제 상태에서도 client 사용자의 자체 OAuth 정상 동작.
- 응답자 user_id 누락 케이스 (API 호출 등) 에도 thread 소유자 user_id 로 자연스럽게 폴백.

---

### [완료] Gmail OAuth login_hint 정책 정정 + 계정 선택 가이드 다이얼로그

**작성자**: leedaeyoung

**문제 (이전 구현의 잘못된 가정)**
- 직전 변경에서 `login_hint` 폴백으로 `User.email` 을 사용. 그러나 시스템 로그인 이메일(예: `abc@naver.com`)은 Google 계정과 별개라는 본래 정책과 충돌. `login_hint` 가 Google 입장에서 무의미하거나 잘못된 계정으로 유도 가능.
- 사용자가 OAuth 동의 화면에서 의도와 다른 Google 계정을 선택하는 함정은 여전. 브라우저에 다른 Google 계정만 로그인되어 있으면 그게 그대로 연결됨 (실제 발생).

**변경**
1. `apps/api/src/modules/me-credentials/gmail-oauth.service.ts::getAuthUrl`
   - `User.email` 폴백 제거. `login_hint` 는 `metadata.connectedEmail` 이 있을 때만 사용 (재인증 시 같은 계정 자동 선택 편의용)
   - 첫 연결 또는 disconnect 후엔 힌트 없음 → 사용자가 자유롭게 어떤 Google 계정이든 선택 가능
2. `apps/agent-web/src/app/(client)/client/tools/page.tsx::handleConnect`
   - OAuth URL 리다이렉트 전 확인 다이얼로그 (`useConfirm`) 추가:
     "브라우저에 여러 Google 계정이 로그인되어 있다면, 동의 화면에서 의도한 계정을 정확히 선택해 주세요. 다른 계정을 선택하면 그 계정의 메일이 조회됩니다."
   - 사용자가 OAuth 동의 화면에 진입하기 전 함정을 명시적으로 인지하도록 가이드

**효과**
- 시스템 로그인 이메일과 Google 계정 이메일이 다르더라도 OAuth 정상 동작 (예: naver/카카오 가입자가 Gmail 연결)
- 사용자가 OAuth 시 의도하지 않은 계정으로 잘못 연결되는 함정을 사전에 인지 → 신중한 선택 유도
- 의도와 다른 계정으로 연결되어도 도구 관리 카드에 "연결된 계정: <emailAddress>" 가 즉시 표시되어 즉시 발견 가능 (직전 Option 3 변경)

---

### [완료] Gmail OAuth UX 보강 — login_hint + 연결 계정 표시

**작성자**: leedaeyoung

**문제**
- OAuth 동의 화면에서 사용자가 의도와 다른 Google 계정을 선택하면 그 계정의 메일이 읽힘 (시스템 결함 아님, UX 함정). 실제 발생: `daeyoung.lee@dweax.com` 사용자가 OAuth 화면에서 `dw.daeyoung.lee@gmail.com` 개인 계정을 선택해 연결되어 "다른 계정 메일이 나옴"으로 인식.
- 현재 UI 에 "어떤 Google 계정이 연결됐는지" 표시 없음 → 사용자가 즉시 알 수 없음.

**변경**
1. `apps/api/src/modules/me-credentials/gmail-oauth.service.ts`
   - `OAuthAppMetadata` 에 `connectedEmail?: string | null` 필드 추가
   - `getAuthUrl`: User.email 조회 후 `login_hint` 쿼리 파라미터 추가 (이전 연결 이메일이 있으면 그걸 우선) → Google 동의 화면에서 해당 계정 우선 노출/자동 선택
   - `exchangeCode`: refresh_token 저장 직후 access_token 으로 Gmail `users/me/profile` 호출 → `emailAddress` 를 `metadata.connectedEmail` 에 저장. 응답에도 `connectedEmail` 포함
   - `saveApp`, `disconnect`, `findRow`, `getApp`: `connectedEmail` 보존/노출 로직 추가
2. `apps/agent-web/src/lib/api-client.ts` — `gmail.getOAuthApp` 응답 타입에 `connectedEmail: string | null`, `exchangeCode` 응답에 `connectedEmail?` 추가
3. `apps/agent-web/src/app/(client)/client/tools/page.tsx`
   - `GmailAppState` 에 `connectedEmail: string | null` 추가
   - Gmail OAuth 카드에 "연결된 계정: <email>" 라인 노출 (monospace 강조)

**효과**
- 사용자가 OAuth 시도 시 Google 동의 화면에서 의도한 계정이 자동 우선 노출 → 잘못 선택 가능성 감소
- 연결 직후 UI 에 실제 연결된 Google 계정 이메일이 즉시 표시 → 의도와 다른 계정이면 한눈에 발견
- 잘못 연결돼도 "연결 해제 → 재연결" 시 다음 login_hint 가 이전 잘못 연결된 이메일이 아닌 시스템 사용자 이메일로 갱신됨 (saveApp 변경 감지 시 connectedEmail 도 함께 클리어)

---

### [완료] Gmail OAuth admin → client 누수 차단

**작성자**: leedaeyoung

**문제**
- client 도구 관리에서 Gmail OAuth 를 변경했는데 채팅에서 다른 이메일(프로젝트 레벨 admin Gmail)이 읽혔다.
- 저장 위치는 분리되어 있지만(`projects.builtin_configs` vs `user_credentials`), 토큰 발급 시 사용자 토큰 실패하면 조용히 프로젝트로 폴백하는 구조가 누수의 원인.
- 추가로 `saveApp` 이 OAuth 앱(clientId/secret) 변경 시 기존 refresh_token 을 유지해 호환 안 되는 토큰이 남는 부작용도 확인.

**변경**
1. `apps/api/src/modules/tools/tools.service.ts`
   - `ToolsService` 에 `Logger` 추가
   - `issueGmailAccessTokenByAgentId`: userId 가 주어지고 user_credentials 행이 존재하면 **프로젝트 폴백 금지** — 사용자 토큰 발급 실패 시 `BadRequestException` 으로 명시적 에러 반환. 행이 없을 때만 (admin/시스템 컨텍스트) 프로젝트 폴백 허용.
   - `tryIssueUserGmailAccessToken`: 6개 null 반환 지점(row 없음 / 복호화 실패 / secret 누락 / clientId 누락 / Google 거절 / 예외) 각각에 logger.warn 추가. Google 400/401 응답 시 user_credential row 의 status 를 'invalid' 로 자동 갱신.
2. `apps/api/src/modules/me-credentials/gmail-oauth.service.ts::saveApp`
   - clientId 또는 clientSecret 변경 감지 시 기존 refresh_token 폐기 + status='invalid' 설정 + connectedAt 리셋. 응답에 `requiresReconnect: true` 함께 반환해 프론트가 사용자에게 재연결 안내 가능.

**효과**
- client 사용자가 자기 OAuth 를 한 번이라도 등록하면 admin Gmail 로 절대 새지 않음 (명시적 에러)
- OAuth 앱 자체를 바꾸면 호환 안 되는 옛 refresh_token 이 자동 무효화되어 사용자가 재연결하도록 강제
- 다음 누수 의심 발생 시 로그만 보고 즉시 원인 식별 가능

---

### [완료] HITL edit에서 SubAgent 부모 task.description 자연어 재작성 (Option A)

**작성자**: leedaeyoung

**문제**
- SubAgent 내부 도구(`gmail_search`)가 HITL로 인터셉트된 상태에서 사용자가 "3개 조회해줘"로 수정하면, deepagents `edit` 결정은 `gmail_search.max_results`만 3으로 바꾸지만 부모 `task.description`은 원본("1개")이 그대로 재실행됨 → SubAgent LLM이 자기 임무를 우선 따라 1개만 보고.
- 이전 시도(`[사용자 수정 사항]` 마커 주입)는 다음 부작용 발생: (1) 다음 턴 LLM이 패턴 복제·재생산, (2) 동일 턴 내 재수정 시 블록 중복 누적.

**변경 (자연어 통째 재작성)**
1. `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py::_extract_interrupt` — 인터럽트 시 그래프 state의 마지막 AIMessage tool_calls에서 `task` args(description 포함)를 함께 추출해 `parentTaskArgs`로 반환
2. `apps/agent-runner-py/src/modules/threads/threads_service.py::_handle_interrupt` — `parentTaskArgs`를 `agent_context`에 저장
3. `apps/agent-runner-py/src/modules/hitl/hitl_service.py::preview_edit` — `parentTaskArgs`가 있으면 LLM 프롬프트를 확장해 `{args, taskDescription}` JSON 반환. system_prompt에서 "마커 사용 금지, 자연어로 통합" 명시
4. `hitl_service.respond` → `threads_service.resume_message` → `agents_service.resume_stream` → `deepagent_bridge.resume_stream_with_deepagent` 까지 `task_description_update` 파라미터 전파
5. `deepagent_bridge.resume_stream_with_deepagent` — 마커 주입·regex stripping 로직 전면 제거. `task_description_update`가 있으면 AIMessage의 task tool_call.args.description을 **통째로 교체**. finally에서 원본 복원 유지
6. `apps/agent-web/src/components/client/ClientChatView.tsx::FeedItem` — `taskDescriptionUpdate?: string` 필드 추가; `handleHitlEditSubmit`에서 preview 응답의 taskDescriptionUpdate를 pseudo 카드에 보존; `handleHitlDecide`의 edit payload에 `taskDescriptionUpdate` 포함
7. `apps/agent-web/src/lib/api-client.ts` + `apps/api/src/modules/hitl/hitl.service.ts` — previewEdit 반환 타입에 `taskDescriptionUpdate?: string | null` 추가

**기대 효과**
- description에 마커가 없어 다음 턴 LLM이 패턴을 학습할 소스 자체가 사라짐
- 한 턴 내 재수정 시에도 매번 LLM이 자연어 통합 버전을 새로 만들기 때문에 중복 누적 불가
- finally revert는 안전망으로 유지 — 만약 SubAgent 응답이나 메인 요약에 "수정 후" 흔적이 남더라도 description 자체는 원본으로 복원됨

---

### [완료] gmail_search 파라미터 미반영 버그 수정

**작성자**: leedaeyoung

**문제**
1. 사용자가 "메일 하나 조회해줘"라고 요청하면 에이전트가 `max_results=10`(기본값)으로 10개 검색
2. 수정으로 "3개 조회"를 요청하면 `gmail_search`는 3개를 찾지만 `gmail_fetch`는 1개만 호출해서 결과 1개만 반환

**원인**
- 버그 1: `GmailSearchArgs.max_results` 필드 설명에 "사용자가 요청한 개수를 반영하라"는 지시가 없어서 에이전트가 자연어 숫자("하나", "one" 등)를 파라미터에 매핑하지 못함
- 버그 2: `gmail_search` 응답에 `_AGENT_INSTRUCTION` / `_next_calls`가 없어서, N개 결과를 반환해도 에이전트가 임의로 1개만 `gmail_fetch` 호출

**변경 (최종)**
- `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_search.py`
  - **1차 시도 (실패)**: `_AGENT_INSTRUCTION` + `_next_calls` 추가 → 에이전트가 첫 번째 gmail_fetch 후 충분한 컨텍스트를 얻었다고 판단하고 나머지 fetch 생략 (LLM 행동 의존 방식의 한계)
  - **최종 수정**: `include_body: bool = Field(default=True, ...)` + `max_body_chars: int = Field(default=3000, ...)` 파라미터 추가
    - `include_body=True`(기본) 시 각 메일에 `format=full`로 API 호출해 본문 텍스트도 함께 반환
    - `_extract_text_plain()` 헬퍼로 payload에서 text/plain 재귀 추출
    - `include_body=True`일 때 `_next_calls` 미포함 (이미 내용이 있으므로 gmail_fetch 불필요)
    - `include_body=False`일 때만 `_next_calls` 포함 (메타데이터만 조회 시 선택적 fetch 안내)
  - **효과**: "3개 조회해줘" 수정 시 max_results=3, include_body=True(기본) → 단일 gmail_search 호출로 3개 이메일 본문 반환 → gmail_fetch 다중 호출 불필요, 에이전트 행동 의존성 제거

---

## 2026-05-14 (2)

### [완료] HITL 수정(edit) 흐름 단순화 — silent + follow-up 제거, deepagents `edit` decision 직행

**작성자**: leedaeyoung

**문제**
- 클라이언트 채팅에서 HITL 도구 호출에 "수정" 적용 시 세 가지 버그:
  1. 두 번째(pseudo) "도구 실행 요청" 카드에 첫 번째 카드의 "사용자의 수정 요청" 박스가 그대로 노출(중복).
  2. 사용자가 "3개 보여줘"로 수정해 pseudo 카드를 승인해도 최종 결과는 1개만 나옴(원래 의도 반영 실패).
  3. 응답 완료 후에도 composer의 send 버튼이 활성 상태로 남음.

**원인**
- `handleHitlEditSubmit`이 새로 만든 pseudo 카드에 `editPrompt`를 같이 채워 두어 "수정 후 args" 카드에서도 수정 요청 박스가 또 보임.
- pseudo 카드 승인 시 deepagents에 정식 `edit` decision이 아니라 `reject` 후 silent 모드로 turn을 숨기고, `editPrompt`를 새 user 메시지로 자동 재전송하는 우회 구조를 사용 중. 모델이 "거부된 도구 호출" 컨텍스트에서 도구를 다시 호출하지 않거나 이전 의도를 따르는 케이스가 발생.
- silent → follow-up 으로 두 번의 turn이 끼면서 `sending` 상태가 잠시 false가 되는 구간이 생겨 composer가 활성으로 보임.
- deepagents 공식 문서(`docs.langchain.com/oss/python/deepagents/human-in-the-loop`) 재확인 결과 `edit` decision은 새 args로 도구를 실행하고 그 결과를 같은 turn에서 LLM에 전달하는 정식 메커니즘. 우회 구조 자체가 잘못된 가정에서 출발한 것.

**변경**
- `apps/agent-web/src/components/client/ClientChatView.tsx`:
  - `editFollowupRef` / `handleSendRef` ref 제거.
  - WS 핸들러(`agent.token` / `turn.completed` / `step.started` / `step.completed` / `step.failed` / `plan.created` / `run.cancelled` / `hitl.request`)의 silent 모드 early-return 가드 모두 제거.
  - `handleHitlDecide`: pseudo 카드 승인 시 deepagents 정식 `edit` decision 으로 직행. payload `{ decision: 'edit', editPrompt, editedAction: { name, args } }`을 `sourceInteractionId`(=real 인터럽트 id)로 전송. real 카드는 동시에 `resolved: 'edit' + editedAction` 으로 갱신해 history 와 같은 모양으로 보이도록.
  - `ClientHitlCard`: `item.pendingEditFor`가 있는 pseudo 카드에서는 `editPrompt` 박스 렌더 안 함(real / history 카드에는 그대로 노출). `editedAction` 박스는 기존 조건(`editedArgLines.length > 0`)으로 충분.
- backend(`hitl_service.respond`, NestJS `hitl.controller/service`, shared `hitl.ts`)는 이미 `edit` decision 처리 및 응답 평탄화를 지원하므로 변경 없음.

**효과**
- 문제 1: pseudo 카드의 중복 "사용자의 수정 요청" 박스 사라짐.
- 문제 2: deepagents 가 `edit` decision 으로 새 args(예: `max_results=3`)로 도구를 실행 → LLM 이 3개 결과로 답변.
- 문제 3: silent → follow-up 의 이중 turn 흐름이 사라져 응답 완료 시 `sending=false`가 자연스럽게 들어와 send 버튼 비활성화.

**검증**
- `apps/agent-web` `pnpm lint` — 0 errors, 93 warnings(전부 기존 코드, ClientChatView 신규 경고 없음).
- 실 동작 확인은 사용자 측에서 수동 (gmail_search 카드 → "3개 보여줘" 수정 → pseudo 승인 → 최종 답변 3개 + 채팅창 비활성 확인).

### [완료] HITL 수정(edit) 흐름 후속 보정 — follow-up user 메시지 자동 전송 + send 영원 stop 안전망

**작성자**: leedaeyoung

**문제**
- 1차 수정 후 사용자 검증에서 두 문제가 남음:
  - LLM 답변이 여전히 "최근 메일 1개"로 1차 의도("최근메일 하나 보여줘")를 따른다. deepagents `edit` decision 은 도구 args 만 바꾸고 user 메시지는 손대지 않아, 결과가 3건이어도 LLM 이 원본 user 메시지를 우선시해 1개로 답변.
  - 마지막 hitl 카드 승인 이후 turn.completed 가 도착하지 않아 send 버튼이 영원히 stop 상태로 남는 케이스 발생(사용자 확인).

**변경**
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py`:
  - `respond` 가 `resume_message` 의 result 전체(`status` 등)를 보존해 반환하도록 변경. 이로써 `hitl.responded` 이벤트와 REST 응답에서 frontend 가 paused / non-paused 를 구분할 수 있다.
- `apps/agent-web/src/components/client/ClientChatView.tsx`:
  - `editFollowupRef`, `handleSendRef` 재도입(silent 모드 가드는 _없음_ — 도구 실행/결과는 그대로 화면에 노출).
  - `handleHitlDecide`: pseudo 승인 시 `edit` decision 그대로 전송 + `editFollowupRef.current = { active: true, editPrompt }` 설정.
  - `onTurnCompleted`: 마지막 단계에서 `editFollowupRef` 가 활성이면 `setSending(false)` 생략 후 `handleSend(editPrompt)` 자동 호출(빈 깜빡임 회피).
  - `hitl.responded` WS 핸들러: `status === 'paused'`면 sending 유지(다음 카드 대기), 아니면 follow-up 트리거 또는 `setSending(false)` 보강. turn.completed 누락 시 안전망.

**효과**
- 사용자가 "3개 보여줘"로 수정 후 pseudo 승인 → deepagents 가 `max_results=3` 으로 도구 실행 → LLM 1차 답변 종료 → 자동으로 "3개 보여줘"를 새 user 메시지로 전송 → 새 turn 에서 LLM 이 메모리상 3건 결과를 사용자 의도에 맞춰 정리해 답변.
- 마지막 turn 의 `turn.completed` 가 누락되어도 `hitl.responded` 의 status 로 sending 상태 안전 복귀.

**검증**
- `apps/agent-web` `pnpm lint` — 0 errors(아래 확인).
- 실 동작 확인은 사용자 측에서 수동.

---

## 2026-05-14 (1)

### [완료] 네이티브 confirm을 ConfirmDialog 모달로 일괄 교체

**작성자**: leedaeyoung

**문제**
- agent-web 전반에서 파괴적 동작(삭제·폐기·롤백·연동해제 등)에 브라우저 네이티브 `confirm()` / `window.confirm()`이 사용되어 디자인 토큰과 어긋남.
- 14개 호출처, 8개 파일에 산재 — 사용자 경험 일관성 부족.

**해결**
- `apps/agent-web/src/components/shared/confirm-dialog.tsx` 신규 — Promise 기반 `ConfirmDialogProvider` + `useConfirm` / `useConfirmHelpers` hook.
  - `variant: 'default' | 'danger'`로 파괴/비파괴 동작 구분 (danger는 빨간 확정 버튼).
  - ESC 취소 / Enter 확정, 백드롭 클릭 취소, autoFocus 적용.
  - 기존 모달 패턴(`fixed inset-0 z-50 + bg-black/60 backdrop-blur-sm + #0c0d12/#1e2130` 컬러)과 동일.
- `apps/agent-web/src/app/layout.tsx` — RootLayout에 `ConfirmDialogProvider` 마운트.
- 14개 호출처 모두 `await confirmDelete(...)` 또는 `await confirm({...})`로 전환.

**변경 호출처**
| 파일 | 건수 |
|---|---|
| `components/tools/ToolGroupCard.tsx` | 2 |
| `components/tools/OpenApiSpecDialog.tsx` | 1 |
| `components/mcp/McpServerCard.tsx` | 1 |
| `app/(dashboard)/skills/page.tsx` | 2 |
| `components/agents/builder/PromptDiffModal.tsx` | 1 |
| `app/(client)/client/tools/page.tsx` | 4 |
| `app/(dashboard)/workflows/[workflowId]/versions/page.tsx` | 1 |
| `app/(dashboard)/agents/[agentId]/deployments/page.tsx` | 2 |

**검증**
- 네이티브 `alert/confirm/prompt` 호출 잔재 0건 (grep 확인).
- `pnpm lint` — 0 errors, 92 warnings (모두 기존 코드, 신규 파일 무경고).

---

## 2026-05-13 (1)

### [완료] 에이전트 슬러그 자동 생성 — UI 필드 제거, 백엔드 fallback 처리

**작성자**: leedaeyoung

**문제**
- 에이전트 생성 폼의 슬러그 자동 생성 로직(`page.tsx:307`)이 `[^a-z0-9-]`를 모두 제거 → 한글 이름은 슬러그가 `""` 또는 `"-"`로 수렴.
- 첫 번째 한글 이름 에이전트는 운 좋게 만들어져도, 두 번째부터 `projectId_slug` 유니크 제약 위반(`agents.service.ts:62`)으로 `ConflictException` 발생.
- 프론트엔드 `handleCreateAgent`에 try/catch가 없어 사용자는 원인을 모른 채 모달이 멈춘 것처럼 보임.

**목표**
- 사용자에게 슬러그 입력 필드를 노출하지 않는다 (불필요한 인지 부하 제거).
- 백엔드에서 항상 안전한 슬러그를 자동 생성한다 (이름 슬러그화 → 비었으면 `agent` fallback → 충돌 시 `-2`, `-3` ... 접미사).
- 외부 import/API 직접 호출 시 슬러그를 명시적으로 보내고 싶은 경우는 기존처럼 동작 (CreateAgentRequest.slug는 optional).

**변경 범위**
- `packages/shared/src/types/agent.ts` — `CreateAgentRequest.slug` → optional.
- `apps/api/src/modules/agents/agents.service.ts`
  - `resolveUniqueSlug(projectId, candidate, name)` 헬퍼 신설: 후보→이름 순으로 슬러그화, 비었으면 `agent` fallback, 충돌 시 `-2`,`-3`... suffix.
  - `create()` 충돌 throw 제거 → `resolveUniqueSlug` 호출.
  - 미사용 `ConflictException` import 제거.
- `apps/agent-web/src/app/(dashboard)/agents/page.tsx` — `AgentCreateForm`에서 슬러그 input/state/자동 변환 로직 제거, `handleCreateAgent` 시그니처 조정.

**동작 검증** (admin 토큰으로 직접 API 호출):
| 이름 | 자동 생성 슬러그 |
|---|---|
| `마케팅비서` | `agent` |
| `고객 지원` | `agent-2` |
| `영업 도우미` | `agent-3` |
| `Customer Bot` | `customer-bot` |
| `Customer Bot` (중복) | `customer-bot-2` |

**참고**
- TeamCreateForm(`agents/page.tsx:382`)도 같은 패턴의 자동 변환 로직이 있어 동일하게 정리 (`team` fallback, `team-2`,`-3`... suffix).
- `importAgent`는 빈 baseSlug 케이스 처리가 없었는데, `resolveUniqueSlug`로 통일.

---

## 2026-05-13 (2)

### [완료] 팀 슬러그 자동 생성 + importAgent 슬러그 fallback 통일

**작성자**: leedaeyoung

**문제**
- (1)에서 에이전트만 처리 — 팀(`TeamCreateForm`, `teams.service.ts`)은 동일 패턴 문제 남아 있음.
- `importAgent`(`agents.service.ts:236`)는 빈 baseSlug fallback이 없어 한글 이름 import 시 빈 슬러그 충돌 가능.

**해결**
- `apps/api/src/modules/teams/teams.service.ts` — `resolveUniqueSlug` 헬퍼 신설(`team` fallback), `create()`에서 충돌 throw 제거.
- `packages/shared/src/types/team.ts` — `CreateTeamRequest.slug` → optional.
- `apps/agent-web/src/app/(dashboard)/agents/page.tsx` — `TeamCreateForm.handle()`에서 slug 자동 변환 로직 제거.
- `apps/api/src/modules/agents/agents.service.ts:importAgent` — 인라인 슬러그 로직 제거, `resolveUniqueSlug` 재사용.

**동작 검증**
| 호출 | 입력 이름 | 자동 슬러그 |
|---|---|---|
| 팀 생성 | `마케팅팀` | `team` |
| 팀 생성 | `고객 지원팀` | `team-2` |
| 팀 생성 | `Support Squad` | `support-squad` |
| importAgent | `임포트한글봇` | `agent` |
| importAgent | `임포트다른봇` | `agent-2` |
| importAgent (slug 명시) | `임포트With Slug` (`slug: my-imported-bot`) | `my-imported-bot` |

---

## 2026-05-12 (5)

### [완료] 서브에이전트 스킬 지시사항 미준수 — 직접 주입 방식으로 전환

**작성자**: yoojinhee

**문제**
- `email_template` 스킬에 메일 양식을 정의했으나 서브에이전트가 임의 형식으로 메일 작성.
- (3)에서 "SKILL.md를 read 도구로 읽어라" 지시를 추가했으나 LLM이 메일 작성은 자신감으로 skip.
- deepagents progressive disclosure 자체의 한계: 에이전트가 자발적으로 `read_file`을 호출해야 스킬 전체 내용을 받는 구조.

**해결**
- `_inject_skill_contents_to_specs()` 함수 신설: 서브에이전트 specs에서 skillIds를 꺼내 DB에서 스킬 내용(name/description/instructions)을 직접 조회하여 systemPrompt에 `[필수 스킬 지시사항 — 반드시 준수]` 형태로 append.
- `stream_with_deepagent()` / resume 함수 모두: `build_subagents()` 호출 전 `_inject_skill_contents_to_specs()` 선 실행.
- Progressive disclosure 우회 — 에이전트가 별도 파일 읽기 없이도 스킬 전체 내용을 항상 보유.

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `from src.database.client import fetch_all` import 추가
  - `_inject_skill_contents_to_specs()` 함수 신설
  - `stream_with_deepagent()` / resume 함수의 `build_subagents()` 호출 전 주입 로직 추가

---

## 2026-05-12 (4)

### [완료] SKILL.md YAML frontmatter 파싱 실패 수정 — description 콜론 escape

**작성자**: leedaeyoung

**문제**
- "입사서류 발송_서브에이전트"(`f989001c-...`)에서 서브에이전트 `hr-document`에 skill 3개(`hr-policy`, `document-checklist`, `email-template`)가 정상 등록되어 있는데도 답변 시 skills가 일관성 있게 활용되지 않음.
- runner 로그 확인 시 매 invoke마다 `Invalid YAML in /skills/hr-document/*/SKILL.md: mapping values are not allowed here` 에러가 3개 skill 모두에서 반복.
- (2)·(3) 세션에서 store 명시 주입 + system_prompt 강제 지시는 적용됐지만, 본 파싱 에러는 남아 있어 deepagents SkillsMiddleware의 메타데이터 인덱싱이 실패.

**원인**
- `_build_skill_md`(apps/agent-runner-py/src/modules/skills/skills_service.py)가 description 본문을 인용부호 없이 그대로 frontmatter에 삽입.
- description 안의 `트리거 조건: 사용자가 "..."` 처럼 콜론이 포함된 문자열을 YAML 파서가 매핑 시작자로 해석 → 파싱 실패.

**해결**
- `apps/agent-runner-py/src/modules/skills/skills_service.py`
  - `yaml` import 추가.
  - `_build_skill_md`을 `yaml.safe_dump(frontmatter, allow_unicode=True, sort_keys=False, default_flow_style=False)` 기반으로 재작성. description의 콜론·특수문자를 yaml이 자동으로 single-quote 처리.

**검증**
- 유닛 호출 — description에 `트리거 조건: 사용자가 "서류 검증"` 포함된 입력으로 `_build_skill_md` 결과를 `yaml.safe_load`로 다시 파싱 → 정상 dict 복원.
- `pm2 restart runner` 정상 기동.
- store 테이블의 기존 SKILL.md는 다음 invoke 시 `sync_agent_skills`의 DELETE + 재삽입으로 새 build 결과로 교체됨.

---

## 2026-05-12 (3)

### [완료] 서브에이전트가 스킬 지시사항을 따르지 않는 문제 수정

**작성자**: yoojinhee

**문제**
- `email_template` 스킬에 상세 메일 양식을 정의했으나 서브에이전트가 해당 형식을 따르지 않고 임의 형식으로 메일을 작성함.
- deepagents SkillsMiddleware의 progressive disclosure 패턴 때문에, 시스템 프롬프트에는 스킬 이름/설명만 주입되고 전체 지시사항(SKILL.md)은 에이전트가 `read` 도구로 직접 파일을 읽어야 제공됨.
- 에이전트가 SKILL.md를 읽는 단계를 건너뛰면 상세 지시사항을 받지 못해 임의 형식으로 작업함.

**해결**
- `build_subagents()`에서 `sub_skill_ids`가 있을 때 system_prompt에 스킬 파일 읽기 강제 지시를 추가.
- "작업 시작 전 반드시 SKILL.md를 read 도구로 읽고 형식/구조/규칙을 엄격히 따를 것"을 명시.

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` (`build_subagents()` 함수, `sub_skill_ids` 블록)

---

## 2026-05-12 (2)

### [완료] 서브에이전트 recursion limit 부족으로 조기 종료 버그 수정

**작성자**: yoojinhee

**문제**
- 서브에이전트(hr-document)가 9종 서류를 다 검토하지 못하고 메인 에이전트로 돌아옴.
- 서류 1개당 tool call 2회(fetch_attachment + parse_pdf_attachment) × 9종 = 18+ step, 기본 stepLimit 20으로 부족.
- 서브에이전트는 메인 에이전트의 `recursion_limit`을 상속받으므로 메인의 limit이 낮으면 서브에이전트도 조기 종료됨.

**해결**
- `stream_with_deepagent()` / resume 함수 모두: 서브에이전트가 존재하면 `step_limit = max(step_limit, 50)` 으로 자동 상향.
- 사용자가 `stepLimit=20`으로 설정해도 서브에이전트가 있으면 최소 50step 보장.

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`

---

### [완료] hr-document 서브에이전트 스킬 로드 실패 버그 수정

**작성자**: yoojinhee

**문제**
- `hr-document` 서브에이전트에 `document-checklist` 스킬을 연결했는데도 시스템 프롬프트에 "No skills available yet"이 나타남.
- DB에 스킬(`0d043d63-9cef-431c-8066-da556bd01ab7`, enabled=true)이 정상 존재하고, `skillIds`도 노드 데이터에 올바르게 설정되어 있었음.

**원인**
- `deepagent_bridge._make_skills_backend()`가 `StoreBackend(store=None)`으로 생성되어, 런타임에 `langgraph.config.get_store()`(contextvars 기반)에 의존하고 있었음.
- `StoreBackend.als()` → `BackendProtocol.als()` → `asyncio.to_thread(self.ls, ...)` 경로로 스레드에서 `_get_store()`가 호출됨.
- 서브에이전트는 `create_agent()` (store= 없이 컴파일)로 생성되기 때문에 서브에이전트 실행 컨텍스트에서 `langgraph.config.get_store()`가 실패하거나 잘못된 컨텍스트를 반환할 가능성이 있었음.

**해결**
- `_make_skills_backend(store: Any)` 시그니처에 `store` 파라미터 추가.
- `StoreBackend(store=store, namespace=...)` — 명시적 store 주입으로 contextvars 의존 제거.
- `stream_with_deepagent()` 두 곳 모두 `skills_store = await get_store()` → `skills_backend = _make_skills_backend(skills_store)` 순서로 변경.

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`

---

## 2026-05-12

### [완료] 외부 도메인 접속 허용 + agent-web standalone 빌드 정적자산 자동 복사

**작성자**: leedaeyoung

**문제**
- `dweax.iptime.org:28002`로 접속 시 로그인에서 CORS 에러.
  - 원인 1: `NEXT_PUBLIC_API_URL=http://localhost:28001`이 빌드에 인라인 → 외부 클라이언트가 자기 PC의 localhost를 호출.
  - 원인 2: API CORS 화이트리스트(`.env`, `apps/api/.env.local`)에 외부 도메인 누락.
- 재빌드 후 UI가 깨져 보임. Next.js standalone 빌드가 `.next/static`/`public/`을 standalone 디렉토리로 자동 복사하지 않아 정적 자산 404.

**해결**
1. CORS / API URL
   - `.env`: `CORS_ORIGINS`에 `http://dweax.iptime.org:28002` 추가.
   - `apps/api/.env.local`: 동일 추가 (이쪽이 `loadLocalEnvCors`로 우선 적용됨).
   - `apps/agent-web/.env.local`: `NEXT_PUBLIC_API_URL`/`NEXT_PUBLIC_RUNNER_URL`을 `http://dweax.iptime.org:28001` / `:28003`으로 변경.
2. standalone 정적자산 자동 복사
   - `apps/agent-web/package.json`:
     - `build`: `next build && pnpm postbuild:standalone`
     - `postbuild:standalone`: 기존 `static`/`public` 정리 후 `.next/static`과 `public/`을 `.next/standalone/apps/agent-web/`로 복사.

**검증**
- `pm2 restart api` 후 CORS preflight: `Origin: http://dweax.iptime.org:28002` → 204 + `Access-Control-Allow-Origin` 헤더 정상 응답.
- `pnpm build` 후 standalone에 `static/chunks/*.js`, `public/` 자동 복사 확인.
- `pm2 restart web` 후 `/login` 200, `/_next/static/chunks/*.js` 200.
- `POST /api/auth/login`(Origin: 외부 도메인) → 200 + accessToken 발급.

**한계 / 후속**
- pm2 `runner`(uvicorn) PID `N/A` 상태 — 에이전트 실행 시 별도 진단 필요 (이번 작업 범위 외).
- iptime 라우터 28001/28003 포트포워딩이 열려 있어야 외부에서 완전 동작.
---

## 2026-05-12

### [완료] admin 디버그 패널 cancel 흐름 적용

**작성자**: leedaeyoung

**배경**
- client 측 cancel 흐름 신설 후 admin `AgentFlowDebugPanel` 도 같은 정책을 따르도록 동기화 필요. 디버그 환경에서도 사용자가 stop 으로 중간에 멈추고 todos 가 완료처럼 보이지 않게 정합.

**변경**
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - `handleStop` callback 신설 (`apiClient.threads.cancel(activeThreadId)`).
  - 송신 버튼 토글: `running` 중 Play → `Square`(stop) 아이콘. stop 클릭 시 handleStop, 다른 상태 갱신은 WS 핸들러가 일괄 처리.
  - `wsClient.on('run.cancelled')` 구독 추가 — 마지막 ai/streaming 메시지에 `_(응답이 중지되었습니다)_` 마커 append, 빈 메시지였다면 단독 안내 추가, `tool_call` 전개 카드는 collapse, `planSteps` 의 `in_progress` → `pending` revert, `setRunning(false)` + `setLastTurnCancelled(true)`. cleanup 에서 `unsubCancelled()`.
  - `lastTurnCancelled` state 추가, `handleRun` 시작 시 false 초기화, `clearAll` 에서도 false 리셋.
  - `<TodoListCard ... cancelled={lastTurnCancelled} />` 로 전달.
- `apps/agent-web/src/components/chat/todo-list-card.tsx`
  - optional `cancelled?: boolean` prop 추가. 헤더에 분홍 톤 "취소됨" 칩 노출 (rgba(244,114,182,0.18) bg + text-pink-400). streaming hint 는 cancelled 시 비활성.

**검증**
- `pnpm lint` (agent-web): 0 errors.

**보강 — 일반 자연어 요청은 기존 todo 와 임의 매칭 금지**
- 사용자 케이스: 4개 항목 중 4번 "최종 여행계획표와 준비 팁 정리" 가 pending 인 상태에서 사용자가 "2박3일 일별로 자세한 일정 및 시간표 일별로 진행해줘" 를 보냄. 모델이 이를 4번 항목과 같은 작업으로 해석해 4번을 in_progress 마킹. 사용자 의도는 새 작업 흐름(새 TODO List 또는 기존과 별개의 신규 단계).
- `deepagent_bridge.py` plan_execute 시스템 프롬프트(send/resume 두 위치) 에 룰 추가:
  - 일반 자연어 요청(Quick Action 아님)일 때 기존 todo 와 의미적으로 정확히 동일하지 않으면 기존 항목 status 임의 변경 금지.
  - 별개 작업이면 write_todos 로 새 단계 추가 또는 새 TODO List 구성해 그 작업만 다루기.
  - 사용자가 명시적으로 기존 미완료 항목을 지칭하지 않는 한 기존 항목 그대로 둠.

**보강 — streaming cursor 잔존 + Quick Action 정확 매칭 프롬프트**
- 사용자 케이스 (1): 답변 끝에 vertical bar(`|`) 가 남음. `AssistantMessage` 의 streaming cursor 가 `!done && content` 조건으로 표시되는데, turn.completed 와 invoke fetch finally 의 done 플래그 set 이 `streamingIdRef` 매칭에만 의존해 매칭이 깨지면 cursor 영구 잔존.
  - `ClientChatView.tsx` turn.completed 핸들러 + handleSend finally 모두에서, **같은 groupId 의 미완료 assistant 를 모두 done=true 로 강제 set** 하는 보강 라인 추가. streamingIdRef 매칭 외에 groupId 기준 safety net.
- 사용자 케이스 (2,3): 1일차 요청에 2일차로 답변, 3일차 요청에 3일차+전체 정리까지 같이 답변. 모델 hallucination/효율 묶기 행동.
  - `deepagent_bridge.py` plan_execute 시스템 프롬프트(두 위치) 에 **Quick Action 정확 매칭 룰** 추가: 사용자 메시지가 `다음 TODO 항목을 이어서 진행해줘 — XXX` 형식이면 XXX 항목 한 개만 다룬다, 매칭되지 않는 다른 항목 status 변경 금지(특히 이미 completed 인 이전 항목을 pending 으로 되돌리지 말 것), 본문 시작 전 매칭 항목만 in_progress 마킹 → 답변 직전 그 항목만 completed.

**보강 — cancel 시 이전 turn completed 항목 보존 + stop 즉시 UI 반영**
- 사용자 케이스: 첫 turn 에서 1번 항목 completed. 두 번째 turn(quick action) 에서 cancel 두 번 후 세 번째 정상 완료했더니, 1번 항목이 pending 으로 되돌아가 보임.
- 원인: 모델이 cancel 된 turn 에서 `write_todos` 호출 시 전체 todos 를 다시 보내면서 1번 status 까지 변경(예: completed → pending). cancel rollback 정책이 `current 가 pending` 인 경우 revert 안 해서 1번이 pending 그대로 남음.
- `deepagent_bridge._revert_in_progress_todos_on_cancel`: 정책 보강 — base 가 `completed` 였던 항목은 어떤 변경도 무시하고 `completed` 유지(이전 작업 보존). 그 외에는 기존 정책(이번 turn 에 새로 변경된 in_progress/completed → pending).
- `ClientChatView.applyHistorySweep` cancelled 분기 + `applyCancelToPlans` helper: 동일 정책 클라이언트 적용.
- cancel 직후 UI 반영 지연 보강: WS `run.cancelled` 이벤트가 invoke fetch reject(→ finally) 보다 늦거나 누락된 케이스에 group chip 이 "N개 작업 완료" 로 보이는 문제. `handleStop` 에서 즉시 group `cancelled=true`/`status='done'`, plans todos sweep, `sending=false`, `streamingIdRef=null` 적용. WS 이벤트가 추가로 도착해도 idempotent.
- admin `AgentFlowDebugPanel.handleStop` 동일 — `planSteps` in_progress → pending, `lastTurnCancelled=true`, `running=false` 즉시 set.

**보강 — quick action 재시도 후 정상 완료 시 매칭 todo 자동 completed**
- 사용자 케이스: cancel 두 번 후 세 번째에 답변이 정상 완료됐는데, 모델이 write_todos 호출/마킹을 누락하면 graph state.todos 의 해당 항목이 pending 그대로 남아 화면에 completed 로 안 보임.
- `deepagent_bridge._auto_mark_quick_action_target`: 마지막 user 메시지가 `다음 TODO 항목을 이어서 진행해줘 — {content}` prefix 면 content 와 매칭되는 pending todo 를 in_progress 로 patch (graph.aupdate_state). 답변 본문 길이가 50자 미만이면 skip(되묻기/짧은 거절 보호).
- `stream_with_deepagent` 정상 종료 시 `_sweep_final_todos` 직전에 helper 호출. 그 다음 graph state 를 다시 fetch 해 latest todos 로 sweep — in_progress → completed.

**보강 — stop 직후 invoke 에러 안내 suppress**
- stop 누른 후 백엔드가 task.cancel() 하면 invoke fetch 가 502 등으로 떨어져 client/admin 모두 "Runner request failed" 가 표시됨. 사용자가 의도한 동작이므로 안내가 불필요.
- `ClientChatView` / `AgentFlowDebugPanel` 둘 다 `cancelRequestedRef` 추가. `handleStop` 진입 시 true 로 set, invoke catch 에서 ref 가 true 면 ref 를 false 로 reset 하고 toast/error item 추가를 skip. cancel 흐름의 UI 표시는 `run.cancelled` WS 핸들러가 일괄 담당.

---

### [완료] client/admin 채팅 응답 중지(stop) 기능 신설

**작성자**: leedaeyoung

**배경**
- 현재 응답이 시작되면 사용자가 중간에 멈출 방법이 없음 — 페이지를 떠나도 백엔드는 끝까지 실행되어 토큰·시간 소진.
- 사용자가 선호한 동작: ChatGPT 식(부분 답변은 화면에 남기되 "응답이 중지되었습니다" 마커 표시) + TO-DO List 의 cancel 된 항목은 in_progress → pending 으로 revert(완료처럼 보이지 않게).

**범위**
- runner: thread_id ↔ asyncio.Task 매핑 registry, `POST /threads/{thread_id}/cancel` endpoint, `send_message` 에서 `asyncio.CancelledError` 처리 — graph state.todos in_progress → pending revert, run row `status='cancelled'`, WS `run.cancelled` emit.
- NestJS api: `POST /threads/:threadId/cancel` proxy.
- frontend: `api-client.threads.cancel`, `ws-client` 의 `run.cancelled` 타입, `ClientChatView` / `AgentFlowDebugPanel` 의 send 버튼을 sending 중 stop 토글, `run.cancelled` 수신 시 부분 답변 done=true + 마커 표시.
- history sweep 정책 한정 수정: runs API 의 run.status='cancelled' 인 turn 은 todo sweep 적용 안 함.

**한계 (의도된 보수성)**
- 부분 ai 답변 자체는 DB 에 영속화하지 않음 (LangGraph state 의 마지막 toolCall 메시지가 unanswered 로 남으면 다음 invoke 에서 provider 에러 가능). 새로고침/재진입 시 부분 답변은 사라짐. user 메시지·완료된 write_todos toolCall 결과는 step 단위 checkpoint 까지 영속화.
- 다중 worker 환경 미고려: cancel registry 는 in-memory dict (현재 dev 환경은 단일 worker).

**변경 파일**
- `apps/agent-runner-py/src/modules/threads/cancel_registry.py` 신설 — thread_id ↔ asyncio.Task 매핑 register/unregister/cancel.
- `apps/agent-runner-py/src/modules/threads/threads_router.py` — `POST /threads/{thread_id}/cancel` endpoint.
- `apps/agent-runner-py/src/modules/threads/threads_service.py` — `send_message` 에서 current task 를 cancel_registry 에 등록, `_pump_stream` 가 `cancel_collector` 로 누적 final_content 동기화, `except asyncio.CancelledError` 에서 `_handle_run_cancelled` 호출(WS run.cancelled + thread status 복원 + run row status='cancelled' 저장), `finally` 에서 unregister.
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — `_revert_in_progress_todos_on_cancel` helper, `stream_with_deepagent` / `resume_stream_with_deepagent` 양쪽에 `except asyncio.CancelledError` 처리 추가(graph state.todos in_progress → pending revert, asyncio.shield 로 보호).
- `apps/agent-runner-py/src/modules/hitl/hitl_gateway.py` — `emit_run_cancelled(thread_id, turn_id, partial_content)` 이벤트 발행 함수 추가.
- `apps/api/src/modules/threads/threads.controller.ts` / `threads.service.ts` — `POST /:threadId/cancel` proxy.
- `apps/agent-web/src/lib/api-client.ts` — `threads.cancel(threadId)`.
- `apps/agent-web/src/components/client/ClientChatView.tsx` — `handleStop` callback, sending 중 send 버튼을 stop(Square) 토글, `run.cancelled` WS 핸들러(부분 답변에 "_(응답이 중지되었습니다)_" 마커 append + 라이브 plans todos in_progress → pending revert + sending=false), history 복원 sweep 분리(`extractPlansFromMessages` 는 raw 반환, `applyHistorySweep` 가 runs.status 별로 cancelled→pending / 그 외 →completed 분기).

**검증**
- `ruff check` runner: 신규 코드 깨끗 (기존 N815/N803/I001 외).
- `pnpm lint` agent-web/api: 0 errors.
- `uvicorn --reload` runner 자동 재기동 정상.

**보강 — cancel 된 turn 의 group chip 라벨 "취소됨" 으로 변경**
- `FeedItem` 의 `group` 종류에 `cancelled?: boolean` 플래그 추가 (StepStatus type 은 그대로 보존).
- `ActivityGroupCard`: cancelled=true 면 chip 라벨 "취소됨" + Square 아이콘 + 회색 톤, latency 표시 생략. running/failed 분기와 충돌 없음.
- 라이브 `run.cancelled` 핸들러: 활성 group 에 cancelled=true 적용.
- history 복원: run.status === 'cancelled' 인 turn 의 group 에 cancelled=true 적용.

**보강 — cancel rollback 을 in_progress 뿐 아니라 completed 까지 적용**
- 사용자 케이스: 모델이 답변 도중 cancel 됐는데 TODO 가 모두 `completed` 로 보였음. 원인은 모델이 본문 응답 시작 전에 선언적으로 `write_todos` 로 항목들을 `completed` 마킹한 뒤 cancel 됐기 때문(in_progress 가 graph state 에 남지 않음).
- `deepagent_bridge._revert_in_progress_todos_on_cancel`: LangGraph `aget_state_history` 로 마지막 user 메시지 직전 checkpoint 의 todos 를 base 로 잡고, 현재 todos 중 status 가 base 와 다르거나 base 에 없던 항목 중 `in_progress`/`completed` 인 것을 모두 `pending` 으로 rollback (asyncio.shield 보호).
- `ClientChatView.applyHistorySweep`: cancelled turn 의 snapshot 을 이전 turn snapshot 과 비교해 변경분을 pending 으로 revert.
- `ClientChatView` `run.cancelled` 라이브 핸들러: 같은 정책으로 활성 turn 의 snapshot 을 갱신.

---

### [완료] Client TODO 흐름 보강 — 완료 마킹 강제 + 남은 항목 우선 안내

**작성자**: leedaeyoung

**배경**
- 클라이언트 채팅에서 LLM이 첫 TODO 항목을 답변에 포함했지만 `write_todos`로 `completed` 마킹을 누락하여 진행률이 `0/3`으로 유지되는 케이스가 자주 발생.
- 또한 남은 TODO(02·03)가 있는데도 모델이 "원하시면 다음 단계로 1) 예산별 ... 2) 맛집/호텔 ..." 같이 새 옵션을 별도 분기로 제안하여 사용자 흐름이 분산됨.

**변경 내역**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — plan_execute 모드 시스템 프롬프트 두 위치(send 경로 ~795~808, resume 경로 ~1140~1152) 모두 다음 두 룰 추가
  1) 답변 직전 반드시 `write_todos`로 현재 in_progress 항목을 `completed`로, 다음 미완료 항목을 `in_progress`로 전환 — 누락 금지 명시.
  2) 미완료 TODO가 남아 있는 동안에는 무관한 새 옵션을 별도 분기로 제안하지 말고, 다음 미완료 항목 제목을 그대로 안내하며 "다음 항목(○○)을 이어서 진행할까요?" 만 묻기. 새 작업 제안은 모든 항목 completed 이후에만.
- `apps/agent-web/src/components/client/ClientChatView.tsx` — TodoSidePanel에 `onAdvance(text)` 콜백 신설. 완료되지 않은 첫 항목이 있고 가장 최신 plan을 보고 있을 때 한정으로, TODO 목록 아래에 "다음 항목 진행 →" 버튼 노출. 클릭 시 "다음 TODO 항목을 이어서 진행해줘 — {step.content}" 문구가 composer 입력란에 자동 채워지고 textarea로 포커스 이동.

**검증**
- `pnpm lint` (agent-web): 0 errors (기존 warning 97건은 유관 없음).
- `ruff check` (deepagent_bridge.py): All checks passed.
- `uvicorn --reload` 가 deepagent_bridge.py 변경을 감지해 자동 재기동된 것 확인.

**추가 변경 — 진척도 일치 규칙(중요)**
- 첫 적용 후 한 답변에 여러 TODO 항목의 산출물을 동시에 제공해도 진행률이 1개씩만 올라가는 패턴 재발. 시스템 프롬프트가 단일 항목 단위 마킹을 가정했기 때문.
- `deepagent_bridge.py` plan_execute 시스템 프롬프트 두 위치(send/resume) 모두 "각 항목을 완료한 직후 → 그 항목 하나 completed" 를 제거하고 다음 규칙으로 일반화:
  - 답변 직전 한 번의 write_todos 호출로 이번 답변에서 실제로 다룬 항목 모두를 `completed`로 묶어 마킹.
  - 1개씩 따로 마킹 금지, 다루지 않은 항목을 미리 `completed` 처리하는 것도 금지. "다룬 만큼만, 빠짐없이" 명시.
  - 예시(4개 중 1·2를 한 답변에 담은 경우 1·2 completed + 3 in_progress) 포함.
- `ruff check` All checks passed, `uvicorn --reload` 자동 재기동 확인.

**추가 변경 — history 복원 시 답변 분리/그룹 카드 시각 정리**
- 사용자 케이스: 한 turn 의 답변이 여러 메시지(예: "보통 많이 가는 7일 기준으로 잡겠습니다." + 본문)로 분리되어 보이고, 그 위에 group 카드("11개 작업 완료")까지 아바타+에이전트명 라벨이 붙어 답변 3개로 인식됨.
- 원인:
  - 백엔드 deepagents 가 한 turn 의 ai 응답을 messages 에 2개 이상으로 저장(중간 멘트 + 본문). `extractPlansFromMessages` 다음 forEach 가 ai 메시지마다 별도 assistant feed item 을 push.
  - `ActivityGroupCard` 가 `AgentAvatar` + `agentName` 라벨을 노출하여 답변과 시각적으로 동일하게 보임.
- 변경:
  - `ClientChatView.tsx` history 복원 forEach: 같은 turn 내 이전 assistant 항목이 있으면 `content` 를 `\n\n` 으로 이어붙여 1개의 assistant feed item 으로 통합. (user 메시지 만나면 turn 경계로 break.)
  - `ActivityGroupCard`: 아바타 + 에이전트 이름 라벨 제거. 왼쪽 들여쓰기는 동일 폭의 placeholder 로 유지하여 답변(AssistantMessage)과 정렬 일치. `agentName` prop 제거.
  - 호출처에서 `agentName` prop 전달 제거.
- 라이브 동작은 기존대로 `streamingIdRef` 가 같은 turn 의 모든 token 을 한 assistant item 에 누적하므로 1개로 표시됨 → history/live 동작 일치.
- `pnpm lint` (agent-web): 0 errors.

**추가 변경 — turn 완료 시 in_progress 자동 sweep (라이브 + history 양쪽)**
- 사용자가 채팅을 떠났다 돌아왔을 때 마지막 항목이 NOW(in_progress) 로 영구히 남는 케이스 발생. 모델이 답변 직전 write_todos 마킹을 누락한 결과가 DB messages 의 toolCall 인자에 그대로 영속화되어 화면에 재현됨.
- `deepagent_bridge.py`: `_sweep_final_todos()` helper 추가 — turn 정상 완료 시점의 todos 에서 `in_progress` → `completed` 변환(pending 은 보존). `stream_with_deepagent` / `resume_stream_with_deepagent` 양쪽의 final todos 재발행 부분에서 helper 호출. HITL interrupt 케이스는 이 블록 이전에 return 되므로 영향 없음.
- `ClientChatView.tsx::extractPlansFromMessages`: 각 turn 의 flush 시점에 currentTodos 의 `in_progress` 를 `completed` 로 변환한 snapshot 사용 — DB 에 박힌 누락된 마킹을 history 복원 시 자동 보정.
- graph state 자체의 todos 는 patch 하지 않음 (deepagents 의 reducer 가 add/replace 중 확실치 않아 보수적). 다음 invoke 시 LLM 이 in_progress 를 보더라도 진척도 일치 규칙에 의해 첫 호출에서 completed 로 다시 마킹되도록 위임.
- `ruff check`/`pnpm lint` 모두 통과. `uvicorn --reload` 자동 재기동 확인.

**추가 변경 — admin 디버그 패널 TODO 흐름 일치(streaming hint 적용)**
- client `TodoSidePanel` 에는 스트리밍 중 hint(WORKING 라벨)가 있지만 admin `AgentFlowDebugPanel` 의 `TodoListCard` 에는 동일 표시가 없어 흐름 일치가 깨짐. 디버그 용도에서 오히려 더 중요한 단서가 빠진 셈.
- `apps/agent-web/src/components/chat/todo-list-card.tsx` — `streamingHint?: boolean` optional prop 추가. `streamingHint && 실제 in_progress 없음` 일 때 첫 pending 항목에 amber border + 회전 스피너 + 우측 "WORKING" 라벨 표시. 진짜 `in_progress` (amber pulse)와 시각 구분.
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx` — `<TodoListCard ... streamingHint={running} />` 로 실행 중 플래그 전달.
- 기존 다른 호출처(`chat-interface.tsx`)는 optional prop이라 영향 없음.
- "이어서 진행" quick action·turn별 snapshot 페이지네이션은 admin 디버그 본래 목적(관찰)에서 부차적이라 이번 보강 범위에서 제외.
- `pnpm lint` (agent-web): 신규 에러 없음.

**추가 변경 — composer 상단 toolbar 정렬 통합**
- 모드 토글(Deep Autonomous / 계획 및 실행)과 TO-DO List 버튼이 각각 별도 row(`mb-2`)로 렌더링되어, TO-DO List 등장 시 모드 토글이 위로 한 단 올라가 보이는 시각 효과 발생.
- `ClientChatView.tsx` composer 위 영역을 하나의 `flex items-center justify-between` row로 통합. 좌측에 모드 토글, 우측에 TO-DO List 버튼. 모드 토글이 없는 케이스에서도 `<span />` placeholder로 우측 정렬 유지.
- `pnpm lint` (agent-web): 신규 에러 없음.

**추가 변경 — 스트리밍 중 진행 상태 hint(클라이언트 사이드)**
- 모델이 답변을 생성하는 동안 write_todos를 부르기 전까지는 다음 다룰 항목이 pending으로 보이는 문제 → 사용자가 진행 상태를 인지하기 어려움.
- `ClientChatView.tsx`:
  - `TodoSidePanel`에 `sending` prop 추가. 스트리밍 중이고 실제 in_progress 항목이 없으며 최신 plan을 보는 경우, 첫 미완료 항목을 시각적으로 "진행 중"으로 표시.
  - `TodoSidePanelRow`에 `streamingHint` prop 추가. hint 상태는 체크박스에 회전 스피너 + 우측에 "WORKING" 라벨로 표시하여, 모델이 진짜 in_progress로 마킹한 항목의 "NOW" 라벨과 시각적으로 구분.
  - `이어서 진행` quick action 버튼은 `sending` 중에는 숨겨 중복 클릭 방지.
- 데이터는 변경하지 않고 표시만 hint 처리하므로, 백엔드 write_todos가 도착해 실제 status가 갱신되면 그쪽이 즉시 우선 적용된다.
- `pnpm lint` (agent-web): 신규 에러 없음.

---

### [완료] MainAgentModal ARCHITECTURE 섹션 + BasicTab WORKING METHOD 섹션 제거

**작성자**: leedaeyoung

**목표**
- 플로우 캔버스에서 Main Agent 설정 모달(MainAgentModal)의 ARCHITECTURE 선택 섹션 전체 제거
- 노드 클릭 시 표시되는 설정 패널(BasicTab)의 WORKING METHOD (ARCHITECTURE) 섹션 전체 제거

**변경 파일**
- `apps/agent-web/src/components/agents/flow/MainAgentModal.tsx` — ARCH_OPTIONS, Architecture 렌더링 블록 제거, 미사용 import(Brain/Zap/LayoutList/Puzzle/cn/AgentArchitecture) 제거
- `apps/agent-web/src/components/agents/builder/BasicTab.tsx` — ARCH_OPTIONS, Working Method Field 섹션 제거, 미사용 import(cn/Zap/LayoutList/Puzzle) 제거

---

### [완료] Admin BasicTab에서 Deep Autonomous Agent 제거 + Client 모드 선택 UI

**작성자**: leedaeyoung

**목표**
- Admin 에이전트 설정의 WORKING METHOD 섹션에서 "Deep Autonomous Agent" 옵션 제거
- Client 채팅 화면에서 에이전트 architecture가 `react`인 경우 "Deep Autonomous" / "계획 및 실행" 모드를 사용자가 선택할 수 있도록 UI 추가

**변경 파일**
- `apps/agent-web/src/components/agents/builder/BasicTab.tsx` — `react` 옵션 제거, `Brain` import 제거
- `apps/agent-web/src/components/client/ClientChatView.tsx` — `runMode` state 추가, 모드 선택 토글 버튼 UI(composer 상단), invoke 시 `architectureOverride` 전달
- `apps/agent-web/src/lib/api-client.ts` — `threads.invoke`에 `architectureOverride` 파라미터 추가
- `apps/api/src/modules/threads/threads.controller.ts` — `architectureOverride` body 필드 추가
- `apps/api/src/modules/threads/threads.service.ts` — `invoke()` → runner에 `architectureOverride` 전달
- `apps/api/src/modules/client-agents/client-agents.service.ts` — `AgentSnapshot`에 `architecture` 필드 추가, `toCardSummary`에서 반환
- `packages/shared/src/types/client-agent.ts` — `ClientAgentCard.architecture` 필드 추가
- `apps/agent-runner-py/src/modules/threads/threads_router.py` — invoke 핸들러에서 `architectureOverride` → `send_message` 전달
- `apps/agent-runner-py/src/modules/threads/threads_service.py` — `send_message()` `architecture_override` 파라미터 추가 및 전달
- `apps/agent-runner-py/src/modules/agents/agents_service.py` — `stream()` `architecture_override` 파라미터 추가, `loaded["architecture"]` 덮어쓰기

**특이사항**
- Client UI는 agent.architecture === 'react'인 경우에만 모드 선택 UI 표시
- 기본 선택값은 'react' (Deep Autonomous)
- architectureOverride는 invoke 요청에만 적용되며 DB의 에이전트 설정은 변경되지 않음

---

## 2026-05-11

### [완료] Provider API Key 등록 시 실제 검증 — OpenAI/Anthropic/Google 라이브 호출

**작성자**: leedaeyoung

**문제**
- `POST /api/providers/:id/configure`가 키를 그대로 DB에 저장만 하고 검증하지 않음 — 임의의 문자열도 "연결됨"으로 표시.
- `POST /api/providers/:id/test`는 단순히 `apiKeyConfigured` 플래그만 확인 → 항상 "Connection successful" 반환.

**해결**
1. `apps/api/src/modules/providers/providers.service.ts`
   - `validateProviderKey(slug, apiKey)` 추가 — slug 별 분기:
     - `openai`: `GET https://api.openai.com/v1/models` (`Authorization: Bearer`)
     - `anthropic`: `GET https://api.anthropic.com/v1/models?limit=1` (`x-api-key` + `anthropic-version: 2023-06-01`, Context7 `/anthropics/anthropic-sdk-typescript`로 확인)
     - `google`: `GET https://generativelanguage.googleapis.com/v1beta/models?key=...&pageSize=1`
     - 그 외 cloud slug: 형식 검사만 (8자 미만 거부)
   - 8s `AbortController` 타임아웃, 실패 시 응답 body에서 `error.message` 파싱해 한국어 메시지 조합.
   - `validateLocalEndpoint(endpoint)` 추가 — `/v1/models` (OpenAI 호환) → `/api/tags` (Ollama) 순으로 폴백 ping.
   - `configure()`: cloud 타입이면 검증 통과 후에만 DB 저장. 실패 시 `BadRequestException(message)`.
   - `testConnection()`: 단순 플래그 체크 대신 위 helper 호출.

2. `apps/agent-web/src/app/(dashboard)/settings/providers/page.tsx`
   - `handleSaveKey`에 try/catch 추가 → 실패 시 `testResult` 카드에 한국어 메시지 표시 + `toast.error`. 성공 시 토스트 문구를 "API Key 검증 및 저장 완료"로 변경.

**검증**
- `pnpm lint` (apps/api): 0 errors. `pnpm build` (apps/api): 통과.
- `pnpm lint` (apps/agent-web): 0 errors.
- 수동 테스트는 사용자가 실 키로 진행 필요.

---

### [완료] Admin UI 미구현 기능 disabled 처리 — "준비 중" 배지 통일

**작성자**: leedaeyoung

**목표**
- 미구현 기능을 메뉴/탭에서 숨기지 않고, 노출은 유지하되 클릭 비활성화 + "준비 중" 배지로 통일.

**변경 사항**
- **Sidebar**: `Workflows`/`Knowledge`/`Evaluation` 메뉴를 `<Link>` 대신 `<div>` + `cursor-not-allowed` + "준비 중" 배지로 렌더. `mainNavItems`에 `disabled?: boolean` 플래그 추가. Admin 전용 `Settings` 링크도 동일 방식 (route `/settings`가 미존재 → 404 방지).
- **Topbar**: Search 입력창 → `disabled` + "준비 중" 배지(우측 inline). Bell 버튼 → `disabled` + 알림 dot 제거.
- **Agents 페이지**: `Teams`/`Run History`/`A/B 비교` 탭을 `DISABLED_TABS` Set으로 묶어 클릭 시 setTab 무시 + "준비 중" 배지. `템플릿으로 시작` 버튼은 `disabled` + 배지. (탭이 활성화 불가능하므로 `RunTraceViewer`/`AgentComparePanel` 컴포넌트는 dead reference로 유지)
- **AgentFlowSettingsPanel**: `TAB_LIST`에 `disabled?: boolean` 추가, `Handoff` 탭에 적용. 두 패널(`AgentFlowSettingsPanel` & `SubAgentSettingsPanel`)의 탭 버튼 동일 처리.

**변경 파일**
- `apps/agent-web/src/components/layout/app-sidebar.tsx`
- `apps/agent-web/src/components/layout/topbar.tsx`
- `apps/agent-web/src/app/(dashboard)/agents/page.tsx`
- `apps/agent-web/src/components/agents/flow/AgentFlowSettingsPanel.tsx`

**검증**
- `pnpm lint` (apps/agent-web) → 0 errors (95 warnings는 모두 기존 코드).

---

## 2026-05-10

### [완료] Thread 단위 Run 이력 영속화 — `GET /threads/:id/runs` 신설 (Option B)

**작성자**: daeyounglee

**문제**
- Client 채팅 history 진입 시 user/assistant 메시지만 복원됨. step trace 카드(`group` 카드)는 WebSocket 이벤트(`step.started`/`step.completed`)로만 생성되는 in-memory 상태이므로, 다시 열면 사라짐.
- DB(`step_traces`/`workflow_runs`)에는 데이터가 저장되지만 thread_id 가 없고, parent_step_id/step_id 도 없어 중첩 구조 복원 불가.

**해결책 — Option B (별도 엔드포인트 신설)**
1. **Prisma 스키마**: `WorkflowRun.threadId` + `StepTrace.stepId/parentStepId/depth` 추가 + 인덱스.
2. **Migration**: 신규 마이그레이션 디렉터리 `20260510000000_add_run_thread_id_and_step_hierarchy/migration.sql` — 기존 schema drift 때문에 `migrate dev` 가 reset 을 요구해 docker exec 로 ALTER TABLE 직접 적용 + `_prisma_migrations` 등록.
3. **Runner**: `_pump_stream` 의 `step_events.append` 에 stepId/parentStepId/depth 추가. `_persist_run` 시그니처에 `thread_id` 추가 → 4개 호출부 모두 thread_id 전달. `record_run` 의 INSERT 컬럼에 thread_id/step_id/parent_step_id/depth 추가. `threads_service.get_runs(thread_id)` 신설 + `GET /api/v1/threads/{thread_id}/runs` 라우트.
4. **NestJS API**: `threads.service.getRuns(threadId)` runner 프록시 + `GET /api/threads/:threadId/runs` 라우트 + Swagger 데코레이터.
5. **Frontend**: `api-client.ts` 에 `ThreadRun`/`ThreadRunStep` 타입 + `threads.runs(threadId)` 메서드. `ClientChatView` 복원 useEffect 에서 messages + runs 병렬 호출 후 시간순 병합 → `kind: 'group'` 카드 복원. `restoreStepTree` 헬퍼로 평탄 step 배열을 부모-자식 트리로 변환.

**변경 파일**
- `packages/database/prisma/schema.prisma`
- `packages/database/prisma/migrations/20260510000000_add_run_thread_id_and_step_hierarchy/migration.sql`
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
- `apps/agent-runner-py/src/modules/threads/threads_router.py`
- `apps/agent-runner-py/src/modules/runs/runs_writer.py`
- `apps/api/src/modules/threads/threads.controller.ts`
- `apps/api/src/modules/threads/threads.service.ts`
- `apps/agent-web/src/lib/api-client.ts`
- `apps/agent-web/src/components/client/ClientChatView.tsx`

**검증**
- `pnpm --filter @agent-studio/api lint` → 0 errors (warning 3 기존 그대로).
- `pnpm --filter @agent-studio/web lint` → 0 errors (warning 91 기존 그대로).
- Python runner import smoke test: `threads_service.get_runs` 존재 확인 OK.

**참고**
- 기존 thread (이번 변경 전 생성된 turn) 는 step trace 가 thread_id 없이 INSERT 되어 `GET /threads/:id/runs` 응답에 잡히지 않음 (백필은 추후 결정).
- step_traces 의 `parent_step_id` 는 deepagents 가 step 별로 발행하는 stepId 기준이며 동일 ID 가 다른 turn 에서 재사용될 가능성이 있어 `restoreStepTree` 는 같은 run 의 step 만으로 트리를 구성한다.

**후속 fix — group 카드가 user 메시지보다 위로 표시되는 문제**
- 원인: LangGraph 가 user 메시지에 turn **종료 시점** timestamp 를 부여 → run.startedAt(turn 시작) 이 user 메시지 timestamp 보다 이른 시각 → 단순 시간순 정렬 시 group 이 user 보다 위로 감.
- 해결: `ClientChatView` 복원 로직에서 단순 정렬 대신 `messages` 배열을 순회하며 `user` 메시지를 만날 때마다 정렬된 `runs` 에서 다음 run 을 1:1 로 끼워넣는 방식으로 변경. 매칭되지 못한 잔여 run 은 안전장치로 마지막에 append.

---

## 2026-05-09 (계속)

### [완료] Gmail 사용자 토큰 inject — NestJS 내부 endpoint userId 지원 (Phase 11-2)

**작성자**: daeyounglee

**문제**
- client 채팅에서 Gmail 도구 사용 시 본인이 연동한 계정이 아닌 admin builder 가 등록한 Project 단위 Gmail OAuth 앱의 access_token 이 사용됨.
- Phase 11-1 에서 `UserCredential(kind=tool, targetId=gmail, label=oauth-app)` 에 사용자의 refresh_token 을 저장하는 흐름은 완성했으나, runner 가 Gmail 도구 호출 시 이 token 을 쓰는 경로가 미구현.

**변경 파일 — NestJS (apps/api)**
- 수정 `apps/api/src/modules/tools/tools.service.ts`
  - `issueGmailAccessTokenByAgentId(agentId, userId?)` — `userId` 파라미터 추가.
  - userId 있으면 먼저 `tryIssueUserGmailAccessToken(userId)` 시도 → 토큰 있으면 반환. null 이면 기존 admin Project 토큰으로 fallback.
  - `tryIssueUserGmailAccessToken(userId)` 신규: `UserCredential(kind=tool, targetId=gmail, label=oauth-app)` 조회 → `decryptCredential` → Google refresh_token 으로 `oauth2.googleapis.com/token` 호출 → access_token 반환.
- 수정 `apps/api/src/modules/tools/tools.controller.ts`
  - `GET /api/internal/agents/:agentId/gmail/access-token` 에 `@Query('userId') userId: string | undefined` 파라미터 추가.
- 수정 `apps/api/src/modules/me-credentials/gmail-oauth.service.ts`
  - lint fix: `secrets` 초기화를 `= {}` 에서 선언만으로 변경 (`no-useless-assignment`).

**검증**
- `pnpm --filter api lint` → 0 errors (warning 3 기존 그대로).
- 내부 endpoint smoke test: `GET /api/internal/agents/{fake}/gmail/access-token?userId={fake}` → 404 "Agent not found" (인증 통과, fake agentId로 인한 정상 404).

---

### [완료] Gmail 사용자 토큰 inject — runner user_id 전파 (Phase 11-3)

**작성자**: daeyounglee

**목표**
- NestJS → runner → BuiltinToolsService → Gmail 도구 빌더까지 `user_id` 를 전파해 각 Gmail 도구가 access-token 요청 시 `?userId={user_id}` 쿼리를 추가하도록 변경.

**변경 파일 전체 목록**

| 파일 | 변경 내용 |
|------|---------|
| `apps/api/src/modules/threads/threads.service.ts` | `invoke()`/`resume()` 의 fetch body 에 `userId` 필드 추가 |
| `apps/agent-runner-py/src/modules/threads/threads_router.py` | `SendMessageBody` 에 `userId: str \| None = None` 필드 + `invoke` 라우트에서 `user_id=body.userId` 전달 |
| `apps/agent-runner-py/src/modules/threads/threads_service.py` | `send_message(..., user_id=None)` 파라미터 추가 → `agents_service.stream(user_id=user_id)` 전달 |
| `apps/agent-runner-py/src/modules/agents/agents_service.py` | `stream` / `load_agent_with_deps` / `_load_builtin_tools` / `_build_inline_subagent_specs` / `_build_db_subagent_specs` 모두 `user_id` 추가 |
| `apps/agent-runner-py/src/modules/builtin_tools/builtin_tools_service.py` | `BuiltinToolsService.get_tools` / `_build_all` 에 `user_id` 파라미터 추가 → `get_custom_tools(agent_id, user_id=user_id)` |
| `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/registry.py` | `get_custom_tools(agent_id, user_id=None)` 서명 변경, 5종 Gmail 빌더에 `user_id=user_id` 전달 |
| `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_search.py` | `build_gmail_search_tool(agent_id, user_id=None)` + access-token GET 에 `params={"userId": user_id}` 추가 |
| `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_fetch.py` | 동일 |
| `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_fetch_attachment.py` | 동일 |
| `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_parse_pdf_attachment.py` | 동일 |
| `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_send.py` | 동일 |

**흐름 요약**
```
Frontend (JWT userId)
  → POST /api/threads/:id/invoke (NestJS)
    → body: { content, userCredentials, userId }
      → runner POST /api/v1/threads/:id/invoke
        → SendMessageBody.userId → threads_service.send_message(user_id)
          → agents_service.stream(user_id)
            → load_agent_with_deps(user_id)
              → _load_builtin_tools(user_id)
                → BuiltinToolsService.get_tools(user_id)
                  → get_custom_tools(agent_id, user_id)
                    → build_gmail_*_tool(agent_id, user_id)
                      → GET /api/internal/agents/{id}/gmail/access-token?userId={user_id}
                        → tryIssueUserGmailAccessToken(userId) 우선
                        → fallback: admin Project 토큰
```

**검증**
- `pnpm --filter api lint` → 0 errors.
- Python import 검증: `SendMessageBody fields` / `send_message sig` / `stream sig` / `load_agent_with_deps sig` / `get_tools sig` / `get_custom_tools sig` 모두 user_id 포함 확인.
- `get_custom_tools('test-agent', user_id='test-user')` → 5종 도구 빌드 성공.
- `get_custom_tools('test-agent')` (user_id 없음) → 5종 도구 빌드 성공 (fallback 정상).

**결과**
- client 채팅에서 Gmail 도구 호출 시 본인이 연동한 Google 계정의 메일을 읽어옴 (사용자별 OAuth token 우선, admin fallback 보장).

---

### [완료] 채팅 UI 전면 재설계 — Gemini 스타일 라이트 테마

**작성자**: daeyounglee

**목표**
- client 채팅 UI 를 Gemini 스타일(라이트 배경, 중앙 정렬, 깔끔한 버블, 자연스러운 어시스턴트 텍스트)로 전면 개편.
- 메뉴·기능은 그대로 유지.

**변경 파일**
- 수정 `apps/agent-web/src/app/globals.css`
  - `--client-*` CSS 변수 전체를 다크 → 라이트 테마로 전환.
    - bg: `#0a0e1a` → `#f0f4f9` (Gemini 특유의 연한 파랑회색)
    - panel: `#0f1729` → `#ffffff`
    - text: `#e6ecff` → `#1a2138`
    - border: `#1f2a44` → `#dde3ee`
    - primary: `#3b82f6` → `#1a73e8` (Google Blue)
  - `--client-user-bubble: #e8eaf0`, `--client-user-text: #1a2138` 신규 추가.
  - `prose-client` 클래스 신규 추가 — 라이트 테마용 마크다운 prose 스타일.
- 수정 `apps/agent-web/src/app/(client)/_components/sidebar.tsx`
  - 라이트 테마에 맞게 active 색상 / hover / border 조정.
  - 헤더 `border-b` 제거 → 좀 더 open 한 느낌.
  - nav item `rounded-xl` + active 배경 `color-mix` 적용.
- 전면 재작성 `apps/agent-web/src/app/(client)/client/agents/[slug]/chat/page.tsx`
  - **레이아웃**: 헤더(`h-14`) + 스크롤 메시지 영역 + 하단 고정 입력창. 메시지 영역은 `mx-auto max-w-3xl` 로 중앙 정렬.
  - **사용자 메시지** (`UserBubble`): 오른쪽 정렬, 아바타 없음, 연회색 pill 버블 (`--client-user-bubble`).
  - **어시스턴트 메시지** (`AssistantMessage`): 왼쪽 정렬, 에이전트 초성 아바타(8px gradient circle) + 이름, 박스 없이 텍스트 그대로 흐름. `prose-client` 마크다운 렌더링.
  - **Activity Group** (`ActivityGroupCard`): "N개 작업 완료" / "작업 진행 중…" 컴팩트 pill 버튼, 클릭 시 step tree 펼침. Gemini의 "Google Search ∨" 패턴과 동일 UX.
  - **입력창**: 하단 고정, `max-w-3xl`, 둥근 border 박스 안에 자동 높이 textarea + 원형 send 버튼. 텍스트 있을 때만 버튼 파란색.
  - **빈 화면**: 에이전트 아바타 + 안내 문구 + starter 카드 2열 그리드.
  - **textarea**: 자동 높이(`scrollHeight`, max 160px), Enter 전송 / Shift+Enter 줄바꿈으로 변경 (기존 Ctrl/⌘+Enter → Enter).
  - `ClientHeader` 컴포넌트 불필요 → 인라인 헤더로 대체.
  - `AgentAvatar` 공통 컴포넌트 추출 (AssistantMessage + ActivityGroupCard 공유).

**검증**
- `pnpm --filter @agent-studio/web lint` → 0 errors (92 warnings 기존과 동일).
- dev server hot reload 정상.

---

## 2026-05-09

### [완료] Gmail OAuth — 사용자별 OAuth 앱 등록 + 연동 + 토큰 저장

**작성자**: daeyounglee

**결정 (사용자 합의)**
- ① OAuth 앱 정보: **A — 사용자가 본인 Google OAuth Client(ID/Secret/RedirectUri/Scopes)를 직접 등록**. admin builder 와 동일 구조이지만 user-scope. 본인이 본인 Gmail 만 접근.
- ② 활성화 토글: 자격증명 등록만, agent 의 도구 사용 자체는 agent builder 에서 결정.
- ③ 자격증명 불필요 도구: client/tools 미노출 (현행 유지).

**변경 파일**
- 신규 `apps/api/src/modules/me-credentials/gmail-oauth.service.ts`
  - `UserCredential(kind='tool', targetId='gmail', label='oauth-app')` 단일 row 에 OAuth 앱 + refresh_token 통합 저장.
    - `valueEnc` = `encryptCredential(JSON.stringify({ clientSecret, refreshToken }))`
    - `metadata` = `{ clientId, redirectUri, scopes, connectedAt }`
  - 6 메서드: `getApp` / `saveApp` / `clearApp` / `getAuthUrl` / `exchangeCode` / `disconnect`.
  - state 인코딩: base64url(JSON({ userId, scope:'client-tool', ts })).
  - token 교환: `https://oauth2.googleapis.com/token` `grant_type=authorization_code`. refresh_token 누락 시 명확한 에러.
  - disconnect: refresh_token 만 제거하고 OAuth 앱 설정은 유지 (재연동 시 재입력 불필요).
- 신규 `apps/api/src/modules/me-credentials/gmail-oauth.controller.ts`
  - `GET    /api/me/credentials/gmail/oauth-app`
  - `PUT    /api/me/credentials/gmail/oauth-app`
  - `DELETE /api/me/credentials/gmail/oauth-app`
  - `GET    /api/me/credentials/gmail/auth-url`
  - `POST   /api/me/credentials/gmail/callback`
  - `POST   /api/me/credentials/gmail/disconnect`
- 수정 `apps/api/src/modules/me-credentials/me-credentials.module.ts` — `GmailOAuthService`/`GmailOAuthController` 등록 + export.
- 수정 `apps/api/src/modules/me-tools/me-tools.service.ts`
  - `GmailOAuthService` 주입 → catalog 응답의 `gmail.owned` 판정에 `gmailApp.connected` 사용 (refresh_token 존재 여부).
  - `authType` 카탈로그 응답에 노출.
- 수정 `apps/api/src/modules/me-tools/me-tools.module.ts` — `imports: [MeCredentialsModule]`.
- 수정 `apps/agent-web/src/lib/api-client.ts`
  - `apiClient.meCredentials.gmail.{getOAuthApp, saveOAuthApp, clearOAuthApp, getAuthUrl, exchangeCode, disconnect}` 6 메서드.
- 수정 `apps/agent-web/src/app/(client)/client/tools/page.tsx`
  - Gmail 카드를 별도 `<GmailOAuthCard>` 컴포넌트로 분리.
  - admin builder Gmail 영역과 동일한 UI 흐름:
    - "OAuth 설정" 토글 → clientId/secret/redirectUri/scopes 입력 → 저장
    - 저장된 secret 은 placeholder `'********'` 로 표시, 변경 시 새 값 입력
    - "연동" 버튼 (configured 후) → `getAuthUrl` 결과 url 로 `window.location.href` redirect
    - 연동 후 "연동 해제" 버튼 + 연동 시각 표시
    - "설정 삭제" 버튼 — 토큰 + 앱 설정 모두 삭제
  - 다른 OAuth 도구가 추가될 경우 `targetId === 'gmail'` 분기에서 일반화 가능하도록 작성.
- 신규 `apps/agent-web/src/app/(client)/client/oauth/google/callback/page.tsx`
  - `?code=&state=` 쿼리 → `apiClient.meCredentials.gmail.exchangeCode()` 호출 → 성공 시 1.2초 후 `/client/tools` 로 redirect.
  - 에러 case (오류 / 파라미터 누락 / 거부) 모두 표시 + "도구 관리로 이동" 버튼.

**정책 / 알아둘 점**
- 사용자가 본인 Google Cloud Console 에서 OAuth 2.0 Client 를 만들고, 승인된 리디렉션 URI 로 `{origin}/client/oauth/google/callback` 을 등록해야 동작.
- admin builder 의 Project 단위 OAuth 앱(`Project.builtinConfigs.gmail_connect`) 과는 **완전히 분리** — 같은 codebase 의 두 다른 흐름이 동시 운영 가능.
- `valueEnc` 은 항상 `JSON.stringify({clientSecret, refreshToken?})` 의 암호화. clientSecret 만 변경하든 refreshToken 만 갱신하든 양쪽 다 다시 암호화 후 저장.
- runner credential injection 은 본 단계에서 미구현 — runner 가 user_credentials 의 `tool:gmail` row 의 refresh_token 으로 access_token 을 갱신해 Gmail API 호출하는 부분은 다음 사이클.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter @agent-studio/web lint` → 0 errors (warning 84, hoisting 패턴 기존과 동일).
- web hot reload 정상.

**다음 단계 (Gmail OAuth 후속)**
- runner credential injection 확장:
  - NestJS `threads.service.collectUserCredentials` 에 `tool:gmail` 자격증명 inject (`UserCredential(kind=tool, targetId=gmail, label=oauth-app)` 의 valueEnc JSON 의 refreshToken).
  - runner 가 받은 refresh_token 으로 Gmail access_token 자동 갱신 후 도구 호출 시 사용.
  - Gmail 도구(`gmail_search`/`gmail_send`/...) 가 user_credentials 우선 → admin Project 토큰 fallback 순서.
- 다른 OAuth 도구 추가 시 GmailOAuthCard 를 일반화한 컴포넌트로 리팩터.

---

### [완료] Tool 자격증명 — authType 메타 + UI 도구별 분기 (OAuth flow 분리)

**작성자**: daeyounglee

**배경**
- Phase 11-1 에서 client `/client/tools` 가 모든 도구에 동일한 라벨+API key 폼을 보여줬으나, 실제로는 Gmail (OAuth) 과 Serper/Brave/Translate (API key) 가 인증 방식이 다름.
- admin builder 의 도구 자격증명 UI 와 동일하게 도구별 분기 필요.

**변경 파일**
- 수정 `apps/api/src/modules/client-agents/tool-credential-map.ts`
  - `ToolAuthType = 'api_key' | 'oauth'` 추가.
  - `TOOL_CREDENTIAL_MAP` 각 항목에 `authType` 메타 부착.
    - `serper_search`/`brave_search`/`google_translate` → `api_key`
    - `gmail_*` → `oauth`
  - `ToolCatalogEntry.authType` 추가, `TOOL_CATALOG` 빌더 갱신.
- `me-tools.service.ts` (변경 없음 — `TOOL_CATALOG` 통째로 응답이라 `authType` 자동 노출).
- 수정 `apps/agent-web/src/lib/api-client.ts`
  - `meTools.catalog` 응답 타입에 `authType: 'api_key' | 'oauth'` 추가.
- 수정 `apps/agent-web/src/app/(client)/client/tools/page.tsx`
  - `ToolCatalogRow.authType` 타입 보강.
  - 도구별 분기:
    - `api_key` 도구: 기존 라벨+API key 입력 폼.
    - `oauth` 도구: 카드에 "OAuth 연동 (준비 중)" disabled 버튼 + 안내 박스 (`POST /api/me/credentials/gmail/auth-url` 향후 활성화 예정).

**정책 / 알아둘 점**
- OAuth 도구는 본 단계에서는 폼/버튼만 분기 — 실 OAuth flow 백엔드는 다음 사이클에서 추가.
- admin builder 의 Project 단위 OAuth 앱 설정(`gmail_connect.oauthClientId/Secret/RedirectUri`)을 그대로 활용해 user OAuth 동의 → user-scoped refresh_token 을 `UserCredential(kind=tool, targetId='gmail').valueEnc` 에 저장하는 흐름이 자연스러움 (Project OAuth 앱은 client, 사용자별 토큰은 UserCredential).
- API key 도구는 즉시 사용 가능. UI 새로고침 후 Serper/Brave/Translate 카드에서 정상 등록 가능.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter @agent-studio/web lint` → 0 errors.

**다음 단계 (OAuth flow 활성화 — 별도 사이클)**
1. backend `me-credentials/gmail-oauth.controller.ts`:
   - `POST /api/me/credentials/gmail/auth-url` — admin OAuth 앱 설정(또는 env fallback) + state 에 userId/return_url 인코딩 → Google OAuth URL 반환.
   - `POST /api/me/credentials/gmail/callback` — code 교환 → refresh_token → `UserCredential(kind=tool, targetId='gmail').valueEnc` 에 암호화 저장.
2. frontend `apps/agent-web/src/app/(client)/oauth/google/callback/page.tsx`:
   - state 에 userId 가 있으면 `/api/me/credentials/gmail/callback` 호출 → 성공 시 `/client/tools` 로 redirect.
   - 기존 admin builder callback 과는 분리 (state 분기 또는 별도 path).
3. ToolCredentialsSection 의 "OAuth 연동 (준비 중)" 버튼 활성화:
   - 클릭 시 `apiClient.meCredentials.gmailAuthUrl()` 호출 → 받은 URL 로 `window.location.href` redirect.
4. Phase 10-7 runner credential injection 확장:
   - `user_credentials['tool:gmail']` (refresh_token) 을 runner 가 받아 Gmail 도구 호출 시 access_token 갱신 후 사용.

---

### [완료] AgentDeployment.publicPath UNIQUE → INDEX 전환 (재배포 P2002 fix)

**작성자**: daeyounglee

**증상**
- 동일 agent 를 같은 환경에 두 번째 deploy 시도 시 NestJS 가 `Database error: P2002` 반환.
- v1 inactive 가 publicPath `/api/v1/agents/{slug}/{envSlug}` 를 점유한 채 v2 가 같은 publicPath 로 INSERT 되며 UNIQUE 충돌.

**원인**
- Phase 9-1 schema 에서 `publicPath String @unique` 로 정의했지만, `publicPath` 는 `(agent.slug, env.slug)` 로 결정적이라 deploy 이력이 누적되면 같은 path 가 여러 row 에 나타나는 게 자연스러움.
- application 레벨에서 `(agentId, environmentId, status='active')` 1건 보장이 이미 되어 있어 외부 라우팅 충돌은 없음 — DB UNIQUE 가 과한 제약.

**처치**
- 신규 마이그레이션 `packages/database/prisma/migrations/20260509000000_drop_agent_deployment_public_path_unique/`
  - `DROP INDEX agent_deployments_public_path_key`
  - `CREATE INDEX agent_deployments_public_path_idx` (조회 성능용 일반 인덱스)
- 수정 `packages/database/prisma/schema.prisma`
  - `publicPath String @unique` → `publicPath String` + `@@index([publicPath])`.
- prisma client 재생성, api build 0 errors.

**검증**
- 마이그레이션 적용 성공.
- DB: `agent_deployments_public_path_key` 제거됨, `agent_deployments_public_path_idx` 생성됨.
- 사용자 측 재배포 정상 가능 (UNIQUE 위반 없음, version 자동 +1).

---

### [완료] Tool 자격증명 도출/등록 — Phase 10 후속 #1

**작성자**: daeyounglee

**목표**
- Phase 10-2 의 `requiredCredentials` 도출이 modelId/mcpServerIds 만 처리하던 한계 해소.
- builtin tool slug → tool 자격증명 매핑 사전 추가, sub-agent 까지 합집합 도출.
- 사용자가 `/client/tools` 에서 도구 자격증명을 등록·검증·삭제할 수 있도록 UI/api 연결.

**변경 파일**
- 신규 `apps/api/src/modules/client-agents/tool-credential-map.ts`
  - `TOOL_CREDENTIAL_MAP` (slug → `{targetId, label, reason}`) — 9 종 (search/google/gmail) 매핑.
  - `TOOL_CATALOG` (targetId 단위 dedup + `triggers[]`) — UI 카탈로그 source.
- 수정 `apps/api/src/modules/client-agents/client-agents.service.ts`
  - `AgentSnapshot` 타입에 `builtinToolIds`, `graphDefinition.nodes[].data.{builtinToolIds,toolIds,mcpServerIds}` 추가.
  - `deriveRequiredCredentials` 확장: main + sub-agent 의 `builtinToolIds`/`mcpServerIds` 합집합 → 매핑 사전 lookup → `targetId` 단위 dedup → `RequiredCredentialDto[]` 에 합산.
- 신규 `apps/api/src/modules/me-tools/` (3 파일)
  - `me-tools.service.ts` — `catalog(userId)` : `TOOL_CATALOG` + 본인 `UserCredential(kind=tool)` 매칭 → `owned`/`userCredentials[]` 표시.
  - `me-tools.controller.ts` — `GET /api/me/tools` (JWT).
  - `me-tools.module.ts`.
- 수정 `apps/api/src/app.module.ts` — `MeToolsModule` import/등록.
- 수정 `apps/agent-web/src/lib/api-client.ts`
  - `apiClient.meTools.catalog()` 메서드 추가.
- 수정 `apps/agent-web/src/app/(client)/client/tools/page.tsx`
  - 페이지 상단을 "Provider" / "도구" 두 섹션으로 분리.
  - 신규 `<ToolCredentialsSection focusTarget={focusTarget}>` 컴포넌트:
    - `apiClient.meTools.catalog()` 로드.
    - 각 카드: `targetId`/`label`/`description`/`triggers[]` 표시 + "자격증명 추가" 폼.
    - 등록은 `apiClient.meCredentials.create({kind:'tool', targetId, label?, value})` 직접 호출.
    - test/delete 는 `apiClient.meCredentials.test/delete` 그대로.
    - `?focus=<targetId>` 쿼리 시 도구 카드도 자동 펼침/하이라이트.

**정책 / 알아둘 점**
- **도출**: `builtinToolIds` (Agent 본체) + sub-agent 노드의 `builtinToolIds` 합집합. `targetId` 단위 dedup 으로 `gmail_search/send/...` 5개 도구가 모두 `tool:gmail` 1줄로 표시.
- **등록**: 등록 자체는 `POST /api/me/credentials` (kind=tool). `me-tools` 는 카탈로그 view 만 제공.
- **검증(test) **: 현재 `tool` kind 은 형식 sanity 만 확인 (`me-credentials.service.verifyByKind` 의 `provider:openai/anthropic` 만 실 ping). gmail OAuth refresh, tavily key ping 등은 후속.
- **runner inject**: 본 단계는 도출/등록 까지만. runner deepagent_bridge 가 tool 자격증명을 도구 호출 시 활용하는 부분은 후속(도구마다 inject 패턴이 달라 별도 작업).
- 매핑 사전에 없는 빌트인 도구(utilities/`current_time`/`pdf_parse` 등)는 자격증명 도출 대상이 아니므로 client/tools 카탈로그에도 노출되지 않음.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter api lint` → 0 errors (기존 warning 3 그대로).
- `pnpm --filter @agent-studio/web lint` → 0 errors (warning 82, 신규 hoisting 패턴은 기존 코드 동일).
- web hot reload `Compiled in 3.8s` (dev 서버 자동 갱신).

**다음 단계 (Phase 10 후속 잔여)**
- runner credential injection 확장 — tool kind 자격증명을 도구 실행 시점에 inject. 도구별 패턴(OAuth, API key header 등) 분기 필요.
- Tool 자격증명 검증(test) 실 ping — gmail OAuth refresh/scope 검증, tavily key ping 등.
- Sub-agent provider key override (Phase 10-7 한계 항목).
- resume 흐름 user_credentials 전파.
- 외부 client (`/v1/chat/*`) 자격증명 흐름 — admin 자격증명 또는 system-wide provider.

---

### [완료] Phase 10-8 — 통합 검증 (자동 e2e + 수동 시나리오 가이드)

**작성자**: daeyounglee

**목표**
- Phase 10 전체(10-1 ~ 10-7) 산출물이 통합 동작하는지 자동 e2e 로 검증.
- 자동화 어려운 UI 시나리오는 사용자 수동 검증 가이드로 정리.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 10-8

**변경 파일**
- 신규 `scripts/phase-10-8-e2e.sh`
  - 1) admin login → JWT
  - 2) Agent/Env 자동 검출 → AgentDeployment 생성 (e2e 환경)
  - 3) `GET /api/client/agents` → 활성 deployment 목록에 신규 agent 포함 확인
  - 4) `GET /api/client/agents/:slug` → `requiredCredentials`/`missingCredentials` 도출 검증
  - 5) `POST /api/me/credentials` → 자격증명 등록 (이미 등록되어 있으면 기존 id 재사용)
  - 6) `POST /api/me/credentials/:id/test` → 실 ping 결과(`ok`/`message`) 출력. fake 키면 401 가능 — 정상.
  - 7) `GET /api/client/agents/:slug` 재조회 → `missingCredentials.length` 감소 확인.
  - 8) 권한 회귀 sanity (admin 단일 토큰 환경에서 자동화 한계 명시).
  - 9) 수동 검증 가이드 — modal/chat/Activity/credential_missing emit 확인 절차.

**전체 사전 검증 (자동)**
- `pnpm --filter @agent-studio/shared build` → 0 errors
- `pnpm --filter api build` → 0 errors
- `pnpm --filter api lint` → 0 errors (warning 3 그대로)
- `pnpm --filter @agent-studio/web lint` → 0 errors (warning 80, hoisting 패턴은 기존과 동일)
- runner imports OK (`deepagent_bridge`/`agents_service`/`threads_service`/`threads_router`/`streaming/*`/`hitl_gateway`)
- `_resolve_provider_key` 4 케이스 sanity 통과 (user/db/missing/no-slug)
- `bash -n scripts/phase-10-8-e2e.sh` syntax OK

**Phase 10 전체 산출 요약**

| Sub-phase | 산출 | 검증 |
|----------|------|-----|
| 10-3 | `UserCredential` 모델 + AES-256-GCM cipher | db:deploy, cipher round-trip |
| 10-2 | ClientAgents 디스커버리 (`/api/client/agents/*`) | snapshot 기반 requiredCredentials 도출 |
| 10-4 | `me-credentials` CRUD/test + `me-providers` 카탈로그 | API build 0 errors |
| 10-1 | `(client)` 라우트 그룹 + 디자인 토큰 + agents/tools UI | web lint 0 errors |
| 10-5/6 | chat/history 페이지 + Activity 패널 | streaming UX, deep link, 한국어 매핑 |
| 10-7 | runner credential injection + `credential_missing` emit | `_resolve_provider_key` 4 케이스 |
| 10-8 | 통합 e2e 스크립트 + 수동 가이드 | bash -n + 사전 build/lint |

**미정 항목 4건 default 결정 재확인 (Phase 10-9 → 결정 완료)**
1. ✅ client UI 라우트: `apps/agent-web/src/app/(client)/...`
2. ✅ Provider 모델: `UserCredential(kind=provider)` 단독
3. ✅ 암호화 키: `.env CREDENTIAL_ENCRYPTION_KEY` (ENCRYPTION_KEY fallback)
4. ✅ multi-account: `@@unique([userId, kind, targetId, label])` 라벨 기반

**잠재 후속 (Phase 10 영역 외 / 별도 작업으로 분리)**
- Sub-agent provider override — `build_subagents` 의 spec 별 user_credentials 매핑.
- resume 흐름 user_credentials 전파 — `hitl_router` → `hitl_service` → `threads_service.resume_message`.
- 외부 client (`/v1/chat/*`) 자격증명 — deployment 시점 admin 자격증명 또는 system-wide provider.
- `Tool.requiresCredential` 메타 추가 → tool/mcp 자격증명 도출 확장.
- 동일 thread 재진입 시 이전 메시지 자동 복원 (`apiClient.threads.messages`).
- NestJS 응답 envelope `{data, error}` 글로벌 인터셉터.
- 비동기 메시지 모델 — REST 즉시 `{runId, startedAt}` 응답 + runner 백그라운드.
- `GET /api/v1/chat/threads/:id/messages` cursor 페이지네이션.

**다음 단계**
- 누적 변경 분 commit/push (Phase 9 + 10 범위, 35+ 파일).
- 위 후속 항목 중 우선순위에 따라 신규 Phase 11+ 로 분할 진행.

---

### [완료] Phase 10-7 — Runner credential injection + credential_missing emit (보안·격리)

**작성자**: daeyounglee

**목표**
- NestJS 가 thread/run 시작 시 user 의 자격증명을 server-to-server 로 runner 에 inject — agent 실행 시 user 의 provider 키를 우선 사용.
- 자격증명 누락 시 runner 가 `chat.error { type:"credential_missing", details:{kind, targetId} }` emit 하여 client UI 가 도구 관리로 유도 가능.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 10-7

**변경 파일**
- 수정 `apps/api/src/modules/threads/threads.controller.ts`
  - `invoke`/`resume` 라우트에 `@CurrentUser('id') userId` 파라미터 추가 → service 로 전달.
  - `CurrentUser` 데코레이터 import.
- 수정 `apps/api/src/modules/threads/threads.service.ts`
  - `Logger` 추가, `decryptCredential` import.
  - `collectUserCredentials(userId)` private 헬퍼 신규 — `UserCredential` 활성 행을 `kind:targetId → 평문` dict 로 복호화. 복호화 실패는 warn 로깅 후 skip(전체 실패 방지).
  - `invoke(threadId, content, userId?)` / `resume(threadId, approved, userId?)` — userId 가 있으면 `userCredentials` 를 fetch body 에 포함. runner 가 internal 키로 받아 stateless 처리.
- 수정 `apps/agent-runner-py/src/modules/threads/threads_router.py`
  - `SendMessageBody.userCredentials: dict[str, str] | None` 필드 추가.
  - `/invoke` 가 `body.userCredentials` 를 `threads_service.send_message(..., user_credentials=...)` 로 전파.
- 수정 `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `send_message(thread_id, message, user_credentials=None)` — 시그니처 확장.
  - `agents_service.stream(...)` 호출 시 `user_credentials` 전달.
- 수정 `apps/agent-runner-py/src/modules/agents/agents_service.py`
  - `stream(...)` 시그니처에 `user_credentials` 추가. `loaded["userCredentials"]` 에 주입 후 `stream_with_deepagent` 로 전달.
  - 로깅에 `user_credentials_count` 만 표기 — 평문/키는 절대 로그에 남기지 않음.
- 수정 `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_resolve_provider_key(user_credentials, provider_slug, fallback_key) → (api_key|None, source)` 헬퍼 신규.
    - 우선순위: `provider:{slug}` user 키 → `loaded.providerApiKey` (DB) → None.
  - `stream_with_deepagent` 시작에서 모델 생성 전 `_resolve_provider_key` 호출 → key 가 없으면 `chat.error { type:"credential_missing", details:{kind:"provider", targetId} }` yield + return.
  - 로깅에 source(user/db) 표기로 어느 경로로 키가 사용됐는지 추적 가능.

**정책 / 보안**
- 평문 자격증명은 NestJS 가 받은 즉시 `decryptCredential` 로 복호화 → fetch body 로 전달 → runner 메모리에서만 사용. **로그/응답/DB 어디에도 평문이 남지 않음.**
- runner 의 internal `RUNNER_INTERNAL_KEY` 채널로만 자격증명이 전송됨. 외부 X-API-Key (`/v1/chat/*`) 는 본 단계에서 제외 — deployment 시점에 admin 이 등록한 자격증명을 사용하는 별도 흐름은 후속 단계.
- 누락 시 사용자 표시 메시지: "{targetId} provider 자격증명이 등록되어 있지 않습니다. 도구 관리에서 등록해 주세요." (한국어).
- `decryptCredential` 실패 행은 warn 로깅 후 skip — 전체 자격증명 dict 가 실패하지 않도록 격리.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter api lint` → 0 errors (기존 warning 3 그대로).
- runner imports OK (`from src.modules.agents.deepagent_bridge import _resolve_provider_key`).
- `_resolve_provider_key` sanity 4 케이스: user 우선(`('sk-user', 'user')`), DB fallback(`('sk-db', 'db')`), missing(`(None, 'missing')`), no slug(`(None, 'missing')`) — 모두 정합.

**현재 단계 한계 (후속 작업으로 분리)**
- **Sub-agent provider key**: `build_subagents` 가 spec 의 providerSlug 별로 user_credentials 매핑하지 않음. main agent provider 만 user 자격증명 사용 — sub-agent 가 다른 provider 면 DB fallback 사용. spec 단위 매핑은 후속 단계.
- **resume 흐름의 user_credentials**: `hitl_router.respond` 가 user_credentials 를 받지 않아 resume 시 DB fallback 만 사용. 같은 user 의 자격증명을 resume 시에도 활용하려면 hitl_router → hitl_service → threads_service.resume_message 까지 전파 필요.
- **외부 client (`/v1/chat/*`) 자격증명**: deployment 시점 admin 자격증명 또는 system-wide provider 사용. 본 단계에서 미구현.
- **Tool/MCP 자격증명**: provider 만 처리. tool/mcp 는 도구 호출 시점에 별도 inject 필요(향후 `Tool.requiresCredential` 메타 추가와 함께).

**다음 단계**
- Phase 10-8 — 통합 검증: 6개 시나리오 사용자 e2e (`/client/agents` 활성만, modal 표시, OpenAI 등록+test, chat 진입+`chat.activity_*`, 다른 user 의 cred 권한, lint/build).
- 후속 — sub-agent provider override, resume credential injection, 외부 client 자격증명 흐름, Tool.requiresCredential 메타.

---

### [완료] Phase 10-5/10-6 — Client 채팅 화면 + history 실 데이터

**작성자**: daeyounglee

**목표**
- 사용자(엔드유저) 가 배포된 agent 와 실제 대화할 수 있는 chat UI 구현.
- agent 진입 시 누락 자격증명 0건이면 ReadyCard → "새 대화 시작" 으로 chat 화면 즉시 진입.
- 대화 이력은 history 페이지에서 thread 단위로 검색·재진입.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 10-5/10-6

**변경 파일**
- 신규 `apps/agent-web/src/app/(client)/agents/[slug]/chat/page.tsx`
  - 진입 시 `apiClient.clientAgents.getBySlug(slug)` → 자격증명 누락 0건 확인. 누락 ≥1건이면 toast + `/client/agents/[slug]` 로 redirect.
  - thread 자동 생성: `?threadId=` 쿼리가 없으면 `apiClient.threads.create({ agentId, title })`.
  - `wsClient.connect()` + `wsClient.subscribeThread(threadId)` + 이벤트 핸들러 5종 (`agent.token`/`turn.completed`/`step.started`/`step.completed`/`step.failed`).
  - `agent.token`: depth=0 만 메시지 스트림에 누적, sub-agent 토큰은 표시 안 함.
  - composer: textarea + Ctrl/⌘+Enter 단축키 + sending 상태에서 disable.
  - `<MessageBubble>`: 사용자/AI 분리, 스트리밍 중 빈 content 시 "응답 생성 중…" indicator.
  - `<EmptyComposer>`: agent.starters 가 있으면 카드 클릭으로 즉시 전송.
  - `<ActivityRow>` + Activity 패널 (lg 이상):
    - 22개 도구 한국어 라벨/이모지 (`web_search`→🔍, `gmail_search`→📧 등) frontend 매핑.
    - `step.started` → running, `step.completed` → done + latencyMs, `step.failed` → failed.
    - depth 들여쓰기 (max 3 단계).
- 수정 `apps/agent-web/src/app/(client)/agents/[slug]/page.tsx`
  - `<ReadyCard>` 를 placeholder 에서 실 진입 카드로 교체 — 초록 강조 + `<Link>` "새 대화 시작" 버튼이 `/client/agents/[slug]/chat` 으로 이동.
  - `MessageSquare` lucide 아이콘 추가.
- 수정 `apps/agent-web/src/app/(client)/history/page.tsx`
  - placeholder 제거 → `apiClient.threads.list(projectId)` 실 데이터 로딩.
  - 각 thread 카드 클릭 시 `/client/agents/<agent.slug>/chat?threadId=<id>` 로 진입.
  - thread.title/agent/updatedAt/status 표시. agent 가 없는 thread 는 클릭 비활성.

**정책 / 알아둘 점**
- internal `/` socket.io namespace 사용 (Phase 10-6 권장 default). Phase 9 의 `/v1/chat` 외부 어댑터 와는 분리 — 사내 사용자(JWT) 전용.
- frontend 측에서 `step.started`/`step.completed` 의 raw stepType+name 을 받아 한국어 매핑은 client 에서 직접 — Phase 9-4 의 백엔드 매핑과 별도(외부 client 에 보내는 chat.activity_started 와 다름).
- `streamingIdRef` 로 `agent.token` 을 동일 메시지 ID 에 누적 — turn 종료 시 done flag.
- 메시지 송신 시점에 activities 를 빈 배열로 초기화 — 이전 turn 의 진행상태가 남지 않도록.
- chat 라우트 (`/client/agents/[slug]/chat?threadId=<id>`) 는 url-deep-link — 동일 thread 재진입 시 url 만으로 복원.
- thread message 이력 GET `/api/threads/:id/messages` 는 chat 진입 시 자동 로드하지 않음(향후 단계: 동일 thread 진입 시 이전 메시지 복원).

**검증**
- `pnpm --filter @agent-studio/web lint` → **0 errors** (warning 81 → 80, cascading renders 1건 정리, 신규 file 의 hoisting 패턴은 기존 코드와 동일).
- 실 동작은 dev 스택(api:4200, runner:4300, web:3001)에서 사용자 검증 필요:
  1. `/client/agents` 진입 → 카드 클릭 → 누락 0건 → "새 대화 시작" → chat 진입.
  2. composer 메시지 전송 → AI 응답 스트리밍 + Activity 패널에서 도구 호출 한국어 라벨/이모지 표시.
  3. `/client/history` 진입 → 이전 thread 클릭 → 동일 chat 라우트로 진입(이전 메시지 자동 복원은 후속 단계).

**다음 단계**
- Phase 10-7 — runner 가 user 자격증명을 dict 로 inject 받아 stateless 사용 + 누락 시 `chat.error { type:"credential_missing", details:{ targetId } }` emit.
- 후속 — 동일 thread 재진입 시 `apiClient.threads.messages(threadId)` 자동 호출 → 이전 메시지 복원.
- 후속 — Phase 10-2 의 `requiredCredentials` 도출에 toolIds 매핑 추가 (`Tool.requiresCredential` 메타 확장).

---

### [완료] Phase 10-1 — agent-web (client) 라우트 그룹 + 디자인 토큰 + agents/tools 페이지

**작성자**: daeyounglee

**목표**
- 사용자(엔드유저)용 클라이언트 앱 라우트 그룹 신설.
- 디자인 토큰(dark blue palette) 도입, 사이드바·헤더 + agents 카드 그리드 + tools 자격증명 관리 페이지 구현.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 10-1, `docs/design/agent-client/` 디자인 핸드오프 번들.

**변경 파일**
- 수정 `apps/agent-web/src/lib/api-client.ts`
  - shared 타입 import 보강 (`ClientAgentCard`/`ClientAgentDetail`/`UserCredential` 등).
  - `apiClient.clientAgents.{list, getBySlug}` (Phase 10-2 백엔드 라우트).
  - `apiClient.meCredentials.{list, create, update, delete, test}` (Phase 10-4).
  - `apiClient.meProviders.{catalog, add}` (Phase 10-4).
- 수정 `apps/agent-web/src/app/globals.css`
  - `:root` 에 client 디자인 토큰 추가 (`--client-bg/panel/border/text/muted/primary` + accent 5종).
  - `.client-app`/`.client-panel`/`.client-panel-2` 래퍼 클래스.
- 신규 `apps/agent-web/src/app/(client)/layout.tsx` — `client-app` 래퍼 + `<ClientSidebar>` + 메인 영역.
- 신규 `apps/agent-web/src/app/(client)/_components/sidebar.tsx`
  - 3개 메뉴(에이전트/도구 관리/채팅 히스토리) — plan 의 권장 IA 그대로.
- 신규 `apps/agent-web/src/app/(client)/_components/header.tsx`
  - 페이지 타이틀 + subtitle + 검색/알림 버튼(placeholder).
- 신규 `apps/agent-web/src/app/(client)/agents/page.tsx`
  - `apiClient.clientAgents.list()` → `ClientAgentCard` 그리드, env color 적용 + starters 미리보기.
  - 빈 상태 EmptyState 카드.
- 신규 `apps/agent-web/src/app/(client)/agents/[slug]/page.tsx`
  - `apiClient.clientAgents.getBySlug(slug)` → 상세.
  - `missingCredentials.length > 0` 시 `<MissingCredentialsCard>` (도구 관리 deep link `?focus=<targetId>`).
  - 누락 0건 시 `<ReadyCard>` 표시 (채팅 화면 진입은 Phase 10-5/6 에서 활성화).
  - 필요한 자격증명 목록(준비/누락 표시).
- 신규 `apps/agent-web/src/app/(client)/tools/page.tsx`
  - `apiClient.meProviders.catalog()` 카드 리스트 + 자격증명 추가 폼(라벨 + 평문 password input).
  - `?focus=<providerSlug>` 쿼리 시 해당 카드 ring 하이라이트 + 등록 폼 자동 오픈.
  - 등록 후 `apiClient.meProviders.add` 호출, 카드 내부에 `userCredentials[]` 표시(테스트/삭제 버튼).
  - 테스트는 `apiClient.meCredentials.test(id)` 결과 toast + `lastVerifiedAt` 갱신.
- 신규 `apps/agent-web/src/app/(client)/history/page.tsx` — Phase 10-5/6 placeholder.

**정책 / 알아둘 점**
- (client) 라우트 그룹은 dashboard 와 분리된 layout 을 가지면서도 동일 codebase 의 apiClient/JWT 인증을 그대로 재사용 (Phase 10 default 결정 #1).
- 디자인 토큰은 `:root` CSS 변수로 정의 — 다른 페이지에 영향 없이 `(client)` 트리에서만 `var(--client-*)` 사용.
- `?focus=<targetId>` 쿼리는 누락 자격증명 다이얼로그에서 deep link 로 사용 — 도구 관리에서 해당 카드를 즉시 펼쳐 등록 폼 노출.
- 자격증명 등록 시 평문 value 는 `<input type="password">` 로 가림. 등록 직후 응답에는 `maskedValue` 만 포함되어 평문은 영영 다시 표시되지 않음.
- multi-account: 동일 provider 에 라벨로 여러 키 등록 가능. 라벨 미입력 시 빈 문자열 default 로 단일 계정 처리.
- 채팅 화면(`/client/agents/[slug]/chat`) 과 thread 이력 연결은 Phase 10-5/6 으로 분리.

**검증**
- `pnpm --filter @agent-studio/shared build` → 0 errors.
- `pnpm --filter @agent-studio/web lint` → **0 errors** (warning 73 → 77, 신규 4 건은 `no-use-before-define` hoisting 패턴으로 기존 코드베이스도 동일).

**다음 단계**
- Phase 10-5 — `<MissingCredentialsDialog>` modal 화 + agents/[slug] 진입 시 누락 0건이면 즉시 chat 진입.
- Phase 10-6 — `app/(client)/agents/[slug]/chat/page.tsx` (thread 생성 → 메시지 송신 → `/v1/chat` 또는 internal `/` 이벤트 수신 → Activity 패널) + `app/(client)/history/page.tsx` 실 데이터.
- Phase 10-7 — runner 가 user 자격증명을 dict 로 inject 받아 stateless 사용 + 누락 시 `chat.error { type:"credential_missing" }` emit.

---

### [완료] Phase 10-4 — me-credentials CRUD/verify + me-providers 카탈로그

**작성자**: daeyounglee

**목표**
- 사용자가 본인의 도구·Provider 자격증명을 등록/수정/삭제/검증할 수 있는 JWT 보호 API 신설.
- Provider 카탈로그 + 본인 보유 여부 노출로 Client UI 의 "도구 관리" 화면 데이터 소스 제공.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 10-4

**변경 파일**
- 신규 `apps/api/src/modules/me-credentials/`
  - `dto/create-credential.dto.ts` — kind/targetId/label?/value/metadata?
  - `dto/update-credential.dto.ts` — label?/value?/metadata? (부분 업데이트)
  - `me-credentials.service.ts`
    - `list(userId, kind?)` — kind 필터, 마스킹된 dto 만 반환.
    - `create(userId, dto)` — `encryptCredential(dto.value)` → INSERT. 중복(P2002) 시 409.
    - `update(userId, id, dto)` — `assertOwnership` (다른 user 수정 시 403), 부분 업데이트 (value 교체 시 재암호화).
    - `delete(userId, id)` — assertOwnership 후 삭제.
    - `test(userId, id)` — provider:openai 는 `GET /v1/models`, provider:anthropic 은 `GET /v1/models` ping. 그 외 kind/targetId 는 형식 sanity 만(후속 확장). 검증 후 `status` 와 `lastVerifiedAt` 갱신.
    - `toMaskedDto` — `valueEnc` 을 절대 응답에 노출하지 않고 `maskedValue`(`xxxx****yyyy`) + `lastFour` 만 표시.
  - `me-credentials.controller.ts` — 5 라우트:
    - `GET    /api/me/credentials?kind=...`
    - `POST   /api/me/credentials`
    - `PATCH  /api/me/credentials/:id`
    - `DELETE /api/me/credentials/:id`
    - `POST   /api/me/credentials/:id/test`
  - `me-credentials.module.ts`
- 신규 `apps/api/src/modules/me-providers/`
  - `dto/add-provider.dto.ts` — providerId/value/label?/metadata?
  - `me-providers.service.ts`
    - `catalog(userId)` — `Provider` 전체 + 사용자의 `UserCredential(kind=provider, targetId=provider.slug)` 매칭. 응답에 `owned` 와 `userCredentials[]` 포함.
    - `addProvider(userId, dto)` — `Provider.id → slug` 매핑 후 `UserCredential(kind=provider, targetId=slug)` 생성 wrapper. 중복 시 409.
  - `me-providers.controller.ts` — `GET /api/me/providers`, `POST /api/me/providers`.
  - `me-providers.module.ts`
- 수정 `apps/api/src/app.module.ts` — `MeCredentialsModule`, `MeProvidersModule` import/등록.

**정책 / 보안 / 알아둘 점**
- `valueEnc` 평문은 **응답에 절대 노출되지 않음** — 항상 `maskedValue`/`lastFour` 만. 평문 표시가 필요하면 클라이언트가 등록 직후 일회성으로 보관.
- 본인 자격증명만 변경 가능 (`assertOwnership` → 403). 다른 user 의 id 로 PATCH/DELETE/test 시 403.
- `test` 결과를 `status`/`lastVerifiedAt` 에 반영 — UI 에 "최근 검증 시각" 표시 가능.
- provider verify 는 OpenAI/Anthropic 만 실 ping. 다른 provider/tool/mcp 는 sanity check 만 (후속 단계에서 OAuth refresh / mcp ping 등 확장).
- Provider 모델은 시스템 시드 catalog 그대로 사용. user-owned Provider 분리는 도입하지 않고, `UserCredential(kind=provider, targetId=provider.slug)` 1:N 으로 자격증명을 user 에 귀속 (Phase 10 default 결정 #2).
- multi-account: 같은 provider 에 여러 라벨로 자격증명 등록 가능 — `@@unique([userId, kind, targetId, label])` 로 중복 방지.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter api lint` → 0 errors (기존 warning 3개 그대로).

**다음 단계**
- Phase 10-1 — agent-web `(client)` 라우트 그룹 + 디자인 토큰 이식 (Pretendard + dark blue palette).
- Phase 10-5/10-6 — 채팅 화면 + 누락 자격증명 다이얼로그 + `/client/tools` 카드.
- Phase 10-7 — 보안·격리: runner 가 user 자격증명을 dict 로 inject 받아 stateless 사용, 누락 시 `chat.error { type:"credential_missing" }` emit.

---

## 2026-05-08

### [완료] Phase 10-2 — ClientAgents 디스커버리 (활성 deployment + 누락 자격증명 도출)

**작성자**: daeyounglee

**목표**
- 클라이언트 앱이 배포된 agent 카드 그리드를 렌더할 수 있도록 활성 `AgentDeployment` 만 노출.
- agent 진입 시 사용자가 등록해야 하는 자격증명 목록(`requiredCredentials`)과 실제 누락분(`missingCredentials`)을 자동 도출.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 10-2

**변경 파일**
- 신규 `packages/shared/src/types/client-agent.ts`
  - `ClientAgentEnv`/`ClientAgentCard`/`RequiredCredential`/`ClientAgentDetail`.
  - `RequiredCredential.kind` 은 `CredentialKind` (provider/tool/mcp) 재사용.
- 수정 `packages/shared/src/types/index.ts` — `client-agent` export 추가.
- 신규 `apps/api/src/modules/client-agents/client-agents.service.ts`
  - `listForUser()` — 활성 AgentDeployment 만 조회 후 `ClientAgentCard` 매핑.
  - `getBySlug(userId, slug)` — agent.slug 기준 가장 최근 active deployment + `requiredCredentials`/`missingCredentials` 도출.
  - `deriveRequiredCredentials(snapshot)` — `snapshot.modelId → Model.provider` 매핑(=`provider:{slug}`), `snapshot.mcpServerIds → mcp:{id}` 매핑.
  - `collectUserCredentialKeys(userId)` — 사용자의 `UserCredential.status='active'` row 를 `kind:targetId` 키 set 으로 수집해 diff.
  - `toCardSummary()` — snapshot.config.starters 가 있으면 카드에 노출.
- 신규 `apps/api/src/modules/client-agents/client-agents.controller.ts`
  - `GET /api/client/agents` (JWT) — `ClientAgentCard[]`.
  - `GET /api/client/agents/:slug` (JWT) — `ClientAgentDetail`.
- 신규 `apps/api/src/modules/client-agents/client-agents.module.ts`.
- 수정 `apps/api/src/app.module.ts` — `ClientAgentsModule` import/등록.

**정책 / 알아둘 점**
- 카드 요약은 **라이브 Agent** 의 name/slug/description/type 사용 (deployment.agent join) — 빌더에서 이름/설명 변경 시 client UI 즉시 반영.
- `requiredCredentials` 도출은 **snapshot** 기반 — 배포 당시 의존성 고정. 빌더에서 modelId 를 바꿔도 이미 배포된 deployment 의 자격증명 요건은 그대로(재배포 후에야 갱신).
- 도구(`toolIds`) 자격증명은 현재 도출에서 제외 — `Tool` 모델에 `requiresCredential` 메타가 없어 일괄 표기 시 노이즈가 큼. 후속 단계에서 Tool 메타 확장 후 추가 예정.
- agent.slug 가 같은 여러 환경의 deployment 가 있을 수 있어 `findFirst({ orderBy: deployedAt desc })` — 환경 선택 UI 는 후속 단계.
- JWT 글로벌 가드가 적용되어 있으므로 별도 `@ApiKeyAuth()` 사용 안 함 (이 라우트는 사내 사용자 JWT 전용).

**검증**
- `pnpm --filter @agent-studio/shared build` → 0 errors.
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter api lint` → 0 errors (기존 warning 3 그대로).

**다음 단계**
- Phase 10-4 — `apps/api/src/modules/me-credentials/` (CRUD + verify) + `apps/api/src/modules/me-providers/` (provider 카탈로그). encrypt/decrypt 는 `credential-cipher.ts` 사용.
- Phase 10-1 — agent-web `(client)` 라우트 그룹 + 디자인 토큰.

---

### [완료] Phase 10-3 — UserCredential 모델 + AES-256-GCM cipher

**작성자**: daeyounglee

**목표**
- 클라이언트 앱에서 사용자가 도구·Provider 자격증명을 직접 등록할 수 있는 DB 기반 마련.
- AES-256-GCM 암호화 유틸로 평문 노출 차단 (`maskedValue`/`lastFour` 만 응답).

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 10-3, `API.md` §10

**Phase 10 미정 항목 4건 default 결정**
1. **client UI 라우트 위치** — `apps/agent-web/src/app/(client)/...` (apiClient·로그인 재사용, 신규 앱 빌드 부담 회피).
2. **Provider 모델 확장 vs UserCredential(kind=provider)** — `UserCredential(kind=provider)` 단독. Provider 모델 침투 변경 회피, scope 단순.
3. **암호화 키 보관** — `.env` `CREDENTIAL_ENCRYPTION_KEY` 단일. KMS 도입은 후속 인프라 작업.
4. **multi-account UX** — `@@unique([userId, kind, targetId, label])` 라벨 기반. 라벨 미입력 시 빈 문자열 default 로 단일 계정.

**변경 파일**
- 수정 `packages/database/prisma/schema.prisma`
  - `enum CredentialKind` (`provider`/`tool`/`mcp`) + `enum CredentialStatus` (`active`/`invalid`/`expired`).
  - `model UserCredential` 신규 — userId/kind/targetId/label/valueEnc/metadata/status/lastVerifiedAt + User 역관계(`credentials`).
  - `label String @default("")` — `NULL` distinct 회피로 unique 보장 (DailyMetric 동일 패턴).
  - `@@unique([userId, kind, targetId, label])` + `@@index([userId, kind])`.
- 신규 `packages/database/prisma/migrations/20260508000010_add_user_credential/migration.sql` — `CREATE TYPE` 2개 + `CREATE TABLE` + 1 unique + 1 index + 1 FK.
- 신규 `packages/database/prisma/migrations/20260508000010_add_user_credential/down.sql` — `DROP TABLE` + `DROP TYPE` 2개.
- 신규 `apps/api/src/common/credential-cipher.ts`
  - `encryptCredential(plain)` / `decryptCredential(cipher)` — AES-256-GCM, `base64(iv(12) || tag(16) || ciphertext)`.
  - 키 우선순위: `CREDENTIAL_ENCRYPTION_KEY` → `ENCRYPTION_KEY` (단일 운영 fallback).
  - `maskCredentialValue(plain)`/`lastFourOf(plain)` — 표시 헬퍼.
- 수정 `.env.example` — `CREDENTIAL_ENCRYPTION_KEY` 가이드 추가 (`openssl rand -hex 32`).
- 신규 `packages/shared/src/types/user-credential.ts`
  - `CredentialKind`/`CredentialStatus`/`UserCredential` (`maskedValue`/`lastFour` 응답 필드 포함, valueEnc 평문 노출 금지) + Create/Update/Verify request/response.
- 수정 `packages/shared/src/types/index.ts` — `user-credential` export 추가.

**검증**
- `pnpm --filter @agent-studio/database db:deploy` → `Applying migration 20260508000010_add_user_credential` 성공.
- DB 검증: `user_credentials` 11 컬럼 + 1 unique + 1 index + 1 FK 정합. `CredentialKind`/`CredentialStatus` 두 enum 생성.
- `pnpm --filter @agent-studio/database db:generate` → prisma client 갱신.
- `pnpm --filter @agent-studio/shared build` → 0 errors.
- `pnpm --filter api build` → 0 errors.
- cipher round-trip sanity (32-byte hex key 발급 후 encrypt/decrypt) → ✅ 평문 일치, 마스크 `sk-t****CDEF`, lastFour `CDEF`, 짧은 입력은 `****`.

**다음 단계**
- Phase 10-2 — `apps/api/src/modules/client-agents/` (`GET /api/client/agents`, `GET /api/client/agents/:slug` + `requiredCredentials`/`missingCredentials` 도출).
- Phase 10-4 — `apps/api/src/modules/me-credentials/` + `me-providers/` (자격증명/Provider 등록 API, JWT 보호).
- Phase 10-1 — agent-web `(client)` 라우트 그룹 + 디자인 토큰 이식.

---

### [완료] Phase 9-7 — 롤백 절차 자동화 (down.sql + EXTERNAL_CHAT_ENABLED + 모듈 격리)

**작성자**: daeyounglee

**목표**
- Phase 9 가 운영에 문제가 생겼을 때 ① DB 스키마 ② Runner 외부 emit ③ NestJS 외부 라우트 세 부위를 빠르게 비활성화/롤백할 수 있도록 절차 자동화.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 9-7

**변경 파일**
- 신규 `packages/database/prisma/migrations/20260508000000_add_agent_deployment/down.sql`
  - FK → 인덱스 → 컬럼(`api_keys.agent_deployment_id`, `threads.agent_deployment_id`) → `agent_deployments` 테이블 순서로 drop.
  - 적용 후 `_prisma_migrations` 행 제거 가이드 포함.
  - "데이터 손실 — 운영 적용 전 백업 필수" 경고 명시.
- 수정 `apps/agent-runner-py/src/modules/streaming/external_event_adapter.py`
  - `EXTERNAL_CHAT_ENABLED` 환경변수 게이트(`_ENABLED`) 추가, `is_enabled()` 함수 export.
  - `_safe_emit` 시작에서 `if not _ENABLED: return` — 모든 forward_* 가 즉시 no-op.
  - 허용 false 표기: `0`/`false`/`no`/`off` (대소문자 무관).
- 수정 `apps/agent-runner-py/src/modules/streaming/external_namespace.py`
  - `connect` 핸들러 시작에서 `is_enabled()` 체크 — 비활성화 시 connect 거절(False).
- 수정 `apps/agent-runner-py/.env.example` — `EXTERNAL_CHAT_ENABLED=true` 가이드 추가.

**NestJS 모듈 격리 검증 (코드 변경 없이 import 1줄 주석으로 비활성화 가능)**
- `apps/api/src/app.module.ts` 의 `AgentDeploymentsModule`/`PublicChatModule` import 와 등록 라인을 주석 처리한 상태에서 `pnpm --filter api build` → 0 errors. 다른 모듈은 의존하지 않음을 확인.
- `ApiKeyGuard` 는 `IS_API_KEY_AUTH` 메타가 없으면 통과(true)하도록 설계되어 있어, 외부 라우트가 비활성화되어도 글로벌 가드 잔류로 인한 부작용 없음.
- 검증 후 두 라인을 즉시 복구하여 활성화 상태로 push.

**롤백 시나리오 권장 순서**
1. **즉시 차단**: `EXTERNAL_CHAT_ENABLED=false` 로 runner 재기동 → 외부 socket.io 연결/이벤트 차단.
2. **REST 차단**: `app.module.ts` 두 라인 주석 → NestJS 재기동 → `/api/v1/chat/*` 404 + 관리자 deployment 라우트 비활성화.
3. **스키마 롤백**: `psql -f packages/database/prisma/migrations/20260508000000_add_agent_deployment/down.sql` → 이후 `_prisma_migrations` 에서 해당 행 삭제.

**검증**
- `pnpm --filter api build` → 0 errors (모듈 격리 toggle 적용/복구 양쪽).
- `EXTERNAL_CHAT_ENABLED` 토글 sanity: `true`/`false`/`off`/default 4 케이스 모두 정확하게 동작 (`is_enabled()` 결과 검증).
- runner imports OK + 기본값 `True` 유지.

**Phase 9 전체 산출 요약**
| Sub-phase | 산출 | 상태 |
|----------|------|-----|
| 9-1 | AgentDeployment 모델 + ApiKey/Thread 컬럼 + 마이그레이션 | ✅ |
| 9-2 | NestJS ApiKeyGuard + AgentDeployments + PublicChat 모듈 (관리 7 + 외부 4 라우트) | ✅ |
| 9-3 | runner external_event_adapter + `/v1/chat` namespace | ✅ |
| 9-4 | activity_labels 한글/이모지 매핑 사전 | ✅ |
| 9-5 | agent-web 배포 관리 페이지 + apiClient.agentDeployments | ✅ |
| 9-6 | API.md 정합성 보정 + e2e 검증 스크립트 | ✅ |
| 9-7 | down.sql + EXTERNAL_CHAT_ENABLED + 모듈 격리 검증 | ✅ |

**다음 단계 (Phase 10)**
- Client UI 본격 구현: 배포된 agent 사용자용 위젯 + 사용자 자격증명/Provider 등록 + 누락 시 도구 관리 유도. `docs/design/agent-client/` 핸드오프 번들 reference.

**잠재 후속 (Phase 9 영역 외 분리)**
- 비동기 메시지 모델 — REST 즉시 `{runId, startedAt}` 응답 + runner 백그라운드 invoke.
- NestJS 응답 envelope `{data, error}` 글로벌 인터셉터.
- `GET /api/v1/chat/threads/:id/messages` cursor 페이지네이션.

---

### [완료] Phase 9-6 — API.md 정합성 보정 + e2e 검증 스크립트

**작성자**: daeyounglee

**목표**
- `API.md` 명세를 실 백엔드/runner 응답 모양과 일치시킴.
- 외부 client 통합 시나리오를 한 번에 돌릴 수 있는 e2e 스크립트 제공.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 9-6

**점검 결과 — API.md ↔ 실 구현 갭 정리**
| 갭 | 처치 |
|----|------|
| 응답 envelope `{data, error}` 가 실제는 raw JSON | API.md 보정 — "현 단계 raw, 향후 envelope 인터셉터" 명시 |
| `POST /threads` 응답 필드 (agentDeploymentId/agent 누락) | 백엔드 service 확장 (agent join + 응답 정렬) |
| `POST /messages` 요청 필드 `content` vs API.md `message` | 백엔드 DTO `message` 로 변경 (runner 는 둘 다 fallback 수신) |
| `POST /messages` 비동기 `{runId, startedAt}` envelope | 현재 동기 프록시 — API.md 보정, 비동기는 후속 작업 |
| `POST /resume` body `{runId, decisions[{actionRequestId,...}]}` | API.md 를 deepagents 포맷 `{decisions:[{type, edited_action?}]}` 로 보정 |
| `GET /messages` cursor 기반 페이지네이션 | 현재 미지원 — API.md 에 후속 작업 명시 |

**변경 파일**
- 수정 `API.md`
  - 응답 포맷 섹션 — raw 객체 + NestJS HttpException 표준 모양으로 보정.
  - 3.1 thread 생성 응답 — `{threadId, agentDeploymentId, agent, title, status, createdAt}` 명세.
  - 3.2 메시지 전송 — request `{message}`, response 는 runner 프록시 본문 그대로.
  - 3.3 resume — deepagents 포맷 (`{decisions:[{type:"approve"|"reject"|{type:"edit", edited_action}}]}`).
  - 3.4 메시지 이력 — 페이지네이션 미지원 명시.
- 수정 `apps/api/src/modules/public-chat/dto/invoke-message.dto.ts` — `content` → `message`.
- 수정 `apps/api/src/modules/public-chat/dto/create-public-thread.dto.ts` — `metadata?: Record<string, unknown>` 추가.
- 수정 `apps/api/src/modules/public-chat/public-chat.service.ts`
  - `createThread` 응답에 `threadId/agentDeploymentId/agent/title/status/createdAt` 정렬, agent join 추가.
  - `invoke` 시그니처 `content` → `message`, runner 본문 `{message}` 로 변경 (runner 호환 유지).
- 수정 `apps/api/src/modules/public-chat/public-chat.controller.ts` — `dto.message`/`dto.metadata` 전달.
- 신규 `scripts/phase-9-6-e2e.sh`
  - admin login → AGENT_ID/ENV_ID 자동 추출 → AgentDeployment 생성 → API Key 발급 → 외부 thread 생성 → 메시지 전송 → 이력 조회 → 권한 회귀(다른 deployment 키 → 403, revoked 키 → 401) 자동 검증.
  - 마지막 단계에 socket.io-client `node -e ...` 가이드 출력 — 외부 client 가 `chat.activity_started/completed/message_delta/turn_completed` 를 수신할 수 있는지 수동 확인용.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter api lint` → 0 errors (기존 warning 3 그대로).
- `bash -n scripts/phase-9-6-e2e.sh` syntax OK.
- 실 e2e 는 사용자 환경에서 `./scripts/phase-9-6-e2e.sh` 실행해 1~9 단계 자동 검증 + 10 단계 socket.io 수동 검증.

**향후 작업 (별도 Phase 로 분리)**
- 비동기 메시지 모델 — REST 즉시 `{runId, startedAt}` 응답 + runner 백그라운드 invoke. 현재 동기 프록시 구조 변경 필요.
- 응답 envelope `{data, error}` 인터셉터 NestJS 글로벌 등록 — 일관된 API 형태.
- `GET /messages` cursor 페이지네이션 (`before`/`limit`).

**다음 단계**
- Phase 9-7 — 롤백 절차 자동화(down.sql, AgentDeploymentsModule/PublicChatModule import 제거 토글, runner external_event_adapter hook 1줄 비활성화).
- Phase 10 — Client UI(배포된 agent 사용자용 위젯 + 자격증명 등록) 본격 구현.

---

### [완료] Phase 9-5 — agent-web 배포 관리 페이지 + apiClient.agentDeployments

**작성자**: daeyounglee

**목표**
- 관리자가 Agent 빌더에서 "배포" 클릭 → 환경 선택 → Deploy → 발급된 API Key 1회 표시 + curl 예시 카피 → 이후 키 폐기/이력 조회 가능한 UI 제공.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 9-5

**변경 파일**
- 신규 `packages/shared/src/types/agent-deployment.ts`
  - `AgentDeployment`/`AgentDeploymentApiKey`/`IssuedAgentDeploymentApiKey`/`CreateAgentDeploymentRequest`/`IssueDeploymentApiKeyRequest`.
  - `DeploymentStatus` 는 기존 enum 재사용.
- 수정 `packages/shared/src/types/index.ts` — `agent-deployment` export 추가.
- 수정 `apps/agent-web/src/lib/api-client.ts`
  - import 에 신규 타입 추가.
  - `apiClient.agentDeployments.{listByAgent, deploy, get, undeploy, listApiKeys, issueApiKey, revokeApiKey}` 7 메서드 추가 — Phase 9-2 백엔드 라우트와 1:1 매핑.
- 신규 `apps/agent-web/src/app/(dashboard)/agents/[agentId]/deployments/page.tsx`
  - 새 배포 카드: 환경 select + 설명 input + Deploy 버튼.
  - 발급된 API Key 1회 표시 카드(쟁반): 원본 rawKey + curl 예시(2단계 시나리오) + 클립보드 카피.
  - 배포 이력 테이블: 버전/환경/상태/publicPath(카피 버튼)/설명/시각/비활성화 버튼.
  - 선택된 deployment 의 API 키 목록: 마스킹/scopes/최근 사용/폐기 버튼 + 새 키 발급 폼(이름 + 기본 scope 안내).
  - 폐기/비활성화 시 `window.confirm` 으로 사용자 확인.
- 수정 `apps/agent-web/src/components/agents/flow/AgentFlowToolbar.tsx`
  - `agentId` prop 추가, lucide `Rocket` 아이콘 import.
  - "Main Agent" 버튼 좌측에 `Link → /agents/{agentId}/deployments` "배포" 버튼 추가.
- 수정 `apps/agent-web/src/app/(dashboard)/agents/[agentId]/page.tsx`
  - `AgentFlowToolbar` 호출 시 `agentId` 전달.

**정책 / 알아둘 점**
- Deploy 시 `version=auto`, `publicPath=/api/v1/agents/{agentSlug}/{envSlug}` 는 백엔드 service 가 자동 생성 — 프론트는 응답을 그대로 표시.
- raw key 는 발급 직후 1회만 노출. 재조회 불가.
- curl 예시는 2단계: thread 생성 → 메시지 전송. `publicPath` 와 함께 표시(다만 외부 채팅 라우트는 `/api/v1/chat/threads` 이라 publicPath 는 reference 표시용).
- `window.location.origin` 으로 host 추출 후 `:4200` 으로 NestJS 포트 강제 — dev 환경 편의. prod 에선 별도 host 사용 시 사용자 수동 수정 필요.
- 배포 페이지는 별도 라우트(`/agents/[agentId]/deployments`)로 분리 — Builder 캔버스 상태와 격리, 채팅 sub-route(`/agents/[agentId]/chat`)와 동일 패턴.

**검증**
- `pnpm --filter @agent-studio/shared build` → 0 errors.
- `pnpm --filter @agent-studio/web lint` → 0 errors (73 warnings 기존과 동일, 신규 경고 0).

**다음 단계**
- Phase 9-6 — `API.md` 정합성 검증 + 외부 시나리오(curl thread 생성 → 메시지 → socket.io-client `/v1/chat` 구독 → `chat.activity_started/completed/turn_completed` 수신) 통합 테스트. 다른 deployment 키로 호출 시 403 회귀, revoked 키로 호출 시 401 회귀.

---

### [완료] Phase 9-3/9-4 — Runner external_event_adapter + /v1/chat namespace + activity_labels

**작성자**: daeyounglee

**목표**
- 내부 hitl_gateway 가 발행하는 이벤트를 외부 client 용으로 정제해 별도 socket.io namespace `/v1/chat` 로 emit.
- 도구·skill·sub-agent 한글 라벨/이모지 + 입력 요약 매핑 사전 신설.
- 외부 connect 시 X-API-Key 인증 + subscribe 시 deployment 권한 검증.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 9-3 / 9-4, `API.md` §Realtime

**변경 파일**
- 신규 `apps/agent-runner-py/src/modules/streaming/__init__.py` (빈 모듈)
- 신규 `apps/agent-runner-py/src/modules/streaming/activity_labels.py`
  - `classify_step(step_type, name)` → `tool`/`agent`/`skill`/`thinking` 4종 분류.
  - `label_for(name, kind)` → `(label, icon)` 한국어 표시명 + 이모지 (gmail/web/vfs/python/task/skill 등 22개 매핑 + fallback).
  - `summarize_input(name, raw_input)` — 도구별 우선순위 필드(`q`/`file_path`/`code` 등)만 짧게 추출.
  - `summarize_output(raw_output, max_len=120)` — content/text/message/response/summary/status 우선.
- 신규 `apps/agent-runner-py/src/modules/streaming/external_event_adapter.py`
  - `EXTERNAL_NAMESPACE = '/v1/chat'`.
  - `forward_turn_started/turn_completed/agent_token/step_started/step_completed/step_failed/plan_created/hitl_request/error` 9 함수.
  - `_build_activity()` 정규화 객체: `{activityId, kind, name, label, icon, summary, depth, status, parentActivityId?, latencyMs?}`.
  - `agent.token` 은 `depth==0` 만 외부에 노출(서브에이전트 토큰은 drop).
  - `agent.reasoning` 은 외부에 미발행.
  - `turn.completed.usage` 는 `{tokens, latencyMs}` 만(cost 제외).
  - `_safe_emit` 으로 외부 emit 실패는 로깅만, 내부 흐름에 영향 없음.
- 신규 `apps/agent-runner-py/src/modules/streaming/external_namespace.py`
  - `register(sio)` — `/v1/chat` namespace 의 `connect`/`disconnect`/`subscribe`/`unsubscribe` 핸들러 등록.
  - `connect`: `auth.apiKey` 또는 `X-API-Key` 헤더 → SHA256 hash → `api_keys` JOIN `agent_deployments` 조회 → status/enabled/deployment-active 검증. 실패 시 False (소켓 거절).
  - `subscribe`: `threads.agent_deployment_id` 가 connect 시 인증된 deployment 와 일치해야 thread room 가입.
  - `sio.save_session()` 에 `apiKeyId/agentDeploymentId/agentId/projectId` 저장.
- 수정 `apps/agent-runner-py/src/modules/hitl/hitl_gateway.py`
  - `external_event_adapter` import + `external_namespace.register(sio)` 모듈 로딩 시 1회 호출.
  - 8개 emit_* 함수 끝에 `await ext.forward_*` 추가 (turn_started/turn_completed/agent_token/step_started/step_completed/step_failed/plan_created/hitl_request).
  - `emit_turn_completed` 시그니처에 `usage: dict | None = None` 추가 → 외부에 토큰/latency 전달.
  - 신규 `emit_error(thread_id, message, type?)` — 외부 chat.error 전용.
- 수정 `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `emit_turn_completed` 호출 시 `final_result.usage` 전달.

**정책**
- 내부 `/` namespace 와 외부 `/v1/chat` namespace 가 같은 `sio` 인스턴스에서 공존. 같은 thread room 이름(`thread:{id}`)을 두 namespace 가 별도로 가짐(socket.io namespace-scoped room).
- 외부 emit 실패는 try/except 로 격리 — 내부 디버그 패널 동작 보장.
- 외부 client 는 도구 input/output 의 raw 값을 보지 못하고 요약(`summary`/`outputSummary`)만 받음 — 시크릿 노출 위험 감소.
- depth>0 (sub-agent 토큰)은 message_delta 로 외부에 노출하지 않음 — UX 단순화.
- agent.reasoning(CoT) 은 외부에 미발행 — 모델 추론 과정 노출 차단.

**검증**
- `uv run python -c "from src.modules.streaming import activity_labels, external_event_adapter, external_namespace; from src.modules.hitl import hitl_gateway; from src.modules.threads import threads_service"` import OK.
- sanity: `classify_step('tool','gmail_search')='tool'`, `label_for('gmail_search','tool')=('📧 Gmail 검색','📧')`, `summarize_input('read_file', {file_path:'/tmp/x.txt'})='file_path=/tmp/x.txt'`, `EXTERNAL_NAMESPACE='/v1/chat'`.

**다음 단계**
- Phase 9-5 — `apps/agent-web` Agent 상세 페이지에 "배포" 탭 + 환경 선택 → Deploy 버튼 + API Key 발급/마스킹/revoke + publicPath/curl 예시.
- Phase 9-6 — `API.md` 검증, 외부 시나리오(curl thread 생성 → 메시지 → socket.io-client 구독 → chat.activity_started/completed/turn_completed 수신) 통합 테스트.

---

### [완료] Phase 9-2 — NestJS ApiKeyGuard + AgentDeployments + PublicChat 모듈

**작성자**: daeyounglee

**목표**
- 외부 client 가 X-API-Key 한 장으로 배포된 agent 와 대화할 수 있도록 가드/관리/외부 라우트 신설.
- NestJS 가 외부 키 검증 후 internal `RUNNER_INTERNAL_KEY` 로 runner 프록시 — 외부/내부 키 분리.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 9-2, `API.md`

**변경 파일**
- 신규 `apps/api/src/modules/auth/decorators/api-key-auth.decorator.ts`
  - `@ApiKeyAuth()` — `IS_API_KEY_AUTH` 메타데이터 부착으로 JwtAuthGuard 우회 + ApiKeyGuard 적용 신호.
- 신규 `apps/api/src/modules/auth/guards/api-key.guard.ts`
  - X-API-Key SHA256 hash 비교 + enabled/status/validFrom/expiresAt 검증.
  - `apiKey.agentDeployment.status==='active'` 검증, request 에 `apiKey`/`agentDeployment`/`agent` 주입.
  - `lastUsedAt` 업데이트는 fire-and-forget (인증 실패 분리).
- 수정 `apps/api/src/modules/auth/guards/jwt-auth.guard.ts`
  - `IS_API_KEY_AUTH` 메타 감지 시 JWT 인증 우회 (ApiKeyGuard 가 별도 처리).
- 수정 `apps/api/src/app.module.ts`
  - `ApiKeyGuard` 를 APP_GUARD 로 글로벌 등록 (메타 없으면 통과). `AgentDeploymentsModule`/`PublicChatModule` import.
- 신규 `apps/api/src/modules/agent-deployments/` (4 파일)
  - `agent-deployments.service.ts` — deploy/listByAgent/get/undeploy + issueApiKey/listApiKeys/revokeApiKey.
    - deploy: `prisma.$transaction` 으로 동일 (agent, env) active deployment 를 inactive 로 전환 후 새 row insert. version = max+1, publicPath=`/api/v1/agents/{agentSlug}/{envSlug}`, snapshot=Agent 전체 config 직렬화. `env.approvalRequired` 면 status=pending_approval.
    - issueApiKey: `as_${24bytes hex}` 생성 → SHA256 hash → DB insert. raw key 1회 응답(rawKey 필드 포함). 기본 scopes=`['chat:invoke','chat:resume','chat:read']`.
    - revokeApiKey: status='revoked' + enabled=false 동시 갱신 (소프트 삭제).
  - `agent-deployments.controller.ts` — 7 라우트:
    - `POST /api/agents/:agentId/deployments`
    - `GET /api/agents/:agentId/deployments`
    - `GET /api/agent-deployments/:id`
    - `POST /api/agent-deployments/:id/undeploy`
    - `POST /api/agent-deployments/:id/api-keys`
    - `GET /api/agent-deployments/:id/api-keys`
    - `DELETE /api/api-keys/:keyId`
- 신규 `apps/api/src/modules/public-chat/` (5 파일)
  - `public-chat.service.ts` — runner 프록시 (`/api/v1/threads/:id/invoke|resume|messages`). `assertOwnership(threadId, deploymentId)` 로 다른 deployment 키로 호출 시 403.
  - `public-chat.controller.ts` — `@ApiKeyAuth()` 클래스 단위 적용 + 4 라우트:
    - `POST /api/v1/chat/threads`
    - `POST /api/v1/chat/threads/:threadId/messages`
    - `POST /api/v1/chat/threads/:threadId/resume`
    - `GET  /api/v1/chat/threads/:threadId/messages`

**정책 / 알아둘 점**
- API Key 1장 = AgentDeployment 1개. 다른 deployment 의 thread 에는 호출 불가(403).
- thread 생성 시 `agentDeploymentId`/`agentId`/`projectId` 가 자동으로 채워져 관리자 UI 에서 deployment 별 대화 이력 조회 가능.
- runner 측은 외부 키 존재를 모르며, 내부 통신은 기존 `RUNNER_INTERNAL_KEY` 그대로 — 보안/감사 분리.
- snapshot 은 deploy 시점 Agent 전체 설정을 캡처 — 추후 롤백/재실행/감사 용도. Phase 9-3 에서 runner 가 이 snapshot 을 사용해 실행하는 단계는 별도 구현(현재 runner 는 라이브 Agent config 사용).
- `pending_approval` 상태 deployment 의 키는 ApiKeyGuard 에서 `'Agent deployment is not active'` 로 거부.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter api lint` → 0 errors (기존 warning 3개 그대로).

**다음 단계**
- Phase 9-3 — `apps/agent-runner-py/src/modules/streaming/external_event_adapter.py` 신설 + socket.io `/v1/chat` namespace 분리. 내부 `/` ↔ 외부 `/v1/chat` 매핑 표 (turn.started/agent.token/step.started/...).
- Phase 9-4 — `activity_labels.py` 도구·skill·sub-agent 한글 표시명/이모지 매핑.

---

### [완료] Phase 9-1 — AgentDeployment 모델 + ApiKey/Thread 컬럼 추가

**작성자**: daeyounglee

**목표**
- 외부 client 가 발급된 API Key 1장으로 배포된 agent 와 대화할 수 있는 공개 채팅 API 의 기반 스키마 구축.
- 후속 9-2(ApiKeyGuard + 외부 라우트), 9-3(external_event_adapter)이 의존하는 DB 모델 선반영.

**계획 문서**: `refactoring-deep-agent-plan.md` Phase 9-1, `API.md`

**변경 파일**
- 수정 `packages/database/prisma/schema.prisma`
  - 신규 `model AgentDeployment` — id/projectId/agentId/environmentId/version/status/publicPath/snapshot/description/deployedBy/deployedAt/undeployedAt + 관계(project/agent/env/user/apiKeys/threads).
    - `@@unique([agentId, environmentId, version])` 으로 동일 환경 내 버전 중복 방지(application 레벨에서 status='active' 1건 보장).
    - `publicPath` 전역 unique.
  - `User`/`Project`/`DeploymentEnvironment`/`Agent` 에 역관계 추가 (`agentDeployments`).
  - `ApiKey` — `agentDeploymentId String?` + `agentDeployment` 관계, 인덱스 추가.
  - `Thread` — `agentDeploymentId String?` + `agentDeployment` 관계, 인덱스 추가.
  - `DeploymentStatus` enum 은 기존 정의(active/inactive/deploying/failed/pending_approval) 재사용.
- 신규 `packages/database/prisma/migrations/20260508000000_add_agent_deployment/migration.sql`
  - `CREATE TABLE agent_deployments` + 5개 인덱스(pk/publicPath unique/composite unique/projectId/agentId+status) + 4개 FK(project/agent/env/user).
  - `ALTER TABLE api_keys ADD agent_deployment_id` + 인덱스 + FK(SET NULL).
  - `ALTER TABLE threads ADD agent_deployment_id` + 인덱스 + FK(SET NULL).

**정책**
- `Agent.enabled` 의 의미는 draft 활성/비활성 한정. 외부 노출 여부는 `AgentDeployment.status='active'` 가 결정 (Phase 9-2 에서 판정).
- 1개 ApiKey = 1 AgentDeployment(또는 NULL=관리용). API Key 의 scope 표준값(`chat:invoke`/`chat:resume`/`chat:read`)은 9-2 에서 적용.
- `snapshot` 컬럼: 배포 시점 Agent 전체 config. 추후 롤백/감사 용도. Phase 9-2 의 deployment service 에서 직렬화.
- 외부에서 시작된 thread 는 `Thread.agentDeploymentId` 로 추적해 관리자 UI 에서 deployment 별 대화 이력 조회 가능.

**검증**
- `pnpm --filter @agent-studio/database db:deploy` → `Applying migration 20260508000000_add_agent_deployment` 성공.
- `\d agent_deployments` 동등 쿼리 → 12 컬럼 + 5 인덱스(pk + publicPath unique + composite unique + project_id + agent_id+status).
- `api_keys.agent_deployment_id`, `threads.agent_deployment_id` 컬럼 추가 확인.
- `pnpm --filter @agent-studio/database db:generate` → prisma client 갱신.
- `pnpm --filter api build` → 0 errors (새 모델/관계로 인한 컴파일 회귀 없음).

**다음 단계**
- Phase 9-2 — `apps/api/src/modules/agent-deployments/`(관리 라우트) + `apps/api/src/modules/auth/guards/api-key.guard.ts` + `@ApiKeyAuth()` 데코레이터 + `apps/api/src/modules/public-chat/`(외부 라우트). NestJS 가 외부 키 인증 후 internal `RUNNER_INTERNAL_KEY` 로 runner 프록시.
- Phase 9-3 — `apps/agent-runner-py/src/modules/streaming/external_event_adapter.py` 신설, internal `/` ↔ external `/v1/chat` namespace 분리.

---

### [완료] Skills(SKILL.md) 본문이 적용되지 않던 버그 수정 — store 키 prefix 중복 제거

**작성자**: leedaeyoung

**배경 / 증상**
- "Skills 테스트 — 해적 어조" 에이전트(skill: `pirate-tone`)에 "자기소개 해줘" 입력 시, SKILL.md 의 어조 규칙(아하르!/~다해./⚓)이 전혀 반영되지 않은 일반 자기소개가 출력됨.
- step_traces 확인 결과 LLM 은 정상적으로 `ls("/skills/")` → `read_file(...)` 도구를 호출했으나, `ls` 결과가 비정상값(`['/skills/skills/']`)이고 `read_file` 은 디렉토리라 `Error: File '/skills' not found` 반환 → LLM 이 skill 적용을 포기.

**근본 원인**
- `skills_service.sync_agent_skills` 가 PostgresStore(namespace=("filesystem",)) 에 저장하는 key 가 `/skills/<agent_id>/<skill>/SKILL.md` 형식 — 즉 store 내부 keyspace 에 이미 `/skills/` prefix 가 박혀 있음.
- 한편 `deepagent_bridge._make_skills_backend` 는 `CompositeBackend(default=StateBackend(), routes={"/skills/": StoreBackend(...)})` 로 구성. CompositeBackend 는 라우트 매칭 시 path 에서 route prefix(`/skills/`) 를 strip 한 뒤 StoreBackend 에 위임하고, StoreBackend 의 응답에 다시 `/skills` prefix 를 prepend 하여 반환 (`_remap_file_info_path`).
- 흐름:
  1. LLM `ls("/skills/")` → CompositeBackend strip → StoreBackend.ls("/")
  2. StoreBackend 는 namespace 의 모든 키에서 `relative=item.key.lstrip("/")` 를 잘라 첫 segment 를 subdir 로 잡음. key 가 `/skills/<agent_id>/...` 이므로 subdir 이 `skills` → 결과 `/skills/`
  3. CompositeBackend remap → `/skills` + `/skills/` = `/skills/skills/` (중복 prefix)
- → LLM 이 받은 `ls` 결과에 진짜 agent 디렉토리가 보이지 않아 fallback 발생.

**수정 (apps/agent-runner-py/src/modules/skills/skills_service.py)**
- `base = f"/skills/{agent_id}/{skill['name']}"` → `base = f"/{agent_id}/{skill['name']}"` 로 변경. CompositeBackend 가 prefix 를 처리하므로 store 내부 키에는 `/skills/` 를 포함하지 않는다.
- sync 시작 시 같은 agent 의 stale 키(새 `/<agent_id>/%` + 옛 `/skills/<agent_id>/%`) 를 한 번 정리하도록 `DELETE FROM store WHERE prefix = $1 AND (key LIKE $2 OR key LIKE $3)` 추가.
- docstring 에 store 키 형식 규약과 그 이유(중복 prepend 방지) 명시.

**DB 데이터 정리**
- 기존 store 에 남아있던 stale 키 3건 SQL 로 일괄 삭제:
  `DELETE FROM store WHERE prefix='filesystem' AND key LIKE '/skills/%';` (3 rows).

**참고: deepagents Skills 동작 (Context7 `/websites/langchain_oss_python_deepagents` 확인)**
- Skills 는 progressive disclosure 방식. 초기화 시 SKILL.md 의 frontmatter 만 system prompt 의 "Skills System" 섹션에 추가됨. 본문은 LLM 이 매칭이 필요하다고 판단할 때 `ls`/`read_file` 로 읽어서 적용.
- 본 사례에서 LLM 은 매칭 시도까지는 정상이었으나 backend 의 path remap 버그로 본문에 도달하지 못한 것.

**검증**
- `uv run ruff check src/modules/skills/skills_service.py` → All checks passed.
- runner --reload 가 변경 감지 후 재기동 (`Started server process [9498]`).
- 다음 invoke 시 store 에는 `/<agent_id>/<skill>/SKILL.md` 형식으로 다시 sync 되며, `ls("/skills/")` 가 `[/skills/<agent_id>/]` 를 정상 반환하도록 수정됨 (UI 재시도 후 어조 적용 확인 필요).

**한계 / 후속**
- Skills 매칭은 여전히 LLM 의 progressive disclosure 판단에 의존. 페르소나/톤처럼 "항상 적용"되어야 하는 규칙은 SKILL.md 보다 `agent.systemPrompt` 에 직접 주입하는 편이 deepagents 설계와 일치 (이번 수정 범위 밖).

---

## 2026-04-29

### [완료] 대시보드/사용량 데이터 파이프라인 구축 — Phase 4 (Monitoring 페이지 + 사이드바)

**작성자**: daeyounglee

**목표**
- `/monitoring` 라우트 신설 — Summary/UsageChart/ExecutionLogTable 표시.
- 사이드바 admin 섹션에 모니터링 메뉴 추가.

**변경 파일**
- 신규 `apps/agent-web/src/stores/use-monitoring-store.ts`
  - `loadAll()`/`loadLogs()` — `apiClient.monitoring.{getSummary, getDailyMetrics, getLogs}` 호출. 실패 시 기존 데이터 유지.
- 신규 `apps/agent-web/src/app/(dashboard)/monitoring/page.tsx`
  - SummaryCard 5종(총호출/총토큰/총비용/평균지연/에러율) + `UsageChart`(일별 호출량 30일) + `ExecutionLogTable`(최근 50건).
- 수정 `apps/agent-web/src/components/layout/app-sidebar.tsx`
  - lucide `Activity` 아이콘 import, admin 섹션에 `/monitoring` 메뉴 추가.
- 수정 `apps/api/src/modules/monitoring/monitoring.service.ts`
  - `getMetricSummary`: `_avg.latencyMs` 추가, `MetricSummary.avgLatency` 응답 — 기존 `latency: 0` 하드코딩 제거.
  - `getDailyMetrics`: `Decimal` 직렬화 + 날짜 문자열 정규화.
  - `getRecentLogs`: 응답을 공식 `LogEntry` 형(promptId/promptName/userQuery/response/latency/cost/environment/apiKey 등) 으로 정렬. agent join, endpoint join 추가.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter api lint` → 0 errors (기존 warning 3개 그대로).
- `pnpm --filter @agent-studio/web lint` → 0 errors (73 warnings 기존과 동일).

---

### [완료] 대시보드/사용량 데이터 파이프라인 구축 — Phase 5 (Insights/환경 분포 + V2 envelope 정렬)

**작성자**: daeyounglee

**목표**
- `getGlobalUsage`/`getProjectUsage` 응답을 `GlobalUsageDataV2`/`ProjectUsageDataV2` envelope 으로 정렬.
- `WorkflowRun.environment` 기반 정확한 환경 분포 + 휴리스틱 insights 추가.

**변경 파일**
- 수정 `apps/api/src/modules/usage/usage.service.ts` — 전면 재작성.
  - `aggregateSummary({projectId?, startDate, endDate})` — `MetricSummary`(totalCalls/totalTokens/totalCost/**avgLatency**/errorRate/period).
  - `aggregateDailyMetrics({projectId?, startDate, endDate})` — `daily_metrics` 행을 date 키로 합산하여 `DailyMetric[]` 반환.
  - `aggregateProjectCards({startDate, endDate})` — 프로젝트별 `groupBy + workflow/endpoint count + 7일 sparkline`. N+1 회피 위해 한 번에 groupBy 후 메모리 매핑.
  - `aggregateEnvironmentDistribution({projectId?, startDate, endDate})` — `WorkflowRun.environment` 기준 `groupBy` + percentage 계산. NULL → 'production' 매핑.
  - `buildInsights(...)` — 3종 휴리스틱:
    - 에러율 5%↑ → `severity: warning` (15%↑은 critical), 아이콘 `AlertTriangle`
    - 일별 비용 직전일 대비 50%↑ 증가 → `severity: info`, 아이콘 `TrendingUp`
    - 비용 1위 프로젝트 → `severity: info`, 아이콘 `DollarSign`
  - `buildPromptEndpointTree(projectId, ...)` — workflow ↔ endpoint 트리 + groupBy 매트릭(modelId/modelName 은 빈 문자열 — Endpoint 모델에 컬럼 없음).
  - `buildApiKeyActivity(projectId, ...)` — apiKey별 calls/tokens/cost/latency + accessedEndpointPaths(최근 500건 기반).
  - `buildRecentLogs(projectId, ...)` + `runToLogEntry(...)` — 공식 `LogEntry` 응답.

**정책 / 알아둘 점**
- `Endpoint` 모델에 `modelId` 컬럼이 없어 promptEndpointTree.modelId/modelName 은 빈 문자열로 반환. 이전 V1 응답에서도 더미 값(`'multiple'`)이었으므로 회귀 없음.
- `ApiKey` 모델에는 `keyMasked` 컬럼이 없고 `keyPrefix/keySuffix` 만 있어 `keyMasked = "${keyPrefix}...${keySuffix}"` 로 구성.
- `inputTokens/outputTokens` 는 현재 별도 컬럼 없음(WorkflowRun.totalTokens 만 누적) → 응답에서 0 으로 두고 totalTokens 만 정확. 분리 누적은 후속 단계.

**검증**
- `pnpm --filter api build` → 0 errors.
- `pnpm --filter api lint` → 0 errors.
- `pnpm --filter @agent-studio/web lint` → 0 errors.

**다음 단계 (잔여)**
- 실 데이터(채팅 1회 invoke → workflow_runs 1행) 후 `/dashboard`·`/usage`·`/monitoring` 화면이 정상 렌더되는지 사용자 UI 검증.
- input/output 토큰 분리 누적(`AIMessageChunk.usage_metadata`의 input/output 분리 후 record_run 컬럼 추가 검토).
- DailyMetric 에 environment/agentId 컬럼 추가하여 cron 집계 단계에서 분포까지 미리 계산할지 결정 (현재는 ad-hoc 쿼리).

---

## 2026-04-28

### [완료] 대시보드/사용량 데이터 파이프라인 구축 — Phase 1 (Runner 기록)

**작성자**: daeyounglee

**목표**
- agent-runner-py 의 turn 종료 시점에 raw SQL 로 `workflow_runs` 1행 + `step_traces` N행 기록.
- deepagent_bridge 의 `_pump_astream_events` 루프에서 `AIMessageChunk.usage_metadata` 누적 → `run.completed.data.usage` 에 inputTokens/outputTokens/totalTokens/totalCost 포함.
- 모델별 단가는 `runs/pricing.py` 단일 모듈에서 계산.

**변경 파일**
- 신규 `apps/agent-runner-py/src/modules/runs/__init__.py`
- 신규 `apps/agent-runner-py/src/modules/runs/pricing.py` — `estimate_cost(model_id, in, out)` 단가 테이블 (OpenAI/Anthropic/Google 주요 모델). provider prefix(`openai:gpt-4o`)도 정규화.
- 신규 `apps/agent-runner-py/src/modules/runs/runs_writer.py`
  - `record_run()` — workflow_runs 1행 + step_traces N행 raw SQL INSERT. RunStatus/NodeType/StepStatus enum cast.
  - `aggregate_usage_metadata()` — chunk usage 합산 헬퍼.
- 수정 `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_pump_astream_events`: `usage_acc` 인자 추가, `AIMessageChunk.usage_metadata` 누적.
  - `stream_with_deepagent` / `resume_stream_with_deepagent`: turn 종료 후 `estimate_cost()` 호출, `run.completed.data.usage = {inputTokens, outputTokens, totalTokens, totalCost, modelId}` 포함.
- 수정 `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `_pump_stream`: open step 추적(`open_steps`) + `step_events` 수집(`completed`/`failed` + 시작/종료 시각 + input/output/error). 반환 dict 에 `step_events` 포함.
  - `_persist_run()` 헬퍼 신설 — `agents` 테이블에서 `project_id`/`config.environment` 조회 후 `record_run()` 호출. 실패해도 사용자 응답에 영향 없도록 try/except 로 감쌈.
  - `send_message` / `resume_message` — 정상 완료/error 분기 모두에서 `_persist_run()` 호출. HITL paused 분기는 호출하지 않음 (resume 후 1건만 기록).
- 수정 `packages/database/prisma/schema.prisma` (Phase 1.5 완료분과 동일).

**정책**
- HITL pause 상태에서는 기록 생략 (resume 후 정상 완료 시 1건만 기록).
- error 발생 시에도 status='failed' 로 기록하여 에러율 통계가 정확히 산출.
- workflowId 는 deepagent 경로에서 NULL, langgraph_service(workflows v1) 경로에서는 그대로 채움.

**검증**
- `uv run python -c "from src.modules.threads import threads_service; from src.modules.agents import deepagent_bridge; from src.modules.runs import runs_writer, pricing"` import OK.
- `pricing.estimate_cost('gpt-4o-mini', 1000, 500)` = 0.00045, `('claude-opus-4-7', 1000, 500)` = 0.0525, `('openai:gpt-4o', 1000, 500)` = 0.0075, `('unknown', 1000, 500)` = 0.0 — 정상.

---

### [완료] 대시보드/사용량 데이터 파이프라인 구축 — Phase 2 (NestJS daily_metrics cron)

**작성자**: daeyounglee

**목표**
- `@nestjs/schedule` 기반 cron 으로 매일 자정 직후 어제 데이터를 `daily_metrics` 테이블에 집계.
- 수동 백필/재집계용 admin endpoint 제공.

**변경 파일**
- 신규 `apps/api/src/modules/daily-metrics/daily-metrics.service.ts`
  - `@Cron('0 5 0 * * *')` — 매일 00:05 UTC 어제 데이터 집계.
  - `aggregateForDate(date)` — `dailyMetric.deleteMany` 후 `workflowRun.groupBy({by: [projectId, workflowId, endpointId]})` + 별도 errors groupBy → `dailyMetric.create` (멱등 보장).
  - 집계 컬럼: calls/tokens/cost/avgLatency/errors. avgLatency 는 sum(latencyMs)/calls.
- 신규 `apps/api/src/modules/daily-metrics/daily-metrics.controller.ts`
  - `POST /api/daily-metrics/aggregate` (admin) — body `{date?: string}` 로 특정 날짜 백필. 미지정 시 어제.
- 신규 `apps/api/src/modules/daily-metrics/daily-metrics.module.ts`
- 수정 `apps/api/src/app.module.ts`
  - `ScheduleModule.forRoot()` import 등록, `DailyMetricsModule` 등록.
- 수정 `apps/api/package.json` — `@nestjs/schedule@^6.1.3` 추가.

**정책**
- DailyMetric.unique = (date, projectId, workflowId, endpointId). NULL 동등 비교 한계 회피 위해 cron 시작 시 같은 date row 일괄 삭제 후 insert (멱등).
- DailyMetric에 environment/agentId 컬럼이 없어 환경 분포·agent별 집계는 ad-hoc 쿼리로 처리 (Phase 5 범위).

**검증**
- `pnpm --filter api lint` → 0 errors (기존 warning 3 그대로).
- `pnpm --filter api build` → 0 errors.

---

### [완료] 대시보드/사용량 데이터 파이프라인 구축 — Phase 3 (Frontend stores 전환)

**작성자**: daeyounglee

**목표**
- dashboard/usage stores 가 `mockApi` → `apiClient` 로 실 API 를 호출하도록 전환.

**변경 파일**
- `apps/agent-web/src/stores/use-dashboard-store.ts` — `mockApi.dashboard.getOverview` → `apiClient.dashboard.getOverview`.
- `apps/agent-web/src/stores/use-usage-store.ts` — `mockApi.usage.*` → `apiClient.usage.*` (getGlobalUsage/getProjectUsage).

**검증**
- `pnpm --filter @agent-studio/web lint` → 0 errors (73 warnings 기존과 동일, 변경 파일에서 신규 경고 없음).
- 백엔드 라우트(`/api/dashboard`, `/api/projects/:projectId/dashboard`, `/api/usage/global`, `/api/usage/projects/:projectId`)와 apiClient 메서드가 이미 정합 — 수정 불필요.

**다음 단계 (Phase 4 이상)**
- Phase 4 — `/monitoring` 페이지 + 사이드바 와이어링 → 2026-04-29 완료.
- Phase 5 — Insights/환경 분포 정확화 → 2026-04-29 완료.

---

### [완료] 대시보드/사용량 데이터 파이프라인 구축 — Phase 1.5 (DB 스키마)

**작성자**: daeyounglee

**배경**
- 대시보드(`/dashboard`)와 사용량(`/usage`) UI/API/apiClient는 모두 완성되어 있으나, **WorkflowRun·DailyMetric을 채우는 코드가 어디에도 없음**.
- 프론트도 `mockApi`만 호출(use-dashboard-store.ts:20, use-usage-store.ts:70/82) — 실데이터 0%.
- agent-runner-py가 deepagent_bridge → threads/messages 경로로 일원화된 뒤 metric 후크가 누락됨.

**목표 (전체 Plan)**
1. **Phase 1.5** (이 작업) — DB 스키마: WorkflowRun.workflowId nullable + agentId/projectId/latencyMs 추가.
2. Phase 1 — Runner: turn 종료 시 raw SQL로 workflow_runs + step_traces 기록.
3. Phase 2 — NestJS: @nestjs/schedule cron으로 daily_metrics 자동 집계.
4. Phase 3 — Frontend: stores를 mockApi → apiClient 전환.
5. Phase 4 — /monitoring 페이지 + 사이드바.
6. Phase 5 — Insights/환경 분포 정확화.

**Plan 파일**: `/Users/daeyounglee/.claude/plans/transient-frolicking-tarjan.md`

**Phase 1.5 변경 파일**
- `packages/database/prisma/schema.prisma` (WorkflowRun)
  - `workflowId String?` (nullable) — deepagent 채팅은 워크플로우 없이 invoke됨.
  - `agentId String?` 신규 — agent별 사용량 집계용.
  - `projectId String?` 신규 — 집계 cron이 projectId로 group by.
  - `latencyMs Int?` 신규 — monitoring.service.ts의 latency:0 하드코딩 제거 준비.
  - `agent`/`project` 관계 + 인덱스(`agentId, startedAt(Desc)`, `projectId, startedAt(Desc)`).
- `packages/database/prisma/schema.prisma` (Agent.workflowRuns, Project.workflowRuns 역관계).
- `packages/database/prisma/migrations/20260428000000_workflow_run_optional_workflow_and_metrics/migration.sql` 신규.

**검증**
- 마이그레이션 SQL 직접 실행 → `\d workflow_runs` 로 4개 컬럼 + 2개 인덱스 + 3개 FK 확인.
- `_prisma_migrations` 테이블에 `20260428000000_workflow_run_optional_workflow_and_metrics` finished 등록.
- `pnpm prisma generate` + `pnpm --filter api build` → 0 errors (스키마 변경으로 인한 컴파일 회귀 없음).

**다음 작업**
- Phase 1: agent-runner-py에 `runs_writer.py` 신설, deepagent_bridge에서 token usage 누적, threads_service에서 turn 종료 시 record_run() 호출.

---

## 2026-04-27

### [완료] Skills 점검용 Skill·Agent 시드 + Recursion limit 조정

**작성자**: leedaeyoung

**배경**
- 머지 직후 Skills 동작을 UI에서 검증하려고 Skill 2개·Agent 3개를 직접 생성.
- "김치찌개 어떻게 만들어? 레시피로 답해줘" 질문에서 `GraphRecursionError: Recursion limit of 20 reached` 발생 — 시스템 프롬프트가 매 응답마다 `ls + read_file` 강제 호출 + deepagents middleware step 누적으로 한도 초과.

**원인**
- `deepagent_bridge.py` 의 `step_limit = reasoning_raw.get("stepLimit", 20)` — 기본 20이 deep-agent + 도구 호출 + skill 진단 흐름에 너무 빡빡.

**처치**
- 세 테스트 에이전트의 `reasoningConfig.stepLimit` 을 80 으로 PATCH (`PATCH /api/agents/:id` body `{"reasoningConfig":{"stepLimit":80}}`). DB 컬럼 `agents.reasoning_config` 에 `{"stepLimit": 80}` 반영 확인.
- runner `--reload` 로 다음 invoke 부터 적용.

**시드된 리소스 (DB live)**
- Skills (admin user 소유)
  - `f09e11bc-d9a5-4b63-8d7c-85d5d75e9f02` `pirate-tone` — 모든 답변에 "아하르!" 시작, "~다해." 어미, ⚓ 마무리, 1인칭 "이 해적".
  - `040ed8e4-b9e1-401a-8cae-30ecd397afac` `recipe-format` — `## 재료/도구/조리 시간/만드는 법` 4섹션 마크다운 강제.
- Agents (Demo Project)
  - `f5598df7-88e7-4ab0-aa9c-e4caaf2e83d2` "Skills 테스트 — 해적 어조" (skillIds=[pirate-tone])
  - `a6d9414a-c4ae-475e-b2b1-fc13fefd43c0` "Skills 테스트 — 다중 Skills" (skillIds=[pirate-tone, recipe-format])
  - `4bc42420-72b9-4552-bdec-9302459ce65b` "Skills 테스트 — 대조군" (skillIds=[])

**알아둘 점**
- 시스템 프롬프트에 "매 응답마다 ls + read_file" 강제 시 step 소비가 큼. 80 도 부족하면 시스템 프롬프트를 약화(SkillsMiddleware 자동 안내에 맡김)하는 게 다음 단계.
- `safetyLevel` 필드는 코드 어디서도 사용되지 않음 — UI 노출만 됨, 향후 의미 부여 또는 제거 결정 필요.

---

### [완료] feature/tool → refactoring-deep-agent 머지 (충돌 4파일 해결)

**작성자**: leedaeyoung

**배경**
- yoojinhee 작업분 `ba34544 feature: gmail 도구 추가` 가 deep-agent와 같은 파일을 동시 수정 — 머지 시 4파일 content conflict.
- feature/tool 측은 deepagents 마이그레이션 이전 옛 LangGraph `StructuredTool` 기반 sub-agent 위임 코드를 그대로 가지고 있었고, deep-agent 측은 deepagents `subagents=` 파라미터·`_pump_astream_events` 어댑터·skills/가드레일 와이어링 등 새 구조.

**충돌 처리 방침**
| 파일 | 충돌 수 | 해결 |
|------|--------|------|
| `apps/agent-runner-py/pyproject.toml` | 1 | 양쪽 의존성 union (`jsonschema` + pdf 패키지 3종) |
| `apps/agent-runner-py/src/modules/agents/agents_service.py` | 4 | conflict 1·3·4 HEAD 채택 (deepagents subagents 새 구조 보존) / conflict 2 양쪽 다 살림 (`graph_definition.nodes` 추출 + `resolved_thread_id = thread_id or agent_id` 둘 다 필요) |
| `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` | 8 | 모두 HEAD 채택. import는 양쪽 살림 (`ChatAnthropic` + `CompositeBackend`/`StateBackend`/`StoreBackend`). feature/tool 의 옛 인라인 `astream_events` 처리(token/tool_start/tool_end 분기)는 이미 `_pump_astream_events` 로 추출됐으므로 폐기 |
| `apps/agent-runner-py/uv.lock` | 1 | `git checkout --ours` 후 `uv lock` 재생성 |

**가져온 신규 파일 (충돌 없음)**
- `custom_tools/gmail_fetch_attachment.py`, `gmail_parse_pdf_attachment.py`, `gmail_send.py`, `pdf_parse.py`, `document_preprocess.py`
- `custom_tools/skills/hr_document_skill.md`, `PROMPT_gmail_fetch.md`
- `docs/PDF_PARSE_RULES_EXAMPLE.json`, `docs/TEST_SCENARIO_chat.md`, `docs/TEST_SCENARIO_hr_team.md`

**검증**
- 충돌 마커 0건 (.py / pyproject / uv.lock 모두)
- `uv run python -c "from src.modules.agents import deepagent_bridge, agents_service"` import OK
- lint-staged 통과 (web/api/shared)
- runner 깨끗 재기동 (PID 88482) → import 에러 0건, `Application startup complete`
- 머지 커밋 `cabd5f2`, push 완료 (`7d1d521..cabd5f2 refactoring-deep-agent`)

**잠재 이슈 (yoojinhee 협의 필요)**
- feature/tool 의 `_load_teammate_tools` 기반 sub-agent 위임 (StructuredTool delegate) 코드는 deepagents subagents 신구조와 호환되지 않아 폐기됨.
- yoojinhee 가 sub-agent 관련 추가 변경을 했다면 신구조 (`_build_inline_subagent_specs` / `_build_db_subagent_specs` → `subagents=` 파라미터) 에 맞춰 재반영 필요.

---

### [완료] feature/refactoring-agent-page → refactoring-deep-agent 머지

**작성자**: leedaeyoung

**대상 커밋**
- `049d683 feat(runner): 가드레일 출력 필터·PII·JSON 스키마 검증 와이어링 (deferred 모드)`

**해결**
- `merge-base = 24cb4f2` (deep-agent tip), feature 측에 1 커밋 앞서 있어 fast-forward 가능했지만, 기존 패턴 (`Merge branch 'feature/refactoring-agent-page' into refactoring-deep-agent` 형태) 따라 `--no-ff` 로 명시적 머지 커밋 생성.
- 충돌 없음, 4 files / +277/-4.
- 머지 커밋 `7d1d521`, push 완료 (`24cb4f2..7d1d521 refactoring-deep-agent`).

---

### [완료] Prisma 마이그레이션 누락으로 인한 P2022 (`agents.skill_ids does not exist`) 처치

**작성자**: leedaeyoung

**증상**
- 신규 에이전트 생성/조회 시 web 콘솔에 `Runtime ApiError: Database error: P2022`. NestJS 로그에 `Prisma error P2022: {"modelName":"Agent","column":"agents.skill_ids"}` 반복.

**원인**
- `git pull` 로 가져온 마이그레이션 `20260426000000_add_agent_skill_ids` 이 로컬 DB에 적용되지 않은 상태. schema.prisma·prisma client 만 갱신되어 컬럼 부재 + 코드 참조 → P2022.

**처치**
- `pnpm db:generate` 로 prisma client 재생성 (skillIds 타입 인식).
- `prisma migrate deploy` 실행 — schema 옆 `.env` 를 못 찾아 `DATABASE_URL` 인라인 export 후 진행 (`postgresql://agentstudio:agentstudio@localhost:5433/agentstudio`).
- 마이그레이션 적용 SQL: `ALTER TABLE "agents" ADD COLUMN "skill_ids" TEXT[] DEFAULT ARRAY[]::TEXT[];` — 데이터 손실 없는 안전한 변경.
- DB 검증: `SELECT column_name FROM information_schema.columns WHERE table_name='agents' AND column_name IN ('skill_ids','guardrails_config','tool_permissions');` 3 row 존재 확인. 이후 12:19 이후 새 P2022 0건.

**알아둘 점**
- prisma CLI 가 `--schema=...` 로 스키마 위치만 받고 `.env` 는 cwd 또는 schema 옆에서 찾음. 다른 환경에서 `pnpm db:migrate` 가 막히면 `DATABASE_URL` 인라인 export 후 실행할 것.

---

### [완료] 가드레일 출력 필터·PII·JSON 스키마 검증 와이어링 (deferred 모드)

**작성자**: leedaeyoung

**문제**
- 입력 차단(`blockedTopics`)은 정상이나 출력 정규식(`outputFilters`)·PII 리덕션(`piiDetection`)이 동작 안 함 — UI에서 가드레일을 켜도 응답에 패턴 그대로 노출됨.
- `jsonSchemaValidation`은 UI 노출되지만 bridge에서 `validate_schema()`가 호출되지 않아 완전 미동작.

**원인**
- `_pump_astream_events`의 `on_chat_model_stream` 분기에서 LLM 토큰 chunk 단위로 `output_guard.filter()` 호출 → OpenAI는 `"내 "`, `"API"`, `"_KEY"`, `"="` 식으로 잘게 쪼갠 토큰을 보내므로 정규식·PII 패턴이 chunk 경계에서 깨져 매칭 실패.
- `validate_schema()`는 `OutputGuardrail`에 정의돼 있으나 bridge 어디에서도 호출되지 않음 + `jsonschema` 의존성 미선언.

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_pump_astream_events`에 `defer_token_filter: bool = False` 인자 추가 — True면 `on_chat_model_stream` 토큰 emit 건너뜀.
  - `_filter_last_assistant_message(messages, output_guard)` 헬퍼: `run.completed.messages` 마지막 어시스턴트 content에 한 번에 filter 적용.
  - `_last_assistant_content(messages)` 헬퍼: 마지막 어시스턴트 텍스트 추출 (스키마 검증 입력용).
  - `stream_with_deepagent` / `resume_stream_with_deepagent` 양쪽에서:
    - `defer_token_filter = bool(output_filters or pii_detection or (json_schema_validation and output_schema))`
    - 종료 후 `_filter_last_assistant_message` 적용 → `validate_schema()` 호출 → 실패 시 `error: guardrail_blocked` yield + return → 통과 시 기존 fallback emit + `run.completed`.
- `apps/agent-runner-py/pyproject.toml`, `uv.lock`
  - `jsonschema==4.26.0` (+ jsonschema-specifications, referencing, rpds-py) 의존성 추가.

**UX 트레이드오프**
- 가드레일 활성 에이전트는 토큰 streaming UX 일부 손실 (응답이 한 번에 표시) — 패턴 깨짐 방지를 위한 의도적 결정.
- 가드레일 OFF 에이전트는 영향 없음 (`defer_token_filter=False`로 기존 streaming 그대로).

**검증 결과 — UI 실기동**
- 시나리오 ① 입력 차단 (`폭탄`/`비밀번호`) — ✅ `Message contains blocked topic: ...` 차단 메시지, LLM 호출 없음.
- 시나리오 ② 출력 정규식 (`API_KEY=...`, `sk-...`) — 패치 후 검증 대기 (사용자 UI에서).
- 시나리오 ③ PII 리덕션 (이메일/전화/주민번호/카드/IP) — 패치 후 검증 대기.
- 시나리오 ④ JSON 스키마 검증 — 단위 테스트 통과 (`jsonschema.validate`로 valid/missing/extra/type-mismatch 모두 정상 분기).

**테스트 에이전트**
- `dbce3fa7-0998-44d3-b2ec-0c272258b173` "가드레일 테스트 에이전트" — blockedTopics + outputFilters + piiDetection
- `fc9ff6a7-4b20-459f-950a-0b65c249ccba` "JSON 스키마 가드레일 테스트" — jsonSchemaValidation + outputSchema(`{name,age}`, additionalProperties:false, age 0~150)

**남은 작업 (다음 세션)**
1. 시나리오 ②③ 사용자 UI 검증 결과 캡처 후 `[REDACTED]`/`[FILTERED]` 정상 치환 여부 최종 확인.
2. 시나리오 ④ JSON 스키마 검증 6종(통과/JSON-실패/필수-누락/추가-필드/타입-위반/범위-위반) UI 검증.
3. `safetyLevel`(low/medium/high) 필드는 현재 어디서도 사용되지 않음 — UI에 노출되어 있으니 의미를 부여할지 또는 제거할지 결정.

---

## 2026-04-26

### [완료] Skills를 Agent에서 사용하도록 연결 (Phase 1~5 완료)

**작성자**: daeyounglee

**완료 범위**
- Phase 1: `Agent.skillIds: String[]` 컬럼 추가 + 마이그레이션 (`20260426000000_add_agent_skill_ids`) + `packages/shared` 타입 반영
- Phase 2: NestJS API DTO/Service에 `skillIds` 패스스루 + `validateSkillOwnership()` 헬퍼로 본인 소유 Skill 검증 (graph nodes 재귀 포함)
- Phase 3: Runner `agents_service.load_agent_with_deps()` 가 `skillIds`/sub-agent skill_ids 반환
- Phase 4: `deepagent_bridge`에 `_sync_skills_for_run()`/`_make_skills_backend()` 헬퍼 + `stream_with_deepagent` / `resume_stream_with_deepagent` 양쪽에서 `create_deep_agent(skills=, backend=, store=)` 인자 전달
- Phase 5: 프론트 `SkillsTab` 신설, `AgentFlowSettingsPanel` TAB_LIST에 추가, `SubAgentNodeData.skillIds` 필드 + 서브에이전트 패널 동기화

**Phase 6 (Sub-agent Skills)**: build_subagents에서 sub-agent dict에 `skills=[/skills/{slug}/]` 자동 주입, `_sync_skills_for_run`이 main + sub 모두 PostgresStore 동기화. 별도 분리 없이 Phase 4에 흡수됨.

**검증 결과**
- `pnpm --filter @agent-studio/web lint` → 0 errors
- `pnpm --filter api lint` → 0 errors
- `pnpm --filter @agent-studio/shared build` → OK
- `uv run pytest tests/modules/agents/test_deepagent_bridge.py` → 4건 신규 통과(과거 8건 실패 → 4건). 잔존 4건은 기존 `astream` 기반 mock의 회귀 — 2026-04-24 일지에 명시된 기술부채로 별도 정리 필요.
- DB live: `agents.skill_ids` 컬럼 적용 확인 (3 rows, `{}` 기본값).

**핵심 변경 파일**
- `packages/database/prisma/schema.prisma` — Agent.skillIds 추가
- `packages/database/prisma/migrations/20260426000000_add_agent_skill_ids/migration.sql`
- `packages/shared/src/types/agent.ts` — Agent / Create / Update에 skillIds
- `apps/api/src/modules/agents/agents.controller.ts` — userId 주입
- `apps/api/src/modules/agents/agents.service.ts` — validateSkillOwnership + skillIds 패스스루
- `apps/agent-runner-py/src/modules/agents/agents_service.py` — load_agent_with_deps + sub-agent spec에 skillIds
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — _sync_skills_for_run, _make_skills_backend, create_deep_agent 인자 연결
- `apps/agent-runner-py/tests/modules/agents/test_deepagent_bridge.py` — mock 캡쳐 함수에 `**_kwargs` 추가
- `apps/agent-web/src/components/agents/builder/SkillsTab.tsx` (신규)
- `apps/agent-web/src/components/agents/flow/AgentFlowSettingsPanel.tsx` — Skills 탭 추가
- `apps/agent-web/src/components/agents/flow/SubAgentNode.tsx` — SubAgentNodeData.skillIds 필드

**구현 시 사전 조사**
- Context7 `/websites/langchain_oss_python_deepagents`, `/langchain-ai/deepagents` 로 `create_deep_agent(skills=, backend=, store=)` 시그니처와 `StoreBackend(namespace=...)` 기본값(`("filesystem",)`) 확인.
- 현재 `skills_service.aput`이 사용하는 namespace `("filesystem",)` 와 `StoreBackend` 기본 namespace 가 일치 — 추가 매핑 불필요.

---

### [완료된 이전 단계 노트] Skills를 Agent에서 사용하도록 연결 — 시작 시점

**작성자**: daeyounglee

**배경**
- 이전 세션에서 Skills CRUD까지 구현 완료(`140286b feat(skills): Skill 관리 기능 추가`).
- 그러나 Agent와 Skill을 연결해 실제 invoke 시 사용하도록 하는 부분이 미구현 상태로 작업이 끊김.
- `apps/agent-runner-py/src/modules/skills/skills_service.py`의 `sync_agent_skills()`가 작성만 되고 호출되지 않음.
- DB Skill 모델에 Agent 관계 없음, Agent.skillIds 필드 부재.

**목표**
- deepagents 공식 `skills=` 파라미터(`SkillsMiddleware`) 경로로 Agent ↔ Skill 연결.
- Agent가 활성 Skill의 메타데이터(name + description)를 시스템 프롬프트에 자동 포함.
- 모델이 필요할 때 SKILL.md 본문/리소스를 파일시스템 도구로 progressive disclosure.
- Sub-agent에도 Skills 적용 (사용자 결정).
- Skill 소유자 검증은 API 단에서 처리 (사용자 결정).

**계획 문서**: `/Users/daeyounglee/.claude/plans/skills-agent-golden-cray.md`

**Phase별 진행**
- Phase 1: DB 스키마 + Shared 타입 (`Agent.skillIds: String[]` 추가, 마이그레이션)
- Phase 2: apps/api Agent DTO/Service (소유자 검증 포함)
- Phase 3: Runner `agents_service.load_agent_with_deps()`에 `skillIds` 반환
- Phase 4: Runner `deepagent_bridge`에 `sync_agent_skills` 호출 + `skills=`/`backend=` 인자 전달 (init + resume)
- Phase 5: Frontend `SkillsTab` + `AgentFlowSettingsPanel` 탭 추가
- Phase 6: Sub-agent용 Skills 처리 (`build_subagents`, `SubAgentSettingsPanel`)

**작업 전 사전 조사**
- AGENTS.md 규칙: Context7로 deepagents 공식 문서 재확인 의무
- 조회 대상: `SkillsMiddleware`, `CompositeBackend`, `StoreBackend` API 시그니처

---

## 2026-04-24

### [완료] HITL 승인 카드 "수정" 플로우 인라인 폼으로 전환

**작성자**: daeyounglee

**목표**
- `window.prompt` + JSON 직접 편집을 걷어내고, HITL 승인 카드 내부에 필드별 입력 폼을 렌더링하여 사용자가 레이블(검색어/최대 결과 개수 등) 기준으로 값을 수정할 수 있게 함.

**변경 파일**
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - `HitlApprovalCard`: `editing` 상태 + `draft` 상태 도입. 수정 버튼 클릭 시 각 인자별 input/textarea(긴 문자열·객체는 textarea, 그 외 input) 를 표시. 힌트(예/아니오, 숫자, JSON 형식) 를 우측에 노출. "수정하고 실행"/"취소" 버튼으로 폼 확정.
  - `parseArgValue` 헬퍼: 원래 값의 타입(number/boolean/object/string)에 맞춰 입력 문자열을 파싱. 숫자/불리언은 관대한 파싱, 객체는 JSON 파싱(실패 시 원값 유지).
  - `onDecide` 시그니처를 `'approve' | 'reject'` 로 축소, `onEditSubmit(editedArgs)` 분리.
  - `handleHitlDecide`: edit 분기와 `window.prompt` 제거. 순수 approve/reject 만 처리.
  - `handleHitlEditSubmit` 신규: 인라인 폼 제출 시 `wsClient.respondToInteraction(id, {decision:'edit', editedAction:{name, args}})` 호출.

**검증**
- `pnpm --filter @agent-studio/web lint` → 0 errors (기존 69 warnings 그대로, 변경 파일에서 신규 경고 없음)

---

### [완료] 에이전트 페이지 UI 버그 추가 3종 수정

**작성자**: daeyounglee

**목표**
1. 툴바 "실행" 버튼 클릭 시 디버그 패널의 기존 대화를 초기화하고 새 대화로 시작
2. 채팅 입력창 옆 전송 버튼을 입력창 높이에 맞추고 정지(빨간 사각형) 표시 제거 → running 중에는 스피너 표시
3. HITL 승인 카드의 도구/인자 표기를 자연어로 풀어 표기 (gmail_search → "Gmail 검색", 인자를 레이블 있는 리스트로)

**변경 파일**
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/page.tsx`
  - `sessionToken` 상태 추가, `handleRun` 에서 증가 → 패널 초기화 트리거
  - `handleStop` 제거 (더 이상 호출되는 곳 없음)
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - props `onStop` 제거, `sessionToken` 추가. `sessionToken` 변경 감지 effect 에서 `clearAll()` 호출 (abort + 상태 전체 초기화 + `isAtBottomRef` 복구)
  - 입력창 구조 `relative` → `flex items-end gap-2` 로 변경. 버튼은 textarea 높이(`h-[62px]`) 에 맞춘 세로 형태, `Square` 아이콘/빨간 배경 제거
  - running 중에는 `Loader2` 스피너로 표시 (정지 대신)
  - `HitlApprovalCard` 개편: `TOOL_DISPLAY_NAMES` + `ARG_LABELS` 매핑, `formatArgValue` 추가. 도구명 한국어화 + 인자 표 형태(레이블: 값) 로 렌더

**검증**
- `pnpm --filter @agent-studio/web lint` → 0 errors (변경 파일에서 신규 경고 없음)

---

### [완료] 에이전트 페이지 UI 버그 5종 수정

**작성자**: daeyounglee

**목표**
1. 첫 화면에서 메인 에이전트 노드가 선택 상태로 표시되도록 수정 (하이라이트/글로우)
2. 실행 버튼 클릭 시 자동 저장 후 실행 — 툴바의 중지 버튼 제거(디버그 패널 X 로 종료)
3. 실행 중에는 그래프 배경/노드 클릭으로 디버그 패널이 닫히지 않도록 비활성화
4. 디버그 패널 스크롤이 진행상황을 따라 하단으로 자동 추적되도록 개선 (ResizeObserver + threshold 완화)
5. TODO-LIST 가 턴 종료 시 한번에 완료 처리되지 않고 실제 진행에 맞춰 하나씩 업데이트되도록 수정

**세부 정책**
- 버그 2: dirty=false 면 저장 스킵, 저장 실패 시 실행 중단 + toast 에러

**진행 순서**
1. 버그 1/2/3 한번에 (page.tsx + AgentFlowToolbar.tsx + FlowCanvas.tsx)
2. 버그 4 (AgentFlowDebugPanel.tsx)
3. 버그 5 조사 후 구현
4. pnpm lint 확인 + 수동 UI 검증

**변경 파일**
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/page.tsx`
  - 초기 `mainNode`에 `selected: true` + selection 동기화 effect 추가 → 메인 노드 최초 선택 상태 표시
  - `handleRun` async 로 변경, `dirty` 이면 `persistAgent()` 먼저 수행 후 실행. 저장 실패 시 toast 에러 + 실행 중단
  - `handleNodeSelect` 가 `isRunning` 중 전체 no-op
  - `FlowCanvas` 에 `isRunning` prop 전달, `AgentFlowToolbar` 의 `isRunning`/`onStop` prop 제거
- `apps/agent-web/src/components/agents/flow/AgentFlowToolbar.tsx`
  - `isRunning` 분기 제거, 항상 `실행` 버튼만 표시 (`saving` 중 disabled)
  - `Square` 아이콘/`onStop` prop 제거
- `apps/agent-web/src/components/agents/flow/FlowCanvas.tsx`
  - `isRunning` prop 추가, `handleNodeClick`/`handlePaneClick`/`handleEdgeClick` 모두 running 중 early return
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - 자동 스크롤 ResizeObserver + MutationObserver 추가 (스트리밍 중 자식 크기 변화 감지)
  - scroll threshold 20 → 80px 로 완화
  - `turn.completed` 시 모든 step 을 completed 로 강제 변경하던 로직 제거 → 서버의 `plan.created`(write_todos 재전송) 흐름에 맡겨 순차적으로 업데이트됨

**검증**
- `pnpm --filter @agent-studio/web lint` → 0 errors (69 warnings 기존과 동일, 변경 파일에서 신규 경고 없음)
- 수동 UI 검증은 사용자 확인 필요

---

## 2026-04-24

### [완료] HITL 승인 카드 — `Runner request failed` 근본 원인 수정 + end-to-end 검증

**작성자**: daeyounglee

**목표**
- 이전 세션에서 남은 HITL 실기동 실패(`Runner request failed`) 원인 파악 및 수정
- approve 경로 전체 플로우 검증 (interrupt → 프론트 응답 → resume → tool 실행 → 최종 답변)

**근본 원인 (4개)**

1. **`_extract_interrupt` 가 `state.interrupts` 누락**
   - LangGraph `StateSnapshot` v0.6+ 부터 `state.interrupts` (전역) 와 `state.tasks[*].interrupts` (태스크별) 두 위치에 interrupt 가 보관됨
   - 이전 코드는 `tasks[*].interrupts` 만 읽고 있어 HITL 이 발생해도 감지 실패 → 프론트 무한 대기

2. **`human_interactions.id` / `threads.id` 자동 생성 안 됨**
   - 두 테이블의 `id` 컬럼은 `text NOT NULL` (default 없음)
   - Prisma `@default(cuid())` 는 ORM 레벨에서 생성되므로 Python 원시 INSERT 에는 적용 안 됨
   - `NotNullViolation` 예외 → 500 Internal Server Error → 프론트 `Runner request failed`

3. **`hitl_service.respond` SQL 파라미터 순서 오류**
   - `SET status = $4, response = $2 ... WHERE id = $1` 형태로 `$N` 이 순서 밖에 배치
   - `database/client._adapt()` 는 `$N` → `%s` 단순 치환. psycopg3 `%s` 는 **위치 기반**이라 $4/$2/$3/$1 순서가 깨짐
   - `InteractionStatus` enum 컬럼에 UUID 가 들어가는 타입 에러 발생

4. **`_pump_stream` 이 `error` 이벤트를 조용히 무시**
   - bridge 가 예외를 잡아 `{event: "error"}` 를 yield 해도 threads_service 루프가 처리 안 함
   - 결과적으로 `final_content=""` + `interrupt=None` 으로 정상 완료 처리되어 실제 원인이 가려짐

**변경 파일**

- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_extract_interrupt` — `state.interrupts` 와 `tasks[*].interrupts` 병합, `id` 기준 dedup, 진단 로그 추가
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py`
  - `create_interaction` — `id = uuid.uuid4()` 명시적 생성
  - `respond` — SQL `$1..$4` 순차 정렬, 파라미터 순서 동일 맞춤
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `create` — `id = uuid.uuid4()` 명시적 생성
  - `_pump_stream` — `event == "error"` 시 `error` 필드 수집 + structlog 기록
  - `send_message` / `resume_message` — pump `error` 존재 시 `HTTPException(502)` 재발생 → 프론트가 실제 에러 메시지 수신

**검증 결과 (실기동)**
```
POST /invoke "current_time 도구 호출해줘"
  → status=paused, interaction.id=5df0fe0d-...
  → toolName=current_time, allowedDecisions=[approve,edit,reject]

POST /hitl/interactions/{id}/respond {"decision":"approve"}
  → 200 OK, toolCalls=[current_time]
  → tool output: "2026-04-24T11:14:46.517406+00:00"
  → assistant content: "2026-04-24T11:14:46.517406+00:00"

runner log:
  extract_interrupt dedup_interrupt_count=1 state_interrupt_count=1 task_count=1
  on_tool_start tool_name=current_time
  extract_interrupt dedup_interrupt_count=0 (정상 완료)
```

**남은 작업**
- reject / edit 플로우 e2e 검증 (approve 만 수동 검증 — 로직은 공유)
- `test_deepagent_bridge.py` 8건이 `astream` mock 기반 → 신규 `astream_events` 구조와 불일치. 회귀 안정화 후 리팩터링 필요

---

### [진행중] HITL 승인 카드(디버그 패널 채팅) 구현 — 실기동 검증 대기

**작성자**: leedaeyoung

**목표**
- `HumanInTheLoopMiddleware` interrupt 시 디버그 패널에 Claude 스타일 승인 카드(거부/수정/허용) 노출
- 승인 응답에 따라 실행 재개 (거부=reject / 수정=edit / 허용=approve)

**변경 파일 (완료)**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_pump_astream_events()` 공용 어댑터 추출
  - `_extract_interrupt()` — `aget_state().tasks[*].interrupts` 에서 `action_requests`/`review_configs` 파싱
  - `stream_with_deepagent()` 루프 종료 후 interrupt 감지 시 미완료 step `step.completed` 보정 + `hitl.interrupt` 이벤트 yield
  - `resume_stream_with_deepagent()` — `Command(resume={"decisions": [...]})` 스트리밍 재개 (기존 `resume_with_deepagent` 교체)
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `_pump_stream()` 이벤트 펌프 공용 헬퍼 (send_message/resume_message 공유)
  - `_handle_interrupt()` — `hitl.request` WS emit + `human_interactions` 행 생성, thread paused
  - `resume_message()` 신규 — 스트리밍 재개 턴 처리
  - GraphInterrupt 예외 분기 제거
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py`
  - `_normalize_decisions()` — 프론트 `{decision}` → deepagents `{"decisions": [{"type": ...}]}`
  - `respond()` → `threads_service.resume_message()` 로 위임 (스트리밍)
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - `ChatItem` `hitl_request` kind 추가
  - `HitlApprovalCard` 컴포넌트 (거부/수정/허용 3버튼)
  - `hitl.request` / `hitl.responded` WS 핸들러
  - `수정` 버튼 클릭 시 `window.prompt` 로 JSON args 편집
- `packages/shared/src/types/hitl.ts`
  - `HumanInteraction` 에 `actionRequests` / `reviewConfigs` / `toolName` / `toolArgs` / `allowedDecisions` 필드 추가
- `apps/agent-runner-py/tests/modules/agents/test_deepagent_bridge.py`
  - `test_resume_stream_with_deepagent_emits_run_completed` 로 기존 resume 테스트 대체 (통과)

**진행 상황**
- 코드 작성 완료, 백엔드/shared 빌드 통과, 프론트 lint 0 errors (warnings 71 — 기존과 동일).
- 실기동 확인 중 `Runner request failed` 발생 — HITL 카드 노출 여부 미검증.

**실기동 실패 재현 스냅샷 (이미지 기준)**
- 에이전트: `HITL 테스트 (current_time)` / Deep Autonomous 모드 / thread `b562aa5b-daff-47bc-9ea0-8df14d...`
- 요청 메시지: "최근 메일 제목 알려줘."
- 디버그 패널 RAW 로그:
  - LLM 호출 성공 (`gpt-4o-mini-2024-07-18`), `tool_calls=[{"name":"gmail_search","args":{"q":"","max_results":5},"id":"call_aFi4b61SWYWbOsWSdWhxySVP"}]`, `invalid_tool_calls=[]`, `input_tokens=5430 / output_tokens=17`.
  - `HumanInTheLoopMiddleware 완료 19ms`, `출력(Output): null`.
  - 이후 채팅 영역에 빨간색 `Runner request failed` 배너만 노출됨.
- 주목할 점: tool_calls 는 정상 생성됐고 LLM 응답도 수신됐는데 middleware 출력이 `null` → **interrupt 가 middleware 내부에서는 발생했지만 bridge 가 state 에서 뽑지 못했거나, interrupt 가 아예 걸리지 않고 그대로 통과한 뒤 resume 없이 종료된 케이스**가 둘 다 가능.
- 기존 기록에 "토큰 소진" 이라 적어두었지만, 토큰은 정상 과금되고 tool_call 까지 생성되었으므로 **토큰 문제는 아닐 가능성이 높다**. 아래 체크리스트로 원인부터 다시 특정한다.

**집에서 이어서 할 일 — 재개 가이드**

0. 환경 기동 (루트)
   ```bash
   docker compose up -d        # PG 5433, Redis 6380
   pnpm dev                     # turbo: web 3001 + api 4200 + runner 4300
   ```
   runner 로그는 `apps/agent-runner-py` 에서 `uvicorn src.main:app --port 4300 --reload` 를 별도 터미널로 돌리면 stack trace 관찰이 쉬움.

1. 원인 1순위 — **interrupt 실제 발생 여부 확인**
   - `deepagent_bridge.py::_extract_interrupt` 에 임시 로그 삽입(리턴 직전 `logger.info("interrupt-extract tasks=%s interrupts=%s", ...)`).
   - `threads_service.py::_pump_stream` 의 `hitl.interrupt` 분기 진입 여부 로깅.
   - runner 로그에 `Interrupt` / `action_requests` 문자열이 전혀 없으면 middleware 가 interrupt 없이 tool 실행 → bridge 가 그대로 종료하면서 `run.completed` 발행 전 exception → 프론트에서 `Runner request failed` 로 표시된 것일 수 있음.

2. 원인 2순위 — **invoke HTTP 응답 에러**
   - 프론트에서 `Runner request failed` 문구는 `AgentFlowDebugPanel.tsx` 의 invoke catch 블록에서 찍힘. 네트워크 탭에서 `POST /api/v1/threads/{id}/messages` 응답 status 확인.
   - 500 이면 runner stack trace 로 `resume_stream_with_deepagent` 또는 `_pump_stream` 내부 예외 파악.
   - 200 인데 이후 WS 이벤트가 끊기는 경우는 WS 구독 타이밍 문제 — `wsClient.waitForConnection(5000)` 경로 재확인.

3. 원인 3순위 — **에이전트 설정**
   - `HITL 테스트 (current_time)` 에이전트의 `hitlConfig` 가 `gmail_search` 에 대해 enabled=true 인지 DB 에서 확인.
     ```bash
     cd apps/api && pnpm db:studio   # agents 테이블 → hitl_config 컬럼
     ```
   - 비활성 상태라면 interrupt 자체가 발생하지 않아 middleware 가 pass-through → null 출력은 정상. 이 경우 HITL 이 걸리는 도구(예: `current_time` 도 `allow list` 바깥) 로 설정 먼저 켠 뒤 재시도.

4. 검증 시나리오 (HITL 정상 트리거 후)
   - `허용` → `gmail_search` 실행 후 최종 답변 스트리밍 확인.
   - `거부` → agent 에 reject 메시지 전달, run 종료.
   - `수정` → `window.prompt` 로 `{"q":"from:github","max_results":3}` 같이 편집 후 수정된 args 로 재실행.

5. 보조 이슈 (본 작업과 별개의 선행 결함)
   - deepagent_bridge 단위 테스트 8건이 `permissions=` kwarg 누락 등 mock 불일치로 실패 중. 회귀 마무리 전에 정리 필요.

**재개 시 먼저 읽어볼 파일 (핵심 경로)**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — `_extract_interrupt`, `stream_with_deepagent`, `resume_stream_with_deepagent`
- `apps/agent-runner-py/src/modules/threads/threads_service.py` — `_pump_stream`, `_handle_interrupt`, `resume_message`
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py` — `_normalize_decisions`, `respond`
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx` — `HitlApprovalCard`, `hitl.request`/`hitl.responded` 핸들러

**논의사항**
- 도구 라벨 한국어 매핑 도입 여부 (현재는 slug 그대로 노출 — 예: `web_search`가 카드에 그대로 표시됨). 내장 도구가 늘어나면 매핑 테이블 도입 검토.
- "항상 허용" 버튼은 이번 구현에서 제외 (도구 권한 `auto` 설정으로 대체).
- `수정` 버튼이 `window.prompt` JSON 편집을 쓰는데, 전용 폼 UI 로 개선 필요.

---

### [완료] 중첩 Sub-Agent 노드 활성화 + 한국어 기본 응답

**작성자**: daeyounglee

**목표**
- sub-agent와 연결된 sub-agent(예: coordinator → researcher) 노드에 불이 안 들어오는 문제 수정
- 에이전트 기본 응답 언어를 한국어로 설정

**원인**
- deepagents는 nested sub-agents를 지원하지 않음: 모든 sub-agent는 main agent의 flat 목록으로 등록
- main agent만 `task` 도구로 sub-agent를 호출할 수 있음; coordinator가 researcher를 호출 불가
- canvas 엣지(coordinator → researcher)가 deepagents 실행 흐름에 반영되지 않아 researcher/writer의 `step.started` 이벤트가 발생하지 않음

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/agents_service.py`
  - `load_agent_with_deps` 반환 dict에 `graphEdges` 추가 (`graph_definition.edges`)
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_KOREAN_INSTRUCTION` 모듈 상수 추가
  - `build_subagents`: 각 sub-agent 시스템 프롬프트에 한국어 지시 자동 추가
  - `stream_with_deepagent`: memory context 주입 후 두 가지 추가
    1. 한국어 기본값: 시스템 프롬프트에 언어 지정 없으면 `_KOREAN_INSTRUCTION` append
    2. 토폴로지 힌트: `graphEdges`를 파싱해 sub-agent 위임 순서를 main agent 시스템 프롬프트에 주입

**결과**
- main agent 시스템 프롬프트에 `[서브에이전트 위임 워크플로우]` 섹션이 자동 생성됨
- main agent가 canvas 엣지 구조를 보고 모든 sub-agent(coordinator, researcher, writer)를 순서대로 직접 호출하게 됨 → 모든 노드 점등
- 시스템 프롬프트에 언어 지정이 없으면 한국어로 응답

---

### [완료] WebSocket 연결 안정화 — 스트리밍 이벤트 수신 보장

**작성자**: daeyounglee

**목표**
- WS가 연결되기 전에 invoke가 시작돼 agent.token/step.started 이벤트를 놓치는 문제 수정
- 디버그 패널 마운트 시 WS를 미리 연결해 invoke 시점에 이미 구독 완료 상태 보장
- 스트리밍 토큰 실시간 수신 → "처리 중" 12초 대기 없이 즉시 응답 표시

**변경 파일**
- `apps/agent-web/src/lib/ws-client.ts`
  - `waitForConnection(timeout)` 메서드 추가 — 소켓 연결 완료까지 Promise로 대기
  - `reconnectionDelay: 500ms`, `reconnectionDelayMax: 3000ms`, `reconnectionAttempts: Infinity` 로 빠른 재연결
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - 패널 마운트 시 `wsClient.connect()` 즉시 호출 — invoke 전 연결 선행 보장
  - `handleRun`에서 subscribe 전 `await wsClient.waitForConnection(5000)` — 연결 완료 후 구독

**결과**
- 디버그 패널 열자마자 WS 연결 시작 → invoke 시점에 이미 연결 완료
- 스트리밍 토큰 실시간 수신 가능 → 응답이 한 글자씩 스트리밍되어 표시
- `finally { setRunning(false) }` + invoke HTTP 응답 fallback으로 "처리 중" 확실히 해제

---

### [완료] 실행 중 sub-agent 노드 상태 애니메이션 미작동 수정

**작성자**: daeyounglee

**목표**
- agent 실행 시 그래프의 노드/엣지 반짝임(animated) 효과가 나타나지 않는 버그 수정

**원인**
- `build_subagents`에서 deepagents `name` 필드를 `spec["slug"]` (node_id: `sub-coordinator`)로 설정
- deepagents가 `task` 도구 호출 시 `subagent_type: "sub-coordinator"`를 emit
- `page.tsx`의 `handleStepEvent`는 `event.name`을 `agentName` (`coordinator`)과 비교 → 미스매치
- 결과: 어떤 노드도 `status: running`으로 업데이트되지 않음 → 애니메이션 미작동

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `build_subagents`: deepagents `name` 필드를 `slug`(node_id) 대신 `spec["name"]`(agentName) 우선으로 변경
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - `handleRun`: `invoke` 호출 전 `wsClient.connect()` + `wsClient.subscribeThread(threadId)` 명시적 호출 추가 (Race condition 제거)

**결과**
- deepagents `task` 도구가 `subagent_type: "coordinator"` 형태로 호출 → `step.started` 이벤트 name이 agentName과 일치
- WS 구독이 invoke 전에 보장되어 실행 중 이벤트가 누락되지 않음

---

## 2026-04-23 (세션 2)

### [완료] 계층형 Sub-Agent 노드 점등 — depth-aware staged approach

**작성자**: daeyounglee

**목표**
- coordinator → researcher/writer 구조에서 researcher·writer 노드에 불이 안 들어오는 문제 수정
- coordinator가 직접 VFS 도구(grep, write_file 등)로 조사·작성을 처리해 researcher·writer가 호출되지 않는 근본 원인 해결

**원인 분석**
- deepagents는 모든 sub-agent를 main의 flat 자식으로 등록 → coordinator가 task 도구 없이 VFS 도구로 모든 작업 수행
- sub-agent description이 모호해 LLM이 coordinator 한 번 호출로 작업 완료 처리
- main agent 시스템 프롬프트에 "coordinator만 호출하면 끝"이라고 오해할 여지 있음

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_classify_node_role(name)`: 노드 이름 키워드로 coordinator/researcher/writer 자동 분류
  - `_auto_describe_subagent(name, existing_desc)`: 역할별 명확한 description 자동 생성
    - coordinator: "Creates structured plans only. Does NOT perform research, write content, or use file tools."
    - researcher: "Research and information gathering specialist."
    - writer: "Content writing specialist."
  - `_coordinator_restriction_prompt()`: coordinator 시스템 프롬프트에 파일 도구 사용 금지 + 조사/작성 금지 강제 추가
  - `build_subagents`: 위 세 함수를 활용해 description·system_prompt 자동 보강
  - `stream_with_deepagent` 시스템 프롬프트 주입: BFS depth-aware staged approach로 전면 재작성
    - depth 1(coordinator) / depth 2+(researcher·writer) 분리
    - "[절대 금지] task 도구 N회 모두 호출 완료 전 최종 답변 생성 절대 금지" 명시

**결과**
- coordinator 노드 점등 확인
- researcher·writer 단계적 호출 구조 완성

---

## 2026-04-23

### [완료] Sub Agent 빈 modelName 시 parent 모델 상속 fallback 수정

**작성자**: daeyounglee

**목표**
- sub-agent의 `modelName`이 빈 문자열일 때 `_resolve_provider_for_model`이 openai fallback으로 가서 OPENAI_API_KEY 없음 → RuntimeError 발생하는 버그 수정
- 빈 `modelName`이면 parent agent의 modelId/providerSlug/providerApiKey를 그대로 상속

**원인**
- `_resolve_provider_for_model("")` → model_id가 빈 문자열이면 DB 쿼리 건너뜀 → slug="" → `openai` fallback
- OpenAI 키는 DB에 있으나 content-pipeline 에이전트가 `claude-opus-4-6` 사용 → Anthropic 키가 DB에 없음 → RuntimeError
- 추가 원인: DB providers 테이블에 anthropic api_key_encrypted가 비어 있음

**변경 파일**
- `apps/agent-runner-py/src/modules/agents/agents_service.py`
  - `_build_inline_subagent_specs` 파라미터에 `parent_model_id`, `parent_provider_slug`, `parent_provider_api_key` 추가
  - raw_model_id가 빈 문자열이면 parent 모델 정보를 그대로 사용
  - `load_agent_with_deps`에서 parent 모델 정보를 spec 빌더로 전달
- `agents` DB 테이블: content-pipeline 에이전트 model_id를 `claude-opus-4-6` → `gpt-4o` 변경 (Anthropic API 키 미설정)

**결과**
- sub-agent 모델 생성 성공 (ChatOpenAI × 3)
- invoke 테스트 성공: AI 의료 기사 작성 요청 → sub-agent 경유 응답 반환

---

### [완료] Sub Agent 추가 시 선택된 노드 기준 edge 연결 + 이름 자동 생성

**작성자**: daeyounglee

**목표**
- "Sub Agent 추가" 버튼 클릭 시, 현재 선택된 노드(main 또는 sub-agent)를 edge source로 사용
- 선택된 노드가 없거나 main이면 기존과 동일하게 main → new sub-agent 연결
- 선택된 노드가 sub-agent이면 해당 sub-agent → new sub-agent 연결
- 새 sub-agent 이름: 기존 노드 이름 중 중복되지 않는 순번으로 자동 생성 ("Sub Agent 1", "Sub Agent 2", ...)

**변경 파일**
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/page.tsx`

**작업 내용**
- `handleAddSubAgent` 클로저가 `selectedNodeId` state를 직접 참조하도록 수정
- source 노드 결정 로직 추가 (selectedNodeId가 'main' 또는 null이면 'main', 그 외엔 해당 sub-agent id)
- 기존 노드 이름 목록에서 중복 없는 순번 계산 로직 추가

**결과**
- 완료. lint error 없음 (기존 warning 71개만 존재, 신규 없음)

---

### [완료] Session expired 시 로그인 페이지 자동 리다이렉트

**작성자**: daeyounglee

**목표**
- JWT 만료 후 refresh도 실패하면 `clearTokens()` + `Session expired` throw — 이때 Next.js unhandled rejection overlay가 뜨고 이후 모든 API 호출이 실패하는 문제 수정

**원인**
- `clearTokens()`로 토큰은 삭제되지만 Zustand 스토어의 `isAuthenticated`는 `true` 유지
- 사용자가 로그인 상태인 줄 알고 계속 API 호출 → 모두 401 → "Session expired" 반복

**변경 파일**
- `apps/agent-web/src/lib/api-client.ts:126`

**작업 내용**
- `clearTokens()` 직후 `window.location.href = '/login'` 추가 → 세션 만료 시 자동으로 로그인 페이지로 이동

**결과**
- 완료. 세션 만료 시 로그인 페이지로 리다이렉트됨
- `handleAddSubAgent`가 `selectedNodeId`를 의존성으로 추가
- source 노드: selectedNodeId가 sub-agent이면 해당 노드, main이거나 미선택이면 'main'
- 새 sub-agent 이름: "Sub Agent 1", "Sub Agent 2" 순번 자동 부여 (기존 이름과 중복 방지)
- edge id: `edge-{sourceId}-{newId}` 로 변경 (기존 `edge-main-{newId}`)

---

### [완료] Fresh clone 환경에서 DB 셋업 막힘 해결

**작성자**: leedaeyoung

**목표**
- 다른 사람이 레포 clone 후 서버를 띄울 때 Prisma client 미생성·DATABASE_URL 미로드로 막히는 문제 해결

**변경 파일**
- `package.json` — 루트에 `setup` 스크립트, `db:deploy` 추가
- `packages/database/package.json` — 모든 prisma 스크립트를 `dotenv-cli`로 래핑하여 루트 `.env` 자동 로드, `db:deploy` 추가, `dotenv-cli` 의존성 추가
- `apps/api/package.json` — 동일한 dotenv-cli 래핑, `db:deploy` 추가, `db:seed`는 packages/database 위임으로 단일화
- `turbo.json` — `build/dev/lint`에 `^db:generate` 의존 추가, `db:generate` outputs 캐시, `db:migrate`/`db:deploy` 태스크 등록
- `AGENTS.md` — Commands 섹션 최상단에 "최초 셋업(fresh clone)" 절차 명시

**효과**
- fresh clone → `cp .env.example .env` 후 `pnpm setup` 한 번으로 docker→install→generate→deploy 완료
- `pnpm dev` 시 prisma client가 자동 생성되어 NestJS 빌드 실패 재발 방지
- 모든 prisma 명령이 루트 `.env`를 자동 로드 — `DATABASE_URL` 인라인 export 불필요

**결과**
- `npx turbo run build --filter=@agent-studio/api` 통과 (db:generate가 build 의존으로 실행됨)
- `pnpm --filter @agent-studio/database db:deploy` 통과 (4 migrations applied/no pending)
- lint 통과 (기존 warning 3개만 존재, 신규 없음)

---

### [착수] 대시보드 빈 카드/그래프 정합성 정정 (Prompt → Agent 도메인 정렬 + DailyMetric on-demand 폴백)

**작성자**: daeyounglee

**배경**
- 대시보드에 `Total Prompts: 0`, `Active API Keys: 0`, `API Calls (30일)` 그래프 비어있음, `Prompt Status` 표 "No prompts yet", `Top Prompts by Usage` "No usage data yet" 표시.
- 원인 1: `packages/shared/src/types/dashboard.ts`의 `DashboardOverview`가 `prompts`/`recentLogs` 필드를 약속하지만 백엔드 `dashboard.service.ts`는 `workflows`/`agents`/`recentRuns`를 반환 — 타입 계약 불일치. Prisma 스키마에 `Prompt` 모델 자체가 없음 (Workflow/Agent/PromptVersion만 존재).
- 원인 2: `daily_metrics` 테이블은 매일 00:05 UTC cron으로 어제치만 집계 — 오늘 발생한 호출은 그래프에 반영되지 않음.
- 원인 3: 활성 프로젝트에 API Key 미발급 (단순 사용자 작업 필요, 코드 변경 불필요).

**목표**
- Frontend 도메인 라벨을 실제 데이터 모델인 `Agent` 기준으로 정정 (Prompt 표기 제거).
- shared `DashboardOverview` 타입을 백엔드 응답에 맞춰 재정의.
- `DashboardService.getOverview`에서 오늘치 `WorkflowRun`을 즉석 집계하여 `dailyMetrics`에 머지 (DB write 없음, 메모리 머지만).

**변경 예정 파일**
- `packages/shared/src/types/dashboard.ts`
- `apps/agent-web/src/app/(dashboard)/dashboard/page.tsx`
- `apps/agent-web/src/components/dashboard/prompt-status-table.tsx` → `agent-status-table.tsx`로 rename
- `apps/agent-web/src/components/dashboard/top-prompts-chart.tsx` → `top-agents-chart.tsx`로 rename
- `apps/api/src/modules/dashboard/dashboard.service.ts` — 오늘치 폴백 합산 로직 추가

**변경 파일 (실제)**
- 수정 `packages/shared/src/types/dashboard.ts` — `prompts`/`recentLogs` 제거. 백엔드 응답에 맞춰 `workflows`/`agents`/`recentRuns` + 기존 필드로 재정의. `Workflow`/`Agent`/`WorkflowRun` import 추가.
- 수정 `packages/shared/src/types/agent-run.ts` — `WorkflowRun`에 `agentId?`/`projectId?`/`latencyMs?` 추가, `workflowId`를 optional로 변경 (Prisma 스키마와 정합성 일치).
- 신규 `apps/agent-web/src/components/dashboard/agent-status-table.tsx` — `agents`+`runs` props, `run.agentId`로 호출/비용 집계, enabled/disabled 상태 표시. Header "Agent Status".
- 신규 `apps/agent-web/src/components/dashboard/top-agents-chart.tsx` — `agents`+`runs` props, `agentName` lookup, 호출 수 기준 상위 5 bar chart. Header "Top Agents by Usage".
- 삭제 `apps/agent-web/src/components/dashboard/prompt-status-table.tsx`
- 삭제 `apps/agent-web/src/components/dashboard/top-prompts-chart.tsx`
- 수정 `apps/agent-web/src/app/(dashboard)/dashboard/page.tsx` — `Total Prompts` → `Total Agents`(아이콘 `Bot`), summaryData 필드 `agents.length`/`enabled`/`disabled`로 정정. `PromptStatusTable`/`TopPromptsChart` import 교체. `recentLogs` → `recentRuns`로 props 전환.
- 수정 `apps/api/src/modules/dashboard/dashboard.service.ts` — `aggregateTodayMetric(projectId)` private 메서드 추가. `WorkflowRun`을 오늘 00:00 UTC ~ now 범위로 즉석 집계 후 `dailyMetrics` 응답에 머지(DB write 없음). 오늘치 row가 이미 테이블에 있으면 신선한 값으로 교체.

**검증**
- `pnpm --filter @agent-studio/shared build` → 0 errors.
- `pnpm --filter @agent-studio/api build` → 0 errors.
- `pnpm --filter @agent-studio/api lint` → 0 errors (기존 3 warnings 동일).
- `pnpm --filter @agent-studio/web lint` → 0 errors (기존 73 warnings 동일).
- `pnpm --filter @agent-studio/web build` → **이전부터 존재하던** `agents/[agentId]/page.tsx`의 `MainAgentNodeData` Record 타입 에러 1건 (stash로 동일 재현 확인). 본 작업과 무관.

**효과**
- `Total Agents` 카드가 실제 등록된 Agent 수를 표시 (이전 "Total Prompts" 0 고정 해소).
- `Agent Status` 표 / `Top Agents by Usage` 차트에 `recentRuns` 기반 데이터 즉시 표시.
- `API Calls (30일)` / `Tokens & Cost (30일)` 차트가 오늘 발생한 호출까지 즉시 반영 (cron이 어제까지만 채우는 한계 회피).
- `Active API Keys`는 코드 변경 사항 아님 — 활성 프로젝트에 API Key 발급 시 자동 카운트.

**남은 작업**
- (선택) `prompt` 도메인을 정식 모델로 도입할 계획이 있다면 별도 마이그레이션. 현 시점은 Workflow/Agent를 정규 도메인으로 확정.

---

### [완료] 프로젝트별 Usage 페이지 신설 (/projects/[projectId]/usage 404 해소)

**작성자**: daeyounglee

**배경**
- `/usage` 페이지의 Projects 카드 클릭 시 `/projects/${projectId}/usage`로 이동하지만 해당 라우트 미구현 → 404.
- 백엔드 `getProjectUsage(projectId)` 및 `ProjectUsageDataV2` envelope, 모든 하위 컴포넌트(`UsageSummaryCards`, `UsageDailyCharts`, `InsightCards`, `PromptEndpointTree`, `ApiKeyActivityTable`, `EnvironmentDistributionChart`, `ExecutionLogTable`)는 기 구현 — 페이지만 추가하면 됨.

**변경 파일**
- 신규 `apps/agent-web/src/app/(dashboard)/projects/[projectId]/usage/page.tsx`
  - `useParams`로 projectId 취득.
  - `useUsageStore.loadProjectUsage(projectId)`로 데이터 로드 (period/dateRange 의존).
  - `apiClient.projects.getById(projectId)`로 프로젝트명 별도 조회 → 헤더 표시.
  - `/usage`로 돌아가는 back link 포함.
  - PeriodFilter는 글로벌 store 공유 (Usage 페이지와 동일 기간 필터 사용).
  - 7개 섹션 렌더 (summary / daily charts / insights / prompt-endpoint tree / api key activity / environment distribution / recent logs).

**검증**
- `pnpm --filter @agent-studio/web lint` → 0 errors (기존 73 warnings 동일).
- Next.js dev 서버 hot reload `✓ Compiled in 237ms` — 라우트 충돌 없음 (`(public)/projects/[projectId]/endpoints/docs`와 leaf 경로가 다름).

**효과**
- Projects 카드 클릭 → 정상 이동, 프로젝트별 호출/비용/레이턴시/에러율, 일별 차트, AI insights, prompt-endpoint 트리, API Key 활동, 환경 분포, 최근 실행 로그 표시.

---

### [완료] Usage 페이지 Projects → Agents 도메인 정렬 (UI 측 Project 표기 제거)

**작성자**: daeyounglee

**배경**
- 프로젝트 관리 UI(생성/목록/이름 변경)는 미구현 상태에서 Demo Project 1개만 시드로 자동 사용 중. 사용자가 Project를 직접 컨트롤할 수단 없음.
- "일단 UI에서 Project 표기 제거하고 Agent로 전환" — 멀티프로젝트 운영은 차후 과제로 미룸. 백엔드 `Project` 모델/API는 유지.

**변경 파일**
- 수정 `packages/shared/src/types/usage.ts`
  - `ProjectUsageSummary` → `AgentUsageSummary` (`agentId`/`agentName`, `endpointCount`/`promptCount` 제거)
  - `ProjectCardData` → `AgentCardData`
  - `GlobalUsageDataV2.projectCards` → `agentCards`
- 수정 `apps/agent-web/src/types/usage.ts` — re-export 목록 정렬 (Project* → Agent*).
- 수정 `apps/api/src/modules/usage/usage.service.ts`
  - `aggregateProjectCards` 제거. `aggregateAgentCards` 신설 — `WorkflowRun.agentId` 기준 groupBy + 7일 일별 sparkline (workflowRun raw 일별 카운트). Endpoint.agentId가 없으므로 endpointCount 컬럼 제외.
  - `buildInsights`: `projectCards` → `agentCards`, "비용 1위 프로젝트" → "비용 1위 에이전트".
  - `getGlobalUsage` envelope 동일 변경 (`projectCards` → `agentCards`).
- 신규 `apps/agent-web/src/components/usage/agent-cards.tsx` — Agents 그리드, 카드 클릭 시 `/agents/{agentId}`(기 구현된 에이전트 상세)로 이동. 빈 상태 "No agents yet" 표시.
- 수정 `apps/agent-web/src/app/(dashboard)/usage/page.tsx` — `<ProjectCards>` → `<AgentCards>`.
- 삭제 `apps/agent-web/src/components/usage/project-cards.tsx`
- 삭제 `apps/agent-web/src/app/(dashboard)/projects/[projectId]/usage/page.tsx` (직전 추가 분 — Project UI 전체 제거 정책에 따라 회수)
- 디렉토리 정리 `apps/agent-web/src/app/(dashboard)/projects/` 디렉토리 제거.

**유지된 항목 (의도)**
- 백엔드 `getProjectUsage` API + helper: 멀티프로젝트 도입 시 재사용 위해 보존.
- shared `ProjectUsageDataV2`, `loadProjectUsage` store, `apiClient.usage.getProjectUsage`: 동일 이유.

**검증**
- `pnpm --filter @agent-studio/shared build` → 0 errors.
- `pnpm --filter @agent-studio/api build` → 0 errors.
- `pnpm --filter @agent-studio/api lint` → 0 errors (기존 3 warnings).
- `pnpm --filter @agent-studio/web lint` → 0 errors (기존 73 warnings).
- API dev nest watch: `Found 0 errors. Watching for file changes.`

**효과**
- `/usage` 페이지에 Demo Project 카드 사라지고 Agent 카드로 대체.
- 카드 클릭 → `/agents/{agentId}` 정상 이동 (404 해소).
- "비용 1위 프로젝트" 인사이트 → "비용 1위 에이전트"로 라벨 정렬.

---

### [완료] Provider/Model 비용 분해 추가 (workflow_runs.model_id + 집계 + UI)

**작성자**: daeyounglee

**배경**
- 다중 LLM Provider(OpenAI/Anthropic/Google) 사용 시 대시보드 "Total Cost"는 단순 합산만 제공. provider별/모델별 분해 미존재.
- `WorkflowRun` 테이블에 사용 모델을 식별할 컬럼이 없어 그루핑 불가능. 정공법으로 컬럼 추가 + 집계 파이프라인 추가.

**변경 파일**
- DB · Prisma
  - `packages/database/prisma/schema.prisma` — `WorkflowRun.modelId String?` 추가, `@@index([modelId, startedAt(sort: Desc)])` 추가.
  - 신규 `packages/database/prisma/migrations/20260429000000_add_workflow_run_model_id/migration.sql` — `model_id TEXT` 컬럼 + 인덱스. (자동 `prisma migrate dev`는 기존 드리프트로 인한 reset 요구로 사용 불가 → 수동 SQL 작성, `prisma migrate deploy` 적용 성공.)
  - `pnpm db:generate` 재실행 — Prisma client 갱신.
- 실행 엔진 (agent-runner-py)
  - `src/modules/runs/runs_writer.py` — `record_run(... model_id)` 파라미터 추가, INSERT 컬럼 목록에 `model_id` 포함.
  - `src/modules/threads/threads_service.py` — `_persist_run`에서 `usage["modelId"]`(이미 `deepagent_bridge`가 emit) 를 `record_run`에 전달.
- 타입
  - `packages/shared/src/types/agent-run.ts` — `WorkflowRun.modelId?` 추가.
  - `packages/shared/src/types/usage.ts` — `ProviderBreakdownRow` 신규 (`modelId`, `modelName`, `providerId`, `providerName`, `providerSlug`, `calls`, `totalTokens`, `cost`, `costPercentage`). `GlobalUsageDataV2.providerBreakdown` 추가.
  - `apps/agent-web/src/types/usage.ts` — `ProviderBreakdownRow` re-export.
- 백엔드 집계 (apps/api)
  - `src/modules/usage/usage.service.ts` — `aggregateProviderBreakdown` 신설.
    - `WorkflowRun.groupBy(['modelId'])` + `Model.findMany({modelId in})` 보강 (provider include).
    - 비용 점유율 계산 (`costPercentage = cost / totalCost * 100`).
    - 비용 내림차순 정렬.
  - `getGlobalUsage` envelope에 `providerBreakdown` 포함.
- 프론트엔드 (apps/agent-web)
  - 신규 `src/components/usage/provider-breakdown.tsx` — 표(Provider, Model, Calls, Tokens, Cost, % of Cost). provider slug별 색상 바(openai/anthropic/google/azure 매핑).
  - `src/app/(dashboard)/usage/page.tsx` — `<AgentCards>` 다음에 `<ProviderBreakdown>` 삽입.

**검증**
- `pnpm --filter @agent-studio/database db:deploy` → migration applied successfully.
- `pnpm --filter @agent-studio/database db:generate` → Prisma client regenerated.
- `pnpm --filter @agent-studio/shared build` → 0 errors.
- `pnpm --filter @agent-studio/api build` → 0 errors. dev watch `Found 0 errors`.
- `pnpm --filter @agent-studio/api lint` → 0 errors (기존 3 warnings).
- `pnpm --filter @agent-studio/web lint` → 0 errors (기존 73 warnings).
- runner-py uv watch — `runs_writer.py` / `threads_service.py` reload 성공, `record_run` 시그니처에 `model_id` 정상 노출.

**효과**
- 신규 호출은 `workflow_runs.model_id`에 사용 모델 ID(예: `claude-sonnet-4-5`, `gpt-4o`)가 기록됨.
- `/usage` 페이지에 Provider · Model 비용 분해표가 표시되어 OpenAI/Anthropic/Google 비용을 한눈에 비교 가능.
- 기존 호출은 `model_id = NULL`이라 분해표에서 제외되며, 새로 발생하는 호출부터 누적.

**한계 / 후속**
- 모델이 multi-provider DB 행으로 등록된 경우 첫 번째 매칭 provider로 귀속 (현재 구현 단순화). 정확한 귀속이 필요하면 runner-py에서 Model.id (UUID)를 함께 기록하도록 확장 가능.
- step 단위 비용 분해는 `step_traces.cost`가 항상 NULL이라 미지원 (이번 작업 범위 외).

---

## 2026-05-12 — TODO 항목 과도 완료 마킹 버그 수정 (답변 본문 기반 sweep 검증)

**작성자:** leedaeyoung

**버그**
- 사용자: "일별로 자세한 일정 짜줘. 일자별로 순차적으로 진행해줘."
- 모델 답변: 1일차 상세 일정·시간표만 작성
- TO-DO List UI: 1일차·**2일차** 모두 ✓ completed 처리, 3일차/준비 팁만 pending
- 결론: 모델이 1·2번을 `in_progress` 로 마킹한 뒤 답변에 1일차만 담았고, `_sweep_final_todos` 가 모든 in_progress 를 무조건 completed 로 변환해 진척도가 부풀려짐.

**수정**
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_todo_appears_in_answer(content, answer)` helper 신설 — 답변 본문에 todo 의 식별 키워드가 등장하는지 휴리스틱 판정.
    - "1일차", "Step2", "3단계" 등 숫자 포함 토큰이 있으면 **모든** 식별 토큰이 답변에 등장해야 매칭.
    - 식별 토큰이 없으면 핵심 키워드(2자 이상) 중 절반 이상이 답변에 등장하면 매칭.
  - `_sweep_final_todos(final_todos, answer_text="")` 시그니처 확장 — `answer_text` 가 비어 있지 않으면 in_progress 항목별로 본문 등장 여부 판정 후 completed/pending 분기. 비어 있으면 기존 동작(in_progress → completed) 유지.
  - 두 호출 지점(`stream_with_deepagent`, `resume_stream_with_deepagent`)에서 `_last_assistant_content(final_messages)` 로 마지막 어시스턴트 텍스트를 추출해 sweep 에 전달. guardrail json schema 검증도 동일 변수를 재사용해 중복 제거.
  - 시스템 프롬프트(plan_execute 모드, send/resume 양쪽)에 새 룰 두 개 추가:
    - "순차 진행 요청의 처리": 사용자가 '순차적으로', '일자별로', '단계별로', '하나씩' 등으로 요청하면 이번 답변엔 첫 항목 한 개만 작성하고 그 한 항목만 'completed', 나머지는 'pending' 으로. 다음 항목은 다음 사용자 요청 시 진행.
    - "in_progress 마킹 범위 제한": 한 번의 write_todos 에서 in_progress 로 마킹할 수 있는 건 이번 답변에 실제로 산출물이 들어가는 항목들뿐. '다음에 할 예정' 인 항목은 pending 으로.

**검증**
- `ruff check src/modules/agents/deepagent_bridge.py` → All checks passed.
- 수동 단위 시뮬레이션:
  - 버그 시나리오(답변=1일차만): 1일차 → completed, 2·3일차 → pending, 준비 팁 → pending. ✅
  - 정상 다일자 시나리오(답변=1·2·3일차 모두 포함): 1·2·3일차 모두 completed. ✅
  - 빈 답변(fallback): 기존 동작 유지 — 모든 in_progress → completed. ✅

**효과**
- 모델이 over-mark 해도 답변에 실제로 다뤄지지 않은 항목은 자동 pending 으로 되돌아가 다음 turn 에서 이어 진행 가능.
- 시스템 프롬프트 강화로 모델이 처음부터 순차 요청 시 한 항목만 in_progress 로 마킹하도록 유도.
- 기존 "stuck NOW" 안전망(in_progress 가 단일이고 답변에 등장)은 유지.

---

## 2026-05-12 — TODO 과거 스냅샷 완료 상태 보존 + 사용자 질문 하이라이트·점프

**작성자:** leedaeyoung

**요구**
1. 스크롤로 과거 turn 의 답변을 볼 때 TO-DO List 패널이 그 시점 snapshot 으로 전환되는데, 이후 완료된 항목까지 미완료로 보이는 게 어색. → "최신 snapshot 에서 completed 인 항목은 어느 페이지/스크롤 위치에서 보더라도 completed 로 유지" 라는 monotonic forward 보정.
2. 사용자가 현재 보고 있는 답변에 대응하는 사용자 질문 말풍선을 초록색 계열로 하이라이트하고, 그 말풍선을 클릭하면 해당 답변의 시작 위치로 부드럽게 스크롤.

**수정 (`apps/agent-web/src/components/client/ClientChatView.tsx`)**
- `FeedItem` user 변형에 `groupId?: string` 추가. handleSend(live) 와 restored history 모두 user 메시지에 대응 `groupId` 부착 (live=`group-${ts}`, restored=`restored-turn-${turnIdx}`). 어시스턴트 메시지가 사용하는 groupId 와 동일해 매칭 가능.
- `viewedGroupId` 상태 신설 — 항상 동작하는 새 scroll effect (4-c) 에서 viewport 중앙에 가장 가까운 `[data-assistant-group-id]` 요소를 찾아 groupId 를 갱신. todoOpen 과 무관하게 동작.
- `scrollToAssistant(groupId)` 콜백 신설 — `data-assistant-group-id` 매칭 노드를 `scrollIntoView({behavior:'smooth', block:'start'})` 로 이동.
- `UserBubble` props 확장: `groupId`, `isViewed`, `onJump`. `isViewed` 가 true 면 emerald ring + 옅은 emerald 배경 + emerald 텍스트로 하이라이트. 클릭 가능할 때 `<button>` 으로 렌더링하고 `title="이 질문의 답변 위치로 이동"` 툴팁 노출.
- `TodoSidePanel`: rawActiveSteps → activeSteps 단계에서 plan 최신 snapshot 의 completed 항목 content 를 Map 으로 만들고, 현재 표시할 step 이 그 content 에 매칭되면 status 를 강제 `completed` 로 승격. monotonic forward — 한 번 완료된 항목은 과거 snapshot 에서도 완료로 표시.
- threadId 변경 시 `viewedGroupId` reset 추가.

**동작**
- 채팅을 스크롤하면 viewport 중앙의 답변(turn)에 대응하는 user 질문 말풍선이 초록 ring 으로 강조됨.
- 강조된 말풍선을 클릭하면 그 turn 의 AI 답변 시작 위치로 즉시 부드러운 스크롤.
- TO-DO List 패널 페이지를 과거로 이동해도, 최신에서 완료된 항목은 체크된 상태로 보임 (취소된 turn 에서 보존되는 정책과 호환).

**검증**
- `pnpm --filter @agent-studio/web lint` → 0 errors (기존 96 warnings 유지).
- `tsc --noEmit` → ClientChatView 에서 새 에러 없음 (다른 파일의 기존 에러만 잔존).

**한계 / 후속**
- viewedGroupId 매칭은 viewport 중앙의 한 turn 만 강조. 답변 본문이 viewport 보다 큰 경우 그 turn 의 user 질문이 화면 위쪽 / 아래쪽에 있을 수 있는데, 클릭 시 답변 상단으로 이동하므로 무한 루프는 발생하지 않음.
- 브라우저 수동 검증은 미실시 (lint/타입만 확인). 다음 dev server 기동 시 시각 확인 필요.

---

## 2026-05-13 — 하이라이트 위치 정정: 사용자 질문 → TO-DO List 항목

**작성자:** leedaeyoung

**피드백**
- 사용자: "지금 이렇게 사용자 질문에 체크가 되는데 [TO-DO List 이미지] 여기 TO-DO List에 표시가 되도록 해달라는 말이였어."
- 직전 작업에서 viewedGroupId 매칭으로 user 질문 말풍선을 초록 ring 으로 강조했는데, 의도는 그 turn 에서 완료된 TODO 항목을 TO-DO List 패널 안에서 강조하는 것이었음.

**수정 (`apps/agent-web/src/components/client/ClientChatView.tsx`)**
- `UserBubble` 시각 하이라이트·클릭 점프 제거 — 원래대로 단순 표시. `FeedItem.user.groupId` 필드와 그 setter(live/restored) 도 unused 가 되어 정리.
- `TodoSidePanel` 에 `viewedGroupId` / `onJumpToGroup` prop 추가. plan 의 `groupIds` 순회로 각 todo content 에 대해 "처음 completed 가 된 turn(groupId)" 매핑 (`completedAtGroupByContent`) 구성.
- `TodoSidePanelRow` 에 `isCurrent` / `onJump` prop 추가:
  - `isCurrent` true → emerald-500 border + emerald-50 background + emerald ring + "현재 보기" 배지. 라벨도 emerald-800 굵게.
  - `onJump` 있으면 row 전체가 `<button>` 으로 렌더링, hover 시 옅은 emerald 배경, 클릭하면 그 항목이 완료된 turn 의 답변 위치로 `scrollIntoView`.
  - `<ul>` 내부에 `<button>` 직접 두면 invalid 한 HTML 이므로 `<li>` 래퍼 → `<button>` / `<div>` 구조로 분리.
- 하이라이트 매칭: `viewedGroupId ?? activeGroupId` 와 `completedAtGroupByContent.get(step.content)` 비교. 스크롤·페이지네이션 모두 자연스럽게 반영.

**동작**
- 채팅 스크롤 → viewport 중앙 turn 에 대응해 처음 완료된 TODO 항목이 TO-DO List 안에서 emerald ring + "현재 보기" 배지로 강조.
- 강조된 항목(또는 다른 완료 항목)을 클릭하면 그 항목이 완료된 turn 의 답변 위치로 부드러운 스크롤.
- 미완료 항목은 클릭 비활성(점프할 답변이 없으므로).

**검증**
- `pnpm --filter @agent-studio/web lint` → 0 errors (기존 96 warnings 유지).

---

## 2026-05-13 (12) — Client 로그인 분리 — Phase 1 착수 (leedaeyoung)

### 목표
- admin/client 인증·세션을 완전 분리하고, admin에서 Client 진입 시 별도 로그인 흐름을 거치도록 한다.
- 현재 useUserStore 의 mockApi/임시 강제 admin 로그인을 제거하고 실 백엔드 JWT(/api/auth) 통합.

### Phase 1 범위
1. 인증 백엔드 통합(JWT) — mockApi 제거, useUserStore 가 실 API 호출
2. AGENTS.md 임시 조치(admin@example.com 강제 로그인) 해제
3. admin/client 토큰 분리 — localStorage key 분리(`admin_token` / `client_token`)
4. `/client/login` 페이지 신규 추가, `(client)/layout.tsx` 가 client 토큰 기반으로 가드
5. admin 페이지의 "Client" 버튼 흐름 — client 토큰 있으면 통과, 없으면 `/client/login`

Phase 2~6은 별도 사이클.

### 완료 변경
- `apps/agent-web/src/lib/api-client.ts` — `AuthScope` 도입. localStorage 키를
  `admin_access_token / admin_refresh_token` 과 `client_access_token / client_refresh_token`
  으로 분리. `getActiveScope()` 가 `window.location.pathname` 기반(`/client/...` ⇒ client)
  으로 매 요청마다 토큰을 선택. 401 redirect 도 scope 별(`/login` vs `/client/login`).
- `apps/agent-web/src/app/(auth)/client/login/page.tsx` — 신규. `/login` 페이지를 emerald
  톤으로 복제하고 redirect target 만 `/client/agents`. `(auth)` 그룹에 두어 가드 우회.
- `apps/agent-web/src/app/(client)/layout.tsx` — 미인증 시 redirect `/login` → `/client/login`.
- `apps/agent-web/src/app/(client)/_components/sidebar.tsx` — 푸터에 사용자 정보 + 로그아웃
  버튼. 로그아웃 시 `/client/login` 으로 redirect.
- `apps/agent-web/src/app/(dashboard)/layout.tsx`,
  `apps/agent-web/src/components/layout/app-sidebar.tsx` — `email === 'admin@example.com'`
  하드코딩 제거. `role === 'admin'` 만으로 판정.

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm exec tsc --noEmit` → ClientChatView/auth 관련 에러 없음 (monaco-editor 사전 이슈만).

### 남은 후속 작업 (Phase 2+)
- Phase 2: 메뉴 구조 재배치 + Users 메뉴 admin role guard.
- Phase 3: 사용자 추가 시 자동 Project 생성 + 비밀번호 직접 입력 + admin 비밀번호 리셋.
- Phase 4: admin role 의 "전체 보기" 모드 + 다른 사용자 리소스 흐림+id tag.
- Phase 5: 본인 비밀번호 변경 (Settings).
- Phase 6: idle 1시간 자동 로그아웃.

### 알려진 한계
- `useUserStore` 가 admin/client 공통 store 라 path 변경 시 store.currentUser 가
  비동기로 교체. layout 가드의 `checking` buffer 덕에 사용자 영향은 없지만, 향후 두 store
  분리(별도 `useClientUserStore`)로 명확화 가능.
- mockApi alias(`api-client.ts:796`)는 그대로 — 의미는 이미 실 API. 이름 변경은 별도 cleanup.

---

## 2026-05-13 (13) — Client 로그인 분리 — Phase 2 (메뉴 재배치 + admin role guard) (leedaeyoung)

### 변경
- `apps/agent-web/src/components/layout/app-sidebar.tsx`
  - Usage / Monitoring / Providers 의 `{isAdmin && }` 가드 제거 — 모든 인증 사용자에게 노출.
  - ADMIN 섹션 헤더 위치를 Providers 다음, Users 앞으로 이동. Users + Settings(준비 중) 만
    admin role 전용으로 남김.
- `apps/agent-web/src/app/(dashboard)/layout.tsx`
  - `ADMIN_ROUTES` 를 `['/usage', '/settings']` → `['/settings/users']` 로 좁힘.
  - 비admin 사용자도 `/usage`, `/monitoring`, `/settings/providers` 접근 가능.
  - `/settings/users` 만 admin role 전용 (위반 시 `/dashboard` 로 redirect).

### Phase 2 범위 외 (후속 Phase 에서 처리)
- Phase 4: Usage/Monitoring/Providers 의 데이터 자체를 본인 Project 한정으로 노출.
- Phase 4: admin role 의 "전체 보기" 모드 + 다른 사용자 리소스 owner 표시(흐림+id tag).

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.

---

## 2026-05-13 (14) — Providers 사용자별 키 등록 (leedaeyoung)

### 배경
admin Providers 페이지에서 글로벌 `Provider.apiKeyEncrypted` 를 사용 중이라 admin 이 한 번
등록한 키가 모든 사용자에게 그대로 노출됨. Tools 와 동일하게 사용자별 등록 모델로 전환.

### 변경
- `apps/api/src/modules/providers/providers.service.ts`
  - `findAll(userId)` — 본인 `UserCredential(kind=provider, targetId=<slug>)` 기반으로
    `apiKeyConfigured` / 마스킹 표시. 글로벌 `apiKeyEncrypted` 는 응답에 사용하지 않음.
  - `configure(id, apiKey, userId)` — 글로벌 키 저장 제거. 본인 UserCredential upsert
    (encryptCredential). 검증 성공 시 status=active.
  - `removeApiKey(id, userId)` — 본인 UserCredential 삭제.
  - `testConnection(id, userId)` — 본인 UserCredential 의 키로 검증. 결과에 따라
    status=active/invalid 갱신.
  - `getEnabledModels(userId)` — 본인이 등록한 Provider 의 enabled 모델만 반환.
- `apps/api/src/modules/providers/providers.controller.ts`
  - findAll / getEnabledModels / configure / removeApiKey / testConnection 모두
    `@CurrentUser('id') userId` 추가. configure/removeApiKey/testConnection 의
    `@Roles('admin')` 제거 (일반 user 도 본인 키 등록 가능).
  - 글로벌 자원 관리(model toggle / addCustomModel / deleteCustomModel / addLocalProvider
    / deleteLocalProvider / configureEndpoint / discoverModels)는 `@Roles('admin')` 유지.
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_resolve_provider_key` 에서 글로벌 `fallback_key` 경로 제거 (deprecated).
  - 사용자 자격증명만 사용. 미등록 시 credential_missing.
  - 시그니처는 backwards compat 위해 `fallback_key: str | None` 파라미터 유지(무시).

### 마이그레이션 영향
- 글로벌 `Provider.apiKeyEncrypted` / `apiKeyConfigured` 필드는 deprecated. 기존 데이터는
  남아있지만 무시됨. 별도 cleanup 작업은 차후.
- 기존 운영 환경에서는 모든 사용자가 본인 Provider 키를 다시 등록해야 함.

### 검증
- `pnpm --filter @agent-studio/api lint` → 0 errors (기존 3 warnings 유지)
- `pnpm --filter @agent-studio/api exec tsc --noEmit` → 0 errors
- `_resolve_provider_key({'provider:openai': 'sk-test'}, 'openai', None)` → `('sk-test', 'user')`

---

## 2026-05-13 (15) — 사용자 추가 자동 Project 생성 + 비밀번호 기본값/리셋 (Phase 3) (leedaeyoung)

### 변경
- `apps/api/src/modules/users/users.service.ts`
  - `registerDirect` 를 `prisma.$transaction` 으로 감싸 User 생성 + 본인 Workspace Project
    (`workspace-${userId.slice(0,8)}`) + ProjectMember(role=admin, status=active) 함께 생성.
  - admin 이 새 사용자 추가 즉시 본인 Project 가 매핑돼 client/admin 어느 쪽이든 곧장 접근 가능.
  - 비밀번호 리셋 endpoint(`PATCH /api/users/:id/password` + `changePassword`)는 기존 그대로
    재사용 (admin role 가드 유지).
- `apps/agent-web/src/app/(dashboard)/settings/users/page.tsx`
  - 사용자 추가 모달의 신규 비밀번호 default 를 빈 문자열 → `test1234!` 로 변경. admin 이
    그대로 사용하거나 수정 가능.
  - 비밀번호 변경 모달 상단에 "기본값(test1234!)으로 초기화" 빠른 버튼 추가. 클릭 즉시
    `changePassword(userId, 'test1234!')` 호출 후 toast.

### 검증
- `pnpm --filter @agent-studio/api lint --quiet` → 0 errors
- `pnpm --filter @agent-studio/api exec tsc --noEmit` → 0 errors
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors

### 영향
- `useUserStore.initProject()` 의 client-side fallback (사용자가 Project 없으면 자동 생성)
  은 이제 거의 사용 안 됨 — registerDirect 가 미리 생성하므로. 그래도 fallback 으로 유지
  (기존 데이터의 사용자 + 잘못된 상태 복구용).

---

## 2026-05-13 (16) — 본인 비밀번호 변경 (Phase 5) (leedaeyoung)

### 변경
- `apps/api/src/modules/users/dto/update-user.dto.ts`
  - `ChangeMyPasswordDto` 신규 (`currentPassword` + `newPassword`).
- `apps/api/src/modules/users/users.service.ts`
  - `changeMyPassword(userId, currentPassword, newPassword)` — 현재 비밀번호 검증 후 갱신.
- `apps/api/src/modules/users/users.controller.ts`
  - `PATCH /api/users/me/password` 신규. JWT userId 기반. admin role 가드 없음 (본인용).
- `apps/agent-web/src/lib/api-client.ts`
  - `users.changeMyPassword(currentPassword, newPassword)` 추가.
- `apps/agent-web/src/components/layout/app-sidebar.tsx`
  - Settings 메뉴를 ADMIN(준비 중) → MAIN 섹션으로 이동. `href="/settings/profile"` 활성화.
- `apps/agent-web/src/app/(dashboard)/settings/profile/page.tsx` — 신규.
  - 본인 정보 표시 + 비밀번호 변경 폼.
- `apps/agent-web/src/app/(client)/client/settings/profile/page.tsx` — 신규.
  - 동일 기능을 client UI 톤(emerald + var(--client-*)) 으로 구성.
- `apps/agent-web/src/app/(client)/_components/sidebar.tsx`
  - 푸터 사용자 정보 영역을 `<Link href="/client/settings/profile">` 으로 변환 (클릭 진입).

### 검증
- `pnpm --filter @agent-studio/api lint --quiet` → 0 errors
- `pnpm --filter @agent-studio/api exec tsc --noEmit` → 0 errors
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors

---

## 2026-05-13 (17) — Refresh token race 의도치 않은 로그아웃 방지 (leedaeyoung)

### 배경
백엔드 refresh token 은 single-use (`auth.service.ts:75` deleteMany 후 신규 발급). 한 페이지
진입 시 여러 API 가 동시에 401 받으면 각자 `tryRefresh` 를 호출 → 첫 호출만 성공, 나머지는
stale token 으로 fail → `clearTokens + redirect to /login` 으로 의도치 않은 로그아웃.

### 변경
- `apps/agent-web/src/lib/api-client.ts`
  - `ApiClient` 에 `refreshInFlight: Partial<Record<AuthScope, Promise<boolean>>>` 추가.
  - `tryRefresh(scope)` 가 진행 중인 promise 가 있으면 그것을 재사용 (single-flight).
  - 완료 후 `finally` 로 슬롯 해제.
  - 같은 scope 의 동시 401 들이 단일 refresh 호출을 공유 → race 제거.

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → 0 errors (monaco 사전 이슈 제외)

### Phase 6 (idle 자동 로그아웃)
다음 사이클에서 진행.

---

## 2026-05-14 — Client HITL 카드/Composer UI 마무리 (leedaeyoung)

### 배경
- `ClientChatView` 에 HITL 카드·승인/거부/수정 핸들러는 들어가 있었으나, "수정 요청"
  플로우에서 composer 시각 표시·send 라우팅·취소 UI가 미완성이라 수정 요청문이 일반
  메시지로 전송되는 등 동작이 깨져 있었음.
- 첨부 디자인(이미지) 에 맞춰 카드 레이아웃과 composer 수정 모드 UI 를 마무리.

### 변경 (`apps/agent-web/src/components/client/ClientChatView.tsx`)
- `ClientHitlCard` 전면 재구성
  - 좌측 ShieldCheck 원형 아이콘 + 카드 우측 영역.
  - 상단: "● 안전" 녹색 배지 + "도구 실행 요청" 부제.
  - 본문: 굵은 제목 "에이전트가 {toolLabel} 도구 실행을 요청했습니다." → "도구 이름"
    + 모노스페이스 chip → "실행 인자" 박스 (key: value 모노스페이스 목록).
  - 푸터: 좌측에 수정 입력 중/응답됨 상태 점, 우측에 [거부]/[수정·수정 중]/[승인]
    3버튼. 편집 중일 때 "수정" → "수정 중" 라벨 변경 + sky 배경 강조. 클릭 시 편집
    종료(취소). resolved 시 모든 버튼 disabled.
  - 인자 표시는 `formatHitlArgValue` 로 단일 라인 직렬화 (object/array 는 JSON, 그 외
    원시 타입은 그대로). 빈 값/빈 객체는 자동 숨김.
  - 더 이상 쓰이지 않는 `describeHitlAction` 헬퍼 제거.
- Composer 수정 요청 모드 분기 추가
  - `editingHitl` 일 때 입력창 컨테이너 border 를 sky 로 강조 + 옅은 ring shadow.
  - textarea 위에 chip "↳ {toolName} 수정 요청 [X]" — X 클릭 시
    `handleHitlEditCancel`.
  - placeholder 가 `${toolLabel} 도구 호출을 어떻게 수정할지 알려주세요` 로 교체.
  - 입력창 하단 좌측: "Enter로 수정 요청 보내기 · Esc로 취소" 안내문.
  - send 버튼이 sky 배경으로 변경되고 `handleHitlEditSubmit` 호출. 수정 모드에서는
    Stop 버튼을 표시하지 않음 (sending=true 라도 send 버튼 유지, 단 disabled 가 아닐
    때만 클릭 가능).
- `handleHitlDecide` 가 자신과 같은 interactionId 의 `editingHitl` 을 해제하고
  pending edit message 도 무효화 — 같은 카드에서 편집 시작 후 승인/거부를 누른 경우
  composer 가 수정 모드에 갇히는 버그 방지.
- `CornerDownRight` 아이콘 추가 import.

### 동작
- HITL 인터럽트 도착 시 카드가 채팅 피드에 표시됨. "수정" 클릭 → 카드 푸터의 라벨이
  "수정 중" 으로 바뀌고 좌측에 "● 아래에서 수정 입력 중..." 표시.
- 동시에 composer 가 sky border + chip(↳ toolName 수정 요청 X) + 도구 맞춤 placeholder
  + 안내문으로 전환. send 버튼은 sky.
- Enter / send 버튼 클릭 → `handleHitlEditSubmit` 가 호출되어 도구 호출을 reject 한 뒤
  사용자가 입력한 자연어를 새 메시지로 자동 전송 (`pendingEditMessageRef` + sending=false
  effect 로 연쇄).
- Esc 키 또는 chip 의 X 또는 푸터 "수정 중" 버튼 클릭 → 편집 취소 (composer 복귀).

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm exec tsc --noEmit` → ClientChatView 관련 신규 에러 없음 (monaco 사전 이슈만).
- 브라우저 수동 검증은 미실시 — 다음 dev server 기동 시 시각 확인 필요.

### 한계 / 후속
- 도구 호출을 정식 `decision='edit', edited_action={name,args}` 로 보내지 않고
  reject + 새 메시지 전송 방식으로 유지 (client UX 단순화 의도). 인자 폼 편집이 필요
  하면 admin DebugPanel 패턴(`HitlApprovalCard`) 으로 전환 가능.
- HITL 카드 자체에는 어떤 turn 에 속하는지 표시되지 않음 — 위에 그룹 카드가 있긴 하나
  명시적 연결은 없음. 필요시 후속 사이클에서 groupId 매핑.

---

## 2026-05-14 (2) — Client HITL 카드 "실행 인자" 자연어 렌더링 (leedaeyoung)

### 배경
- 디자인 머지 후 HITL 카드 본문에 `q: in:anywhere`, `user_id: me` 같은 raw key:value
  가 그대로 노출돼 일반 사용자가 이해하기 어렵다는 피드백.
- client 화면은 admin debug panel 과 달리 사용자가 도구 호출을 자연어로 이해하고
  승인/거부할 수 있어야 함.

### 변경 (`apps/agent-web/src/components/client/ClientChatView.tsx`)
- `formatHitlArgValue` 헬퍼 제거.
- `describeHitlArgs(toolName, args): string[]` 신규 — 도구별 자연어 템플릿. 알려진
  도구만 문장으로 변환하고 그 외에는 빈 배열(박스 자체 미표시). 처리 대상:
  `gmail_search` / `gmail_fetch` / `gmail_fetch_attachment` / `gmail_parse_pdf_attachment`
  / `gmail_send` / `web_search` 류 / `read_file` / `write_file` / `edit_file` /
  `pdf_parse` / `document_preprocess` / `python` / `task` / `current_time`.
- `humanizeGmailQuery(q)` 신규 — gmail 검색 쿼리 토큰을 한국어로 변환
  (`in:anywhere` → "모든 메일함", `is:unread` → "읽지 않은 메일", `from:X` → "보낸이 \"X\"" 등).
  미지원 토큰은 따옴표로 감싸 그대로 출력 → 정보 손실 없음.
- `ClientHitlCard` 의 "실행 인자" 박스 라벨을 **"이렇게 동작합니다"** 로 교체.
  내부 렌더링을 모노스페이스 key:value → 일반 텍스트(자연어 문장) 로 변경,
  `whitespace-pre-wrap` 로 본문/코드 같은 다행 값도 그대로 노출.

### 예시
- `gmail_search { q:"in:anywhere", user_id:"me", max_results:1, include_spam_trash:false }`
  → "내 Gmail의 모든 메일함에서 메일 1개을 검색합니다." + "스팸·휴지통은 제외됩니다."
- `gmail_send { to, subject, body }` → "다음 내용으로 Gmail 메일을 전송합니다." +
  받는 사람/제목/본문 라벨링.
- `python { code }` → "Python 코드를 실행합니다." + "실행할 코드:\n{code}".

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → ClientChatView 신규 에러 없음.
- 브라우저 hot reload 로 시각 검증 권장.

---

## 2026-05-14 (3) — Client 채팅 history 에 HITL 카드 복원 (leedaeyoung)

### 배경
- 사용자 피드백: 채팅 thread 를 다시 열면 (history) 이전에 발생한 HITL 카드가 사라짐.
- 원인: `apiClient.hitl.listPending(projectId)` 만 호출 + `status==='pending'` 필터로
  resolved (approved/rejected) interaction 이 누락. 또한 `HitlController.@Controller('api')`
  로 인해 라우트가 `/api/api/...` 로 잘못 등록돼 있었고, api-client 는 `/api/...` 로 호출하던
  탓에 listPending 호출 자체도 404 였음 (catch 로 silently swallowed).

### 변경
- `apps/api/src/modules/hitl/hitl.service.ts` — `listByThread(threadId)` 신규.
  thread 의 모든 HITL interaction 을 createdAt 오름차순으로 반환.
- `apps/api/src/modules/hitl/hitl.controller.ts`
  - `@Controller('api')` → `@Controller()` 로 수정. 다른 controller(`@Controller('users')` 등)
    와 동일한 컨벤션 + global prefix `/api` 결합으로 라우트가 `/api/...` 로 정상 등록.
    (기존 `/api/api/projects/.../interactions` 는 실수. 호출자 부재로 노출되지 않음.)
  - `GET threads/:threadId/interactions` 신규 라우트.
- `apps/agent-web/src/lib/api-client.ts` — `apiClient.hitl.listByThread(threadId)` 추가.
- `apps/agent-web/src/components/client/ClientChatView.tsx`
  - history 복원 effect 에서 `listPending(projectId)` → `listByThread(threadIdProp)`.
  - `i.status` 를 `HitlDecision | undefined` 로 매핑:
    - `pending` → undefined (활성 카드, 버튼 활성)
    - `approved` → `'approve'`
    - `rejected` / `cancelled` / `timed_out` → `'reject'`
  - 복원된 카드는 `resolved` 가 사전 설정돼 ClientHitlCard 의 푸터에 "승인됨/거부됨"
    상태로 표시되며 모든 버튼 disabled (이미 ClientHitlCard 의 `buttonsDisabled = Boolean(resolved)`
    로직이 처리).
  - 복원 시 feed 를 `ts` 오름차순으로 정렬 — run 들 사이의 정확한 위치에 HITL 카드를
    삽입. (run startedAt vs interaction createdAt 으로 자연스러운 순서 보장).
  - `projectId` 의존성 제거 (effect deps 에서도 제거).

### 검증
- `pnpm --filter @agent-studio/api lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → ClientChatView 신규 에러 없음.
- `curl http://localhost:28001/api/threads/test/interactions` → 401 (route 등록 확인).
- Nest watch 로그: `Mapped {/api/threads/:threadId/interactions, GET} route` 정상 출력.

### 한계
- "edit" 으로 응답한 interaction 도 DB 에는 reject 로 기록(현재 client UX 가 reject + 새
  메시지 전송 패턴). 복원 시 "거부됨" 으로 보임. 정식 `decision='edit'` 로 보내려면
  ClientChatView 의 `handleHitlEditSubmit` 변경 필요.

---

## 2026-05-14 (4) — HITL 히스토리 복원 race + 응답 평탄화 (leedaeyoung)

### 배경
- 직전 사이클 후에도 새로 만든 thread 의 history 에서 HITL 카드가 여전히 안 보임.
- 두 가지 추가 원인 식별:
  1. **race**: `apiClient.threads.runs(...).then(setFeed(items))` 와 `apiClient.hitl.listByThread(...).then(setFeed((prev)=>...))` 가 병렬 실행. listByThread 가 먼저 끝나 setFeed(prev=>...) 로 HITL 을 prev=[] 에 append 한 직후, runs 의 setFeed(items) 가 들어와 HITL 을 전부 덮어씀.
  2. **응답 형태 불일치**: `HumanInteraction` 행의 `actionRequests` / `reviewConfigs` 가 `agent_context` jsonb 안에 있는데, 클라이언트와 shared 타입은 top-level 필드로 가정. WS 이벤트는 runner 가 emit 직전에 평탄화하지만, REST 응답은 평탄화 없음.

### 변경
- `apps/agent-web/src/components/client/ClientChatView.tsx`
  - 복원 effect 의 `Promise.all([messages, runs])` 에 `apiClient.hitl.listByThread(threadIdProp)` 를 합쳐 단일 `.then()` 으로 처리. runs 항목과 HITL 항목을 함께 만들고 `ts` 오름차순 정렬 후 단일 `setFeed(merged)` — race 제거.
  - 별도 listByThread effect chain 삭제.
- `apps/api/src/modules/hitl/hitl.service.ts`
  - `listByThread` 가 raw Prisma row 를 그대로 반환하지 않고, `agent_context.actionRequests` 첫 항목을 기반으로 `toolName / toolArgs` 와 매칭되는 `allowedDecisions` 를 추출해 top-level 로 함께 노출. WS 이벤트(`emit_hitl_request` 직전 enrichment) 와 동일한 모양.

### 검증
- `pnpm --filter @agent-studio/api lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → ClientChatView 신규 에러 없음.
- Nest watch 로그 `Found 0 errors` 확인.
- 브라우저 새로고침 후 시각 검증 권장.

---

## 2026-05-14 (5) — HITL 카드가 user 메시지 앞에 표시되는 순서 버그 (leedaeyoung)

### 배경
- 직전 사이클로 HITL 카드는 history 에 보이게 됐지만, **user 메시지보다 위에** 나타남.
- 원인: LangGraph state 의 메시지에는 `timestamp` 필드가 없어 ClientChatView 의
  `msg.timestamp ? ... : lastUserTs + idx` 가 항상 fallback (`Date.now() + idx`) 으로
  떨어짐. 사용자 메시지 ts 가 페이지 로드 시각이 되어, 과거 시각인 HITL.createdAt 보다
  훨씬 늦어진다. 시간순 정렬 결과 HITL 이 user 앞에 위치.
- 또한 group ("N개 작업 완료") 카드 ts 가 `userTs + 1` 로 고정돼 있어 HITL.createdAt
  과의 상대 순서도 어색했음.

### 변경 (`apps/agent-web/src/components/client/ClientChatView.tsx`)
- 메시지 복원 루프를 1:1 매칭된 run 의 timestamp 로 anchor:
  - `currentRun` 변수 도입. user 메시지 처리 시점에 다음 run 을 consume 한다.
  - `userTs = msg.timestamp ?? currentRun.startedAt ?? fallback`.
  - `assistantTs = msg.timestamp ?? currentRun.completedAt ?? fallback`.
  - group `ts` 를 `userTs + 1` → `currentRun.completedAt ?? userTs + 1` 로 변경.
- 결과 정렬 순서: **user → HITL(들) → group("N개 작업 완료") → assistant**.
  HITL.createdAt 는 자연스럽게 run.startedAt 와 run.completedAt 사이에 위치하므로
  자동으로 user 와 group 사이에 끼인다.

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → ClientChatView 신규 에러 없음.
- 브라우저 새로고침 후 시각 검증 권장.

---

## 2026-05-14 (6) — 메시지 timestamp 가 응답 시점이라 정렬 깨지는 문제 (leedaeyoung)

### 원인
- `apps/agent-runner-py/src/modules/langgraph/langgraph_service.py:_serialize_messages` 가
  각 메시지에 `"timestamp": datetime.now(timezone.utc).isoformat()` 를 stamp.
- 즉 `/api/threads/:id/messages` 응답의 timestamp 는 **메시지 생성 시각이 아니라 응답 시점**.
- 직전 사이클에서 `userTs = msgExplicitTs ?? runStartTs ?? fallback` 로 msgExplicitTs 를
  우선했으나, 그 값이 항상 NOW 라 user 메시지가 HITL.createdAt(과거) 보다 늦어져 정렬 후
  user 가 HITL 뒤로 밀려남.

### 변경 (`apps/agent-web/src/components/client/ClientChatView.tsx`)
- 우선순위 변경:
  - `userTs = runStartTs ?? msgExplicitTs ?? fallback`.
  - `assistantTs = runCompleteTs ?? msgExplicitTs ?? fallback`.
- 백엔드가 datetime.now 를 stamp 하더라도 매칭된 run 의 timestamp 가 있을 때는 그 값을
  먼저 쓴다. 매칭이 없을 때만 msg.timestamp / 시퀀스 fallback 사용.
- 신뢰 불가 이유는 코드 주석에 남김.

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → ClientChatView 신규 에러 없음.
- 브라우저에서 history 진입 시 `user → HITL → group → assistant` 순서 확인 권장.

### 후속
- 백엔드 `_serialize_messages` 의 `timestamp` 가 응답 시점인 점은 그대로 두면 후속 이슈
  소지 있음. 메시지 자체에 정확한 생성 시각을 stamp 하려면 LangGraph `additional_kwargs`
  활용 또는 별도 메시지 메타 테이블 도입 필요. 별도 사이클에서 다룰 것.

---

## 2026-05-14 (7) — HITL 카드를 turn 단위로 명시 삽입 (leedaeyoung)

### 진짜 원인
- DB 데이터를 직접 조회한 결과:
  - thread `6e13e759`: 1개 run (startedAt=06:26:02.710, completedAt=06:26:07.407),
    2개 HITL (createdAt=06:25:56.606 / 06:26:00.814).
  - HITL.createdAt 이 run.startedAt 보다 **6 초 정도 앞선다**. LangGraph 인터럽트로 paused
    상태일 때 만들어졌고, user 승인 후 resume 되어 최종 run row 가 기록되는 패턴.
- 단순 `ts` 정렬로는 절대 `user → group → HITL → assistant` 순서를 보장할 수 없음:
  - `userTs = runStartTs` 로 잡으면 HITL.createdAt < userTs 라 HITL 이 user 앞으로 가버림.
  - `userTs = msgTimestamp` 로 잡으면 백엔드의 응답 시점 stamp 때문에 user 가 가장 뒤로
    밀려나 HITL 이 user 앞으로 가버림.

### 해결책 (`apps/agent-web/src/components/client/ClientChatView.tsx`)
HITL 을 ts 정렬에 맡기지 않고 **해당 turn 의 user 직후에 명시적으로 push**:

1. **`interactionsByRunId` 매핑** — 각 HITL 의 `respondedAt ?? createdAt` 이 run.completedAt
   이하인 가장 빠른 run 에 매칭. 매칭 실패 시 `orphanInteractions[]`.
2. **messages.forEach 루프 안에서 HITL push** — user 메시지 처리 시점에 그 turn 의 run 을
   결정한 뒤 `user → group → 해당 turn 의 HITLs (createdAt 순) → assistant` 순으로 items 배열에
   삽입.
3. **턴 단위 `ts` 부여** — turn anchor 위에 ms 단위 offset (user=anchor, group=anchor+1,
   HITL_i=anchor+2+i, assistant=runCompleteTs). 최종 sort 가 turn 단위 순서를 깨지 않는다.
4. **`lastUserTs = 0` 초기값** — 직전 코드에서 `Date.now()` 였던 초기값이 `lastUserTs + 100`
   minimum 로직과 충돌해 user.ts 가 결국 NOW 로 강제됐던 버그 수정.
5. **turn 간 단조 증가 보장** — 매 turn 의 anchor 는 `max(...candidates, lastUserTs + 100)` 로
   잡아 turn N 의 항목이 항상 turn N-1 의 assistant 보다 뒤에 위치.

### 결과 검증 (예상)
thread `6e13e759` 의 6개 HITL 데이터로 트레이싱:
- turn 1 user.ts = 1747188362710 (= run.startedAt)
- group.ts = 1747188362711
- HITL #1.ts = 1747188362712 (createdAt 무시, turn anchor 기반)
- HITL #2.ts = 1747188362713
- assistant.ts = 1747188367457 (= run.completedAt + 50)

정렬 후 순서: **user → group → HITL #1 → HITL #2 → assistant** ✓

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → ClientChatView 신규 에러 없음.
- 브라우저 hard refresh 후 history 진입 시 시각 검증 권장.

### 한계
- `interactionsByRunId` 매칭은 `respondedAt <= run.completedAt` 휴리스틱. 같은 thread
  안에서 run 이 여럿이고 HITL 이 정확히 어느 turn 에 속하는지 모호한 경우 (예: 연속된
  turn 들이 매우 짧게 발생) 첫 번째 매칭 run 으로 fallback 됨. 실제 데이터로 다중 turn
  케이스 검증 필요.

---

## 2026-05-14 (8) — runner `/threads/:id/runs` 타임스탬프 timezone 누락 → HITL 매칭 실패 (leedaeyoung)

### 문제
사이클 (7) 의 turn-단위 명시 삽입 로직을 적용했는데도 HITL 카드가 user 메시지 **앞에**
표시되는 현상이 지속. 화면 순서: HITL → HITL → user → group → assistant.

### 원인 (재분석)
직접 API 응답을 비교해 발견한 timezone 표기 불일치.

| 엔드포인트 | 출력 예 | 'Z' suffix |
|---|---|---|
| `/api/threads/:id/runs` (Python runner) | `"2026-05-14T06:26:02.710000"` | **없음** |
| `/api/threads/:id/interactions` (NestJS/Prisma) | `"2026-05-14T06:25:58.708Z"` | **있음** |

브라우저 (KST = UTC+9) `new Date()`:
- 'Z' 없는 문자열 → **로컬 TZ 로 해석** → run.completedAt 의 실제 UTC ms 가 9 시간
  과거로 미끄러짐.
- 'Z' 있는 문자열 → UTC 로 정확히 해석.

결과: `c >= anchorMs` (run.completedAt vs HITL.respondedAt) 비교가 항상 false →
모든 HITL 이 orphan 경로로 빠지고 `ts = oIdx (0, 1)` 로 push → 피드 최상단에 표시.

### 해결
`apps/agent-runner-py/src/modules/threads/threads_service.py` `get_runs()` 안에
`_iso_utc(value)` 헬퍼 신설 — DB 의 naive UTC datetime 을 항상 `Z` suffix 가 붙은
ISO 문자열로 직렬화. `runs[]` 와 `steps[]` 의 `startedAt`/`completedAt` 모두 적용.

### 검증
- runner hot-reload 후 `curl /threads/:id/runs` → `"startedAt": "...710000Z"` 확인.
- 브라우저 hard refresh 시 user → group → HITL → assistant 정렬 정상 동작.

---

## 2026-05-14 (9) — HITL "수정 요청" UI 표시 + 거부 응답 메시지 대체 (leedaeyoung)

### 배경
사용자가 HITL 카드의 "수정" 버튼으로 자연어 수정문(예: "5개 가져와줘") 을 보내면 deepagents
공식 edit 결정은 `edited_action: {name, args}` 를 요구하므로 현재 구현은 reject + 새 user
메시지로 우회한다. 그 결과 history 에서:
1. HITL 카드가 "거부됨" 으로 표기됨 (사용자 의도는 "수정")
2. reject 직후 에이전트가 "메일함 접근이 거부되어... 권한을 허용해 주시면..." 같은 거부
   응답 assistant 메시지를 자동 생성해 노출됨 (사용자 의도와 어긋남)

### 해결 (LLM 호출 없이 마커 기반)
**A. HITL 카드 → "수정 요청" 으로 표기**
- frontend `handleHitlEditSubmit` 이 `wsClient.respondToInteraction` payload 에 `editPrompt`
  필드를 함께 실어 보낸다 (`{ decision: 'reject', editPrompt: text }`).
- runner `_normalize_decisions` 는 그대로 reject 로 정규화하지만 response 전체가
  `human_interactions.response` jsonb 에 저장되므로 editPrompt 가 그대로 보존됨.
- `apps/api/.../hitl.service.ts` `listByThread` 가 `response.editPrompt` 를 top-level 로
  평탄화. `packages/shared/HumanInteraction` 에 `editPrompt?: string` 추가.
- `ClientChatView` `buildHitlItem` 이 editPrompt 가 살아 있으면 status(=rejected) 무시하고
  `resolved='edit'` + `editPrompt` 를 FeedItem 에 attach.
- `ClientHitlCard` 가 `item.editPrompt` 있으면 sky border 박스로 "사용자의 수정 요청: ..."
  표시.

**B. 거부 응답 assistant 메시지 → "사용자의 수정 요청" 노티로 대체**
- 새 ref `editFollowupAssistantRef = useRef<string | null>(null)` 신설. 수정 submit 시
  editPrompt 를 set, agent.token 핸들러가 새 assistant 를 생성할 때 attach 후 즉시 해제.
- restored 경로는 forEach 안에서 turn 단위 `pendingEditFollowup` 변수로 같은 turn 의 첫
  assistant 에 attach.
- `AssistantMessage` 컴포넌트가 `editFollowup` prop 있으면 본문 markdown 대신 sky 카드
  형태의 시스템 노티 `🛠 사용자의 수정 요청 · {editPrompt}` 로 대체 렌더링.

### 변경 파일
- `packages/shared/src/types/hitl.ts` — `editPrompt?: string`.
- `apps/api/src/modules/hitl/hitl.service.ts` — listByThread 평탄화.
- `apps/agent-web/src/components/client/ClientChatView.tsx`
  - FeedItem `hitl_request.editPrompt`, `assistant.editFollowup` 추가.
  - `handleHitlEditSubmit` payload 에 editPrompt 포함 + ref 세팅.
  - agent.token 핸들러 / restored forEach 모두에서 마커 attach.
  - `ClientHitlCard` 에 "사용자의 수정 요청" 박스 추가.
  - `AssistantMessage` editFollowup 분기 추가.

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/api lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/api exec tsc --noEmit` → 0 errors.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → ClientChatView 신규 에러 없음.
  (사전 존재 `monaco-editor` 모듈 미설치 에러는 본 작업과 무관.)

### 한계
- 자동 전송되는 후속 user 메시지("5개 가져와줘") 는 현재 그대로 노출됨. editPrompt 가 HITL
  카드에도 노출되므로 시각적으로 약간 중복 — 이 부분은 사용자 피드백 받은 후 별도 결정.
- live 흐름에서 reject 후 에이전트가 거부 응답을 전혀 생성하지 않는 케이스는
  editFollowupAssistantRef 가 다음 turn 으로 leak 될 수 있으나 useEffect 내 안전 클리어로
  방어함.

---

## 2026-05-14 (10) — HITL "수정" 흐름 재설계: preview-edit + pseudo 확인 카드 (leedaeyoung)

### 배경
사이클 (9) 의 reject + auto-resend 우회는 다음 한계가 있었음:
- deepagents 가 reject 후 동일 args 로 도구를 재시도해 HITL #2 가 same args 로 떠버림 (수정
  의도가 deepagents 에 전달되지 않음).
- reject 직후 에이전트가 "권한 거부" 류 거부 응답을 자동 생성 → 사용자 의도와 무관한
  메시지가 채팅에 끼어듦.
- 자동 전송된 후속 user 메시지가 새 turn 으로 처리되어 흐름 분기.

사용자 의도: "HITL #1(수정 요청 표기) → HITL #2(수정된 args 로 재확인) → 승인 → 도구
실행 → 답변" 한 줄.

### 공식 문서 확인 (Context7 `/websites/langchain_oss_python_deepagents`)
> "modify the arguments of a tool call **before it is executed**"

deepagents 의 `edit` 결정은 args 를 바꾼 뒤 **새 interrupt 없이 바로 실행**된다. 즉 사용자가
원하는 HITL #2 는 프런트가 추가한 UI 확정 단계여야 한다.

### 해결 — preview-edit 엔드포인트 + pseudo 카드
**A. runner (agent-runner-py)**
- `POST /api/v1/hitl/interactions/{id}/preview-edit` 신규.
- 동작: interaction 의 `actionRequests[0]` 에서 name/args 로드 → LLM 으로 자연어 수정문 →
  새 args JSON 변환 → `{ name, args, originalArgs }` 반환.
- LLM 선택: 우선 interaction 의 agent 가 설정한 provider/model (사용자가 이미 결제 중인
  모델). DB 미설정 시 `OPENAI_API_KEY` → `ANTHROPIC_API_KEY` 폴백.
- **DB 와 deepagents 상태는 절대 변경하지 않음** — 순수 변환 헬퍼.

**B. NestJS API**
- `HitlService.previewEdit(interactionId, editPrompt)` — runner 위임 프록시.
- `HitlController` `POST interactions/:id/preview-edit` 라우트 추가.
- `apiClient.hitl.previewEdit()` 메서드 추가.

**C. 프런트엔드 (ClientChatView)**
- `FeedItem.hitl_request` 에 `pendingEditFor?: { sourceInteractionId }` 추가.
- `handleHitlEditSubmit` 재작성:
  1) `apiClient.hitl.previewEdit(sourceInteractionId, text)` 호출 → 새 args 수신.
  2) 편집 대상이 real HITL → resolved='edit' + editPrompt 로 마킹 + 새 pseudo 카드 push.
     편집 대상이 pseudo 카드 (이미 한 번 수정 후 또 수정) → 그 카드의 args 만 갱신.
  3) deepagents 에는 사용자가 pseudo 카드를 승인할 때 비로소 `edit` 결정 전송.
- `handleHitlDecide(item, decision)` — pendingEditFor 가 있으면 approve →
  `{ decision: 'edit', editedAction: {name, args} }`, reject → `{ decision: 'reject' }`.
- `handleHitlEditStart(item)` — feedItemId, sourceInteractionId 함께 보관.
- 사이클 (9) 의 pendingEditMessageRef + editFollowupAssistantRef + editFollowup 마커 로직
  완전 제거 (자동 reject + 거부 응답 발생이 없으므로 불요).
- `ClientHitlCard` onDecide/onEditStart 시그니처를 item 전체를 받도록 변경. 편집 진행 중
  (previewing=true) 동안 버튼 비활성화.

### 변경 파일
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py` — `preview_edit()` 신규.
- `apps/agent-runner-py/src/modules/hitl/hitl_router.py` — 라우트 등록.
- `apps/api/src/modules/hitl/hitl.service.ts` — `previewEdit()` 프록시.
- `apps/api/src/modules/hitl/hitl.controller.ts` — POST `preview-edit` 라우트.
- `apps/agent-web/src/lib/api-client.ts` — `hitl.previewEdit()`.
- `apps/agent-web/src/components/client/ClientChatView.tsx` — 전체 재배선.

### 검증
- `pnpm --filter @agent-studio/web lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/api lint --quiet` → 0 errors.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` → ClientChatView 신규 에러 없음.
- `pnpm --filter @agent-studio/api exec tsc --noEmit` → 0 errors.
- runner 라이브 호출 검증: `curl /preview-edit` 입력 `"3개로 변경해줘"` → response
  `max_results: 10 → 3` 정확 변환 확인.

### 사용자 흐름 (live)
1. user "최근메일 10개 알려줘"
2. HITL #1 (max_results=10) — real
3. 사용자 "수정" → "3개로 변경해줘"
4. preview-edit 호출 → max_results=3 으로 변환
5. HITL #1 resolved='edit' + editPrompt 박스, 그 직후 pseudo HITL #2 (max_results=3)
6. 사용자 pseudo #2 승인 → deepagents 에 `{decision:'edit', editedAction:{name, args}}` 송신
7. 도구 실행 (3개 검색) → 결과 → assistant 답변

### 한계
- preview-edit 호출은 LLM 추가 호출 1회 (agent 모델 사용 시 사용자 비용에 합산).
- pseudo 카드는 deepagents 상태가 아니므로 history reload 시에는 보이지 않음. HITL #1 에
  editPrompt 박스로만 의도가 보존되며, 최종 args 는 `response.editedAction` 에 저장됨
  (현재 UI 는 별도로 그 args 를 보여주진 않음 — 후속 사이클에서 검토).

---

## 2026-05-14 (11) — HITL edit 사용자 의도 주입 + history 수정 후 args 표시 (leedaeyoung)

### 배경
사이클 (10) 후 사용자 검증에서 두 가지 잔여 문제:
1. "최근메일 하나 알려줘" → "수정" → "3개 알려줘" 흐름에서 답변이 1개만 나옴.
   DB 확인 결과 edit decision 과 새 args(max_results=3) 는 정상 전송됐으나 후속 도구
   호출(`gmail_fetch`) 이 1회만 실행되고 답변도 1개. 원인은 deepagents `edit` 결정이
   **도구 args 만 바꾸고 원본 user 메시지("하나 알려줘") 는 그대로 두기 때문** —
   LLM 이 원본 user 의도를 따라 1건만 응답.
2. history reload 시 카드가 1장만 보이고 "수정 후 args(3개)" 가 어디에도 표시되지 않음.
   DB 의 `response.editedAction` 은 살아 있지만 listByThread 응답에 미노출.

### 해결

**A. 사용자 의도 주입 (deepagents resume 시 HumanMessage 추가)**
- `deepagent_bridge.resume_stream_with_deepagent(user_message=None)` 시그니처 확장.
  `user_message` 가 있으면 `Command(resume={decisions}, update={"messages":[HumanMessage(...)]})`
  로 그래프 state 에 새 HumanMessage 함께 주입. langgraph 표준 add_messages reducer 가
  append 처리.
- `agents_service.resume_stream(user_message=None)` 통과.
- `threads_service.resume_message(user_message=None)` 통과.
- `hitl_service.respond()` — response.decision='edit' + response.editPrompt 가 문자열이면
  resume_message 에 user_message 로 전달.

**B. 프런트엔드 editPrompt 동행 전송**
- pseudo 카드 생성 시 `editPrompt` 필드 보존.
- `handleHitlDecide` pendingEditFor + approve 경로 payload 에 `editPrompt` 포함 →
  runner 의 respond 가 동일 텍스트를 새 HumanMessage 로 deepagents state 에 주입.

**C. history 카드에 "수정 후 args" 박스 추가**
- `apps/api hitl.service.ts listByThread` — `response.editedAction` 평탄화해 row 결과에
  `editedAction: {name, args}` 노출.
- `packages/shared types/hitl.ts HumanInteraction` 에 `editedAction?: {name, args}` 추가.
- `ClientChatView` history 빌더 `buildHitlItem` 에서 editedAction 을 FeedItem 으로 전파.
- `ClientHitlCard` 에 `editedArgLines` 계산 + 녹색 "수정 후 도구 실행" 박스 렌더링.
  → 카드 1장에 "원본 args → 사용자 수정 요청 → 수정 후 args" 3단 표시.

### 변경 파일
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — resume Command update.
- `apps/agent-runner-py/src/modules/agents/agents_service.py` — user_message 통과.
- `apps/agent-runner-py/src/modules/threads/threads_service.py` — user_message 통과.
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py` — editPrompt 추출/전달.
- `apps/api/src/modules/hitl/hitl.service.ts` — listByThread 매핑에 editedAction 추가.
- `packages/shared/src/types/hitl.ts` — `HumanInteraction.editedAction` 필드 추가.
- `apps/agent-web/src/components/client/ClientChatView.tsx` — FeedItem 확장, history 빌더,
  카드 렌더링, payload 에 editPrompt 동행.

### 검증
- `ruff check` 신규 에러 0 (기존 import 정렬 4건은 baseline, 이번 PR 무관).
- `pnpm --filter api tsc --noEmit` → 0 errors.
- `pnpm --filter web tsc --noEmit` → ClientChatView/HumanInteraction 관련 신규 에러 없음
  (monaco-editor 누락은 기존 이슈).

### 공식 문서 근거
- deepagents `human-in-the-loop` (Context7 `/websites/langchain_oss_python_deepagents`):
  edit decision 은 `edited_action.args` 만 바꾸며, user 의도 갱신은 별도로 그래프 state
  에 HumanMessage 를 추가해야 함.
- langgraph `Command(resume=..., update=...)` 표준 — 한 번의 호출로 인터럽트 재개 +
  state 업데이트 동시 가능.

---

## 2026-05-14 (12) — HITL edit 흐름: reject + silent + auto follow-up 로 재설계 (leedaeyoung)

### 배경
사이클 (11) 의 `Command.update={"messages":[HumanMessage]}` 주입이 실 동작에서 OpenAI
API 의 메시지 시퀀스 제약을 위반함:
```
Error 400: An assistant message with 'tool_calls' must be followed by tool messages
responding to each 'tool_call_id'.
```
HITL interrupt 는 `AIMessage(tool_calls=[...])` 직후에 발생하므로, 그 시점에 HumanMessage
를 끼우면 tool_calls 와 ToolMessage 사이에 끼어버려 다음 LLM 호출이 4xx 로 실패. 결과적으로
turn 이 실패 처리되어 사용자 답변이 나오지 않음.

### 해결 — 사용자 제안 채택
> "수정했는지 거부했는지 승인했는지는 알 수 있으니, 수정일 땐 거부 답변 LLM 응답을 안 보여
> 지게 하면 어떨까"

**Frontend 단독 처리** (백엔드 변경 불필요):
1. pseudo HITL #2 승인 시 deepagents 에 `{decision:'reject', editPrompt, editedAction}`
   전송. backend 는 reject 만 처리하지만 DB response 에 editPrompt/editedAction 이 함께
   기록되어 history 카드 렌더링에 활용.
2. `editFollowupRef` 를 `{active:true, editPrompt}` 로 설정해 silent 모드 진입.
3. silent 모드 동안 `agent.token`, `step.started/completed/failed`, `plan.created` 이벤트는
   UI 에서 무시. deepagents 가 생성한 "도구 사용 거부" 답변과 부수 step 이 화면에 노출되지
   않는다.
4. `turn.completed` 도착 시 silent 해제 + `setSending(false)` + `setTimeout(0)` 으로
   `handleSend(editPrompt)` 호출. editPrompt 가 새 user 메시지로 같은 thread 에 새 turn
   을 시작하고, 정상 흐름(도구 호출 → HITL 또는 답변)으로 진행된다.
5. safety net: silent 모드 중 `hitl.request` 또는 `run.cancelled` 도착 시 silent 해제 +
   follow-up 취소 → 사용자가 직접 처리. 자동 승인 금지 (보안).

### 사이클 (11) 롤백
- `deepagent_bridge.resume_stream_with_deepagent` user_message 인자 제거, `Command.update`
  제거.
- `agents_service.resume_stream` / `threads_service.resume_message` user_message 인자
  제거.
- `hitl_service.respond` editPrompt 추출 코드 제거.

### 변경 파일
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — (11) 롤백.
- `apps/agent-runner-py/src/modules/agents/agents_service.py` — (11) 롤백.
- `apps/agent-runner-py/src/modules/threads/threads_service.py` — (11) 롤백.
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py` — (11) 롤백.
- `apps/agent-web/src/components/client/ClientChatView.tsx`:
  - `editFollowupRef`, `handleSendRef` 추가.
  - WS handlers (`agent.token`, `turn.completed`, `step.*`, `plan.created`, `run.cancelled`,
    `hitl.request`) 에 silent 가드 / follow-up 트리거.
  - `handleHitlDecide` pseudo+approve 분기를 reject + editFollowup 설정으로 변경.

### 검증
- `pnpm --filter web tsc --noEmit` → 0 errors (monaco-editor 외).
- `ruff check` 신규 에러 0.

### 사용자 흐름 (live, 목표)
1. user "최근메일 하나 알려줘"
2. HITL #1 (max_results=1)
3. 사용자 "수정" → "3개 알려줘" → preview-edit → max_results=3 변환
4. pseudo HITL #2 (max_results=3)
5. 사용자 pseudo #2 승인 → reject 전송 + silent 모드 ON
6. (silent) deepagents 가 거부 답변 생성 → turn.completed (UI 안 보임)
7. 자동으로 `handleSend("3개 알려줘")` → 새 user 메시지로 새 turn 시작
8. 새 HITL 카드 등장 → 사용자 승인 → 도구 실행 → 답변 (3개 정보)

### 한계 / 후속 검토
- 새 turn 에서 deepagents 가 도구를 호출하면 HITL 이 또 발생해 사용자가 한 번 더 승인해야
  한다. cycle 9 에서 사용자가 어색하다고 지적한 흐름과 유사하지만, 이번엔 첫 거부 답변이
  완전히 숨겨지므로 시각적으로는 "수정 카드 승인 → 새 user msg → 새 카드 → 승인 → 답변"
  으로 자연스럽다.
- silent 모드 중 `hitl.request` 가 들어오면 follow-up 을 포기한다. reject 후 deepagents 가
  거부 답변 대신 다른 도구를 시도하는 케이스. 그대로 사용자에게 노출 (자동승인 금지).

---

## 2026-05-15 — Gmail OAuth source 분기 (admin vs client) | leedaeyoung

### 목표
admin 페이지(studio)와 client 페이지의 Gmail OAuth 자격증명을 완전히 분리한다.
두 페이지의 계정은 이미 분리되어 있고 각자 인증할 수 있으므로, 각 페이지에서
인증한 OAuth 만 사용하도록 명시적 분기점을 추가한다.

- `source='studio'` → 프로젝트(admin) OAuth 만 사용 (사용자 OAuth 폴백 금지)
- `source='client'` → 사용자 OAuth 만 사용 (프로젝트 폴백 금지)
- `source` 미지정 → 기존 동작 보존 (userId 있으면 user 우선, 없으면 프로젝트)

### 변경 파일

#### NestJS API
- `apps/api/src/modules/tools/tools.controller.ts` — `GET /internal/agents/:id/gmail/access-token`
  에 `source` 쿼리 추가.
- `apps/api/src/modules/tools/tools.service.ts` — `issueGmailAccessTokenByAgentId(agentId,
  userId?, source?)` 분기:
  - `studio` → projectId 로만 발급.
  - `client` → userId 없거나 user OAuth 미등록이면 400. 프로젝트 폴백 금지.
  - 미지정 → 기존 동작.
- `apps/api/src/modules/threads/threads.controller.ts` / `threads.service.ts` —
  `invoke`/`resume` 에 `source` body 필드 추가, runner 로 전달.
- `apps/api/src/modules/hitl/hitl.controller.ts` / `hitl.service.ts` —
  `/interactions/:id/respond` 에 `source` body 필드 추가 (호환·로깅용. 실제 분기는 WS).

#### Runner (Python)
- `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/registry.py` —
  `get_custom_tools(agent_id, user_id?, source?)`.
- `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_*.py` (5 files) —
  각 도구 빌더가 `source` 를 NestJS access-token 엔드포인트 query 로 전달.
- `apps/agent-runner-py/src/modules/builtin_tools/builtin_tools_service.py` —
  `BuiltinToolsService.get_tools(..., source?)` / `_build_all(..., source?)`.
- `apps/agent-runner-py/src/modules/agents/agents_service.py` —
  `_build_tools` / `_build_inline_subagent_specs` / `_build_db_subagent_specs` /
  `load_agent_with_deps` / `stream` / `resume_stream` 모두 `source` 전파.
- `apps/agent-runner-py/src/modules/threads/threads_router.py` — `SendMessageBody.source`,
  `invoke` 핸들러가 source 전달.
- `apps/agent-runner-py/src/modules/threads/threads_service.py` —
  `send_message(..., source?)` / `resume_message(..., source?)`.
- `apps/agent-runner-py/src/modules/hitl/hitl_router.py` /
  `apps/agent-runner-py/src/modules/hitl/hitl_service.py` —
  `respond(..., source?)`.
- `apps/agent-runner-py/src/modules/hitl/hitl_gateway.py` — `hitl.respond` WS 이벤트
  payload 에서 `userId`/`source` 추출 → `respond()` 로 전달.

#### Frontend (agent-web)
- `apps/agent-web/src/lib/api-client.ts` — `threads.invoke(threadId, content,
  architectureOverride?, source?)`, `threads.resume(threadId, approved, source?)`,
  `hitl.respond(interactionId, response, source?)`.
- `apps/agent-web/src/lib/ws-client.ts` — `respondToInteraction(..., source?)` 가
  `hitl.respond` payload 에 `source` 포함.
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/chat/page.tsx` — `source='studio'`.
- `apps/agent-web/src/components/agents/builder/TestTab.tsx` — `source='studio'`.
- `apps/agent-web/src/components/agents/builder/PlaygroundTab.tsx` —
  invoke/resume 모두 `source='studio'`.
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx` —
  invoke + 2x respondToInteraction(approve/reject, edit) 모두 `source='studio'`.
- `apps/agent-web/src/components/client/ClientChatView.tsx` —
  invoke + respondToInteraction 모두 `source='client'`.

### 검증
- `pnpm --filter @agent-studio/api lint` → 0 errors (사전 warning 3 개 유지).
- `pnpm --filter @agent-studio/api build` → 통과.
- `pnpm --filter web lint` → 0 errors (사전 warnings 만).
- Python `inspect.signature` 로 send_message / resume_message / stream / resume_stream
  / load_agent_with_deps / hitl.respond / get_custom_tools / BuiltinToolsService.get_tools
  모두에 `source` 파라미터가 마지막에 추가됨을 확인.

### 흐름 (after)
1. Admin chat → `apiClient.threads.invoke(..., 'studio')`
   → NestJS body.source='studio' → Runner SendMessageBody.source='studio'
   → agents_service → builtin_tools → gmail_* → NestJS `/internal/.../access-token?source=studio`
   → projectId 로만 토큰 발급.
2. Client chat → `apiClient.threads.invoke(..., 'client')`
   → ... → `?source=client&userId=...` → user OAuth 만 사용. 미등록이면 400 명시.
3. HITL resume:
   - admin: `wsClient.respondToInteraction(..., 'studio')` → hitl_gateway → respond(source='studio')
     → resume_message → resume_stream → load_agent_with_deps(source='studio') → 도구도 source 분기.
   - client: 동일 경로, `source='client'`.

---

## 2026-05-18 13:43 — leedaeyoung — 채팅 첨부파일 + 스레드별 Docker sandbox 도입 (Phase 1 시작)

### 목표
- 사용자가 채팅에 파일 업로드 → 에이전트가 분석/코드 실행할 수 있게 함.
- deepagents `SandboxBackendProtocol`을 docker-py로 구현한 `DockerSandbox`로 thread 단위 ephemeral 컨테이너 운영.
- 첫 MVP는 admin AgentFlowDebugPanel 한정. python:3.12-slim 이미지 고정, 네트워크 open, 디스크 영구 보관.

### 범위 (Task #5~#11)
1. DB: `Attachment` 모델 + Prisma 마이그레이션
2. Runner: `src/modules/deep/docker_sandbox.py` — execute/upload_files/download_files + thread→container 매핑 + idle 정리
3. Runner: `/api/v1/threads/{id}/attachments` 업로드/다운로드/삭제 엔드포인트, 로컬 디스크(`./data/attachments/<threadId>/`) 저장
4. Runner: `deepagent_bridge`에서 thread sandbox 백엔드 주입 + SandboxSyncMiddleware로 첨부 → `/workspace/`
5. API: NestJS `/api/threads/:id/attachments` 프록시 라우트
6. Web: AgentFlowDebugPanel composer 첨부 UI(버튼/DnD/썸네일/진행률)
7. 검증: PDF/이미지/CSV 시나리오, lint/typecheck

### 합의된 디폴트
- MVP 대상: admin 우선
- 영속화: 디스크 영구 보관 (`./data/attachments/<threadId>/<uuid>-<filename>`)
- Sandbox 이미지: `python:3.12-slim` 고정 (Phase 2에서 prebuilt 이미지 고려)
- 네트워크: open (pip install 가능; 운영 모니터링 권장)

### 참조 deepagents 공식 문서 (Context7 조회 완료)
- `/websites/langchain_oss_python_deepagents`:
  - backends (CompositeBackend, FilesystemBackend, custom SandboxBackend 패턴)
  - sandboxes (Daytona/Modal/Runloop/AgentCore 사례 — DockerSandbox는 동일 protocol로 직접 구현)
  - going-to-production (SandboxSyncMiddleware 패턴)

### 결과 (2026-05-18 14:14)
- 모든 phase 완료 (Task #5~#11).
- 변경 파일 요약:
  - `packages/database/prisma/schema.prisma` — `ThreadAttachment` 모델 추가
  - `packages/database/prisma/migrations/20260518000000_add_thread_attachments/migration.sql`
  - `apps/agent-runner-py/pyproject.toml` — `docker>=7.1.0`, `python-multipart>=0.0.20` 추가
  - `apps/agent-runner-py/src/config.py` — `ATTACHMENT_STORAGE_DIR`, `ATTACHMENT_MAX_BYTES`
  - `apps/agent-runner-py/src/modules/deep/docker_sandbox.py` — DockerSandbox + SandboxManager (신규)
  - `apps/agent-runner-py/src/modules/deep/sandbox_middleware.py` — AttachmentSyncMiddleware (신규)
  - `apps/agent-runner-py/src/modules/attachments/{__init__.py,attachments_service.py,attachments_router.py}` (신규)
  - `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — sandbox 백엔드 주입 + sync middleware
  - `apps/agent-runner-py/src/main.py` — attachments router 등록 + SandboxManager start/shutdown
  - `apps/api/src/modules/threads/{threads.controller.ts,threads.service.ts,threads.module.ts}` — 첨부 프록시
  - `apps/agent-web/src/lib/api-client.ts` — `attachments` 그룹 + `ThreadAttachment` 타입
  - `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx` — composer 첨부 UI

### 검증
- DB: `psql` 로 `thread_attachments` 테이블 생성 확인.
- Runner: lint(ruff) 0 errors, import 스모크 OK.
- Runner attachments REST: POST upload / GET list / GET download 모두 정상 (`/tmp/test-attachment.txt` 30bytes 라운드트립 성공).
- DockerSandbox: thread-test-001 컨테이너 생성 → `echo hello && python3 -c "print(2+2)"` → exit 0 / "hello\\n4" → `upload_files` → `cat /workspace/data.txt` 라운드트립 성공 → release.
- API: lint 0 errors, /api/docs 200.
- Web: tsc 신규 에러 0(monaco 사전 이슈는 pnpm install 후 해소), agents 페이지 200.

### 특이사항·후속 과제
- 마이그레이션은 `prisma migrate dev` 가 drift 로 reset 을 요구해 수동 SQL 적용 + `_prisma_migrations` 수동 등록함. 다음 fresh setup 에선 정상 적용 예정.
- 컨테이너 idle TTL 30분, memory 512MB, CPU 1코어, 네트워크 bridge(open) — Phase 2 운영 정책 보강 예정.
- 클라이언트 페이지(`ClientChatView`)에는 아직 미적용 (admin MVP). 필요 시 동일 패턴 복제.
- Sandbox 이미지는 `python:3.12-slim` 고정. pandas/numpy 가 자주 쓰이면 prebuilt 이미지 도입 검토.

---

## 2026-05-18 14:35 — leedaeyoung — Sandbox prebuilt 이미지 도입

### 목표
- 채팅 첨부 PDF/DOCX/XLSX/HWP/이미지를 LLM 이 즉시 분석할 수 있도록 자주 쓰는 파이썬 라이브러리를 사전 설치한 sandbox 이미지를 빌드.
- 매 thread 첫 사용 시 pip install 비용 제거 + LLM 의 라이브러리 선택 일관성 ↑.

### 변경
- `apps/agent-runner-py/docker/sandbox/Dockerfile` (신규) — python:3.12-slim + pypdf/pymupdf/python-docx/openpyxl/xlrd/pandas/numpy/matplotlib/pillow/chardet/python-magic/requests/bs4/lxml + pyhwp(best-effort). libmagic1 시스템 dep 포함. matplotlib 폰트 캐시 prebuild.
- `apps/agent-runner-py/src/modules/deep/docker_sandbox.py` — `SANDBOX_IMAGE` 를 `agentstudio-sandbox:0.1` 로 변경. `_ensure_image()` 추가 — 로컬 미존재 시 Dockerfile 자동 빌드 → 실패 시 registry pull fallback.
- `apps/agent-runner-py/src/modules/deep/sandbox_middleware.py` — `SANDBOX_PRELOADED_LIBS_HINT` 추가, system prompt 에 라이브러리 목록 노출.
- `package.json` — `pnpm sandbox:build` 스크립트 추가.

### 검증
- `pnpm sandbox:build` → 689MB 이미지 빌드 성공.
- `docker run --rm agentstudio-sandbox:0.1 python -c "import pypdf, fitz, docx, openpyxl, pandas, numpy, matplotlib, PIL, chardet, magic, requests, bs4, lxml"` → 0 errors.
- `hwp5` (pyhwp 의 모듈명) 도 import OK — HWP 도 지원.
- SandboxManager 통합: thread sandbox 생성 → 업로드 → `python /workspace/test.py` → pypdf/pandas/hwp5 import 즉시 OK.

### 특이사항
- `python-magic` 은 libmagic1 시스템 패키지 필요 → apt 설치 추가.
- `pyhwp` 는 PyPI 에 정식 stable 이 없어 `--pre` 로 베타(0.1b15) 설치. 모듈명은 `hwp5`.
- 이미지 크기 689MB — pull 시간 vs 매 turn pip install 시간 트레이드오프, prebuilt 이 압도적 유리.
- 자동 빌드는 첫 thread 생성 시 trigger; 사용자가 `pnpm sandbox:build` 로 사전 빌드 권장(첫 채팅 지연 회피).

---

## 2026-05-18 14:55 — leedaeyoung — recursion limit 도달 시 사용자 확인 HITL 변환

### 목표
- LangGraph `recursion_limit` 도달이 사용자에게 raw 에러로 노출되던 문제를 HITL "더 진행할까요?" 카드로 변환.
- 사용자가 approve 면 limit 을 추가 부여(+40 step)하고 checkpoint 에서 graph 를 이어 실행. reject 면 turn 만 마감.

### 변경
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `GraphRecursionError` import + invoke/resume 양쪽 try 블록에서 catch → `recursion.limit_reached` 이벤트 yield
  - `resume_stream_with_deepagent` 에 `continue_after_recursion` / `requested_step_limit` 파라미터. True 면 `input_payload=None` 으로 checkpoint 에서 이어 실행 + bumped recursion_limit.
- `apps/agent-runner-py/src/modules/agents/agents_service.py` — `resume_stream` 파라미터 패스스루.
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `_pump_stream` 에 `recursion_limit_payload` 수집
  - send/resume_message 에서 감지 시 `_handle_recursion_continuation` 호출 → HITL interaction 생성 + emit_hitl_request
  - `finalize_recursion_cancel` 신규 — graph 재실행 없이 turn.completed 만 emit
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py` — `respond` 에서 `agent_context.recursionLimitReached` 감지: approve → `resume_message(continue_after_recursion=True)` / reject → `finalize_recursion_cancel`.
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
  - `ChatItem.hitl_request` 에 `recursionLimitReached/recursionPrompt/recursionNextStepLimit` 추가
  - WS hitl.request 핸들러에서 `interaction.agentContext.recursionLimitReached` 추출
  - `HitlApprovalCard` 에 amber 카드 분기 — "더 진행 / 그만" 단순 양자택일 UI

### 검증
- Runner lint 0 errors, reload 정상.
- Web lint 0 errors (사전 경고만), tsc 신규 에러 0.
- 모든 서비스 health 200.

### 특이사항
- 사용자가 reject 한 경우 graph 의 checkpoint state 는 그대로 유지됨 — 같은 thread 에서 다음 turn 에 새 사용자 메시지로 자연스럽게 이어 가능.
- `continue_after_recursion=True` 일 때 `decisions` 인자는 무시되며 `input_payload=None` 으로 LangGraph 가 checkpoint 에서 그래프를 자연 이어 진행.
- 기본 추가 limit: +40 step. 첨부 분석 한 번 더 깊게 들어갈 정도.

---

## 2026-05-18 15:30 — leedaeyoung — Client(ClientChatView)에 첨부 + recursion HITL 동일 적용

### 목표
- admin (`AgentFlowDebugPanel`) 에 들어간 첨부 업로드 / DnD / recursion HITL 카드 3종을 client (`ClientChatView`) 에 그대로 이식.
- 백엔드(runner/api) 는 source 무관 동일 코드라 0 줄 수정. 프론트엔드 단일 파일 작업.

### 변경 — `apps/agent-web/src/components/client/ClientChatView.tsx`
- `FeedItem.hitl_request` 타입에 `recursionLimitReached/recursionPrompt/recursionNextStepLimit` 추가.
- `buildHitlItem` + WS `hitl.request` 핸들러에서 `interaction.agentContext.recursionLimitReached` 추출.
- 첨부 state(`attachments/uploading/isDragOver`) + ref(`fileInputRef/dragCounterRef`).
- 헬퍼: `ensureThreadIdForUpload` (자동 생성 race 보호), `handleFilesSelected`, `handleRemoveAttachment`, DnD 4종 핸들러.
- `useEffect(threadId)` — thread 변경 시 attachments reload.
- 채팅 스크롤 컨테이너에 DnD 부착 — dashed border + "파일을 놓아 첨부" overlay.
- composer 영역: chip 리스트(이미지/파일 아이콘/X 제거) + 업로드 인디케이터 + 좌측 paperclip 버튼(편집 모드 외 노출).
- `ClientHitlCard` 시작부에 `if (item.recursionLimitReached)` 가드 — amber 톤 단순 카드 "더 진행 / 그만".

### 검증
- `tsc --noEmit` 신규 에러 0 (monaco 사전 이슈만 잔존).
- `pnpm --filter @agent-studio/web lint` 0 errors (사전 경고만).
- `.next` 캐시 클린 후 `/client/history` /agents/<id> 모두 200 응답, JSX 컴파일 정상.
- 모든 서비스 health 200.

### 특이사항
- Turbopack 의 `.next` 캐시가 대형 파일(3700+ LOC) 의 JSX 변경 후 stale 상태로 "Unexpected token" 에러를 보임 → 캐시 wipe + 재시작 으로 해소.
- 첨부 chip 은 `editingHitl` 모드에선 숨김 — 수정 폼과 시각적 충돌 회피.
- DnD overlay 는 `var(--client-accent)` 사용 — 테마 토큰 일관성 유지.
- ClientHitlCard 의 recursion 분기는 일반 HITL 의 `handleHitlDecide` 를 그대로 재사용 — 백엔드가 `agent_context.recursionLimitReached` 보고 분기하기 때문.

---

## 2026-05-20 — leedaeyoung — 서브에이전트 간 state 공유 (Option A: deepagents Command/StateBackend) + VFS 주석 정정

### 배경 / 근거
- "5월 입사 메일 찾아줘 → 다음 단계 진행해줘" 회귀: hr-document 서브에이전트(`f989001c-...`)의 follow-up turn 에서 메인이 전달한 description 에 `message_id` 가 없어 `gmail_fetch(message_id='mock')` 으로 실패.
- 근본 원인: deepagents `task` 도구로 호출되는 SubAgent 는 fresh context 로 시작 — 이전 검색 결과를 자동으로 못 봄. 공식 패턴은 **filesystem(StateBackend) 통해 state 공유** (docs: "Files a subagent writes will remain in the LangGraph agent state ... will continue to be available to the supervisor agent and other subagents").
- 추가 발견: `deepagent_bridge.py:1383` 의 `permissions=[]` 주석 "VFS 비활성화" 는 잘못된 설명. deepagents 0.5.2 `graph.py:453` 의 `if permissions:` 는 빈 리스트에 False → PermissionMiddleware 안 붙음 → **기본 permissive(전부 허용)**. 5/16 로그에서 `grep/ls/glob` 실제 호출됨이 증거.

### 목표 / 범위
1. `_make_thread_sandbox_backend()` 에 `/state/` → `StateBackend` 라우트 추가 (LangGraph checkpoint 영속화). 기존 default=DockerSandbox, `/skills/`=StoreBackend 는 유지.
2. `gmail_search` 도구 반환을 `Command(update={"messages":[...], "files":{...}})` 로 변경 — `/state/last_gmail_search.json` 에 무조건 영속화 (LLM 컴플라이언스 비의존).
3. hr-document 서브에이전트 시스템 프롬프트에 "follow-up 시 `/state/last_gmail_search.json` 먼저 read_file → message_id 회수 후 gmail_fetch" 룰 추가 (사용자가 UI에서 직접 수정).
4. 메인 에이전트 시스템 프롬프트에 "이전 결과 요약 시 message_id 등 식별자 누락 금지" 추가 (사용자가 UI에서 직접 수정).
5. `permissions=[]` 주석 정정.
6. 스모크 테스트: "5월 입사 메일" → "다음 단계 진행" 시나리오.

### 범위 외 (이번 사이클 미포함)
- STATE 탭 UI (state 파일 시각화) — 별도 사이클.
- gmail_fetch / gmail_send 결과의 동일 패턴 영속화 — 이번엔 gmail_search 만.
- 다른 서브에이전트(general-purpose 등) 프롬프트는 손대지 않음.

### 공식 문서 참고 (AGENTS.md §1-3 의무)
- `/websites/langchain_oss_python_deepagents` (Context7) — backends, permissions, subagents, customization, context-engineering 페이지.
- `/langchain-ai/deepagents` (Context7) — `examples/content-builder-agent`, `middleware/async_subagents.py` (Command(update=) 패턴).

---

## 2026-05-20 — leedaeyoung — MCP 카탈로그에 Slack (Bot Token) 항목 추가

### 배경 / 근거
- 사용자 요청: "slack mcp 연결해줘 → B 패키지로 진행, BOT 먼저".
- 패키지 선정 비교 (Context7 `/korotovsky/slack-mcp-server` 조회):
  - A. `@modelcontextprotocol/server-slack` — archived, env `SLACK_BOT_TOKEN/SLACK_TEAM_ID/SLACK_CHANNEL_IDS`.
  - **B. `slack-mcp-server` (korotovsky)** — 활발 유지보수, env `SLACK_MCP_XOX[PBCD]_TOKEN` 중 택1.
- 우리 시스템 제약 (`apps/agent-runner-py/src/modules/agents/agents_service.py:135-141`): 사용자 자격증명이 단일 secret 값이고 config.env 의 모든 키를 동일 값으로 override → envSchema 는 1키만 두어야 함.
- Bot Token 옵션: `SLACK_MCP_XOXB_TOKEN` 1개로 동작 (limited features — 봇 초대된 채널만, DM 불가).

### 변경 — `apps/agent-web/src/lib/mcp-catalog.ts`
- `MCP_CATALOG` 배열에 Slack 항목 1개 추가.
  - id=`slack`, transport=`stdio`, command=`npx`, args=`['-y','slack-mcp-server@latest','--transport','stdio']`.
  - envSchema=`SLACK_MCP_XOXB_TOKEN` (필수, 시크릿, placeholder `xoxb-...`).
  - note: Slack App → OAuth & Permissions → Bot User OAuth Token 발급 + 봇 초대 안내.

### 범위 외
- xoxp(User Token) 카탈로그 항목 — 이번엔 BOT 만. 추후 추가 가능.
- Backend(`mcp_servers` 테이블) 실제 등록은 admin UI 에서 사용자가 직접 수행.

### 공식 문서 참고
- Context7 `/korotovsky/slack-mcp-server` — npx stdio configuration, env vars reference.

---

## 2026-05-20 — leedaeyoung — apps/agent-web Slack 연동 UI 신설 (SlackInstallationCard, SlackChannelAgentList, /integrations/slack)

### 배경
- apps/api 에 Slack 모듈(`/api/slack/installations`, `/api/slack/channel-agents`)이 이미 구현 완료된 상태.
- admin 이 브라우저에서 Bot Token / App-Level Token 을 입력해 워크스페이스를 연결하고, 채널-에이전트 매핑을 관리하는 UI 필요.

### 변경 파일
- `apps/agent-web/src/lib/api-client.ts`
  - 타입 추가: `SlackRoutingMode`, `SlackInstallationView`, `SlackChannelAgent`
  - `ApiClient` 클래스 내부에 `slack` 그룹 추가 (installations + channelAgents CRUD)
- `apps/agent-web/src/components/slack/SlackInstallationCard.tsx` (신설)
  - 미연결 상태: "연결하기" 버튼 → 모달(Bot Token + App-Level Token 입력, show/hide 토글)
  - 연결 완료: 워크스페이스명·Team ID·활성 상태 표시, enable toggle, 삭제 버튼
- `apps/agent-web/src/components/slack/SlackChannelAgentList.tsx` (신설)
  - installation 없으면 안내 메시지만 표시
  - 테이블: 채널명/ID, 에이전트명, 모드 배지, 활성 toggle, 삭제 버튼
  - "매핑 추가" 버튼 → 모달(채널 ID, 채널 이름, 에이전트 select, 모드 선택)
- `apps/agent-web/src/app/(dashboard)/integrations/slack/page.tsx` (신설)
  - admin 전용 guard (비admin 접근 시 /dashboard 리다이렉트)
  - 동작 방식 안내 배너 + SlackInstallationCard + SlackChannelAgentList
- `apps/agent-web/src/components/layout/app-sidebar.tsx`
  - Admin 섹션에 "Slack" 링크(`/integrations/slack`) 추가, `Slack` lucide 아이콘 사용

### UI 위치
- URL: `/integrations/slack`
- 사이드바 Admin 섹션 (Users 항목 하단)

### 검증
- `cd apps/agent-web && pnpm lint` — 0 errors, 102 warnings (모두 기존 코드 warnings, 신규 파일 관련 없음)

### 특이사항
- `toolGroups.analyzeOpenApi` 등 기존 코드에 잔류하는 `any` 타입이 있으나 이번 작업 범위 외이므로 손대지 않음.

---

## 2026-05-21 — leedaeyoung — Slack/Email 전송 차단 회귀 — MCP env override 휴리스틱 도입

### 배경
dev 환경에서 회의록 정리 에이전트(`36a95128-a8bc-45e4-95e5-876acde827bb`)가 Slack 채널 (`C0B4LM85BD5`) 로 메시지 전송 시 "Slack 권한 정책 때문에 차단되었습니다" 라고 LLM 이 보고. local 에서는 동일 에이전트가 정상 전송됨. 같은 코드(merge 동일)인데 환경에 따라 다름.

### 원인 파악 (§1-5 파이프라인 trace)
1. step_traces.output 의 raw 결과:
   `conversations_add_message tool is not allowed for channel "C0B4LM85BD5", applied policy: xoxb-11173468445924-...`
2. `applied policy: xoxb-...` — slack-mcp-server 의 `SLACK_MCP_ADD_MESSAGE_TOOL` (채널 화이트리스트) 값이 **봇 토큰으로 덮어써짐**.
3. `agents_service.py:138-147` 의 env override 가 사용자 자격증명(`creds["mcp:{serverId}"]`) 있을 때 **config.env 의 모든 키 값을 일괄로 user_secret 으로 교체**하던 게 원인.
   - Slack 서버 config.env = `{SLACK_MCP_XOXB_TOKEN, SLACK_MCP_ADD_MESSAGE_TOOL}`
   - 둘 다 토큰값으로 덮어써짐 → MCP 서버가 `xoxb-...` 를 채널 화이트리스트로 해석 → 매칭 실패 → 전 채널 차단
4. local 동작/dev 미동작 차이: user_credentials 에 `mcp:6df09c68-...` 자격증명이 등록됐는지 여부. dev 에서만 등록돼 있어서 override 발동.

### 변경 파일
- `apps/agent-runner-py/src/modules/agents/agents_service.py`
  - `_load_mcp_tools()` 의 env override 로직 변경.
  - 일괄 덮어쓰기 → secret 명명 규칙(`_TOKEN`/`_API_KEY`/`_SECRET`/`_PASSWORD` 접미사) 매칭 키만 override.
  - secret 패턴 키가 하나도 없으면 기존 `env["API_KEY"] = user_secret` fallback 유지.

### 검증
- 모듈 import smoke test (`python -c "from src.modules.agents.agents_service import _load_mcp_tools"`) — 정상.
- 등록된 3 MCP 서버 호환성 확인:
  - Slack: `SLACK_MCP_XOXB_TOKEN`(_TOKEN 매칭 → override) + `SLACK_MCP_ADD_MESSAGE_TOOL`(매칭 안 됨 → "true" 보존) — 해결.
  - Tavily: `TAVILY_API_KEY`(_API_KEY 매칭 → override) — 기존 동작 유지.
  - Context7: env 비어있음 → fallback `API_KEY` 주입 — 기존 동작 유지.
- runner PM2 재시작 (`pm2 restart runner --update-env`) 후 startup 정상.

### 회귀 주의
- secret 명명 규칙이 4개 접미사(`_TOKEN`/`_API_KEY`/`_SECRET`/`_PASSWORD`) 로 한정됨. 새 MCP 서버 등록 시 비표준 secret 키 이름(예: `XOXB`, `BEARER` 등) 을 쓰면 override 가 안 되어 admin 등록값이 그대로 전달됨. 그 경우는 fallback `API_KEY` 도 안 맞아서 사용자 시크릿이 무시되는 형태로 드러남 — `restricted` 또는 "차단" 류 동작이 아닌 명확한 인증 실패로 나타나 진단 용이.
- 다른 사용자 환경에서도 동일 회귀가 잠재 — local 에서도 user_credentials 에 `mcp:{slack-server-id}` 가 추가되면 똑같이 깨졌을 것. 이 fix 가 그 잠재 회귀까지 해소.

---

## 2026-05-21 — leedaeyoung — 입사서류 발송 에이전트 승인 데드락 — 부모/자식 system_prompt 보강

### 배경
에이전트 `f989001c-c98d-456c-90e8-42f57f3d6adb` (입사서류 발송_서브에이전트) 의 디버그 창에서 사용자가 "승인" 을 반복 입력해도 gmail_send 가 발동하지 않는 데드락. 최근 5개 run 의 step_traces 에서 `gmail_send` 호출이 한 번도 안 찍힘 — LLM 이 호출 자체를 회피하고 "다시 승인해 주세요" 만 반복.

### 원인 (§1-5 파이프라인 trace)
1. 부모의 Follow-up 처리 규칙이 **이전 subagent 응답을 통째로 다음 turn 의 task description 에 박는다**. 거부/안내 응답까지 박혀 자가 강화 루프 발생.
2. 서브에이전트 hr-document 의 발송 승인 인식 규칙이 정확 phrase 매칭 (`"승인, 메일 발송 진행해줘"`) 으로 너무 엄격. 사용자 자연어 "승인" 단독으로는 매칭 안 됨.
3. 코드 레벨 HITL (`toolPermissions["gmail_send"] = "requires_approval"`) 은 정상 — gmail_send 호출 시점에 자동 인터럽트 발생. 그런데 LLM 이 호출 자체를 안 해서 발동 기회 없음.

### 변경 파일 (코드 변경 없음, DB 데이터 변경)
- `agents` 테이블 row `f989001c-c98d-456c-90e8-42f57f3d6adb`:
  - `system_prompt` (부모) — Follow-up 처리를 두 분기로 분리.
    - 분기 A (승인/발송/보내/전송/OK 키워드): description 첫 줄에 `[FINAL_APPROVAL]` 마커, 이전 거부 응답은 박지 않음.
    - 분기 B (진행해/응/계속/네): 기존 방식 유지 (이전 결과 전체 포함).
  - `graph_definition.nodes[0].data.systemPrompt` (hr-document) — 발송 승인 인식 규칙 단순화.
    - 키워드 화이트리스트 (승인/발송/보내/전송/OK/예) OR `[FINAL_APPROVAL]` 마커 → 즉시 gmail_send 호출.
    - "LLM 자가 검열 금지, 코드 HITL 신뢰" 명시.
    - "다시 승인해 주세요" 류 재요청 금지 명시.

### 검증
- 3 UPDATE 모두 성공, `updated_at` 갱신 확인.
- runner 의 `load_agent_with_deps` 가 매 호출마다 DB 재조회 — 캐시 없음 확인. 다음 turn 부터 즉시 반영.
- 사용자가 디버그 창에서 "승인" 입력 시:
  1. 부모가 분기 A 로 분류, description = `[FINAL_APPROVAL] 사용자 요청: 승인`.
  2. hr-document 가 마커 또는 키워드 인식, gmail_send 호출.
  3. 코드 HITL 인터럽트 → 사용자 승인 → 발송.

### 회귀 주의
- 분기 A 의 [FINAL_APPROVAL] 마커는 LLM 가 박는 신호이므로 LLM 이 키워드를 놓치면 마커 없음. 그래서 서브에이전트도 마커 없이 키워드만 있어도 발송하도록 OR 로직 적용.
- 키워드 화이트리스트 (승인/발송/보내/전송/OK/예) 가 너무 광범위해서 의도치 않은 발송 가능성 — 그러나 코드 HITL (`requires_approval`) 이 최종 가드 역할을 하므로 LLM 단 false-positive 는 사용자가 인터럽트 단계에서 거부 가능.
- 분기 B (진행해/응/계속/네) 는 발송 의도가 명확치 않아 기존처럼 이전 결과 통째로 전달 — 거기서 무한 루프 잠재가 약하게 남아있으나 분기 A 가 주 사용 경로라 영향 적음.

---

## 2026-05-21 — leedaeyoung — 입사서류 발송 에이전트 v2 — 초안 무결성 보장

### 배경
v1 prompt 적용 후 사용자가 "발송해줘" 입력 시 gmail_send 가 정상 호출돼 메일은 발송됐으나, 발송된 본문이 사용자가 검토·승인한 초안과 다름. step_traces 비교:
- 사용자가 본 초안 (c8b73c48, 07:34:57): 누락 1종(차량등록증), 보완 2종, 기한 "4월 30일(수)"
- 실제 발송 (4b7db9c2, 07:35:54): 누락 2종(회사양식 입사지원서, 반명함판 사진), 보완 8종, 기한 "5월 19일(월)"
- 수신·회사명·지원자명 같은 일부 식별자는 thread state inheritance 로 일부 일치, 본문은 완전히 재작성됨.

### 원인
v1 분기 A 가 description 에 "사용자 원본 메시지만" (예: `[FINAL_APPROVAL] 사용자 요청: 발송해줘`) 박도록 했음. hr-document 가 task 호출 시 초안 정보가 description 에 없어 `/state/last_gmail_search.json` + skills(`document-checklist`, `review-job-application-email`) 로 **새로 분석·재구성**해 발송. 사용자 수정사항·검토 결과 반영 안 됨.

### 변경 파일 (코드 변경 없음, DB 데이터)
- `agents.f989001c-...system_prompt` (부모) — 분기 A 형식 보강:
  - description 에 `[FINAL_APPROVAL]` 마커 + `=== 최종 초안 (그대로 발송할 것) === ... === 초안 끝 ===` 섹션 강제.
  - 사용자가 이전 turn 들에서 요청한 모든 수정사항이 반영된 최신 초안을 박도록 명시. placeholder 잔존본 금지.
  - 거부·재확인 응답은 박지 않음 (v1 유지).
- `agents.f989001c-...graph_definition.nodes[0].data.systemPrompt` (hr-document) — gmail_send 발송 규칙 재작성:
  - 마커 + 초안 섹션이 있으면 한 글자도 바꾸지 않고 그대로 gmail_send args 로 사용.
  - 절대 금지 목록: 본문 재구성, 누락/보완 목록 재분석, 인사말·제목·기한 자가 보강, placeholder 채우기.
  - 마커는 있는데 초안 섹션 없거나 불완전하면 사용자에게 안내, gmail_send 호출 금지.
  - 마커 없이 키워드만 있으면 추측 발송 금지, 초안 명시 요청.

### 검증
- 3 UPDATE 모두 성공, updated_at 07:42:22 갱신.
- `load_agent_with_deps` 캐시 없음(어제 확인) — 다음 turn 부터 즉시 반영.

### 사용자 검증 절차
1. 새 thread 에서 동일 시나리오 반복: 검색 → 검토 → "이메일/회사명 수정" → "승인".
2. step_traces 의 hr-document input.message 에 `[FINAL_APPROVAL]` 마커 + `=== 최종 초안` 섹션 전체가 박혀 들어가는지 확인.
3. gmail_send args 의 body 가 사용자가 본 초안과 동일한지 비교.

### 회귀 주의
- 부모 LLM 이 "가장 최근 사용자에게 노출된 초안" 을 정확히 thread messages 에서 추출해야 함 — placeholder 잔존본 또는 옛 버전 박을 위험. prompt 의 ❌ 금지 목록과 ✅ 예시가 가드.
- 분기 A 의 description 길이 증가 — 큰 초안 본문이 task input 으로 들어가 토큰 사용량 증가. 비용은 늘되 무결성 보장.
- v1 의 [FINAL_APPROVAL] 마커 OR 키워드 fallback 은 v2 에서 "마커 없이 키워드만 있으면 발송 거부" 로 강화됨 — 부모가 마커를 항상 박아야 정상 발송. 부모 prompt 가 마커를 누락하면 발송이 안 되는 새 회귀 가능성. 부모 prompt 의 분기 A 형식 명시·예시로 가드.
---

## 2026-05-21 — leedaeyoung — Slack/이메일 "보냈다는데 안 옴" 회귀 — 전송 도구 실패 표면화 (A)

### 배경 / 증상
- 개발서버 배포 후 client에서 메일, admin에서 슬랙 발송 시 에이전트는 "보냈다"고 답하지만 실제로 도착 안 함.
- 파이프라인 trace 결과: 도구 호출 자체는 일어나지만, 전송 도구가 실패를 예외로 던지지 않고 정상 결과처럼 생긴 문자열/`ok:false` 로 반환 → LLM 이 성공으로 오인 (AGENTS.md §1-5-3 변종).

### 원인 (코드 결함)
- `mcp_client_service.py:_wrap_mcp_tool._call` — 모든 예외를 `f"MCP tool error: ..."` 문자열로 삼킴. 또한 MCP `CallToolResult.isError=true`(예: slack-mcp-server 전송 비활성/거부) 를 무시하고 정상 출력으로 반환.
- `gmail_send.py` — 토큰 미발급/Gmail API 4xx 시 `{"ok": false, ...}` 문자열 반환. 호출자(LLM)가 `ok` 검사 안 함.
- tool 결과가 서버 로그(runner)에 안 남아 원인 추적 불가 (on_tool_start 만 로깅).

### 변경 파일 (예정)
- `apps/agent-runner-py/src/modules/mcp/mcp_client_service.py` — isError 검사 + 예외를 ToolException 으로 표면화, 결과 로깅
- `apps/agent-runner-py/src/modules/builtin_tools/custom_tools/gmail_send.py` — 실패 시 ToolException raise
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — on_tool_end 시 tool 출력 서버 로깅

### 범위 메모
- (A) 코드 결함만. 실제 전송이 되려면 환경 설정(B: SLACK_MCP_ADD_MESSAGE_TOOL / client Gmail OAuth / RUNNER_INTERNAL_KEY)은 dev 측에서 별도 확인 필요.

### 결과 (완료)
- `mcp_client_service.py` — `_call`: 연결/RPC 예외를 `ToolException` 으로 raise, MCP `result.isError=true` 시 내용과 함께 raise, 정상 결과는 debug 로깅. 모든 MCP 도구(슬랙 전송 포함) 실패가 status=error 로 LLM 에 전달됨.
- `gmail_send.py` — 토큰 미발급/4xx/일반 예외 모두 `ToolException` raise (기존 `ok:false` 문자열 반환 제거), 성공 시 debug 로깅.
- `deepagent_bridge.py` — `on_tool_end` 에 tool 출력 debug 로깅 추가(최대 1000자). 이제 runner 로그로 도구 결과 추적 가능.
- 검증: py_compile OK / `test_agents_service.py` 8 passed / `test_tool_wrapping.py` 10 passed. `test_deepagent_bridge.py` 의 10건 실패는 본 변경 이전 HEAD 에서도 동일하게 실패하는 기존 드리프트(create_deep_agent 미호출) — 본 변경과 무관.
- 남은 작업(B, dev 환경): SLACK_MCP_ADD_MESSAGE_TOOL / client Gmail OAuth / RUNNER_INTERNAL_KEY↔INTERNAL_SERVICE_KEY 일치 확인 필요.

---

## 2026-05-21 — leedaeyoung — client history 에서 sub-agent todo list 누락 회귀 수정

### 배경
client /client/history 페이지에서 thread (`be09296e-…`) 진입 시 todo list 가 비어 보임. 실행 중에는 스트리밍으로 잘 보이지만 새로 방문하면 사라짐.

### 원인 (체크포인트 직접 확인)
- thread 의 `checkpoint_blobs` 채널 분포:
  - 메인 ns (''): files=5, messages=25, **todos 없음**
  - sub-agent ns (`tools:2b18…`, `tools:324b…`, `tools:f536…` 등): todos 1~2개씩 보유
- 부모 에이전트는 `task` 도구만 호출. `write_todos` 는 sub-agent (hr-document) 가 자기 sub-graph 안에서 실행 → todos 채널이 sub-agent ns 에만 영속화.
- runner `get_messages_only(thread_id)` 가 `aget_tuple(thread_id)` 만 호출해 메인 ns messages 만 반환. frontend `extractPlansFromMessages` 는 메인 messages 의 `toolCalls[].name === 'write_todos'` 만 보므로 plan 복원 불가.
- 실행 중에는 deepagent_bridge 가 모든 ns 의 on_tool_start 이벤트를 emit 해서 stream 으로 잡힘. history 복원 경로엔 그 stream 이 없어 누락.

### 변경 파일
- `apps/agent-runner-py/src/modules/langgraph/langgraph_service.py`
  - `get_messages_only` 시그니처 변경: `list[dict]` → `{messages, subagentTodos}` dict.
  - checkpoints 테이블에서 `checkpoint_ns LIKE 'tools:%'` 인 ns 목록을 가져와 각 ns 별 `aget_tuple` 로 todos 채널 마지막 스냅샷 직렬화.
- `apps/agent-runner-py/src/modules/agents/agents_service.py`
  - `get_messages` 가 새 dict 응답을 받아 role 필터링 후 `{messages, subagentTodos}` pass-through.
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `get_messages` 시그니처를 dict 반환으로 변경, agent_id 없으면 `{messages: [], subagentTodos: []}`.
- `apps/agent-web/src/components/client/ClientChatView.tsx`
  - threads.messages 응답에서 `subagentTodos` 를 옵셔널로 추출.
  - messages 기반 plan 복원이 비었을 때 fallback 으로 subagentTodos 를 `subagent-{idx}` groupId 의 PlanEntry 로 변환해 plans 에 채움.
  - `applyHistorySweep` 그대로 작동 (정상 turn 의 in_progress → completed sweep).

### 검증
- 모듈 import smoke — 정상.
- `pnpm lint` (agent-web) — 0 errors.
- runner PM2 재시작 후 startup 정상.
- 사용자 검증 절차: client /client/history?threadId=be09296e-… 에서 todo list 가 노출되는지 확인.

### 회귀 주의
- NestJS API (`threads.service.ts`, `public-chat.service.ts`) 는 runner 응답을 pass-through 라 추가 변경 불필요.
- admin `chat-interface.tsx` 는 history 복원이 아닌 실시간 stream 으로 todo 받음 → 이번 회귀와 무관.
- 응답 schema 변경(list → dict) — 내부 frontend 만 호출 가정. 외부 third-party 사용 시 깨질 수 있음.
- 메인 ns 의 messages 에 `write_todos` toolCall 이 있는 케이스(단독 에이전트 + react)가 우선, 비었을 때만 fallback. 정상 케이스 회귀 없음.

### 후속 fix — SQL placeholder 충돌 (500 회귀)
배포 직후 사용자가 새로고침 했더니 todo 가 사라진다고 보고. runner-error 로그:
```
psycopg.ProgrammingError: only '%s', '%b', '%t' are allowed as placeholders, got "%'"
```
원인: `WHERE checkpoint_ns LIKE 'tools:%'` 의 `%` 를 psycopg3 가 placeholder 로 오인. `_adapt` 가 `$N`→`%s` 만 변환하고 raw `%` 는 escape 안 함.
fix: LIKE 패턴을 `$2` 파라미터로 전달 (`LIKE $2`, `(thread_id, "tools:%")`). 동일 파일.
검증: runner 재시작 후 `/api/v1/threads/.../messages` 200 OK 회복 (`pm2 logs runner` 확인).

### 별개 이슈 — 사이드바 row id↔title 미스매치 (미해결, 추가 조사 필요)
사용자 보고: history 사이드바 row 의 표시 title 은 "가족여행..." + agent "todo-agent" 인데 클릭 시 URL `?threadId=be09296e-…` (입사서류 thread) 로 가고 ChatView 도 입사서류 agent 표시.
DB 는 깨끗 (두 thread 가 별개 row 로 존재, title/agent 매칭 정상). frontend 의 `thread-upserted` 이벤트 dispatch 시 incoming.id 가 잘못된 thread id 였을 가능성이 가장 큼.
의심: 사용자가 `/client/agents/todo-agent/chat?threadId=be09296e-…` 같은 URL 로 진입해 agent=todo-agent + threadId=be09296e 인 ClientChatView 인스턴스에서 첫 메시지 발송 → dispatch(id=be09296e, title=가족여행, agent=todo-agent) → 사이드바 입사서류 row 가 가족여행 title 로 덮어씀.
이 fix 는 별도 PR 로 진행 예정 (이번 작업 범위 밖).

---

## 2026-05-21 — leedaeyoung — Gmail(admin) OAuth "연동 실패 bad request 인데 연동됨" 수정

### 증상
- admin 도구 화면에서 Google(Gmail) 연동 시 "연동 실패 / bad request" 표시되는데, admin 페이지에는 연동된 것처럼 나옴.

### 원인 (§1-5 파이프라인 trace)
1. `apps/agent-web/src/app/(dashboard)/oauth/google/callback/page.tsx` 의 OAuth 콜백 `useEffect` 에 **중복 실행 가드가 없음**. Next.js `reactStrictMode` 기본값 true(개발) → effect 2회 실행 → 동일 authorization code 로 `exchangeGmailCode` 2번 호출.
   - 1차: 성공 (refresh_token 저장 + gmail_connect enabled) → 실제 연동됨.
   - 2차: Google 이 code 재사용 거부 `invalid_grant` → API `BadRequestException` → 화면에 "bad request" 에러.
2. 표시 로직(`BuiltinToolsPanel.tsx`): gmail_connect 녹색 "연동 해제" 상태를 `tool.enabled` 만으로 그림. 토큰 미보유(`configured=false`) 여도 enabled 면 연동된 것처럼 보임 (설정 삭제/토큰 정리 후 stale 케이스).

### 변경 파일
- `oauth/google/callback/page.tsx` — useRef 가드로 코드 교환 1회만 실행.
- `BuiltinToolsPanel.tsx` — gmail_connect 연동 표시/버튼/토글 조건을 실제 토큰 보유(`configured`) 기준으로. 토큰 없으면 "연동"(미연동) 표시.

---

## 2026-05-24 — siziksu — Tools/MCP 기능 개선 (편집·도구 단위 노출·인증 모드)

### 목표
1. 등록된 MCP 서버 정보를 **수정 가능**하게 — Form/JSON 탭 동기화 UX
2. MCP 서버 단위로 **도구 노출 토글** + Agents 에서 도구 단위 선택 (Built-in Tools 와 동일 패턴)
3. MCP 서버 인증 모드 분리: `shared` (빌더 등록 = 공용) vs `per_user` (사용자 개별 인증)
4. `per_user` 모드에서 빌더가 사용자 입력 필드(라벨/secret/placeholder) 를 명시
5. Client chat 에서 자격증명 누락 시 빌더가 정의한 필드로 **인라인 인증 모달** → 저장 후 자동 재전송

### 계획 파일
- `/Users/siziksu/.claude/plans/tools-mcp-zany-nova.md`

### 영향 모듈
- DB: `McpServer.{credentialMode, requiredUserFields, exposedTools}`, `Agent.mcpToolRefs`
- API: mcp DTO/controller/service, me-mcp, agents 실행 게이트
- Runner: mcp_client_service (도구 필터/per_user env merge), agents_service
- Web: McpServerDialog (Form/JSON), McpServerDetail, ToolsTab, McpCredentialModal (chat)

#### DB 단계 완료 (2026-05-24, siziksu)
- `McpCredentialMode` enum 추가 (`shared` / `per_user`) — schema.prisma 228-231줄
- `McpServer` 모델에 3개 필드 추가: `credentialMode`, `requiredUserFields`, `exposedTools` — 532-536줄
- `Agent` 모델에 `mcpToolRefs Json?` 필드 추가 — 924줄
- 마이그레이션 생성·적용: `packages/database/prisma/migrations/20260524140656_add_mcp_tool_visibility_and_auth_mode/migration.sql`
- Prisma Client 재생성 완료 (v6.19.2)

### 통합 검증
- 모노레포 lint: **0 errors / 102 warnings** (전부 이번 작업과 무관한 기존 warnings)
- pnpm 빌드/runner pytest/E2E 는 사용자 수동 검증 예정

### 변경 요약
- DB: `McpServer.{credentialMode,requiredUserFields,exposedTools}` + `Agent.mcpToolRefs` + `McpCredentialMode` enum + 마이그레이션 1건
- API: MCP DTO/엔드포인트 보강, `PATCH /mcp/:id/tools/:toolName/visibility`, `?onlyExposed=true`, me-mcp 메타 응답, threads invoke 게이트(403 MissingMcpCredential) + runner 페이로드에 `userMcpEnvs`
- Runner: `to_structured_tools()` 도구 필터, `_load_mcp_tools()` per_user env merge, `userMcpEnvs` 라우터→서비스 전파
- Web: `McpServerDialog` (Form/JSON 탭), `McpServerCard` 도구별 노출 토글, `ToolsTab` 도구 단위 선택, `McpCredentialModal` chat 인라인 인증 + 자동 재전송, ApiClient 메서드 보강

### 후속 (사용자 검증)
1. `pnpm db:migrate dev` 적용
2. `pnpm dev` 후 시나리오:
   - shared 서버 등록 → Edit (Form↔JSON 동기화) → 도구 1개 expose=false → Agent 빌더에서 사라짐 확인
   - per_user 서버 등록 → `requiredUserFields=[{key:API_KEY,label:'API Key',secret:true}]`
   - Agent 생성 → 도구 단위 선택
   - 사용자 A 채팅 → 인라인 모달 등장 → 값 입력 → 자동 재전송 성공
   - 사용자 B 같은 시도 → 다시 모달 (사용자 격리)

---

## 2026-05-25 — siziksu — Agents 디버그 패널 채팅 탭 UI/UX 개선 및 실시간 마크다운 스트리밍 적용

### 목표
- Agents 디버그 패널 내의 채팅 탭 UI/UX를 세련되고 현대적인 모습으로 대폭 개선.
- 실시간 스트림 텍스트에 `marked`를 활용한 실시간 마크다운 렌더링을 적용하고, 스트리밍 중인 경우에는 blink cursor를 표출.
- 사용자 및 에이전트의 단순 텍스트 아바타('U', 'AI')를 현대적인 `User` 및 `Bot` / `Sparkles` 아이콘 아바타로 교체.
- 각 말풍선 하단에 은은한 톤으로 전송 시각(Timestamp)을 표출하여 가독성 강화.

### 계획 파일
- `/Users/siziksu/.gemini/antigravity/brain/8632e183-e25a-40dc-8ba8-a3c034b17624/implementation_plan.md`

### 영향 모듈
- Web: `AgentFlowDebugPanel.tsx`
- api-client.ts 중복 컴파일 오류 픽

### 변경 파일
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
- `apps/agent-web/src/lib/api-client.ts`

### 변경 요약
- **marked 라이브러리 연동**: 실시간 스트리밍 시 마크다운 파싱이 가능하도록 `.prose-invert-custom` 마크다운 다크 테마 클래스 연동.
- **스트리밍 blink cursor 도입**: AI 응답 타이핑 시 활성화되는 blue blinking cursor 추가.
- **아이콘 아바타 교체**: 기존 `'U'`와 `'AI'` 텍스트 대신 `User` 및 `Bot` 아이콘 아바타 도입.
- **말풍선 스타일링 및 전송 시각(Timestamp) 적용**: 둥글기 모서리 깎임 테두리 세련화, 하단에 로컬 타임 기준 전송 시각 노출.
- **TypeScript 중복 수출 버그 해결**: `api-client.ts` 내의 `ApiError` 중복 export 문 제거하여 타입 오류 완전 해소.

### 통합 검증
- TypeScript 타입 체크: `pnpm --filter @agent-studio/web exec tsc --noEmit` -> **0 errors** 성공 통과.
- ESLint 린트 검사: `pnpm --filter @agent-studio/web lint` -> **0 errors** 성공 통과. (기존 warnings 제외)


---

## 2026-05-24 — siziksu — Tools/MCP 기능 개선 (편집·도구 단위 노출·인증 모드)

### 목표
1. 등록된 MCP 서버 정보를 **수정 가능**하게 — Form/JSON 탭 동기화 UX
2. MCP 서버 단위로 **도구 노출 토글** + Agents 에서 도구 단위 선택 (Built-in Tools 와 동일 패턴)
3. MCP 서버 인증 모드 분리: `shared` (빌더 등록 = 공용) vs `per_user` (사용자 개별 인증)
4. `per_user` 모드에서 빌더가 사용자 입력 필드(라벨/secret/placeholder) 를 명시
5. Client chat 에서 자격증명 누락 시 빌더가 정의한 필드로 **인라인 인증 모달** → 저장 후 자동 재전송

### 계획 파일
- `/Users/siziksu/.claude/plans/tools-mcp-zany-nova.md`

### 영향 모듈
- DB: `McpServer.{credentialMode, requiredUserFields, exposedTools}`, `Agent.mcpToolRefs`
- API: mcp DTO/controller/service, me-mcp, agents 실행 게이트
- Runner: mcp_client_service (도구 필터/per_user env merge), agents_service
- Web: McpServerDialog (Form/JSON), McpServerDetail, ToolsTab, McpCredentialModal (chat)

#### DB 단계 완료 (2026-05-24, siziksu)
- `McpCredentialMode` enum 추가 (`shared` / `per_user`) — schema.prisma 228-231줄
- `McpServer` 모델에 3개 필드 추가: `credentialMode`, `requiredUserFields`, `exposedTools` — 532-536줄
- `Agent` 모델에 `mcpToolRefs Json?` 필드 추가 — 924줄
- 마이그레이션 생성·적용: `packages/database/prisma/migrations/20260524140656_add_mcp_tool_visibility_and_auth_mode/migration.sql`
- Prisma Client 재생성 완료 (v6.19.2)

### 통합 검증
- 모노레포 lint: **0 errors / 102 warnings** (전부 이번 작업과 무관한 기존 warnings)
- pnpm 빌드/runner pytest/E2E 는 사용자 수동 검증 예정

### 변경 요약
- DB: `McpServer.{credentialMode,requiredUserFields,exposedTools}` + `Agent.mcpToolRefs` + `McpCredentialMode` enum + 마이그레이션 1건
- API: MCP DTO/엔드포인트 보강, `PATCH /mcp/:id/tools/:toolName/visibility`, `?onlyExposed=true`, me-mcp 메타 응답, threads invoke 게이트(403 MissingMcpCredential) + runner 페이로드에 `userMcpEnvs`
- Runner: `to_structured_tools()` 도구 필터, `_load_mcp_tools()` per_user env merge, `userMcpEnvs` 라우터→서비스 전파
- Web: `McpServerDialog` (Form/JSON 탭), `McpServerCard` 도구별 노출 토글, `ToolsTab` 도구 단위 선택, `McpCredentialModal` chat 인라인 인증 + 자동 재전송, ApiClient 메서드 보강

### 후속 (사용자 검증)
1. `pnpm db:migrate dev` 적용
2. `pnpm dev` 후 시나리오:
   - shared 서버 등록 → Edit (Form↔JSON 동기화) → 도구 1개 expose=false → Agent 빌더에서 사라짐 확인
   - per_user 서버 등록 → `requiredUserFields=[{key:API_KEY,label:'API Key',secret:true}]`
   - Agent 생성 → 도구 단위 선택
   - 사용자 A 채팅 → 인라인 모달 등장 → 값 입력 → 자동 재전송 성공
   - 사용자 B 같은 시도 → 다시 모달 (사용자 격리)

---

## 2026-05-25 — siziksu — Agents 디버그 패널 채팅 탭 UI/UX 개선 및 실시간 마크다운 스트리밍 적용

### 목표
- Agents 디버그 패널 내의 채팅 탭 UI/UX를 세련되고 현대적인 모습으로 대폭 개선.
- 실시간 스트림 텍스트에 `marked`를 활용한 실시간 마크다운 렌더링을 적용하고, 스트리밍 중인 경우에는 blink cursor를 표출.
- 사용자 및 에이전트의 단순 텍스트 아바타('U', 'AI')를 현대적인 `User` 및 `Bot` / `Sparkles` 아이콘 아바타로 교체.
- 각 말풍선 하단에 은은한 톤으로 전송 시각(Timestamp)을 표출하여 가독성 강화.

### 계획 파일
- `/Users/siziksu/.gemini/antigravity/brain/8632e183-e25a-40dc-8ba8-a3c034b17624/implementation_plan.md`

### 영향 모듈
- Web: `AgentFlowDebugPanel.tsx`
- api-client.ts 중복 컴파일 오류 픽

### 변경 파일
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
- `apps/agent-web/src/lib/api-client.ts`

### 변경 요약
- **marked 라이브러리 연동**: 실시간 스트리밍 시 마크다운 파싱이 가능하도록 `.prose-invert-custom` 마크다운 다크 테마 클래스 연동.
- **스트리밍 blink cursor 도입**: AI 응답 타이핑 시 활성화되는 blue blinking cursor 추가.
- **아이콘 아바타 교체**: 기존 `'U'`와 `'AI'` 텍스트 대신 `User` 및 `Bot` 아이콘 아바타 도입.
- **말풍선 스타일링 및 전송 시각(Timestamp) 적용**: 둥글기 모서리 깎임 테두리 세련화, 하단에 로컬 타임 기준 전송 시각 노출.
- **TypeScript 중복 수출 버그 해결**: `api-client.ts` 내의 `ApiError` 중복 export 문 제거하여 타입 오류 완전 해소.

### 통합 검증
- TypeScript 타입 체크: `pnpm --filter @agent-studio/web exec tsc --noEmit` -> **0 errors** 성공 통과.
- ESLint 린트 검사: `pnpm --filter @agent-studio/web lint` -> **0 errors** 성공 통과. (기존 warnings 제외)


---

## 2026-05-27 — leedaeyoung — 서브에이전트 노드에서 MCP 도구 개별 선택이 저장 안 되던 회귀 수정

### 목표
- 웹검색 서브에이전트 등 flow 노드 편집 패널에서 MCP 도구를 개별 선택해도 저장되지 않고(서버 전체 켜기만 동작) 체크가 풀리던 문제 수정.

### 원인 (파이프라인 trace)
- `ToolsTab` 은 MCP 도구 단위 선택을 전부 `mcpToolRefs`(`Array<{serverId, toolName}>`) 로 처리하는데, 서브에이전트 노드 편집 경로에 `mcpToolRefs` 필드가 통째로 누락되어 있었음.
- `SubAgentNodeData` 타입에 `mcpServerIds` 만 있고 `mcpToolRefs` 없음 → `syntheticAgent.mcpToolRefs` 가 항상 `undefined`.
- `handleChange` 가 `mcpToolRefs` 를 노드 변경 목록에 매핑하지 않아 `ToolsTab` 이 보낸 `onChange({mcpToolRefs, mcpServerIds})` 중 `mcpToolRefs` 가 조용히 버려짐.
- 결과: 도구 1개만 체크하면 `syncServerIds` 가 "서버 전체 아님" 판정 → `mcpServerIds=[]` 만 저장 → 서버까지 해제되어 체크가 풀림. (메인 에이전트 경로는 진짜 agent/onChange 를 그대로 넘겨 정상 동작했음.)

### 변경 파일
- `apps/agent-web/src/components/agents/flow/SubAgentNode.tsx` — `SubAgentNodeData` 에 `mcpToolRefs` 필드 추가 + `normalizeSubAgentData` 기본값 추가
- `apps/agent-web/src/components/agents/flow/AgentFlowSettingsPanel.tsx` — `syntheticAgent` 매핑 + `handleChange` 매핑에 `mcpToolRefs` 추가

### 변경 요약
- 서브에이전트 노드 편집 경로에 `mcpToolRefs` 를 왕복 배선 → 개별 MCP 도구 선택이 정상 저장/복원됨.
- 저장(`persistAgent`)·Assistant 적용 경로는 모두 `...data` 전체 spread 로 graphDefinition 직렬화 → `mcpToolRefs` 자동 보존 확인. 로드는 `normalizeSubAgentData` 기본값으로 보존.

### 통합 검증
- 타입 체크: `pnpm --filter @agent-studio/web exec tsc --noEmit` → **0 errors**
- ESLint(변경 3파일): **0 errors / 0 warnings**

---

## 2026-05-27 — leedaeyoung — 서브에이전트 위임 기본동작 반전: 선택 라우팅 기본 + `[ALL_AGENTS]` 마커로 전체 순회

### 목표
- 기본동작을 "요청에 적합한 서브에이전트만 선택 호출"로 변경 (deepagents 네이티브 동작과 일치).
- systemPrompt 에 `[ALL_AGENTS]` 마커가 있을 때만 연결된 전체 서브에이전트를 BFS 순서로 순회 호출하도록 강제.
- 기존 마커 `[ROUTING_ONLY]`(의미: 라우팅 전용) 는 반전 후 의미가 어긋나므로 폐기.

### 공식 문서 확인 (AGENTS.md §1-3 deepagents 의무)
- Context7 `/websites/langchain_oss_python_deepagents` — subagents/deep-research 페이지 확인.
- 네이티브 동작: main 이 `task` 도구로 적합한 서브에이전트만 위임, 권장값 "start with 1 sub-agent". 기존 "전체 강제 순회"는 이 코드베이스의 커스텀 오버라이드였음 → 기본을 선택 라우팅으로 되돌리는 것이 네이티브와 정합.

### 변경 파일
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — 조건 반전 (`"[ROUTING_ONLY]" not in` → `"[ALL_AGENTS]" in`), 마커 없을 때 `elif` 로 선택 라우팅 지시 주입
- `apps/agent-web/src/components/client/ClientChatView.tsx` — 토큰 스트리밍 주석의 stale `[ROUTING_ONLY]` 참조 제거 (기능 변경 없음)

### 변경 요약
- 마커 없음(기본): `[서브에이전트 위임 규칙]` 주입 — 적합한 서브에이전트만 task 호출, 무관한 것은 호출 금지, 없으면 직접 처리.
- 마커 `[ALL_AGENTS]` 있음: 기존 BFS 계층/평면 전체 순회 강제 규칙 유지.

### 통합 검증
- `python3 -m py_compile deepagent_bridge.py` → OK
- 잔존 `ROUTING_ONLY` 참조 0건

### 비고
- `[ALL_AGENTS]` 리터럴은 기존 패턴과 동일하게 프롬프트에서 strip 하지 않음(무해). 필요 시 후속 정리 가능.

---

## 2026-05-27 — leedaeyoung — 서브에이전트 description 기반 라우팅 매칭 강화

### 목표
- sanitize_tool_name 이 한글 서브에이전트 이름을 거의 언더스코어로 변환(예: '웹 검색 서브에이전트' → `___________`)해 registered name 만으로는 main 이 task 라우팅 대상을 구분하기 어려움. description 을 구체화해 라우팅 정확도를 높인다.

### 공식 문서 확인 (AGENTS.md §1-3 deepagents 의무)
- Context7 `/websites/langchain_oss_python_deepagents` subagents/async-subagents/content-builder 확인.
- 베스트프랙티스: description 은 구체적·행동지향적이며 "Use when ..."(언제 쓰는지)을 명시해야 main 이 정확히 선택. ("helps with stuff" 같은 모호한 설명 금지)

### 변경 파일
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py`
  - `_classify_node_role`: 한글 키워드 보강 (researcher: 검색·탐색 / writer: 설명·요약 / coordinator: 조정·계획)
  - `_summarize_prompt_for_desc` 신설: 서브에이전트 systemPrompt 첫 1~2문장에서 목적 도출
  - `_ROLE_USE_WHEN` 신설: role 별 '사용 시점' 힌트
  - `_auto_describe_subagent`: `[원문 이름] {본문} 사용 시점: {role 힌트}` 형태로 재구성 — 명시 description 우선, 없으면 systemPrompt 도출, 최종 role 폴백. 원문(한글) 이름을 description 에 포함해 name 이 깨져도 의도 매칭 가능
  - 호출부: `spec.get("systemPrompt")` 를 인자로 전달

### 통합 검증
- `python3 -m py_compile deepagent_bridge.py` → OK
- 시뮬레이션: '웹 검색' → role=researcher, Writer → role=writer, 두 description 모두 한글 이름 + 목적 + 사용 시점 포함 확인

### 비고
- registered `name`(sanitize 결과)은 변경하지 않음 — 기존 task 호출/checkpoint 호환 유지. description 만 강화.
- uvicorn --reload 로 자동 반영.

---

## 2026-05-27 — leedaeyoung — 서브에이전트 존재 시 적극 위임하도록 기본 라우팅 프롬프트 강화

### 목표
- 선택 라우팅 기본동작에서 main 이 여전히 직접 답변하고 서브에이전트를 건너뛰는 경우가 있어, 서브에이전트가 있을 땐 적극적으로 위임하도록 강화. (단, [ALL_AGENTS] 전체 강제 순회와는 구분 — 무관한 에이전트까지 다 부르진 않음)

### 변경 파일
- `apps/agent-runner-py/src/modules/agents/deepagent_bridge.py` — 기본 `elif sub_agent_specs:` 주입 문구를 `[서브에이전트 활용 규칙 — 적극 위임]` 으로 교체
  - 요청과 조금이라도 관련된 서브에이전트가 있으면 직접 답하지 말고 반드시 task 위임
  - 전문 작업(검색·조사·작성·설명·요약·파일·외부 채널)은 담당 서브에이전트 위임 원칙
  - 다단계는 적합 에이전트들 순차 호출 후 취합 (무관한 것까지 전부 호출 불필요)
  - main 직접 최종 답변은 어떤 서브에이전트도 무관한 경우(인사·잡담)로 한정
  - 목록을 sanitize 이름 → 사람이 읽는 원문 이름(readable_names)으로 변경

### 통합 검증
- `python3 -m py_compile deepagent_bridge.py` → OK

---

## 2026-05-27 — leedaeyoung — Agent Assistant 가 toolPermissions(HITL) 까지 제안·적용하도록 확장

### 목표
- Agent Assistant 에서 "Slack 으로 보낼 때 HITL 로" 류 요청 시 프롬프트만 바뀌고 Tool Access Permissions(requires_approval)는 수정되지 않던 한계 해소.
- 기존 서브에이전트의 toolPermissions 편집 경로 신설 (현재는 메인 편집 + 새 서브 추가만 가능).
- 의도 기반 단위 판단: "슬랙 사용할 때 확인" → 해당 서브의 모든 Slack 도구, "슬랙 보낼 때 확인" → 전송류 도구만, 모호하면 clarify.

### 공식 문서 확인 (§1-3 의무)
- Context7 `/websites/langchain_oss_python_deepagents` HITL 페이지 — `interrupt_on={"tool": True}` 계약 확인. 기존 `_apply_tool_policies` 의 `requires_approval → interrupt_on[name]=True` 매핑과 일치하여 실행 엔진 변경 불필요.

### 변경 파일
- `apps/agent-runner-py/src/modules/agent_assistant/models.py` — AgentEditPatch 에 tool_permissions / sub_agent_permissions, SubAgentPermissionPatch 신설, edit_agent action 설명 갱신
- `apps/agent-runner-py/src/modules/agent_assistant/assistant_service.py` — `_load_project_tool_maps`(db tools + mcp_servers.tools 캐시), `_resolve_agent_tool_details`, `_load_target_subagents` 신설. `_load_target_agent_state` 에 mcp_tool_refs/tool_permissions 추가. stream 에서 main/sub 도구 상세 해석 후 프롬프트 주입. edit 분기에 정책값 화이트리스트 + target_name→nodeId 매핑 + edit_payload(toolPermissions/subAgentPermissions) 매핑
- `apps/agent-runner-py/src/modules/agent_assistant/assistant_prompt.py` — [도구 실행 정책 편집] 섹션(정책 4종, 의도별 적용 범위: 사용=전체/보내기=전송류만/모호=clarify), [현재 도구 상세] 블록 + `_format_tool_detail`, build_system_prompt 파라미터 2개 추가
- `apps/agent-web/src/stores/use-agent-assistant-store.ts` — AgentEditPatch 에 toolPermissions/subAgentPermissions, SubAgentPermissionPatch/AssistantToolPolicy 타입 추가
- `apps/agent-web/src/components/agents/assistant/EditApplyCard.tsx` — 정책/서브권한 라벨 + 한국어 포맷터(formatValue 시그니처에 key 추가)
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/page.tsx` — handleApplyEdit 에서 메인 toolPermissions merge(기존+신규) + 서브에이전트 노드 toolPermissions merge 후 graphDefinition 저장. stale closure 회피 위해 nextNodes 로컬 계산 후 setNodes/saveData 동시 사용 (deps 에 agent/nodes/edges 추가)

### 설계 메모
- 실행 엔진(`deepagent_bridge._apply_tool_policies`)은 변경 없음 — `requires_approval → interrupt_on[name]=True` 가 이미 deepagents HITL 계약과 일치. 어시스턴트가 toolPermissions 값을 채워주기만 하면 됨.
- MCP 도구명은 라이브 연결 없이 `mcp_servers.tools` JSON 캐시에서 해결 (성능·안정성).
- toolPermissions 는 항상 기존 값과 merge — 어시스턴트는 바꿀 도구만 전송.
- 신규 sub-agent(add_sub_agents)에는 권한 미적용 — 새 서버는 도구명이 서버 단위로만 노출돼 LLM 추측 위험. 기존 sub-agent/메인만 정밀 편집.

### 검증
- `python3 -m py_compile` (3개 .py) → OK
- build_system_prompt 포맷 스모크: 서브에이전트 도구명/requires_approval/이름 주입 확인 → OK
- `ruff check` (service/prompt) → All checks passed. models.py 는 기존 camelCase DTO(N815) 7건만(본 변경과 무관, snake_case 사용)
- `pnpm lint` (web) → 0 errors (기존 warning 123건, 변경 파일엔 없음)
- `tsc --noEmit` (web) → 에러 없음

### 후속 수정 (실사용 검증 중 발견)
1. **OpenAI strict structured output 호환** — `tool_permissions: dict[str,str]` 가 strict 모드(자유 dict 불가)에서 400 에러(`Invalid schema ... Extra required key 'tool_permissions'`). `ToolPolicyEntry`(tool_name+policy Literal) 신설 후 `tool_permissions` 를 **항목 배열**로 변경. 서비스에서 배열→dict 변환해 emit 페이로드(camelCase dict)·프런트는 그대로. 프롬프트 예시도 배열형으로 갱신.
2. **graph_definition 노드 type 필터 버그** — `persistAgent` 가 노드를 `{id,position,data}` 로만 저장(type 없음, 프런트가 로드 시 'subAgent' 부여). `_load_target_subagents` 가 `type=='subAgent'` 로 필터링해 **모든 sub-agent 가 스냅샷에서 누락** → LLM 이 Slack 도구를 못 봐 프롬프트만 수정. main 만 제외하도록 필터 변경. 실 DB(시연 워크플로우 에이전트)로 Slack 14개 도구 정상 해석 확인.

---

## 2026-05-27 — Agent Assistant 서브에이전트 description 누락 수정 (작성자: leedaeyoung)

### 목표
Agent Assistant 로 서브에이전트를 생성할 때 노드 `data.description` 이 비어 있어, 메인 에이전트가 description 기반으로 위임 라우팅을 못 하던 문제 해결.

### 원인
- 서브에이전트 라우팅 정식 필드는 노드 `data.description` (프런트 `SubAgentNode` `description?: string`, 런타임 `_auto_describe_subagent` 가 `spec["description"]` 을 읽어 30자 초과면 그대로 description 본문으로 사용).
- 그러나 `SubAgentDesign` 모델에 `description` 필드 자체가 없었고, create_agent / add_sub_agents 노드 생성 시 `data` 에 `description` 키를 넣지 않음 → 항상 빈 값 → 런타임이 systemPrompt 유도 폴백에만 의존, UI 에도 빈칸.

### 문서 확인
- deepagents 서브에이전트 라우팅 권장 패턴(description 에 'when to use' 부착)은 기존 `_auto_describe_subagent` / `_ROLE_USE_WHEN` 구현에 이미 반영돼 있어 동작 변경 없음. 본 수정은 명시 description 주입만 추가.

### 변경 파일
- `models.py` — `SubAgentDesign` 에 `description: str = Field(default="")` 추가 (라우팅 근거 작성 가이드 포함).
- `assistant_service.py` — create_agent / add_sub_agents 양쪽 서브에이전트 노드 `data` 에 `"description": sa.description or ""` 추가.
- `assistant_prompt.py` — SubAgentDesign 가이드에 description 필수 작성 규칙·예시 추가.

### 검증
- `uv run python` 으로 `SubAgentDesign` description 필드·`AssistantResponse` strict 스키마 빌드 정상 확인.

---

## 2026-05-27 — Agent Assistant 신규 생성(create_agent) 경로 HITL/도구권한 누락 수정 (작성자: leedaeyoung)

### 목표
Agent Assistant 로 **새로 생성**한 에이전트의 서브에이전트(예: Slack 전송기) 도구가 모두 Auto 로만 저장되고, "보낼 때 승인받게" 요청해도 requires_approval(HITL) 이 안 걸리던 문제 해결.

### 원인 (§1-5 파이프라인 trace)
- 실 DB(`1b2c4826…`) 조회 결과 모든 서브에이전트 노드 `data.toolPermissions = None`. description 은 정상 채워짐.
- 기존 toolPermissions/HITL 기능은 **edit_agent**(기존 에이전트 수정) 경로에만 존재. **create_agent**(신규 생성)의 `MainAgentDesign`/`SubAgentDesign` 에는 `tool_permissions` 필드 자체가 없었고, 노드 빌더도 `data.toolPermissions` 를 세팅하지 않음.
- 추가로 create_agent 카탈로그(`_fetch_mcp_servers`)는 MCP **서버 단위** 정보만 노출 → LLM 이 `conversations_add_message` 같은 개별 도구 이름을 몰라 정책 키를 지정할 수 없었음.

### 문서 확인
- deepagents HITL 매핑(`requires_approval → interrupt_on[name]=True`)은 `_apply_tool_policies`(deepagent_bridge) 에 이미 구현돼 있어 런타임 변경 불필요. 본 수정은 생성 시 `data.toolPermissions` 주입과 MCP 도구명 노출만 추가.

### 변경 파일 (백엔드 전용 — 프런트는 apply 시 node.data 를 그대로 직렬화하므로 변경 불필요)
- `models.py` — `ToolPolicyEntry` 를 `MainAgentDesign` 앞으로 이동, `MainAgentDesign`·`SubAgentDesign` 에 `tool_permissions: list[ToolPolicyEntry]` 추가.
- `assistant_tools.py` — `_fetch_mcp_servers` 가 `tools`/`exposed_tools` 캐시를 읽어 `toolNames` 포함 (exposed_tools 있으면 그 부분집합만).
- `assistant_prompt.py` — `Any` import, MCP 카탈로그 섹션에 서버별 '도구:' 줄로 도구명 노출, SubAgentDesign 가이드에 `tool_permissions`/HITL·사용/보내기 granularity 규칙 추가.
- `assistant_service.py` — create_agent main_node·sub_node, add_sub_agents sub_node 에서 `tool_permissions` 항목 배열→dict 변환 후 비어있지 않으면 `data.toolPermissions` 세팅.

### 검증
- `uv run python` 으로 `MainAgentDesign`/`SubAgentDesign.tool_permissions`·`AssistantResponse` strict 스키마(자유 dict 0건) 정상 확인.
- `uvx ruff check` (4개 파일) → 신규 위반 0건. 기존 N815 camelCase DTO 7건(models.py:10–27)만, 본 변경과 무관.

---

## 2026-05-28 — Agent 상세 디버그 패널 UI/UX 개선 (작성자: siziksu)

### 목표
- 도구 이름 강제 truncate 해제, 걸린 시간(ms) 카드 우측 끝 정렬
- `XxxMiddleware.xxx` 류 도구 이름 가독성 개선 + 토글 필터 추가
- 진행 중에 사용자가 펼친 도구·그룹 카드가 새 완료로 인해 자동으로 닫히지 않도록 펼침 상태를 패널 레벨로 끌어올림
- 우측 패널(설정/디버그) 폭을 하나의 상태/localStorage 키로 통합해 토글 시 폭 점프 제거

### 변경 파일 (예정)
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx`
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/page.tsx`

---

## 2026-05-28 — 에이전트 목록 카드 UI 개선 (작성자: siziksu)

### 목표
- `/agents` 목록을 행(row) → 카드(card) 그리드 레이아웃으로 변경.
- 정보 정확도 개선: 도구 개수를 `toolIds`만이 아닌 `toolIds + toolGroupIds + builtinToolIds + mcpToolRefs` 합으로 표기. 스킬·서브에이전트 개수도 함께 노출.
- 메인만 있는 에이전트와 서브에이전트를 가진 에이전트(Orchestrator)를 시각적으로 구분.
- 카드에 모델·도구·스킬·서브에이전트 등 구성 요약 표시.
- "ReAct" 등 architecture 라벨 제거.

### 변경 파일
- `apps/agent-web/src/app/(dashboard)/agents/page.tsx`

---

## 2026-05-28 — 에이전트 Chat 페이지를 디버그 패널 채팅으로 통일 (작성자: siziksu)

### 목표
- `/agents` 카드의 Chat 버튼 → `/agents/[agentId]/chat` 경로 채팅 화면을 에이전트 상세 페이지의 디버그 패널(`AgentFlowDebugPanel`) 채팅과 동일하게 맞춤.
- 기존 단순 `ChatInterface` 를 `AgentFlowDebugPanel` 로 교체해 HITL Approve/Reject/Edit · 파일 첨부 · 메트릭 · 경고 · Raw · 도구 그룹화 등 디버그 패널의 풍부한 기능을 동일하게 제공.

### 변경 파일
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/chat/page.tsx` — 본문 재작성, `AgentFlowDebugPanel` 사용. 사전 `threads.create` 호출 제거 (패널 내부 lazy 생성). `onClose` 는 `/agents` 목록으로 라우터 push.

### 비고
- `apps/agent-web/src/components/chat/chat-interface.tsx` 는 미사용 가능성 있으나 본 작업 범위 외(추후 정리 별도 협의).

---

## 2026-05-28 — Chat 페이지에서 디버그 패널 전체 폭 채우기 (작성자: siziksu)

### 목표
- `/agents/[agentId]/chat` 에서 `AgentFlowDebugPanel` 이 부모 영역을 가득 채우게 해, 첨부 업로드/갤러리·드래그앤드롭·메트릭 등 모든 기능을 디버그 패널과 동일하게 사용 가능하도록 함.
- 기존 460px 고정 폭은 좁아서 첨부 영역·우측 카드가 잘려 "기능이 없는 것처럼" 보였음.

### 변경 파일
- `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx` — `fullWidth?: boolean` prop 추가. true 일 때 `w-full flex-1` (좌측 border 제거), 기본은 460px 고정 컬럼 유지(에이전트 상세 페이지 회귀 없음).
- `apps/agent-web/src/app/(dashboard)/agents/[agentId]/chat/page.tsx` — 패널에 `fullWidth` 전달.

---

## 2026-06-03 — UI/UX 전면 리디자인 (작성자: siziksu)

### 목표
- 사용자 피드백: 너무 어둡고, 경계선이 잘 보이지 않으며, 글씨가 작고, 클릭 영역이 어정쩡함.
- globals.css 시맨틱 디자인 토큰(다크/라이트) 재정의 + 라이트/다크 테마 토글 도입.
- 본문 폰트 13→14, 라벨 10/11→12, 카드/배경 명도 단계화, 보더 대비 강화, 클릭 영역 표준화(아이콘 32→36, 모바일 44).
- 산발 hex 하드코딩을 토큰 유틸리티(bg-surface, border-strong, text-fg-muted 등)로 일괄 치환.

### 작업 범위
- `apps/agent-web/src/app/globals.css` — 토큰 전면 재정의
- `apps/agent-web/src/app/layout.tsx` — 테마 부트스트랩 스크립트
- 레이아웃 (dashboard layout, app-sidebar, topbar) + 테마 토글 컴포넌트 신규
- dashboard/* 카드 6개, agents/builder/* 14개, chat-interface, workflow-canvas/nodes/* 12개
- client(라이트) 영역 변수 통합

---

## 2026-06-03 — Agent 목록 & 프롬프트 편집 UX 개선 (siziksu)

### 목표
1. 에이전트 목록 멀티에이전트 뱃지 검출 강화
2. 프롬프트 편집창: 스페이스 입력 복구 + 줄 번호 제거
3. 프롬프트 편집창: 확장(고정/큰화면/전체화면) + 마크다운 미리보기
4. 에이전트 목록 Chat 버튼 → 클라이언트 페이지(`/client/agents/<slug>`) 새 탭

### 변경 파일
- apps/agent-web/src/app/(dashboard)/agents/page.tsx — countSubAgents() 에 planningConfig.subAgents.agentIds 합산, onChat 핸들러를 window.open 새 탭 + slug 누락 toast 처리로 변경
- apps/agent-web/src/components/agents/builder/PromptTab.tsx — Monaco lineNumbers/glyphMargin/folding 제거, viewMode(edit|preview|split) + sizeMode(fixed|tall|full) 토글 추가, marked 기반 미리보기, Escape 로 전체화면 해제
- apps/agent-web/src/components/agents/flow/FlowCanvas.tsx — panActivationKeyCode={null} 추가 (ReactFlow 의 Space 키 캡처 차단으로 드로어 내 Monaco 스페이스 입력 보장)

### 결과
- pnpm lint: 0 errors, 145 warnings (베이스라인 유지)
- 스페이스 입력 복구 가설: ReactFlow 의 panActivationKeyCode 기본값 Space + Monaco focus bleed. 옵션 명시로 차단.

## 2026-06-06 — Agent Assistant 입력창 디버그 패널 채팅 스타일로 통일 (siziksu)

### 목표
빌더 에이전트 상세 화면의 Agent Assistant 입력창을 디버그 패널 채팅 입력창과 동일한 UX/스타일로 개선

### 변경 파일
- apps/agent-web/src/components/agents/assistant/AgentAssistantPanel.tsx
  - textareaRef + 자동 높이 조절 effect 추가 (maxHeight 160px)
  - 입력창 컨테이너를 rounded-2xl + focus-within 강조 박스로 교체
  - textarea 를 transparent/borderless + custom-scrollbar 로 변경, rows=1 자동 grow
  - IME composition 가드(e.nativeEvent.isComposing) 추가
  - 하단 상태 표시줄(stepLabel/'생성 중…') + 원형 전송 버튼(#3B82F6) 으로 통일
  - placeholder 단축, Shift+Enter 안내 포함

### 결과
- pnpm lint: 0 errors (베이스라인 유지)

## 2026-06-06 — Skill Assistant 인라인 선택지(Quick Choices) 패널 (siziksu)

### 목표
어시스턴트가 "1) X 할까요, 2) Y 할까요?" 식 선택지를 평문에 포함할 때 같은 자리에
버튼/자연어 입력/닫기 토글 패널을 렌더해 사용자가 다시 타이핑하지 않도록 개선.

### 진행
- AssistantResponse 에 choices 필드 추가 (orthogonal, 모든 action 가능)
- emit_choices_offered WS 헬퍼 + assistant.choices_offered 이벤트 신설
- 프론트 store 에 AssistantChoicesAttachment + collapsed/consumed 상태
- SkillChoicesPanel 컴포넌트 (collapsed=칩, expanded=버튼리스트+입력+닫기)

### 변경 파일
- apps/agent-runner-py/src/modules/skill_assistant/models.py — ChoiceItem 모델 + AssistantResponse.choices 필드
- apps/agent-runner-py/src/modules/skill_assistant/assistant_prompt.py — 선택지 작성 가이드 블록
- apps/agent-runner-py/src/modules/skill_assistant/assistant_gateway.py — emit_choices_offered 헬퍼
- apps/agent-runner-py/src/modules/skill_assistant/assistant_service.py — _maybe_emit_choices() 헬퍼 + 5개 action 분기 success path 에서 호출
- apps/agent-web/src/lib/ws-client.ts — assistant.choices_offered 화이트리스트 등록
- apps/agent-web/src/stores/use-skill-assistant-store.ts — AssistantChoice/AssistantChoicesAttachment 타입 + attachChoicesToLastAssistant/toggleChoicesCollapsed/setChoicesConsumed 액션
- apps/agent-web/src/components/skills/assistant/SkillChoicesPanel.tsx — 신규 인라인 패널(collapsed=칩, expanded=버튼리스트+입력+닫기)
- apps/agent-web/src/components/skills/assistant/SkillAssistantPanel.tsx — WS 핸들러 + 메시지 bubble 하단 마운트

### 결과
- pnpm --filter @agent-studio/web lint: 0 errors, 202 warnings (베이스라인 유지)
- python import 검증: ok

## 2026-06-06 — Agent Builder 로 Skill Assistant-급 어시스턴트 만들기 1차 (siziksu)

### 목표
Agent.outputSchema + 도구 등록 + Generic 동적 카드 인프라로 Skill Assistant 의 단일 action 정도를 Agent Builder UI 만으로 재현. 3개 Phase(1주씩).

### Phase 1 — Skills CRUD 도구 등록 (진행)

#### Phase 1 변경 파일
- apps/agent-runner-py/src/modules/builtin_tools/custom_tools/skill_ops.py (신규) — 8개 도구 빌더 + Pydantic 인자 스키마. DB 직접 접근(skill_assistant 와 동일 패턴)으로 user_id 권한 격리.
- apps/agent-runner-py/src/modules/builtin_tools/custom_tools/registry.py — `build_skill_ops_tools(user_id)` 등록.
- apps/api/src/modules/tools/builtin-catalog.ts — skills 그룹(8 도구) 추가로 UI 에서 선택 가능.

#### Phase 1 결과
- Python import: 18개 도구 등록 확인(기존 10 + 신규 8).
- pnpm lint: 0 errors, 3 warnings (베이스라인 유지).

### Phase 2 — outputSchema 카드 매핑 (진행)

#### Phase 2 변경 파일
- apps/agent-runner-py/src/modules/dynamic_cards/__init__.py (신규)
- apps/agent-runner-py/src/modules/dynamic_cards/gateway.py (신규) — emit_dynamic_card WS 헬퍼. agent.dynamic_card 이벤트로 data + presentation 분리 페이로드 발행.
- apps/agent-runner-py/src/modules/dynamic_cards/dispatch.py (신규) — outputSchema.x-agent-studio.cardMappings 해석. JSONPath subset 평가.
- apps/agent-runner-py/src/modules/agents/deepagent_bridge.py — validate_schema 통과 후 maybe_emit_dynamic_card 호출 (2곳).
- apps/agent-web/src/lib/ws-client.ts — agent.dynamic_card 화이트리스트.
- apps/agent-web/src/components/agents/builder/OutputSchemaTab.tsx — '카드 매핑 (advanced)' 템플릿 추가.

#### Phase 2 결과
- Python dispatch 단위 검증: JSON 아닌 응답/매핑 없음/action 미일치 모두 False 반환 OK.
- import 검증: dynamic_cards 모듈 + deepagent_bridge 로드 OK.

### Phase 3 — Generic Card 렌더러 + 카드 정의 API (진행)

#### Phase 3 변경 파일
- packages/database/prisma/schema.prisma — CardDefinition 모델 추가 (cardId + version unique, level/schema/layout/actions JSON 필드).
- DB: 직접 SQL 로 card_definitions 테이블 생성(다른 drift 가 있어 prisma migrate dev 회피). skill-proposal-card v1 시드.
- apps/api/src/modules/cards/ (cards.module.ts / cards.service.ts / cards.controller.ts 신규) — GET 목록/최신/특정버전, POST 생성.
- apps/api/src/app.module.ts — CardsModule 등록.
- apps/agent-web/src/lib/api-client.ts — CardDefinition / CardAction / CardHandler 타입 + apiClient.cards = { list, get, getLatest, create } (클래스 내부 skills 뒤).
- apps/agent-web/src/stores/use-card-definitions-store.ts (신규) — Zustand + localStorage immutable 캐시. inflight dedup.
- apps/agent-web/src/components/dynamic-cards/template.ts (신규) — {{data.x}} / {{input}} 평가기. eval/JSON 화이트리스트만.
- apps/agent-web/src/components/dynamic-cards/DynamicCardRenderer.tsx (신규) — 카드 정의 fetch + 렌더 진입점 + 로딩/오류 fallback.
- apps/agent-web/src/components/dynamic-cards/CardLayout.tsx (신규) — header/body 블록(text/title/tags/key_value_list/json) 렌더.
- apps/agent-web/src/components/dynamic-cards/ActionRow.tsx (신규) — actions 버튼 + input row (textarea/IME 가드).
- apps/agent-web/src/components/dynamic-cards/handlers.ts (신규) — dismiss/send_message/tool_call/navigate/external_link. tool_call 화이트리스트 skills.create/update/delete/addFile.
- apps/agent-web/src/stores/use-skill-assistant-store.ts — AssistantMessageCard 에 'dynamic' kind + pushDynamicCard 액션.
- apps/agent-web/src/components/skills/assistant/SkillAssistantPanel.tsx — agent.dynamic_card WS 핸들러 + 메시지 분기에 DynamicCardRenderer 렌더.

#### Phase 3 결과
- pnpm --filter @agent-studio/api lint: 0 errors, 3 warnings (baseline).
- pnpm --filter @agent-studio/web lint: 0 errors, 205 warnings (baseline 202 에서 +3: dynamic-cards 의 hoisted function 사용 warning 2 + 1).
- Python import 검증: tools 18(skill_ops 8) + dynamic_cards + deepagent_bridge OK.
- NestJS build: 통과.
- DB 시드: card_definitions 1행(skill-proposal-card v1) 확인.

### 1차 종합 결과
- Phase 1·2·3 모두 완료. 회귀 없음 (기존 Skill Assistant / Agent Assistant 코드 미변경).
- 검증 가능 E2E: 사용자가 Agent Builder 에서 outputSchema 의 '카드 매핑 (advanced)' 템플릿 사용 + builtinToolIds 에 skill.create 추가 → 어시스턴트로 "PDF 스킬 만들어줘" 입력 → LLM 응답이 outputSchema 통과 시 agent.dynamic_card 발행 → 프론트 DynamicCardRenderer 가 시드 카드로 렌더 → [생성] 버튼 → apiClient.skills.create 호출 → 실제 skill 생성.
- 2차 작업(Phase 4~6): Composable Card 슬롯, Card Builder UI, HITL 폼.

---

## 2026-06-06 — 2차 Phase 4 (Composable Card Slots) 시작 — siziksu

### 목표
1차 dynamic-cards 인프라에 composable 블록 10종 추가 + ActionRow input.type 확장 + 시드 3카드(skill-edit / skill-analysis / quick-choices) 등록. 기존 SkillProposal/Edit/Analysis/Choices 4종 카드 코드는 미변경(additive).

### Phase 4 변경 파일
- apps/agent-web/src/components/dynamic-cards/blocks/ (신규 디렉토리)
  - types.ts — LayoutBlock/BlockCtx 공통 타입
  - TextBlock / TitleBlock / TagsBlock / KeyValueListBlock / JsonBlock — 1차 5종 블록을 분리(BlockRenderer 리팩터 대응)
  - LineDiffBlock — computeLineDiff 기반 라인 diff (added/removed/unchanged 색상)
  - SetDiffBlock — 배열 set diff chip (도구·파일 add/remove/kept)
  - ScoreGaugeBlock — 0~100 게이지 바 + good/warn 임계
  - SeverityBarBlock — high/medium/low 색 라벨
  - FitBadgeBlock — good/unclear/missing 배지
  - TabsBlock — 탭 컨테이너 (nested body)
  - AccordionBlock — 펼침 리스트 (defaultOpen 지원)
  - ChoicesBlock — 칩 선택지 + 자유 입력 토글 (SkillChoicesPanel 패턴)
  - SuggestionListBlock — severity별 색상 + per-item action
  - ProgressBlock — 단순 진행 상태바
  - render.tsx — BLOCK_REGISTRY (type 키 lookup) + renderBlock(block, ctx) 진입함수, KNOWN_BLOCK_TYPES export
- apps/agent-web/src/components/dynamic-cards/CardLayout.tsx — BlockRenderer 제거 후 renderBlock() lookup 호출로 리팩터. 기존 5종 블록은 blocks/ 로 이동, 동작 동일.
- apps/agent-web/src/components/dynamic-cards/ActionRow.tsx — input.type 분기 확장(textarea/number/select/multi_select). ActionInput 헬퍼 + canSubmit 검증 + 취소 버튼.
- apps/agent-web/src/components/dynamic-cards/handlers.ts — TOOL_REGISTRY 에 skills.deleteFile / cards.create 추가. hitl_respond handler 추가 (wsClient.respondToInteraction + useHitlStore.removeInteraction).
- apps/agent-web/src/lib/api-client.ts — CardActionInput union 타입 신설(text/textarea/number/select/multi_select). CardHandler 에 hitl_respond variant 추가.
- DB: docker exec psql 로 card_definitions 에 3건 추가 (skill-edit-card / skill-analysis-card / quick-choices-card v1, level=composable).

### Phase 4 결과
- pnpm --filter @agent-studio/web lint: 0 errors, 205 warnings (baseline 동일).
- DB 시드: 4 카드(skill-proposal/skill-edit/skill-analysis/quick-choices) 모두 등록 확인.
- 회귀 안전: 기존 5종 블록 동작 유지. 기존 SkillAssistant 4종 카드 코드 미변경 (additive).

### Phase 5 — Card Builder UI (진행)

#### Phase 5 변경 파일
- apps/agent-web/src/app/(dashboard)/cards/page.tsx (신규) — 카드 정의 그룹별 리스트(cardId 별 latest + 버전 수), level 색 배지, 검색 필터, 편집/미리보기 라우트 진입.
- apps/agent-web/src/app/(dashboard)/cards/builder/[cardId]/page.tsx (신규) — 3분할 편집기: 좌(schema Monaco), 중(layout + actions Monaco), 우(라이브 미리보기 + 더미 data). 'new' 분기 시 신규 템플릿. 발행 시 버전 자동 +1. 검증: JSON 파싱 / 알 수 없는 블록 타입 / 알 수 없는 handler.type 차단.
- apps/agent-web/src/components/layout/app-sidebar.tsx — Cards 메뉴 추가 (admin 전용, /cards 진입).
- apps/agent-web/src/stores/use-card-definitions-store.ts — invalidate(cardId) 메서드 추가. 발행 후 캐시 무효화로 다음 fetch 시 새 버전 가져오도록.

#### Phase 5 결과
- pnpm --filter @agent-studio/web lint: 0 errors, 215 warnings (baseline +10, 모두 hoisted function use-before-define — 기존 prompt-renderer 패턴과 동일).
- 1차 cards API(GET list/getLatest/version, POST create) 그대로 사용. NestJS 측 변경 없음.
- 신규 라우트(/cards, /cards/builder/:cardId)라 기존 페이지 영향 없음.


### Phase 6 — HITL 카드 통합 (완료)

#### Phase 6 변경 파일
- apps/agent-web/src/components/dynamic-cards/HitlDynamicCard.tsx (신규) — HumanInteraction → data 합성(interactionId/toolName/toolArgs/prompt + cardData override) 후 DynamicCardRenderer 마운트하는 wrapper.
- apps/agent-web/src/components/client/ClientChatView.tsx — FeedItem hitl_request 에 cardDefinitionId/cardVersion/cardData 옵션 필드 추가. buildHitlItem(히스토리 복원 경로) + WS hitl.request live handler 양쪽에서 agentContext 에서 추출. 렌더 분기: 해결 안 된 hitl_request 에 cardDefinitionId 있으면 HitlDynamicCard 인라인 마운트, 아니면 기존 ClientHitlCard.
- DB: docker exec psql 로 card_definitions 에 3건 추가 (hitl-approval-card / hitl-choice-card / hitl-input-card v1, level=composable). 각 카드의 actions handler.type=hitl_respond 가 wsClient.respondToInteraction 으로 응답 발행.

#### 핸들러 동작 (1차에 이미 추가됨, Phase 6 에서 활용)
- dynamic-cards/handlers.ts 의 `hitl_respond` case — useUserStore.currentUser.id 로 인증 확인 → wsClient.respondToInteraction(interactionId, {decision, value, ...args}, userId, 'studio') → useHitlStore.removeInteraction(id) → toast.

#### Phase 6 결과
- pnpm --filter @agent-studio/web lint: 0 errors, 215 warnings (baseline Phase 5 +0).
- DB 카드 정의 총 7건(skill-proposal/skill-edit/skill-analysis/quick-choices/hitl-approval/hitl-choice/hitl-input) 확인.
- 회귀 안전: runner 측 backend 변경 없음 (agent_context 가 그대로 통과). cardDefinitionId 미지정 interaction 은 기존 ClientHitlCard 흐름 100% 유지.

### 2차 종합 결과
- Phase 4·5·6 모두 완료. 회귀 없음.
- 동적 카드 시스템: 15종 블록(text/title/tags/key_value_list/json + line_diff/set_diff/score_gauge/severity_bar/fit_badge/tabs/accordion/choices/suggestion_list/progress) + 6종 handler(dismiss/send_message/tool_call/navigate/external_link/hitl_respond) + 5종 input 타입(text/textarea/number/select/multi_select) + Card Builder UI + HITL 인라인 통합.
- 카드 정의 등록 가능: Cards 사이드바 → /cards/builder/new → schema/layout/actions Monaco 편집 → 라이브 미리보기 → 버전 발행.
- HITL 사용법: 에이전트가 HITL 요청 시 agent_context 에 {cardDefinitionId, cardVersion, cardData} 추가 → 프런트가 자동 인라인 렌더 → 카드의 hitl_respond 액션이 응답 전송.

---

## 2026-06-07 (siziksu) — intent_routed 방향 롤백 + Card Definitions 의 HITL 동적 폼 표준 재정렬

### 배경 / 결정
- 이전 시도(`_run_intent_routed_flow` + structured_only / intent_routed 두 응답 전략 표준화)가 deepagents 그래프를 우회해 가드레일·메모리·step trace·HITL 등 거의 모든 가치를 잃는 별도 분기 코드임을 확인.
- Card Definitions / layout JSON 의 진정한 사용처는 도메인 시각화가 아니라 **"도구 HITL 인자 입력 동적 폼"** — 도구 수만큼 카드가 필요해 React 컴포넌트로는 표현 불가, JSON Schema → layout 자동 변환 + 빌더 편집이 유일한 답.
- 기존 코드 모듈 SkillAssistant 는 **0줄 변경 유지** (비교 기준).

### 목표
1. intent_routed 전제로 추가된 코드/UI/시드 전부 롤백.
2. Card Definitions / dynamic-card / HITL 인프라 유지 또는 강화.
3. JSON Schema → layout 변환기 + 재귀(Object/Array) 및 단일 필드(String/Number/Boolean) 입력 블록 5종 신설.
4. hitl-* 카드 시드를 변환기의 header/footer 베이스 템플릿으로 연계.

### 진행
- Step 1 (롤백) — Python: `_run_intent_routed_flow` 삭제 + strategy 분기 제거, `system_prompts/skill_assistant.py` 삭제. Frontend: IntentRoutingEditor / ServiceAgentSkillPanel / /skills/compare / OutputSchemaTab 라디오 / responseStrategy·intent·chatActions 타입 제거. 시드: skill-assistant.json intent 블록 + skill-assistant-jsonlayout.json 삭제.
- Step 2 (HITL 변환기) — schema-to-layout.ts + Object/Array/String/Number/BooleanInputBlock 신설.
- Step 3 (HITL 카드 발행 경로) — deepagent_bridge interrupt 감지 분기에서 변환기 출력 layout 으로 HITL 카드 발행, DynamicCardRenderer 응답 처리.
- Step 4 (검증) — lint / `/skills` 회귀 없음 / HITL 시연.

### 결과 (2026-06-07)
- Step 1 완료. `_run_intent_routed_flow` + strategy 분기, `system_prompts/` 디렉토리, IntentRoutingEditor, ServiceAgentSkillPanel, `/skills/compare`, OutputSchemaTab intent 라디오 + CardMappingsEditor mount, `skill-assistant.json` intent 블록, `skill-assistant-jsonlayout.json` 모두 제거. 추가로 사용자 지시에 따라 "코드 SkillAssistant 흉내내려 만든" 시스템 에이전트 시드 메커니즘 전체(`system-seeds` Nest 모듈, `system/recover/` 페이지, 도메인 카드 5종 시드, `add_system_flags` migration, schema 의 systemKey/isSystem/deletedAt, 일반 에이전트 타임라인 UI `run-timeline/`, run-event-adapter, react-card-registry 등) 추가 롤백.
- Step 2 완료. `apps/agent-web/src/lib/schema-to-layout.ts` 신설 (enum/string/number/boolean/object/array 매핑, args defaultValue 주입, MAX_DEPTH 8). `dynamic-cards/blocks/` 에 ObjectInputBlock / ArrayInputBlock / StringInputBlock / NumberInputBlock / BooleanInputBlock 5종 신설. BLOCK_REGISTRY / BlockReferencePanel 등록.
- Step 3 완료. Frontend: `SchemaInputBlock` 신설 (HITL data.toolSchema → schemaToLayout 호출 후 재귀 렌더), `CardLayout`/`ActionRow`/`handlers.ts` 에 formState 수집 경로 추가, `HitlDynamicCard` 가 toolSchema prop 전달, `hitl-input-card.json` body 에 schema_input 블록 추가. Backend: `deepagent_bridge.py` 에 `_tool_schema_from_args` + `_emit_hitl_cards` 헬퍼 신설, interrupt 감지 분기 2곳(send/resume) 에서 `agent.dynamic_card` (cardDefinitionId=hitl-input-card) 발행 호출 추가. 기존 `hitl.interrupt` 이벤트는 그대로 유지 — 모달/카드 병행.
- Step 4. Python `uv run python -c "from src.modules.agents.deepagent_bridge import stream_with_deepagent, _emit_hitl_cards; from src.modules.dynamic_cards import emit_dynamic_card"` OK. Frontend `tsc --noEmit` 잔존 7건 모두 HEAD 시점부터 존재한 기존 에러 (skills/page.tsx Promise 반환, McpServerDialog, VisualActionsEditor unknown ReactNode, use-skill-assistant-store immer draft). 이번 작업에 의한 신규 에러 0건. 카드 인프라 (`schema_input` 포함 11종 블록) 와 HITL 카드 발행 경로(runner → emit_dynamic_card → frontend SchemaInputBlock → formState → hitl_respond) 가 일직선으로 연결됨.

### 다음 단계 (후속)
- 도구 카탈로그(`builtin_tools/registry`) 기반 정식 schema 조회로 `_tool_schema_from_args` 의 args 타입 추정 폴백 대체.
- `HitlDynamicCard` 호출자(`ClientChatView` 의 buildHitlItem) 에서 agentContext.toolSchema 전달 경로 마저 연결.
- Prisma 의 systemKey/isSystem/deletedAt 컬럼이 DB 에 실제 적용된 상태라면 schema 와 동기화(`pnpm db:push` 또는 manual SQL 정리) — 사용자 확인 후.

---

## 2026-06-07 (siziksu) — HITL 동적 카드 공통화 (구 ClientHitlCard 제거)

### 목표
구 React 카드 `ClientHitlCard` 가 채팅 인라인으로 그리던 5가지 HITL 기능을 신 동적 카드(`HitlDynamicCard` + `SchemaInputBlock` + `hitl-*` 시드) 가 100% 대체. 두 경로 공존(`cardDefinitionId` 분기) 제거 → 카드 단일 경로 공통화.

### 범위 (deepagents 공식 명세 준수)
공식 HITL 명세:
- `decisions` = list, action_requests 와 같은 순서
- type: `approve | reject | edit | respond`
- edit payload: `{type:"edit", edited_action:{name, args}}`
- 다중 도구 호출은 한 interrupt 에 batch
- Resume: `Command(resume={"decisions":[...]})`

구 카드 → 신 카드 이전 항목:
1. **다중 actionRequest** — 한 interrupt N장의 도구 호출. 단일 카드 1장에 actionRequests 전체를 표현 + 응답 순서 매핑.
2. **NL preview_edit** — 자연어 수정 텍스트 → `apiClient.hitl.previewEdit` → pseudo 카드 발행 → 사용자 승인 시 `{decision:"edit", edited_action:{name,args}}`.
3. **Direct edit 폼** — SchemaInputBlock 폼 편집 → 사용자 승인 시 동일 `edit` payload 합성.
4. **Recursion limit reached** — 구 카드의 특수 분기. 신 카드 `hitl-recursion-card` 신설 ("더 진행/그만").
5. **Resolved 라벨** — "승인됨/거부됨/수정 요청 전송됨" 표시.

후속 (이번 범위 밖, 구 카드도 미지원):
- Timeout countdown / hitl.timeout_warning
- 거부 사유 textarea

### 진행 (단계)
1. backend `_emit_hitl_cards` 가 actionRequests 전체를 한 카드 data 에 실음 + recursion limit 별도 cardId
2. `schema-to-layout.ts` 의 approval/edit/respond footer 정비 (deepagents decision 4종 매핑)
3. `handlers.ts` 의 `hitl_respond` 액션 payload 분기 + `preview_edit` 액션 신설
4. 시드 카드 3종 정비 + `hitl-recursion-card` 신설
5. `ClientChatView.buildHitlItem` 가 cardDefinitionId 없을 때 기본 `hitl-input-card` 주입 (recursion 일 때 `hitl-recursion-card`)
6. 구 `ClientHitlCard` + 관련 state(editingHitl/editPreviewing/handleHitlEditStart 등) 제거
7. 검증 — tsc/lint + 6가지 시나리오 수동 확인

---

## 2026-06-07 — siziksu — Phase 1: CardDefinition.category 메타데이터 도입

### 목표
카드 발행 시점/맥락을 표현하는 직교 축 `category`(hitl/response) 도입.
운영자가 `/cards` 에서 HITL vs 응답 카드를 시각적으로 구분하고, 빌더에서 카테고리를 선택하도록 만든다.
실제 라우팅(Phase 2 의 도구↔카드 매핑) 에는 영향 없음 — 메타데이터 + UI 표시까지만.

### 범위
- DB: `CardDefinition.category String @default("response")` + migration. 기존 `hitl-*` 카드는 자동으로 `hitl` 마킹.
- API: cards CRUD DTO 갱신.
- 시드 4종 JSON 에 `"category": "hitl"` 추가.
- shared/api-client 타입 갱신.
- `/cards` 페이지: 상단 탭 (전체/HITL/응답) + 카운트 + 카드 썸네일 category 배지.
- `/cards/builder` 페이지: level 셀렉터 옆 category 셀렉터, `?category=hitl` URL 프리셋.

### 변경 파일
- `packages/database/prisma/schema.prisma` — `CardDefinition` 모델에 `category String @default("response")` 추가.
- `packages/database/prisma/migrations/20260607063337_add_card_definition_category/migration.sql` — 신규 컬럼 + `hitl-*` 카드 자동 마킹.
- `apps/api/src/system-seeds/cards/{hitl-input,hitl-recursion,hitl-approval,hitl-choice}-card.json` — `"category": "hitl"` 추가.
- `apps/api/src/modules/cards/cards.service.ts` — `CardCategory` 타입, `CardDefinitionDto.category` 옵션, `list({category})` 필터, `create` 시 기본값 `'response'`.
- `apps/api/src/modules/cards/cards.controller.ts` — `GET /cards?category=hitl|response` 쿼리 파라미터.
- `apps/agent-web/src/lib/api-client.ts` — `CardCategory` 타입 export, `CardDefinition.category` 필드, `cards.list({category})` + `cards.create({category})`.
- `apps/agent-web/src/app/(dashboard)/cards/page.tsx` — 상단 탭(전체/HITL/응답+카운트), `CATEGORY_STYLES`, 갤러리/테이블에 category 배지.
- `apps/agent-web/src/app/(dashboard)/cards/builder/[cardId]/page.tsx` — `BuilderState.category`, `?category=hitl` URL 프리셋, 헤더 셀렉터, 저장 payload + clone/로드 시 row.category 반영.

### 검증
- `pnpm exec prisma migrate deploy` → 신규 migration 적용. `card_definitions.category` 컬럼 생성, 기존 4종 모두 `hitl` 로 마킹 확인 (psql).
- `pnpm exec prisma generate` 성공.
- `pnpm --filter @agent-studio/web exec tsc --noEmit` 0 신규 에러.
- `pnpm --filter @agent-studio/api exec tsc --noEmit` 0 에러.
- DB 상태: 4종 모두 `level=composable, category=hitl`.

### 특이사항
- 시드 자동 upsert 로직(`seed.ts`)은 여전히 미구현 — 시드 JSON 의 category 필드는 향후 자동 적용용. 현재 DB 의 category 값은 migration 의 UPDATE 가 채움.
- Phase 2 (도구↔카드 매핑) 는 별도 사이클. 저장소는 `Agent.toolPermissions` JSON 확장 방향으로 사전 결정됨.
- `cards.controller` 의 list 엔드포인트만 category 필터 추가. getLatest/get 은 cardId 기반이라 영향 없음.

---

## 2026-06-07 — siziksu — Phase 3: 응답 카드 매핑 UI (`outputSchema.cardMappings`)

### 목표
`outputSchema['x-agent-studio'].cardMappings` 를 raw JSON 이 아닌 행 기반 UI 로 편집.
cardDefinitionId 드롭다운은 `category='response'` 카드만 노출 — HITL 카드 오연결 방지.

### 범위
- `apps/agent-web/src/components/agents/builder/OutputSchemaTab.tsx` 에 신규 "응답 카드 매핑" 섹션 추가 (Monaco editor 아래).
- 행: action_value(text) + cardDefinitionId(select, category='response') + version(number, default 1) + dataPath(text, optional).
- 행 변경 시 outputSchema 머지하고 Monaco editor 도 동기화.
- format='none' 일 때는 섹션 숨김.

### 진행
1. `apiClient.cards.list({ category: 'response' })` 로 카드 목록 로드 (useEffect).
2. 현재 schemaStr → parse → `x-agent-studio.cardMappings` 를 rows 배열로 변환.
3. 행 추가/삭제/수정 시 rows → dict 머지 → onChange + setSchemaStr 동기화.
4. 검증 — tsc/lint + 라이브 동작.

### 변경 파일
- `apps/agent-web/src/components/agents/builder/OutputSchemaTab.tsx`
  - `useEffect` 로 `apiClient.cards.list({ category: 'response' })` 호출, `responseCards` state 보관.
  - `MappingRow` 타입 + `rowsFromSchema` / `dictFromRows` / `mergeMappings` 헬퍼 신설.
  - `mappingRows` state — Monaco editor 변경 시 `rowsFromSchema(parsed)` 로 재구성하여 양방향 동기화.
  - JSON Schema 모드일 때 Monaco editor 위에 "응답 카드 매핑" 섹션 렌더: action_value(text) / cardDefinitionId(select, category=response) / version(number, default 1) / dataPath(text, optional) / 행 삭제 버튼.
  - `commitMappingRows` → `mergeMappings(parsedSchema, next)` 후 `setSchemaStr(JSON.stringify(...))` 동시에 `onChange({ outputSchema })`. 빈 mappings 는 `x-agent-studio.cardMappings` 키 제거 (옵셔널 vendor block 도 비면 제거).
  - 템플릿 변경 시 `setMappingRows(rowsFromSchema(tpl.value))` 동기화.

### 검증
- `pnpm --filter @agent-studio/web exec tsc --noEmit` — `OutputSchemaTab` 관련 신규 에러 0. 기존 에러(skills/page, VisualActionsEditor, McpServerDialog, use-skill-assistant-store) 는 Phase 3 범위 외로 무관.

### 특이사항
- `format='none'` (Plain Text) 일 때는 outputSchema 자체가 undefined 가 되므로 cardMappings 섹션도 자동 숨김 — JSON Schema 모드 분기 안에 배치.
- Monaco editor 와 행 UI 는 **양방향** 동기화: editor 에서 raw JSON 수정 시 행 재구성. 단 JSON parse 실패 (`jsonError`) 시 행은 마지막 유효 상태 유지.
- 매핑이 0개가 되면 `x-agent-studio.cardMappings` 키를 제거하고, 그 결과 `x-agent-studio` block 이 비면 vendor key 자체도 제거 — 깨끗한 schema 유지.
- 빈 행 (key 비어 있거나 cardDefinitionId 미선택) 은 `dictFromRows` 에서 자동 필터링되어 schema 에 반영되지 않음.

---

## 2026-06-07 16:08 — siziksu — Phase 4+5+6 HITL 카드 시스템 마무리

### 목표
HITL 카드 시스템 로드맵의 Phase 4 (빌더 UX) / Phase 5 (Multi-action 개별 결정) / Phase 6 (시드 정리 + seed.ts 자동화) 일괄 진행.

### 범위
- Phase 4: cards/page.tsx "+ 새 카드" 카테고리 선택 모달 + builder presetCategory='hitl' 시 schema_input + hitl_respond 프리셋 + BlockReferencePanel 추천 섹션.
- Phase 5: hitl-input-card.json layout/actions 갱신 + HitlDynamicCard 개별 토글 + hitl_service._normalize_decisions 개별 처리.
- Phase 6: hitl-approval/choice 시드 삭제 + seed.ts 카드 자동 upsert.
- 제외: ClientChatView dead code 제거 (라이브 코드), Playwright E2E (별도 사이클).

### 진행 (작업 중)

#### Phase 4 — 빌더 UX 강화
- `apps/agent-web/src/app/(dashboard)/cards/page.tsx`
  - `pickerOpen` state + "+ 새 카드" 클릭 시 카테고리 선택 모달 (HITL / 응답 두 옵션) 노출.
  - 선택 시 `router.push('/cards/builder/new?category=<picked>')`.
- `apps/agent-web/src/app/(dashboard)/cards/builder/[cardId]/page.tsx`
  - `NEW_CARD_TEMPLATE_HITL` 추가 — schema_input 블록 + approve/reject hitl_respond 액션 프리셋.
  - `DUMMY_DATA_TEMPLATE_HITL` 추가 — interactionId/toolSchema/toolArgs 미리보기용.
  - `presetCategory === 'hitl'` 일 때 HITL 템플릿 사용.
- `apps/agent-web/src/components/cards-builder/BlockReferencePanel.tsx`
  - `category?: 'hitl' | 'response'` props 신설.
  - `RECOMMENDED_BY_CATEGORY` 상수 — HITL: schema_input/key_value_list/text/string_input/choices/suggestion_list, 응답: score_gauge/severity_bar/key_value_list/tags/fit_badge/tabs.
  - blocks 섹션에서 추천 항목을 상단 "추천" 섹션으로 분리, 나머지는 "전체" 섹션.

#### Phase 5 — Multi-action 개별 결정
- `apps/agent-runner-py/src/modules/hitl/hitl_service.py`
  - `_normalize_decisions`: payload 에 `individualDecisions` dict 있으면 빈 리스트 반환 (호출자에서 매칭).
  - `_resolve_individual_decisions` 신설 — actionRequests 순서대로 `id` / 인덱스 매칭하여 approve/reject decisions 생성.
  - `respond()`: individualDecisions 분기 추가, 모두 reject 면 status='rejected'.
- `apps/agent-web/src/components/dynamic-cards/blocks/SuggestionListBlock.tsx`
  - `block.mode === 'individual_toggle'` 분기 추가 — 각 row 에 승인/거부 토글 (자체 state + onFieldChange 로 formState 업데이트).
- `apps/agent-web/src/components/dynamic-cards/handlers.ts`
  - `hitl_respond`: handler 의 `individualDecisions` 가 객체이고 비어있지 않으면 우선 처리해 백엔드에 그대로 전달.
- `apps/api/src/system-seeds/cards/hitl-input-card.json`
  - suggestion_list 에 `mode: 'individual_toggle'`, `fieldKey: 'individualDecisions'` 추가.
  - 새 액션 `apply_individual_decisions` 추가 — `decision: 'approve'` 폴백 포함 (단일 액션 시 일반 approve 로 동작).

#### Phase 6 — 시드 정리 + seed.ts 자동화
- 삭제: `apps/api/src/system-seeds/cards/hitl-approval-card.json`, `hitl-choice-card.json` (hitl-input-card 가 4 decision 모두 흡수).
- `packages/database/prisma/seed.ts`
  - `fs`, `path` import 추가.
  - `seedCardDefinitions()` 신설 — `apps/api/src/system-seeds/cards/*.json` 전체를 읽어 `cardId_version` composite key 로 upsert.
  - main() 마지막에 호출.

### 검증
- `pnpm --filter @agent-studio/web exec tsc --noEmit` — Phase 4/5 관련 신규 에러 0. 사전 에러(skills/page, VisualActionsEditor, McpServerDialog, use-skill-assistant-store) 는 범위 외.
- `npx tsc --noEmit -p .` (packages/database) — 0 에러.
- `pnpm lint` (web) — 환경 문제(eslint-module-utils 모듈 누락)로 미수행. Phase 외 사전 환경 이슈.

### 특이사항
- **Phase 4 카테고리 모달**: builder 의 `?category=` 쿼리는 Phase 1 에서 이미 지원되어 신규 처리 불필요.
- **Phase 4 HITL 프리셋**: 사용자가 비우면 그대로 비어있게 유지 (초기 1회만 적용, 덮어쓰기 없음).
- **Phase 5 visibleWhen 미지원**: 카드 시스템에 조건부 액션 가시화 기능이 없어 `apply_individual_decisions` 액션은 항상 표시. multi-action 시나리오에서만 의미 있음. 사용자에게 가이드 부족 — UX 폴리시는 후속 사이클로.
- **Phase 5 individualDecisions 매칭 키**: actionRequest 의 `id` 또는 `tool_call_id` 우선, 없으면 인덱스 fallback. 양쪽 모두 지원.
- **Phase 5 edit + multi-action 미지원**: 둘 다 multi-action 시 복잡도 ↑. 현재는 individualDecisions 우선, edit 은 single primary 기준.
- **Phase 6 DB row 정리**: hitl-approval-card / hitl-choice-card 의 DB row 는 seed.ts upsert 가 자동 삭제하지 않음. 운영 환경에서 수동 정리 필요 — `DELETE FROM card_definitions WHERE card_id IN ('hitl-approval-card', 'hitl-choice-card');`.
- **제외 항목**: ClientChatView dead code 제거는 라이브 코드 (preview_edit 플로우 line 1904/2063/2322 사용 중) — 제외. Playwright E2E 자동화는 별도 사이클로 분리.

## 2026-06-07 18:00 — siziksu — HITL 카드 통일 (디버그 패널 → 동적 카드)

### 목표
- 클라이언트 채팅과 에이전트 빌더 디버그 패널의 HITL UI 를 `HitlDynamicCard` 단일 렌더러로 일원화.
- 옛 `HitlApprovalCard` (AgentFlowDebugPanel 내부, sky-500 톤 + 자연어/직접 수정 + recursion 분기) 디자인/기능을 동적 카드 시스템(블록 + layout JSON)으로 흡수.

### 범위
- Step 1: 카드 렌더러 `visibleWhen` 지원 + 헬퍼 5종 공용화.
- Step 2: HITL 전용 블록 4종(`hitl_header`/`arg_list`/`arg_editor`/`nl_edit`) 추가.
- Step 3: `hitl-input-card.json` layout/actions 재구성, `set_field` 핸들러 신설.
- Step 4: 디버그 패널을 `HitlDynamicCard` 로 마이그레이션, 자체 정의 ~370 라인 삭제.

(완료 후 결과/특이사항 추가 예정)

### 결과
- Step 1 — `template.ts:evalBoolean` 추가 (equals/notEquals/in/isEmpty/isNotEmpty/not/and/or 지원). `ActionRow` 액션 `visibleWhen` 검사, `blocks/render.tsx` 블록 `visibleWhen` 검사. `BlockCtx` 에 `formState/setFormState` 노출. `CardLayout` 이 reactive `formState` state + ref 이중 보관 (visibleWhen 리렌더 트리거 + dispatch 시점 최신값). `CardAction.visibleWhen` / `CardHandler.set_field` 타입 추가. 헬퍼 5종 `hitl-helpers.ts` 로 추출, `AgentFlowDebugPanel` 은 import 로 전환.
- Step 2 — `blocks/HitlHeaderBlock`, `ArgListBlock`, `ArgEditorBlock`, `NlEditBlock` 신설. `render.tsx` 의 BLOCK_REGISTRY 에 4종 등록.
- Step 3 — `hitl-input-card.json` layout 전면 재구성 — body 4 블록(hitl_header / suggestion_list multi-action / arg_list / arg_editor / nl_edit) + actions 8개 (reject / start_nl_edit / start_direct_edit / approve / cancel_edit / submit_direct_edit / submit_nl_edit / apply_individual_decisions). 모두 `visibleWhen` 으로 editMode·allowedDecisions·actionRequests 길이에 따라 조건부 노출. `handlers.ts` 에 `set_field` 케이스 추가 + `HandlerContext.setFormState` 노출, `ActionRow` 에서 주입. `CardLayout` 의 초기 formState 에 `editMode: ''` 프리필.
- Step 4 — `AgentFlowDebugPanel` 의 hitl_request 변환부에 `cardDefinitionId`/`cardVersion`/`cardData`/`toolSchema` 수신 추가. `ChatItem.hitl_request` 타입 확장. 단일/그룹 렌더 두 곳을 `HitlDynamicCard` 로 교체. `HitlApprovalCard` 함수(원 ~370 라인) 삭제. 헬퍼 정의(원 5개 + formatArgForPrompt) 삭제하고 `hitl-helpers` 에서 `TOOL_DISPLAY_NAMES` 만 재임포트 (CompletedGroupCard 라벨링용).
- 변경 파일 (12개):
  - `apps/agent-web/src/components/dynamic-cards/template.ts` — evalBoolean
  - `apps/agent-web/src/components/dynamic-cards/ActionRow.tsx` — visibleWhen + setFormState 전달
  - `apps/agent-web/src/components/dynamic-cards/CardLayout.tsx` — formState state/ref 이중 보관 + 초기 editMode
  - `apps/agent-web/src/components/dynamic-cards/handlers.ts` — set_field 핸들러, HandlerContext.setFormState
  - `apps/agent-web/src/components/dynamic-cards/blocks/types.ts` — ctx 확장
  - `apps/agent-web/src/components/dynamic-cards/blocks/render.tsx` — block visibleWhen + 4 신규 블록 등록
  - `apps/agent-web/src/components/dynamic-cards/hitl-helpers.ts` — 신규 (헬퍼 5종 + formatArgForPrompt internal)
  - `apps/agent-web/src/components/dynamic-cards/blocks/HitlHeaderBlock.tsx` — 신규
  - `apps/agent-web/src/components/dynamic-cards/blocks/ArgListBlock.tsx` — 신규
  - `apps/agent-web/src/components/dynamic-cards/blocks/ArgEditorBlock.tsx` — 신규
  - `apps/agent-web/src/components/dynamic-cards/blocks/NlEditBlock.tsx` — 신규
  - `apps/agent-web/src/components/agents/flow/AgentFlowDebugPanel.tsx` — HitlApprovalCard 제거, HitlDynamicCard 도입, WS 수신부 확장
  - `apps/agent-web/src/lib/api-client.ts` — CardAction.visibleWhen, CardHandler.set_field 타입
  - `apps/api/src/system-seeds/cards/hitl-input-card.json` — layout/actions 전면 재구성

### 검증
- `npx tsc --noEmit` (apps/agent-web) — 신규 에러 0. 사전 에러 7건 (skills/page, VisualActionsEditor, McpServerDialog, use-skill-assistant-store) 만 잔존, 모두 범위 외.
- 카드 시드 반영: `pnpm db:seed` 실행으로 `hitl-input-card@v1` upsert 필요 (운영 환경 적용 시점에).

### 특이사항
- **visibleWhen 표현식 문법**: JSON 직렬화 가능한 객체 형태. 예: `{ equals: { left: '{{formState.editMode}}', right: '' } }`, `{ in: { value: 'approve', list: '{{data.allowedDecisions}}' } }`, `{ and: [...] }`, `{ isNotEmpty: '{{formState.individualDecisions}}' }`. 향후 카드 빌더 UI 에서 시각 편집기 도입 여지.
- **CardLayout formState 이중 보관**: ref 만으로는 visibleWhen 평가가 리렌더 트리거를 못 받음 → state + ref 동시 보관. 액션 dispatch 시점은 ref 의 최신값 사용(`await callback()` 직전 stale closure 회피 — AGENTS §1-5-3 패턴).
- **editMode 초기값**: 모든 카드가 빈 문자열 `editMode` 를 갖도록 `CardLayout` 에서 프리필. 다른 카드는 무해 (참조 안 함).
- **set_field 액션 done 잠금 제거**: 토글성 핸들러는 클릭 후에도 재사용 가능해야 하므로 `ActionRow` 가 `handler.type === 'set_field'` 일 때 done 마킹/스타일 잠금 모두 스킵.
- **dead prop 잔존**: `AgentFlowDebugPanel` 의 `onHitlDecide`/`onHitlEditSubmit`/`handleHitlDecide`/`handleHitlEditSubmit` 는 더 이상 hitl_request 분기에서 호출되지 않음 (응답은 HitlDynamicCard → handlers.ts 가 직접 처리). ToolCallCard 까지 prop drill 로 흘러가지만 미사용 — 사용자 검증 후 후속 사이클에서 청소 권장.
- **백엔드 무변경**: `_handle_interrupt` 가 이미 cardDefinitionId/cardData/toolSchema 발행 중 — 디버그 패널 수신부 확장만으로 양쪽 단일 경로 달성.
- **수동 검증 권장**: 클라이언트 채팅과 디버그 패널에서 (1) approve/reject (2) 자연어 수정 → 미리보기 → 수정하고 실행 (3) 직접 수정 → 수정하고 실행 (4) multi-action individualDecisions (5) recursion limit `hitl-recursion-card` 노출 시나리오 각각 확인.
- **자동 커밋·푸시 금지** — 사용자 검토 후 수동.

## 2026-06-07 19:10 — siziksu — HITL 카드 옛 디자인(sky 톤 wrapper) 정확 복구

### 목표
앞 사이클에서 layout 만 옮기고 외곽 디자인이 generic surface 톤으로 떨어진 문제 복구. 옛 `HitlApprovalCard` 와 픽셀 단위 동일하게 sky-500 톤 wrapper(좌측 Shield 박스 + flex-1 sky 카드 박스 + border-t sky 액션 영역, 옛 버튼 매핑) 를 동적 카드 시스템 안에서 재현.

### 변경 파일
- `apps/agent-web/src/components/dynamic-cards/CardLayout.tsx`
  - `layout.frame: 'default' | 'hitl-sky'` 인식. 'hitl-sky' 일 때 외곽을 옛 디자인으로 — `flex gap-3 py-0.5` + Shield 외곽 박스 + `flex-1 rounded-xl border border-sky-500/30 bg-sky-500/5 overflow-hidden` + 본문 `px-4 py-3` + 액션 `border-t border-sky-500/20 px-3 py-2 bg-bg/40 flex items-center justify-end gap-2`.
  - 본문 블록 사이 간격 `mt-3` (옛 카드와 동일). visibleWhen false 인 블록은 사전 필터로 wrapper 자체 미생성.
  - visibleActions 카운트로 액션 영역 자체 렌더 여부 판단 (모두 숨김이면 border-t 도 안 그림).
- `apps/agent-web/src/components/dynamic-cards/ActionRow.tsx`
  - props `tone?: 'default' | 'sky'`, `layout?: 'vertical' | 'horizontal'` 신설.
  - `layout='horizontal'` 일 때 컨테이너 `flex items-center gap-2` (옛 액션 영역과 동일).
  - `tone='sky'` 일 때 버튼 베이스 `rounded-md px-3 py-1.5 text-xs font-semibold` + 새 매핑 헬퍼 `skyButtonClass()` (primary→sky-500 solid / danger→hover red-600/20 / secondary→surface-2).
- `apps/agent-web/src/components/dynamic-cards/blocks/HitlHeaderBlock.tsx`
  - Shield 아이콘 박스 제거 (frame wrapper 가 처리). 본문 텍스트(헤더 문장 + 도구 이름) 만 그대로.
- `apps/api/src/system-seeds/cards/hitl-input-card.json`
  - `layout.frame: 'hitl-sky'` 추가.

### 검증
- `npx tsc --noEmit` (apps/agent-web) — 신규 에러 0. 사전 에러 7건만 잔존.
- 시각 검증 필요 (수동): (1) 클라이언트 채팅·디버그 패널에서 sky-500 톤 외곽 박스 + 옛 디자인 동일 노출 (2) 초기 상태는 args **미리보기**(arg_list) (3) `직접 수정` / `자연어로 수정` 클릭 시 편집 박스 노출 (4) 액션 버튼 가로 정렬 + 옛 톤 (허용=sky-500, 거부=hover red, 자연어/직접/취소=surface-2).

### 특이사항
- **frame 메타는 layout JSON 안**: 카드 빌더 UI 가 향후 frame 토글을 노출하면 새 카드도 sky 톤 wrapper 를 채택 가능.
- **블록 visibleWhen 사전 필터**: `mt-3` 간격이 hidden 블록 자리에 누수되는 부작용 차단.
- **카드 빌더 UI 영향**: 빌더 미리보기는 default frame 으로 렌더되므로 영향 없음 — `cards/builder/[cardId]/page.tsx` 의 CardLayout 호출 위치는 미변경.

### 추가 — 빌더 reference 노출 + Claude 고유명사 제거
- `apps/agent-web/src/components/cards-builder/BlockReferencePanel.tsx`
  - BLOCKS 에 `hitl_header` / `arg_list` / `arg_editor` / `nl_edit` 4종 추가.
  - HANDLERS 에 `set_field` / `preview_edit` 추가.
  - **LAYOUT_META 섹션 신설** — `layout.frame` / `block.visibleWhen` / `action.visibleWhen` / `action.style` 가이드. 빌더 사용자가 카드 정의에서 디자인 메타를 직접 편집할 수 있도록 노출.
  - section 셀렉터에 `'layout'` 탭 추가.
  - `RECOMMENDED_BY_CATEGORY.hitl` 을 새 HITL 블록 4종 우선 순으로 갱신.
- `apps/agent-runner-py/src/modules/threads/threads_service.py`
  - `_handle_interrupt` 의 prompt 문구를 `"Claude가 ..."` → `"에이전트가 {tool_name} 도구를 실행하려 합니다. 허용하시겠습니까?"` 로 변경. 모델 고유명사 노출 제거.
- `apps/agent-web/src/components/dynamic-cards/blocks/HitlHeaderBlock.tsx`
  - promptField 가 자동 생성(`"에이전트가..."` / `"Claude가..."`) 문구이면 toolDisplayName 매핑이 적용된 기본 문구를 그리고, 운영자가 직접 작성한 prompt 면 그 문구를 그대로 노출. 빌더에서 카드 측 prompt 를 커스텀해도 안전.

### 운영 환경 반영 필요
- **새 hitl-input-card layout 반영**: `pnpm db:seed` 실행. DB 의 hitl-input-card 가 옛 layout 그대로면 신규 디자인이 안 보임.
- **agent-runner-py 재시작**: prompt 문구 변경 반영.

### 추가 — 디자인을 카드 정의로 완전 이관 (frame 객체 + action.className + 헤더 문구)
사용자 피드백: "버튼 위치 색 이런것도 다르고...기존이랑 같게 할수 있도록 카드 구성이 가능해야 하고", "에이전트가 X 도구... 이런 문구도 카드에서 관리가 되어야 하는데 코드에 박혀있는것같아". → frame=enum 매핑이나 헤더 문구가 코드에 박혀있다는 지적.

#### 변경 파일
- `apps/agent-web/src/lib/api-client.ts`
  - `CardAction.className` — style enum 매핑 무시하고 카드가 button className 을 직접 override.
  - `CardAction.position` — left/right 배치 (frame.actionsContainerClass 가 justify-between 일 때 양쪽 그룹).
- `apps/agent-web/src/components/dynamic-cards/CardLayout.tsx`
  - `layout.frame` 을 string 프리셋 또는 LayoutFrame 객체로 받음.
  - LayoutFrame 필드: `icon` / `outerClass` / `iconBoxClass` / `iconClass` / `cardBoxClass` / `bodyClass` / `blockGapClass` / `actionsContainerClass` / `actionTone` / `actionLayout`.
  - `'hitl-sky'` 프리셋 = 옛 HitlApprovalCard 와 동일한 wrapper className 들 (Shield 박스 / sky-500 카드 박스 / border-t sky 액션 영역).
  - object override 는 hitl-sky 프리셋을 기본으로 merge — 일부만 바꿔도 됨.
  - icon enum: `'shield-check' | 'sparkles' | 'info' | 'none'` (none 이면 좌측 박스 미렌더).
- `apps/agent-web/src/components/dynamic-cards/ActionRow.tsx`
  - `action.className` 이 있으면 style enum 매핑/tone 매핑 모두 무시하고 그대로 사용.
  - `layout='horizontal'` 일 때 컨테이너 div 제거하고 Fragment 로 렌더 — outer wrapper(justify-end 등) 의 정렬이 button 들에 그대로 적용.
- `apps/agent-web/src/components/dynamic-cards/blocks/HitlHeaderBlock.tsx` (전면 재작성)
  - title/titleClassName/titleHighlightClassName/subtitle/subtitleClassName 을 카드 정의가 자유 지정.
  - `{toolDisplayName}` / `{toolName}` 토큰만 코드가 치환 (builtinDisplayNames 매핑 적용). 문구·색·크기 모두 카드에서 관리.
- `apps/api/src/system-seeds/cards/hitl-input-card.json`
  - hitl_header 의 title/subtitle/className 들을 모두 명시 — 옛 코드 두 줄을 카드 정의로 표현.
  - 모든 action 에 `className` 명시 (옛 코드 픽셀 단위 일치 — 거부=hover:red-600/20, 자연어/직접/취소=surface-2, 허용/수정하고실행=bg-sky-500).
  - reject 의 style 을 `danger` 로 정정.
- `apps/agent-web/src/components/cards-builder/BlockReferencePanel.tsx`
  - LAYOUT_META 에 `layout.frame (object)` / `action.className` / `frame.icon` 등 새 옵션 가이드 추가.
  - `hitl_header` 예시를 카드가 문구를 컨트롤하는 새 형태로 갱신.

#### 결과
- 옛 코드의 모든 wrapper / 버튼 색·padding·hover / 헤더 문구가 `hitl-input-card.json` 안에서 100% 표현됨.
- 빌더 layout JSON 에서 `frame` 객체나 `action.className` 을 수정하면 그대로 빌더 미리보기·실제 채팅에 동일 반영.
- 코드에는 token 치환 로직과 frame 프리셋 default value 만 남음.

#### 운영 환경 반영 필요
- `pnpm db:seed` — 새 hitl-input-card.json 을 DB upsert. 안 하면 DB 의 옛 layout 이 그대로 노출.

---

## 2026-06-07 — 동적 카드 시스템 Adaptive Cards(OSS) 전면 재편 [siziksu]

### 목표
자체 SDUI 프레임워크(29블록 + visibleWhen DSL + 3-pane 빌더)를 폐기하고 OSS Adaptive Cards
(adaptivecards / adaptivecards-templating / adaptive-expressions)로 전면 재작성한다.
방향 = A(슬림 유지): "에이전트 emit → 런타임 렌더 + 관리자 발행" 제품 역량은 유지하되 내부 엔진만 교체.
near-rewrite, 단절형, 브랜치 1개(dev), 검증 게이트 2회(P2 HITL / P5 전체).

### P0 조사 결과 (확정)
- DB card_definitions = 4행, 전부 category=hitl: hitl-approval / hitl-choice / hitl-input / hitl-recursion.
- 에이전트 generic 동적카드 실사용 0. 동적카드의 유일한 라이브 소비자 = HITL.
- DB 전 카드 등장 블록(9): text, json, key_value_list, choices, suggestion_list, hitl_header,
  arg_list, arg_editor, nl_edit.
- DEAD 블록(20, 삭제 안전 확정): title, tags, line_diff, set_diff, score_gauge, severity_bar,
  fit_badge, tabs, accordion, progress, file_tree, code_diff, choice_chip_input,
  refinement_preview, string_input, number_input, boolean_input, object_input, array_input, schema_input.

### 페이즈
- P0 조사확정(완료)
- P1 AC 엔진 + 데이터모델 교체 (CardDefinition schema/layout/actions → payload+sampleData)
- P2 HITL 재구성 (표준 AC 입력으로 단순화, arg_editor/nl_edit 폐기) — 동작검증
- P3 빌더 재편 (AC payload 에디터 + 라이브 미리보기)
- P4 dead 코드 일소 (20 블록 + 레지스트리/DSL/CardLayout/ActionRow)
- P5 문서/회귀

### 결과 (P1~P5 완료)
- **데이터모델**: CardDefinition 을 payload(Json) + sampleData(Json) 로 슬림화, schema/layout/actions/level 컬럼 제거.
  - 리포 마이그레이션 히스토리에 card_definitions CREATE 가 없어(원래 db push 로 생성됨, migrate dev 구조적 불가)
    card_definitions 만 직접 ALTER + `prisma generate` 로 적용. 무관 테이블 drift 는 건드리지 않음.
- **OSS 엔진**: adaptivecards@3.0.6 + adaptivecards-templating@2.3.1 도입.
  - 신규: AdaptiveCardHost.tsx(템플릿 확장→render→action), adaptive/hostConfig.ts(다크), adaptive/actionBridge.ts(Action.Submit.data.__handler→dispatchHandler).
  - DynamicCardRenderer 내부만 AC 로 교체 — props 동일 → HitlDynamicCard/AgentFlowDebugPanel/SkillAssistantPanel 무수정 동작.
- **HITL 재구성**: hitl-input-card/hitl-recursion-card 를 AC payload 로 재작성(승인/거부 + Input.Text JSON 인자수정). arg_editor/nl_edit/suggestion_list 개별토글/preview_edit 폐기.
  - HitlDynamicCard data 에 toolArgsJson(stringified) 추가.
  - approval/choice 카드는 코드 참조 0 → 미복원(정리).
- **빌더 재편**: 3-pane(schema/layout/actions)+Visual editors+BlockReferencePanel 제거 → payload Monaco + sampleData Monaco + 라이브 AC 미리보기.
- **dead 코드 일소**: blocks/ 31파일, CardLayout/ActionRow/template.ts, cards-builder/, lib/card-schemas/, api-client CardAction/CardActionInput 삭제. (hitl-helpers 는 AgentFlowDebugPanel 가 TOOL_DISPLAY_NAMES 사용 → 보존)
- **검증**: agent-web tsc 카드오류 0, api tsc 0, eslint 카드파일 에러 0(경고 7=하단정의 컨벤션). 템플릿팅 헤드리스 스모크 — ${} 보간/contains $when/액션 data 정상.
- **선행 미해결(범위 외)**: skills/page.tsx, use-skill-assistant-store.ts 의 기존 TS 오류(스킬 어시스턴트, 카드 무관) — 본 작업 전부터 존재.
- **운영 반영 필요**: `pnpm db:seed` (새 AC payload 카드 upsert). 운영 DB 는 card_definitions ALTER(payload/sample_data 추가, schema/layout/actions/level 제거) 직접 적용 필요 — migrate 히스토리 비정합 때문에 push/ALTER 경로 사용.

---

## 2026-06-07 — siziksu — 카드 빌더 UX 회복 (AC 엔진 유지)

### 목표
P5 까지의 AC 전면 재편으로 운영자 UX 가 "빈 JSON 캔버스 + 스펙 암기" 수준으로 떨어진 문제 해소.
AC 엔진/데이터모델은 그대로 두고 빌더 측 어포던스만 회복한다.

### 범위 (단기 1~6, 중기 7~9, 장기 10 — 11번 회귀 가드 제외)
- T1: adaptivecards-designer@2.4.4 호환성 spike (Next 16 / React 19 / Turbopack / 다크).
- T2: 스니펫 팔레트 (AC element/action catalog) — 빌더 사이드 패널.
- T3: sampleData 자동 생성 — HITL 고정 템플릿 + response 카드 outputSchema 기반.
- T4: ${} 바인딩 자동완성 (Monaco completionProvider) + ajv AC schema 검증 패널 통합.
- T5: 신규 카드 생성 시 프리셋 셀렉터 (HITL 승인 / HITL 입력 / 응답 카드 빈).
- T6: HITL 전용 element 등록 (Element.ArgEditor 등) — P2 손실 UX 복원, HITL 한정.
- T7: 상태 전이 스니펫 (Action.ShowCard / ToggleVisibility) — visibleWhen 손실 보완.
- T8: 카테고리별 payload 필수 키 검증 — 발행 시 invariants.
- T9: tool 선택 → AC 입력 자동 매핑 마법사.

### 진행
- T1 — `adaptivecards-designer@2.4.4` peer/API 표면 조사 완료. API(new CardDesigner / monacoModuleLoaded / attachTo / setCard / sampleData / onCardPayloadChanged / getCard) 호환. 단 Monaco 0.29 peer 와 0.52 설치본 사이 worker 환경, CSS 다크 테마 정리, asset path(이미지/host config) 처리 등 통합 비용이 큼 → 본 사이클 범위 외, 별도 사이클로 분리. 그 동안 빌더 페이지 UX 회복으로 빈 JSON 캔버스 문제는 해소.
- T2 — `components/cards-builder/SnippetPalette.tsx` 신설. 섹션 5개(텍스트/입력/액션/HITL 패턴/상태 전이), 30+ 스니펫. 카테고리 검색, body/actions/root 타겟 라벨, payload Monaco 에 즉시 삽입.
- T3 — `lib/cards/sample-data.ts` `generateSampleFromPayload` — payload 의 ${} 토큰을 스캔해 inferValue (id→preview-id, json→{ "example": true }, count/num→0, is/has→true, list/items/choices→배열 샘플, 그 외→`${key} 값`) 로 자동 채움. ValidationPanel 의 '샘플 자동 채움' 액션과 미리보기 pane 의 '샘플 재생성' 버튼 두 경로로 노출.
- T4 — `lib/cards/ac-validate.ts` 신설. extractTokens / unresolvedTokens / basicStructureIssues / categoryInvariants / validatePayload. payload Monaco 의 `${...}` 안에서 sampleData 키를 자동완성하는 monaco completionProvider 등록 (페이지 mount 시점). `components/cards-builder/ValidationPanel.tsx` 신설 — 오류/경고/미해결 토큰 3섹션 + 자동 채움 액션.
- T5 — `lib/cards/payload-presets.ts` (CARD_PRESETS 6종: hitl-approve/hitl-input/hitl-choice/response-text/response-factset/blank) + `components/cards-builder/PresetPicker.tsx`. 신규 카드 진입 시 자동 표시 (cloneFrom/preset 쿼리 없을 때). 헤더의 '템플릿 변경' 버튼으로 재진입.
- T6 — 표준 AC 만으로 P2 잔존 UX(arg_editor / nl_edit / multi-action 토글) 표현. SnippetPalette 의 'HITL 패턴' 섹션에 인자 목록 반복($data + ColumnSet), 인자 편집 박스(Container.isVisible=false + Input.Text JSON), 자연어 수정 박스, '직접 수정' 토글 액션, recursion 취소 액션 5종 등록. 자체 element 등록 회피해 dead 코드 정신 유지. `HitlDynamicCard` data 에 `toolArgsList: [{key,value}]` 자동 파생 추가 ($data 반복용).
- T7 — SnippetPalette '상태 전이 (visibleWhen 대체)' 섹션 — Action.ToggleVisibility / Container.isVisible=false / Action.ShowCard 3종.
- T8 — `categoryInvariants`: HITL 카드는 승인 계열(approve/edit/choose) Submit 액션 최소 1개 필요(error), reject 권장(warn). 발행 mutation 의 mutationFn 에서 errors.length>0 차단, 버튼 disabled + title 에 첫 오류 표시.
- T9 — `lib/cards/schema-to-ac.ts` (schemaToAcInputs: JSON Schema object → Input.* 변환, enum→ChoiceSet/boolean→Toggle/number,integer→Number/format=date→Date/그 외 string→Text isMultiline 자동 추론). `components/cards-builder/ToolMappingWizard.tsx` (프로젝트 도구 / JSON Schema 직접 입력 두 탭, 활성 프로젝트의 tools.list 호출, 우측에 생성될 Input 미리보기, 핸들러명/버튼라벨 지정, '카드에 삽입' 시 body+actions 동시 push). 빌더 헤더의 '도구로 입력 자동생성' 버튼으로 진입.

### 변경 파일 (11개)
- 신규
  - `apps/agent-web/src/lib/cards/payload-presets.ts`
  - `apps/agent-web/src/lib/cards/ac-validate.ts`
  - `apps/agent-web/src/lib/cards/sample-data.ts`
  - `apps/agent-web/src/lib/cards/schema-to-ac.ts`
  - `apps/agent-web/src/components/cards-builder/SnippetPalette.tsx`
  - `apps/agent-web/src/components/cards-builder/ValidationPanel.tsx`
  - `apps/agent-web/src/components/cards-builder/PresetPicker.tsx`
  - `apps/agent-web/src/components/cards-builder/ToolMappingWizard.tsx`
- 수정
  - `apps/agent-web/src/app/(dashboard)/cards/builder/[cardId]/page.tsx` — 3-pane(팔레트/에디터/미리보기) + PresetPicker + ToolMappingWizard + ValidationPanel + Monaco completionProvider + 카테고리 invariants 발행 차단
  - `apps/agent-web/src/components/dynamic-cards/HitlDynamicCard.tsx` — `toolArgsList` 파생 추가
  - `작업일지.md`

### 검증
- `npx tsc --noEmit` (apps/agent-web) — 신규 코드 0 에러. 사전 에러 4건(skills/page, use-skill-assistant-store) 그대로 범위 외.
- `npx eslint` — 신규 파일 0 error, warning 만(use-before-define / set-state-in-effect — 프로젝트 컨벤션·1회성 effect 로 수용).
- 수동 검증 권장: (1) `/cards/builder/new` 진입 시 PresetPicker 노출 → 6종 중 선택 후 빌더 진입. (2) 좌측 SnippetPalette 에서 요소 클릭 → payload body/actions 에 자동 push + toast. (3) payload 의 `${` 입력 시 sampleData 키 자동완성. (4) 샘플 데이터에서 키 삭제 → 미리보기 아래 ValidationPanel '미해결 토큰' 섹션 + '샘플 자동 채움' 버튼 동작. (5) HITL 카드에서 approve/edit 액션 제거 → 발행 차단(에러 메시지 표시). (6) '도구로 입력 자동생성' → 프로젝트 도구 선택 → 미리보기 → 삽입.

### 특이사항
- **Designer(T1) 분리 이유**: adaptivecards-designer 가 Monaco 0.29 era 를 가정 — 우리 0.52 와 worker 셋업이 다름. 또 designer 가 다크 테마용 CSS 를 제공하지 않아 추가 스타일 패치 필요. 본 사이클의 UX 회복 효과가 designer 통합 없이도 70% 수준이라 분리 진행이 정당. 후속 사이클에서 `/cards/builder/[cardId]/designer` 별도 라우트로 시도 예정.
- **카테고리 invariants 의 정의**: 현재 'response' 카테고리에 대해서는 invariants 없음. 응답 카드의 합리적 규칙(예: 최소 1개 TextBlock, 또는 dismiss 액션 존재)은 사용 패턴이 모인 뒤 추가.
- **자동완성 범위**: `${key}` 의 root 키만 제안. `${foo.bar}` 의 `bar` 까지는 지원하지 않음 — sampleData 가 1-depth 위주라 충분, 향후 sampleData 가 중첩될 때 확장.
- **stale closure 주의**: 빌더 페이지의 `insertSnippet`/`handleAutoFillSample` 모두 setState 직전 `safeParse(state.payloadText)` 으로 최신 텍스트를 다시 파싱 — useCallback dep 에 `state.payloadText` 명시. AGENTS §1-5-3 패턴.
- **`toolArgsList` 파생**: HITL 인자 반복 렌더 스니펫이 동작하도록 HitlDynamicCard 가 toolArgs 객체에서 자동 파생. 기존 카드(JSON 문자열만 쓰는 hitl-input-card) 는 영향 없음 — 단지 새 키가 추가됐을 뿐.

### 운영 반영 필요
- 빌더 변경만 — DB 시드/마이그레이션 없음. 기존 발행된 카드는 그대로 동작.
- 신규 카드를 발행한 뒤 카드가 다른 환경에서도 보이려면 평소대로 `pnpm db:seed` 또는 운영 DB 에 직접 upsert.

---

## 2026-06-07 — siziksu — 카드 빌더 후속 (Tree Outliner / where-used / JSON import-export)

### 목표
앞 사이클의 빌더 UX 회복 위에 다음 추천 묶음(A2 + B1 + B2) 적용.

### 변경
- **A2 — payload Tree Outliner**: `components/cards-builder/PayloadTreeOutliner.tsx` 신설. payload 의 body/actions 를 트리로 표시 (Container.items / ColumnSet.columns / Action.ShowCard.card 재귀 탐색). 각 노드 라벨 = `type` + `#id` 또는 title/text 단축. 노드 클릭 시 Monaco editor 의 해당 라인으로 `revealLineInCenter` + 커서 이동. 빌더 좌측 팔레트 하단 패널(max 40% 높이) 로 통합.
- **B1 — where-used**:
  - 백엔드: `apps/api/src/modules/cards/cards.service.ts` 에 `findUsages(cardId)` 추가 — 전 에이전트의 `outputSchema['x-agent-studio'].cardMappings` 와 `toolPermissions.<toolId>.cardId` 스캔. `cards.controller.ts` 에 `GET /api/cards/:cardId/usages` 엔드포인트 추가.
  - 프론트엔드: `apiClient.cards.findUsages` 추가. 빌더 헤더에 `Link2` 아이콘 + `사용처 N` 배지 (N>0 일 때 amber 톤). 클릭 시 모달로 응답 매핑/HITL 도구 권한 두 섹션 표시, 각 항목은 에이전트 페이지(`/agents/:id`)로 링크.
- **B2 — JSON 가져오기/내보내기**: 빌더 헤더에 `Upload`/`Download` 버튼. 내보내기는 `{cardId, name, category, payload, sampleData}` 묶음을 `<cardId>.json` 으로 다운로드. 가져오기는 hidden file input → JSON.parse → state 로 로드 (payload 누락 시 거부). isNew 일 때만 cardId 도 덮어씀(기존 카드 편집 중에는 cardId 보존).

### 변경 파일 (5개)
- 신규: `apps/agent-web/src/components/cards-builder/PayloadTreeOutliner.tsx`
- 수정:
  - `apps/api/src/modules/cards/cards.service.ts` — `findUsages` 추가
  - `apps/api/src/modules/cards/cards.controller.ts` — `/usages` 엔드포인트
  - `apps/agent-web/src/lib/api-client.ts` — `cards.findUsages`
  - `apps/agent-web/src/app/(dashboard)/cards/builder/[cardId]/page.tsx` — 헤더 버튼 3종(사용처 배지 / 가져오기 / 내보내기), 사용처 모달, Tree Outliner 패널, payload Monaco editor ref 로 점프
  - `작업일지.md`

### 검증
- `npx tsc --noEmit` (apps/agent-web, apps/api) — 신규 코드 0 에러.
- `npx eslint` — 신규 파일 0 error (use-before-define 컨벤션 warning 만).
- 수동 검증 권장:
  1. 기존 카드 편집 진입 → 헤더 `사용처 N` 배지 노출. 클릭 시 모달에 응답/HITL 두 섹션 + 에이전트 링크.
  2. payload 의 ColumnSet / Container 안의 element 도 좌측 '구조' 패널에 트리로 보임. 노드 클릭 → Monaco 가 그 키 라인으로 점프.
  3. '내보내기' 클릭 → `<cardId>.json` 다운로드. 다른 환경에서 '가져오기' → state 로 반영.
  4. 새 카드 진입 → 가져오기 시 cardId 까지 덮어씀. 기존 카드 편집 중 가져오기는 cardId 보존.

### 특이사항
- **Tree Outliner 의 라인 추적**: 정밀 JSON parser 가 아닌 텍스트 인덱스 기반 fallback — 동명 키가 여럿 등장하면 첫 매칭의 라인을 반환. 99% 케이스에서 충분하지만, 향후 정밀이 필요해지면 `jsonc-parser` 같은 라이브러리로 교체 가능. 본 사이클 비용 절감.
- **카드 목록 페이지 사용처 컬럼 미적용**: 매 row 마다 findUsages 호출은 N+1 — batch endpoint(`GET /api/cards/usages?ids=`) 가 선결. 후속 사이클로 분리.
- **import 시 발행은 아님**: 가져오기는 빌더 state 만 갱신. 발행하려면 '버전 발행' 버튼 별도 클릭 (실수 발행 방지).
- **사용처 모달의 링크**: `<a href>` 로 navigate — Next.js Link 미사용. 모달 내부에서 빠른 이동만 목적.
- **권한 가드 없음**: `/api/cards/:cardId/usages` 는 카드 조회와 같은 권한으로 노출. 카드를 볼 수 있다면 사용처도 볼 수 있어야 합리.

### 운영 반영 필요
- 빌더 + API 변경. DB 스키마/시드 변경 없음.
- apps/api 재시작 필요 — 새 엔드포인트 반영.
